## 2026-07-15 (l) — EVIDENCE BOARD v2: the board becomes an organ (user: "make the evidence board more advanced… an even more integrated system"; FREEZE LIFTED by user the same evening)
- **NEW ⚖️ `evidence_board.py`** (freqtrade-bots container, 10-min loop): the evidence board graduates from passive banner to live organ. Per-key **lifecycle + scoring** (severity × recency-decay × persistence × mechanical corroboration across independent feeds); **synthesis** of cross-feed items no single publisher can see (venue stress ≥ taker veto bar, lens-forward crossing the n4h≥75 ruling floor, lenses NEGATIVE at the floor, drawdown-governor proximity, budget crowding with effective-bets context, coin-veto FLAP detection); **auto-verdicts** for mechanical items (milestones stale after 24h, dislocations fold into the census unless escalating, veto records age out at 48h) with **manual `evidence-review` verdicts always senior**; **phone notify** for new warn/action items (watchdog's NTFY channel, 6h/key gap); and **restrict-only proposals in SHADOW mode** — each names an existing lever (lens veto / clip scale / stress veto / budget), consumed by NOTHING until a review flips `EVBOARD_MODE` (the earn-your-wiring path the L2 light walked). Doctrine unchanged and explicit in the header: never opens positions, never loosens, opportunity flow stays scout→Ticket Taker→brain. Selftested (scoring monotonicity, trend, auto-verdict rules, manual-wins merge, synthesis incl. stale-feed refusal, flap detection); first live cycle: 12 items, 6 synthesized — it immediately authored `board:lens-negative:momentum` (n4h=90, avg4h −1.05%) and `board:veto-flap:ADA` (3 changes in 7d), both previously un-itemized.
- **Dashboard:** new ⚖️ Evidence board card (scores, trend arrows, verdicts, shadow proposals, mode badge) beside the brain card; the 🔔 banner now also honors the organ's auto-verdicts (manual review stays senior in both directions).
- **Freeze lifted** (user: "take off the freeze as I believe this could be a breakthrough") — recorded in CLAUDE.md + agenda header; the 21-Jul review stands, and grading the board's shadow week is now on its agenda (§8). Dip-lens bar NOT changed: the replay sweep (0.05/0.10/0.15) was IDENTICAL on today's tape — the knob doesn't bind yet; re-sweep on the week's tape per agenda item 2.

## 2026-07-15 (k) — /history grows a window selector + max-DD captions (stash salvage part 2; user: "implement your changes even though the freeze is on")
- **`/history?hours=24|168|720|all`** (default 7d unchanged) with the time axis thinned to ≤500 points on long windows; each cohort header (Total/Live/Shadow/Paper/Scanner) now carries its **max peak-to-trough drawdown** for the selected window. Dashboard-only — the review week reads this page, and 7d-fixed made longer arcs invisible. Pure-logic selftested offline (DD math edge cases, window parsing, selector render). Deployed via `railway up` (see (j) note: main pushes don't build pnl-dashboard — memory corrected).

## 2026-07-15 (j) — 7-Jul stash salvage: the three measurement pieces that shouldn't wait for the review (user-approved "anything obviously positive"; measurement only, no strategy logic)
The 7-Jul "pre-OptionB" stash was excavated: most of it is superseded (brain reads paper_trades, /periods unions ledgers, Gap Scout balance persistence) or moot (Trail Blazer/Bounce Catcher/triangular-arb retired; Kraken strategies dormant), and its pair-blacklist actuator lost to Option B (the L4 stake mults) — left for the 21-Jul review. Three evidence pieces were still missing and now accrue a week of data before the review:
- **`paper_trades` schema WIDENED for learning** (`bot_pnl_store.py` — revives the never-deployed 7-Jul b82c5aa design): `side/tag/entry_price/exit_price/size/extra(JSONB)`, all optional, legacy callers unchanged. `side='skip'` is reserved for gate-rejection logs; `fetch_paper_trades` now excludes it so skips can never reach the brain/analyzer, and `/periods` (`fetch_period_pnl`) filters it too. Verified END-TO-END against the fleet Postgres (ALTER+insert+JSONB round-trip+skip-guard; 706 real rows intact; test row removed).
- **🎯 Launch Sniper: the gate's false-negative half is finally durable** (`listing_sniper.py`): every research-gate rejection mirrors to Postgres under `event-listing-sniper-skips` (side='skip', microstructure in extra) — sniper_skips.csv died with each redeploy, so "did the gate dodge rugs or skip winners?" was unanswerable. Closes are also enriched: entry/exit price, size, intel tag, entry-time spread/depth/volume, peak %, hold minutes → feeds the agenda-item-5 go-live-gate restatement, which will judge this book on expectancy.
- **🔀 Gap Scout: booked fills become auditable** (`cross_exchange_arb.py`): per-fill ledger rows (REAL basis, `long-xarb_paper-fill` per the tag contract, route in tag, tops-of-book as entry/exit) + closed/wins/losses published and restored from state/ledger — the dashboard showed `trades=0 0W/0L` forever and the 14-Jul review flagged the cumulative balance as unauditable. `/trades.json?source=paper` also serves the widened columns now.
- NOT salvaged: /history window selector (cosmetic — post-freeze), pair-blacklist emitter + strategy gates (strategy logic; design superseded by L4 mults; review decides if pair-level throttling rides the mult machinery).

## 2026-07-15 (i) — watchdog alerts go to the operator's PHONE (user-requested; infra only, no strategy logic)
- **`fleet_watchdog_svc.py` gains an ntfy.sh push channel** (user: "send to my phone instead of email"). Auth-free pub/sub: the unguessable topic in `NTFY_TOPIC` (pnl-dashboard service env) is the only secret; the phone's ntfy app subscribes to the same topic — no account, no SMTP, no new credentials anywhere. Same transition-based semantics (🚨 alert / updated ≥30-min gap / ✅ recovery): phone push sends first (priority `urgent` for alerts, `default` for recovery), email second if SMTP is ever also configured; either delivery marks the transition sent. Titles are ASCII-sanitized (urllib headers are latin-1; emoji ride as ntfy tags), bodies stay UTF-8. `/watchdog.json` now reports `push_armed` beside `email_armed`. Selftest covers URL/headers/sanitization, unconfigured→False, network-failure→False-never-raises. SMTP remains the (dormant) path for report_emailer's scheduled P&L mails — push replaces email for WATCHDOG alerts only.

## 2026-07-15 (h) — evidence registry: Index Pilot macro-gate REJECTED scripts enter git (docs/hygiene, no runtime)
- **`scripts/backtest_index_pilot_macro{,_long}.py` committed with explicit VERDICT headers** (12-Jul REJECTED on both the short HL window and the 2022-inclusive long window). The validation doctrine says script headers ARE the don't-re-test registry — these two sat untracked on one machine, so the registry had a hole a future session could fall into (rebuilding the SPY/QQQ macro gate). Headers point at `memory/index-pilot-lighter-rejected.md`; this closed the LAST open Index Pilot flavor.
- **`.gitignore`: `scripts/.*.json`** — the hidden offline-rerun backtest caches (~10MB across 6 files) stop cluttering `git status`. `.claude/settings.local.json` stays tracked on purpose (#55/#56 mirror decision — noted in the ignore file so nobody "fixes" it).
- No runtime surface: nothing deploys, nothing restarts. Landed on main only; gate0 not fast-forwarded (no reason to bounce the live services for docs). Freeze exception logged in the agenda §8.

## 2026-07-15 (g) — HOTFIX: review-reminder.yml was invalid YAML (the source of the GitHub failure emails)
- `review-reminder.yml`'s issue body was a multi-line double-quoted string with column-0 lines, which terminated the `run: |` block scalar early — GitHub could not parse the workflow's `name:`, so it raised a FAILED run (and a failure email) on EVERY push (~13 today). Nothing in the fleet was affected; the noise was purely the unparseable file. Rebuilt the body with `printf` (each line a quoted arg whose indentation sits OUTSIDE the quotes) so the YAML is valid and the rendered issue body has no stray spaces. Verified: pyyaml parses it, `push` is not a trigger (so no more per-push runs), body renders clean. The workflow itself is kept — the MCP scheduler trigger still needs a claude.ai connector approval, so this remains the durable 21-Jul review reminder.

## 2026-07-15 (f) — PR #34's evidence changes land on main (user-approved; supersedes the stale gate0 draft)
- **Funding Farmer live-vs-shadow divergence: PAIRED per-coin per-trade** (`market_context.py`, authored 14-Jul in PR #34, cherry-picked 44b2f38). Replaces the whole-book equity-ratio check whose 13-Jul "+5.4%" alert was an artifact (a ~$64 live book divided against the $1k shadow moves ~16x per dollar; live holds only the top slots). New: trade-count-weighted mean of per-trade `pnl_pct` gaps on coins BOTH books closed in 7d (`seen_at`-filtered), ≥3 overlapping coins, alert >1.5pp/trade. This is agenda item 4 (live-vs-shadow execution quality) running a week EARLY — the 21-Jul review reads data instead of writing a spec.
- **Snap Back census `count_enter`** (`lighter_dislocation_bot.py`): per-coin count of prints clearing `ENTER_BPS` (150bps) — the tradeable-density number its promotion review needs.
- PR #34 itself closed as superseded: its third change (vercel.json) duplicated the richer one already on main since 14-Jul (c), and merging into gate0 would have broken the gate0⊂main fast-forward invariant. Landed on main + gate0 fast-forwarded instead. Freeze exception logged (measurement only, no strategy logic).

## 2026-07-15 (e) — operator-queue sweep (user: "attend to all of my queue"; freeze-exception logged in the agenda)
- **🧭 Trail Blazer STOPPED via retirement guard (`hyperliquid_momo_bot.py`).** Retired 11/12-Jul on user sign-off but its momo-bot Railway service kept running (16+ ledger closes after retirement). The service auto-deploys from main and restartPolicy=always would crash-loop an exit, so the guard boots INERT: no data, no trades, no publishes — the stale bot_pnl row gets boot-pruned and stays gone. Ledger history untouched. Resurrect deliberately via `MOMO_RETIRED_OVERRIDE=run`; deleting the idle service remains a console click.
- **NEW `.github/workflows/db-backup.yml` — the fleet's Postgres is now backed up nightly (04:07 AEST).** Every ledger and all brain memory live in one Railway Postgres with an unverified backup story; the workflow pg_dumps it via the Railway CLI's variable injection (account-token pattern from railway-volume.yml — NO new secrets), prefers `DATABASE_PUBLIC_URL` (runners can't reach railway.internal), masks the URL, uploads a custom-format dump as a 30-day artifact on the private repo. Restore: `pg_restore --no-owner --dbname <url> bot_fleet.dump`.
- **NEW `.github/workflows/review-reminder.yml`** — opens a GitHub issue at 09:00 AEST Tue 21-Jul so the scheduled review can't be missed (session cron dies with the container; the MCP scheduler needs an interactive approval). Yearly-recurring cron — the agenda's item 8 says delete the workflow at the review.
- Agenda operator checklist swept + freeze-exception log updated (FLEET_REVIEW_AGENDA_2026-07-21.md). Still operator-only: the scheduler approval click, SMTP vars for the (built, dormant) report_emailer, and hunting the off-repo equities-regime-ibkr publisher host.

## 2026-07-15 (d) — consumption-audit gap fixes: every counted bot now checks what counts it (user-approved: "fix all of the above")
- **🌊 Tide Rider LIVE consumes the fleet long-budget veto (`lighter_trend_bot.py`).** The consumption audit found the only real-money LONG book was counted BY the light but never CHECKED it. Same contract as the family bot/taker (15-Jul audit-fix pattern): fresh payload + mode=enforce + budget full → skip NEW golden-cross entries that cycle; exits/stops/daily-loss rails untouched; missing/stale state fails OPEN; central kill switch `FLEET_RISK_MODE=advisory`. NOTE: the light is red 23/20 at ship time — Tide Rider pauses new entries until the fleet book shrinks, which is the veto doing its job.
- **👨‍👩‍👧‍👦 family books consume the drawdown governor.** `clip_scale` was taker-only ("gate0 advisory until ported") — ported: new-entry stakes scale ×1.0/0.5/0.25 with the same clamp + fail-open semantics, logged when active. Applies at entry sizing only.
- **🎯 Launch Sniper closes are finally learnable:** `reason="long-listing_"+exit` (was raw exit — 337 closes permanently '(untagged)', invisible to per-sleeve learning). New closes bucket under enter_tag `long-listing`; history unchanged.
- **🎯 Perp Sniper joins the exposure view:** publishes `held` ({sym: L/S} by DIRECTION_LONG) in extra — it was `sym_uncovered` in the new concentration block.

## 2026-07-15 (c) — HOTFIX: exposure harvest crashed fleet_risk on the live trend bot's row
- **`held` publishes in two shapes and the (b) exposure harvest assumed one.** Funding/family books publish `extra.held` as a DICT ({coin: 'L'/'S'/tag}) but the LIVE Tide Rider (and the equities ports) publish a LIST (`sorted(meta.keys())`); `held.items()` threw AttributeError on `crypto-trend-daily-lighter`'s row, killing every fleet_risk cycle post-deploy — the light froze (last publish 06:09Z) and consumers correctly failed OPEN, i.e. the long-budget veto went dark on the running fleet. Caught ~25 min in by the post-merge verification poller + the consumption-matrix audit. Fix: `held_items()` normalizes both shapes (list entries side-default long — correct for the long-only publishers of that shape), used in both harvest loops AND the pre-existing PERPS longs/shorts derivation (same latent hazard). Selftest covers dict/list/None + a direct simulation of the crashing row.

## 2026-07-15 (b) — what successful systems have: exposure view + replay harness (user-approved)
- **fleet_risk EXPOSURE VIEW (`exposure_concentration`) — the light counts positions, this says how many BETS they are.** From the ecosystem gap review ("you are 23× long one factor and nothing says so"): the same directional cohort the light counts now also publishes `exposure` — per-symbol pileup (top-8), distinct symbols, **effective independent-bet count (1/Herfindahl)**, crypto-vs-equity-perp cluster split (curated `FLEET_EQUITY_BASES`, env-extensible; ambiguous tickers stay crypto), and `sym_uncovered` (positions whose publisher exposes no symbol detail — the view only claims what it can see). Symbol harvest now ALSO reads `extra.held` (gate0 family books + live Funding Farmer), so those books join `pair_concentration` for the first time. Advisory publish-only (payload + lean history mirror); budgets/light/veto semantics untouched. `--selftest` covers pileup math (4×SOL+1×ETH → eff_n 1.5), clusters, coverage honesty.
- **NEW 🎬 `lighter_ticket_replay.py` — replay the recorded scout tape through the Ticket Taker's REAL code.** The Lighter-native fleet validated by forward-shadow only (days per rule tweak) while the scout was already recording the needed tape (5-min `lighter-market` history with marks since 15-Jul). The harness feeds that history through the taker's own `incredible()` bars and `exit_reason()` TP/SL/max-hold (imported, not reimplemented — it cannot drift from the bot), models fills at snapshot marks with the same 4bps fees, replays the stress veto, and reports per-lens seen/taken/closed/WR/net/exits + unrealized tail. Env-tunable variants (`TT_TP=0.06 python3 lighter_ticket_replay.py`) judge a bar change against recorded reality in seconds. Honest divergences documented in-file: no funding accrual (tape has no rates — long lenses read rich, divergence shorts poor), fixed clips (no ranges), no bus vetoes. Reads Postgres or the public `/bus.json`; writes nothing. Selftest (tp/sl/hold, dip bar, stress veto, one-per-lens, fee math) + first live-tape run green (19 snapshots: divergence TP +$2.10, momentum SL −$1.60, 6 open).

## 2026-07-15 — learning-loop wiring: point the brain at the living fleet (user-approved)
- **Evidence & learning assessment** (`EVIDENCE_AND_LEARNING_REVIEW_2026-07-15.md`): 120 brain runs, ZERO premature throttles (floors/streaks behaving), but the 14-Jul pivot broke the L4 mult loop on both ends — the only consumers were the stopped Kraken strategies, and the family bot's closes dropped the strategy tag so all seven books bucketed as enter-tag `long`. 10/16 ACTIONABLE hypotheses prosecuted retired bots (rows never leave the fetch window → the streak-retire path could never fire); Trail Blazer's paper engine STILL trades (16 closes since 14-Jul — stopping the momo service stays operator-only). Lens-forward was empty purely because scout `marks` began publishing 04:27 today.
- **Family bot rejoins the learning loop (`lighter_family_bot.py`, gate0 cross-merge needed to deploy).** Closes now publish `long-<tag hyphenated>_<exit>` (`ledger_reason`; hyphens because the ledger parser splits at the FIRST underscore — taker precedent 14-Jul (g)) so the brain buckets per strategy tag, and entries apply `fleet_bus.stake_multiplier(b.bot_id, ledger_tag(tag))` — the same helper derives ledger tag AND lookup key, so buckets and lookups can't drift. Reduce-only, clamped [0.5,1.0], fail-neutral, logged when <1.0; NO-OP at deploy (published table empty). `fleet_bus.py` added to Dockerfile.familyshadow. No entry/exit logic touched.
- **Brain LIVENESS filter (`bot_learn.py`).** Hypotheses/diagnoses/multipliers generate only for living bots (not in `cleanup_legacy_bots.LEGACY_BOTS`, ≥1 ledger close in 7d); scorecards keep printing for everyone flagged `[inactive/retired]`; existing dead-bot entries decay via the normal 3-run retire path. `_epoch` now parses the sniper's `'... UTC'` timestamps (liveness + drift joins). Verified with a 3-run fixture: identical losing evidence in a live/stale/retired bot → hypotheses only for the live one (candidate→ACTIONABLE at run 3).
- **Ledger tag contract extracted (`bot_pnl_store.split_reason`)** — the one parser the taker's and family bot's composers round-trip against (unit-tested: 36 tag/exit combos + legacy/funding/sniper edges). `/bus.json` now also serves `brain-lens-forward` (live + history) beside the other brain keys; run_all.sh taker comment corrected to `<side>-<lens>_<exit>`.

## 2026-07-14 (h) — the missing risk leg: measured risk, not counted (user-approved)
- **Fleet DRAWDOWN GOVERNOR (`fleet_risk.dd_governor`).** The fleet had per-bot seatbelts but nothing that said "the whole book is bleeding — shrink". fleet_risk now sums equity over the same authoritative rows the light counts, keeps a 7-day rolling sample window inside its own state (≥30-min gaps, redeploy-safe), and publishes `fleet_equity` / `fleet_dd_7d` / **`clip_scale`** (1.0 → 0.5 past −5% 7d drawdown → 0.25 past −10%; env `FLEET_DD_HALF`/`FLEET_DD_QUARTER`) — state + history. Ticket Taker CONSUMES it (clips scale down, fail-safe neutral on stale state); advisory for the gate0 books until ported. 7-case unit suite + end-to-end drive (seeded −10% → half clips, historized).
- **Ticket Taker: CONSTANT-RISK sizing (`vol_clip`).** Fixed $50 clips carried ~5x different risk across books; now clip = `TT_RISK_USD`(1.5) / expected adverse move (half the daily range, floored 0.5%), bounded $20–$80, fallback $50 when a book has no range data. Per-position clip stored in meta so ledger `pnl_pct`/fees use the real size; the brain's per-lens grading is on pnl_pct so sizing doesn't distort it. Calm books (BTC ~2% range) size up to the cap; wild alts size down to the floor. Selftested across the curve.

## 2026-07-14 (g) — divergence lens + stress veto (user-approved same evening)
- **Ticket Taker learns SHORTS + the FUNDING-DIVERGENCE lens (the strategy nobody in the fleet traded).** Scout gains `tickets.divergence`: when Lighter's funding diverges ≥300pp APR (`SCOUT_DIV_GAP`) from the binance/bybit/HL median for the same liquid symbol, ticket the RECEIVING side (positive gap → short, negative → long), skipping entries that fight the premium (no shorting already-cheap books). Ticket Taker takes them at ≥500pp (`TT_DIV_GAP`) with full SHORT support: signed funding accrual (a short under a positive rate is CREDITED — the lens's whole thesis), side-aware exits, and `PaperBroker`'s signed positions. Live at ship time: 6 divergence tickets, top HOOD +1118.7pp.
- **Stress veto:** when the scout's venue-wide |premium| median ≥15bps (`TT_STRESS_VETO_BPS`; normal tape ~6), Ticket Taker takes NO new entries that cycle (exits keep running) — a whole-venue dislocation means marks are unreliable and every book is one bet.
- **Per-lens brain grading fixed before it mattered:** the ledger's reason parser splits on the FIRST underscore, so the old `long_<lens>_<exit>` tags would have collapsed every lens into one "long" bucket for the brain. New format `<side>-<lens>_<exit>` → enter_tags like `long-breakout` / `short-divergence`; no closes had been recorded yet, so no history was mis-tagged. fleet_risk's short-detection (`"short" in tag`) works with the new tags unchanged.

## 2026-07-14 (f) — KRAKEN RETIREMENT + the scanner gets its trader
- **Kraken bots RETIRED (user instruction: "let's just focus on lighter — less clutter").** The 4 spot originals no longer launch from run_all.sh (main-mode poller retired with them) and all 8 Kraken paper rows (spot four + family four) move to RETIRED_ROWS + boot-prune; their bases move EXPECTED→VARIANT_ONLY so the **-lshadow twins keep displaying as the fleet** and Tide Rider's LIVE row is untouched (retirement matches exact row names — verified against fetch_rows' filter). The main container keeps every fleet organ (pulse/brain/oracle/risk/scout/cleanup/ticket-taker). OPERATOR-ONLY: stop the 4 family Railway services (ONLY_BOT mum/dad/avo-maria/georgia — left functional so they don't crash-loop meanwhile). fleet_risk's `authoritative_row` chain is now live > -lshadow > paper (fresh rows only): the light tracks the Lighter fleet — a documented semantic change (shadow books are modelled capital, but they ARE the managed fleet; the pileup scar applies to any cohort trading one beta).
- **NEW 🎫 Ticket Taker (`lighter_ticket_taker.py`, row `lighter-ticket-taker-lshadow`) — the scanner's designated trader (user ask: "when a scanner finds something incredible, the designated bot absorbs the finding and makes a trade").** A $1,000 SHADOW book that takes only the HIGH-CONVICTION subset of the scout's tickets (bars: breakout range≥0.95 & ≥$1M; dip range≤0.05; momentum ≥+5% & ≥$2M — env-tunable), fills modelled at Lighter mark via the existing PaperBroker (fees), hourly funding drag accrued from the venue's own feed, exits TP+4%/SL−3%/48h max-hold, one new position per lens per cycle, 6 max, $50 clips. **Every close is tagged `long_<lens>_<exit>` into the durable paper_trades ledger, so the brain grades each lens on real forward returns** — the scanner→bot→brain loop that decides which lens has an edge. UNVALIDATED by design (lens rules need forward data — collecting it is this book's job); no live path exists; state persists in bot_state across redeploys; counted in the fleet light (fleet_risk-shaped open_pos). Selftests (bars/exits/accounting incl. funding) + live one-shot green; fleet_risk chain verified against a synthetic lshadow/live/ticket-taker fleet (no double-counting).

## 2026-07-14 (e)
- **Lighter Scout: per-strategy TICKETS (user ask: "a scanner per bot so each bot has an edge").** One scanner engine, per-bot-family lenses — not eight services: `strategy_tickets()` hunts each family's setup across ALL liquid Lighter books instead of a fixed whitelist and publishes ranked candidates in the scout payload (`tickets.breakout` → Breakout Hunter/Dad: at the daily high on volume, not trading rich, longs not paying >100% APR; `tickets.dip` → Dip Buyer/Avo Maria: hard bounded flush to the lows, tagged `trend: unknown_v1` — no candle history keylessly; `tickets.momentum` → Stock Leaders/momo: strongest liquid movers, funding-sane). HONEST FRAME: a scanner amplifies a validated edge, it cannot conjure one — and the brain's diagnosis layer shows entries are NOT every bot's lever (georgia/intraday were exit_too_tight, trend-daily fee-scale). So tickets are ADVISORY + HISTORIZED: the weekly review grades each lens's forward returns against the bots' actual entries, and a lens only earns a consumer feed by that evidence. Live print: 6 breakout / 2 dip / 4 momentum tickets over 88 liquid books; selftest covers lens picks + rich/knife/illiquid exclusions.

## 2026-07-14 (d) — Lighter-first sweep (user-directed)
- **PR #33 integrated onto current main** (the other Jul-14 session's brain L4 + diagnosis + enforcement wiring + backtested bot fixes) — conflicts resolved keeping BOTH sides: enforce mode + venue-aware counting AND the staleness filter + Lighter mirrors; `authoritative_row` now also requires freshness; the bus funding preference (Lighter > shadow > HL) considers fresh rows only; `/bus.json` and `/disloc.json` both kept.
- **Laptop stock bots RETIRED on user instruction (Lighter-first cut).** `equities-momentum-alpaca` + `equities-regime-ibkr` → RETIRED_ROWS + boot-prune; their Lighter ports (Stock Leaders / Index Rider `-lshadow` books) are the bots now. Also resolves the broker-scale distortion ($100k/$250k equity rows). NOTE: the laptop processes run outside this repo — the operator must stop the cron/processes; until then their rows re-upsert harmlessly hidden.
- **Gap Scout signal + persistence fixes (review action item).** The bus dislocation is now `liquid_top_pct` — the same max restricted to unambiguous majors (`DISLOC_BASES`, default BTC/ETH/SOL/XRP/ADA/DOGE/LTC/LINK/DOT/AVAX) — instead of the symbol-collision max that printed 4.6% junk; raw kept in extra for diagnostics. Paper balances (real + gross basis) now persist via bot_state across redeploys (the 25-Jun sniper bug class).
- **Brain visible from outside.** `/bus.json` now serves `learning-brain`, `brain-stake-mults`, `brain-diagnosis` (+ history) — the brain died silently for two days once (5–7 Jul); its outputs are now on the same no-auth surface as the bus. Audited bot_learn v2 read/write paths: guarded end-to-end, ran end-to-end from a no-DB sandbox via the HTTP fallback (121 trades, 1 diagnosis).
- **NEW 🛰️ Lighter Scout (`lighter_market_scout.py`) — the venue-wide market map (user ask: a wider Lighter scanner feeding the brain).** Two keyless calls per run (orderBookDetails + funding-rates) over ALL ~215 books → bot_state `lighter-market` (+history, on `/bus.json`): premium stress (med/p90/max |bps| over ≥$100k books), top liquid premium outliers, **liquid-floored Lighter-native funding extremes** (fixes the tiny-coin-noise flaw of the old bus funding signal), **cross-venue funding divergence** (Lighter APR vs median of binance/bybit/HL — the carry force seen BEFORE entry; live print: DATA +925pp, XPT −591pp), volume surges (≥3×), OI moves (≥25%), listing/delisting diffs. ADVISORY/publish-only per doctrine; fail-silent; looped 5-min in run_all.sh. Selftest + live-verified (198 books, 88 liquid, stress med 6.0bps).

## 2026-07-14 (c)
- **Pipeline-hiccup sweep (user-requested audit follow-up).** Three read/write defects found while auditing what could inhibit collecting/optimising: (1) **market_pulse history window was ~33h, not the intended ~7d** — the 200-entry cap assumed hourly appends but the pulse loops every ~10 min; history now appends at most hourly (`append_hourly`, gap 55 min), so the brain's mood-conditioned analysis gets ~8 days again. (2) **fleet watchdog's position warning was apples-to-oranges** — it summed open_trades over shadows/funding/stocks/scanner rows (64 on a healthy day) against the 20-position directional budget, warning forever; it now counts the directional cohort only (paper + real-live trading rows; gross kept in the snapshot line). (3) **paper_trades had no read endpoint** (funding bots, sniper, live Lighter closes were invisible to outside reviews) — `/trades.json?source=paper` now serves it. Also pruned the retired-bot LABELS dead code (rows deleted + RETIRED_ROWS filters republishes). FLAGGED, needs an owner decision (bots live outside this repo): equities-momentum-alpaca publishes raw $100k-scale Alpaca paper equity and equities-regime-ibkr raw ~$250k IBKR paper equity — 100-250x every other book, swamping cross-bot P&L comparisons and tripping the watchdog daily-loss alert; fix at source (publish vs a recorded baseline, like the Jul-10 live-Lighter fix) or normalize dashboard-side.
- **Dead bots removed at the source (user-approved).** `cleanup_legacy_bots.py` allow-list extended with the dashboard's RETIRED_ROWS set (Bounce Catcher + lshadow, Trail Blazer paper/live/lshadow, regime-switch + lshadow, Loop Scout, trendmomo-4h) and now runs `--apply` once on every freqtrade-bots boot (new COPY in Dockerfile.freqtrade + run_all.sh one-shot) — deletes the frozen `bot_pnl` snapshot rows ONLY; trade ledgers, venue_orders and bot_state history are kept (Manage-panel delete semantics), and a revived bot's row reappears on its next publish. `fleet_risk.py` counting lists pruned to the living cohort (PERPS_LS_BOTS now empty — the whole perps L/S cohort is retired). Belt (staleness filter) + braces (rows gone).
- **fleet_risk: GHOST-EXPOSURE fix — stale bot_pnl rows no longer count.** Post-merge verification of `/bus.json` caught the live light pinned at RED 32L/0S for hours while the real fleet held ~10 longs: `fleet_risk` reads the raw `bot_pnl` table (not the dashboard's filtered view), and rows outlive their bots — decommissioned Bounce Catcher (`perps-rsi-meanrev`, stopped 12 Jul) froze holding 6 longs, Trail Blazer's undisplayed paper row held 16 more. Rows older than 30 min (`STALE_ROW_SEC`) are now ignored in exposure counts AND all signal-bus mirrors (a frozen scanner row must not republish last week's dislocation). Unit-driven with synthetic fresh/ghost/stale rows. PREREQUISITE for the RED-veto enforcement wiring — a ghost-inflated light would throttle live bots for positions nobody holds. Review doc's "current state" stamp corrected (the GREEN 10L/0S figure was read off filtered `/pnl.json`).
- **Cross-bot advisory review (the one the 7-Jul design scheduled) — verdicts + evidence in `CROSS_BOT_ADVISORY_REVIEW_2026-07-14.md`.** Reconstructed the advisory week from the `/trades.json` ledger (121 trades, Jul-03→13; `bot_state_history` is unreachable off-Railway — itself a finding, fixed below). **L2 (fleet-risk light): evidence supports wiring the RED veto** — entries taken at 13+ ledger-visible fleet positions won 18% (n=44, −$18.04, worst bucket); peak visible concurrency 20 with two perps books uncounted; a RED(20L) veto would have cost ~nothing while capping the 26-position-scar tail. Enforcement wiring is bot logic → its own change, not this PR. **L3 (signal bus): do NOT wire dislocation/funding as published** — `xexchange_dislocation_pct` printed 4.64% at review time (a thin-pair/symbol-collision artifact under the 5% ceiling; corroborated by Gap Scout's +$6,095 "real"-basis balance on an engine whose docstring expects flat), and hottest-funding is permanently-extreme tiny-coin noise. Pulse: all 8 entries in the visible window fired during panic and 7 lost *despite* the half-stake gate (n=8 — keep logging, no entry gate yet per doctrine). Next review Jul-21.
- **Gap Scout learns the venue we actually trade: LIGHTER PREMIUM on the signal bus (advisory).** `cross_exchange_arb.py` now polls Lighter's public `orderBookDetails` (one keyless call, all books; guarded fail-silent — Lighter's WAF has blocked other REST from Railway) and publishes the venue premium `mark/index − 1` in bps: per-book for the family watchlist (`BTC,ETH,SOL,SPY,QQQ,XAU`, env `LIGHTER_WATCH`) + median/max |premium| across every active ≥$100k/day book (the venue-stress gauge; floor env `LIGHTER_MIN_QVOL`). `fleet_risk.py` mirrors to `signal-bus` as `lighter_prem_bps` / `lighter_venue_stress_bps` (historized like the rest). Live probe: majors −5…+9bps, RKLB +26bps, SKHYNIX +50bps. Why: taker entries pay the premium, persistent premium leads funding drag (the momo funding-veto A/B measures the trailing version), and fleet-wide |premium| widening is Lighter-specific stress. PUBLISH-ONLY until a review earns a consumer. Pure math in `lighter_premiums()` + selftest. NOTE for gate0: main's `fleet_risk.py` predates the venue-aware `authoritative_row()` — port the mirror to whichever copy the service runs.
- **dashboard `/bus.json`**: read-only, no-auth (same contract as `/pulse.json`) — live `fleet-risk` + `signal-bus` states plus their `bot_state_history` snapshots (`?hours=`, default 24, max 200), so advisory reviews read evidence instead of reconstructing from the trades ledger.
- **`vercel.json`: git deployments disabled.** A Vercel project was connected to this repo and error-failed EVERY commit's status check with "No python entrypoint found" (all recent deployments incl. main production; the fleet deploys on Railway — there is nothing for Vercel to build). `github.enabled=false` + `git.deploymentEnabled=false` stop the doomed deploys and the red X on every PR. Fuller cleanup (operator-only): disconnect the repo in the Vercel project settings.

## 2026-07-14 (b)
- **Brain DIAGNOSIS layer — it can now tell an entry problem from an exit problem from a fee/regime/venue problem.** Born from the dashboard's four "tighten the X entry gates" proposals: two were plumbing artifacts (fixed earlier today — exit reasons masquerading as entry tags), and the deeper flaw was that the brain had ONE prose template for every negative bucket. `bot_learn.diagnose()` now classifies each negative (bot, enter_tag) bucket at n≥10 into **exit_too_tight / venue_execution / fee_bleed / regime_timing / entry_quality / mixed_unclear** using: exit-path loss splits (ledger), **post-exit drift from public Kraken 1h candles** (did price reclaim entry within 24h of the losing exit? — the mechanized form of the manual replay behind the 13-Jul stop widening), fee-scale tests (median loser vs round-trip friction), a **regime-oracle-history join** (new `bot_pnl_store.fetch_state_history`; snapshots every 30 min since 7-Jul), and the venue A/B table. Proposals now name the lever; "tighten the entry gates" is only earned by confirmed entry_quality. Publishes bot_state `brain-diagnosis` (+history); hypothesis keys unchanged so existing streaks carry over. **Validated against known ground truth** (full-July replay, live candles): intraday/georgia trailing-stop bleed → exit_too_tight with 79–100% reclaim (the manual 13-Jul replay found 77–89%); crypto-breakout-4h breakout → entry_quality (22% reclaim, negative fwd — the tide-gate finding); freqtrade-dad → mixed_unclear on murky 11-trade evidence (declines to call it); trend-daily probes → fee-scale, "signals are not the lever" (the CONTROL verdict). 6-scenario unit suite green. Advisory-only: humans + dashboard consume it; nothing trades on diagnoses directly. Drift work bounded (once-per-pair candle cache, ≤120 evaluations/run); all evidence fail-soft — a missing source just removes its rule from contention.

## 2026-07-14
- **Fleet metrics/strategy review + THE CONSUMPTION WIRING — the Jul-14 enforcement review the 07-07 design scheduled** (full write-up: `FLEET_REVIEW_2026-07-14.md`). The audit found every intelligence organ built, publishing, and consumed by NOBODY (oracle/risk-light/signal-bus: zero readers; the brain's per-tag analysis ended as prose). W/L-ratio triage corrected first: sniper (31W/296L but **+$205.70**) and funding-carry (4W/30L but **+$7.25**, Lighter shadow 5W/3L +$8.92) are positive-expectancy KEEPs; the real bleeders were the breakout carriers, mum, and the DayTrader carriers (already fixed 13-Jul).
- **MomoBreakoutV1: BTC-TIDE GATE (backtest-first).** Live bleed: crypto-breakout-4h + freqtrade-dad both 2W/9L (~-$8 each) in 3–13 Jul — 11 pairs broke their own 200-EMA and died in a fear tape (market-wide fakeout wave, invisible to the per-pair filter). `scripts/backtest_momo_tide_gate.py` (Kraken 4h, 30 pairs, 20-Apr→14-Jul, 0.26%/side): as-deployed 121 trades PF 0.79 Σ−94.0% (reproduces the live July bleed); **+ BTC>4h-EMA200 gate: 74 trades PF 1.43 Σ+79.1%**, catastrophe stops 6→1; breadth-gate and half-size variants dominated. Shipped the hard gate via the BTC informative pair (deterministic in `freqtrade backtesting`). Honest caveat: one 3-month window — but the exact bleeding regime, and the validated core (+$201.59/603) earned everything in up-tides.
- **freqtrade-mum (TrendMomoV1): whitelist curated 15→10 (backtest-first).** `scripts/backtest_trendmomo_pairs.py` (exact strategy rules, Kraken 1d, 2yr): DOT/ATOM/NEAR/LTC/AVAX structurally negative (PF 0.08–0.71, Σ−313%) while the kept ten are positive (Σ+863%; BTC 1.79 / ETH 3.03 / XRP 3.75). Same precedent as V4's majors curation. Also softens the 12-Jul "core −29%/4.5y" flag: recent-2yr core is strongly positive; the 4.5y number was the 2022 bear + the trendless five.
- **bot_learn v2 — L4 META-LABELING SHIPPED (the brain's first output bots trade on).** The brain now computes NUMERIC, REDUCE-ONLY per-(bot, enter_tag) stake multipliers from the era-filtered ledger — 0.5× (n≥30, negative, wr<25%), 0.75× (n≥30 wr<40%, or 15≤n<30 wr<25%) — STREAK-GATED (must recur 3 consecutive runs to publish; a recovered tag un-throttles immediately; v1 never sizes up) → bot_state `brain-stake-mults` (updated+ttl_sec freshness contract). Plus a **venue A/B report** every run (paper vs -lshadow/-lighter twins → lessons + state `venue_ab`) so the 13-Jul shadow books feed learning, not just the dashboard. ERA_START updated for the 13/14-Jul strategy changes. Live-data dry run: 121 trades, 9 A/B pairs, 0 mults published (correct — nothing clears the floor yet; the machinery is armed).
- **NEW `fleet_bus.py` — the strategies' read-side bus client (one file owns the fail-safe contract).** `stake_multiplier(bot, tag)` (clamped [0.5,1.0]) + `long_entries_blocked()` + `is_fresh()`; neutral on missing/stale/error; inert in backtests (no DATABASE_URL). All four active strategies consume it: brain multiplier in `custom_stake_amount`, and the **L2 fleet-risk long-budget veto ENFORCED in `confirm_trade_entry`** (side-specific long count — a blown short budget can never freeze long-only spot bots; fail-safe OPEN; checked before DayTrader's throttle so vetoes don't burn candle slots). Central kill switch: `FLEET_RISK_MODE=advisory`. Current book at ship time: 10/20 longs, green, veto idle. `fleet_risk.py` also gained the gate0 venue-aware counting main had missed (09-Jul entry described it; main never got it) + publishes `mode`.
- **L3: sniper intel on the bus.** `listing_sniper.py` publishes classifications to bot_state `listing-intel` every cycle (open book by class, ghost-skip tally, per-position class/venue) — scanner knowledge visible to brain/dashboard instead of dying in-process.
- **Config hygiene:** `bot_name` was stale copy-paste in all 8 active configs ("trend_golden_cross" on four different bots) — set to the real dashboard bot IDs; this is the identity fleet_bus uses for multiplier lookups. Wiring tests: 7-scenario fleet_bus/brain suite green (freshness, clamps, streak-gating, un-throttle, side-specific veto, venue A/B); fleet_risk simulated against the live pnl snapshot end-to-end.

## 2026-07-12
- **Fleet strategy audit → the cross-sectional factor programme → ⚖️ Counterweight shipped (shadow).** The audit found the fleet saturated with duplicated time-series signals: `perps-rsi-meanrev` and `perps-donchian-breakout` had CONVERGED on the identical entry (bottom 15% of the 20-candle range; 33 same-pair-same-day entries in 30d), four spot bots carried a copy-pasted bear_bounce sleeve, and NOTHING ranked coins against each other. Five cross-sectional factors were pushed through the validation doctrine on 150d real HL hourly data (both-halves + mirror + plateau + slip stress): momentum REJECTED (2/40, isolated — `scripts/backtest_xsect_momentum.py`), short-term reversal REJECTED (1/16), low-vol REJECTED (1/12) (`scripts/backtest_xsect_factor_lab.py`), funding-SLOPE rank REJECTED (0/16 — slope is entry timing, not a book; `scripts/backtest_xsect_slope.py`). FUNDING-LEVEL rank PASSED with a real plateau (8/12 green in both halves, whole 72h row, mirrors negative, survives 3x slippage). Shipped as **`lighter_funding_spread_bot.py` (⚖️ Counterweight, row `perps-funding-spread-lshadow`, service `counterweight-shadow`)**: rank 30-coin universe by 72h mean funding, LONG bottom-5 / SHORT top-5, $20 clips ($200 gross), 24h rebalance, market-neutral, funding accrued hourly, HL-backfilled warm boot, no per-position stop (backtest fidelity; 5% daily rail only), REFUSES lighter_live in v1. Publishes `ff_overlap` every loop to quantify kinship with the live Funding Farmer (booted 7/10 — watch it before citing this as a diversifier).
- **Decommissioned (user-approved): 🪃 Bounce Catcher + 🔺 Loop Scout.** Railway services `perps-bot` and `triangular-arb` stopped (`railway down`; empty shells left in the UI for manual deletion). Bounce Catcher ran the identical entry to Trail Blazer's paper bot and was REJECTED for durable edge (12 Jul audit) after being retire-flagged since 22 Jun; Loop Scout carried the 22-Jun retire verdict. Dashboard rows moved to RETIRED_ROWS (incl. the stale Gate-0 `perps-rsi-meanrev-lshadow`); `momo-bot` stays as the range-cluster representative; `yield-harvester-shadow` stays as the funding family's delta-neutral control.

## 2026-07-12
- **Fleet strategy audit → the cross-sectional factor programme → ⚖️ Counterweight shipped (shadow).** The audit found the fleet saturated with duplicated time-series signals: `perps-rsi-meanrev` and `perps-donchian-breakout` had CONVERGED on the identical entry (bottom 15% of the 20-candle range; 33 same-pair-same-day entries in 30d), four spot bots carried a copy-pasted bear_bounce sleeve, and NOTHING ranked coins against each other. Five cross-sectional factors were pushed through the validation doctrine on 150d real HL hourly data (both-halves + mirror + plateau + slip stress): momentum REJECTED (2/40, isolated — `scripts/backtest_xsect_momentum.py`), short-term reversal REJECTED (1/16), low-vol REJECTED (1/12) (`scripts/backtest_xsect_factor_lab.py`), funding-SLOPE rank REJECTED (0/16 — slope is entry timing, not a book; `scripts/backtest_xsect_slope.py`). FUNDING-LEVEL rank PASSED with a real plateau (8/12 green in both halves, whole 72h row, mirrors negative, survives 3x slippage). Shipped as **`lighter_funding_spread_bot.py` (⚖️ Counterweight, row `perps-funding-spread-lshadow`, service `counterweight-shadow`)**: rank 30-coin universe by 72h mean funding, LONG bottom-5 / SHORT top-5, $20 clips ($200 gross), 24h rebalance, market-neutral, funding accrued hourly, HL-backfilled warm boot, no per-position stop (backtest fidelity; 5% daily rail only), REFUSES lighter_live in v1. Publishes `ff_overlap` every loop to quantify kinship with the live Funding Farmer (booted 7/10 — watch it before citing this as a diversifier).
- **Decommissioned (user-approved): 🪃 Bounce Catcher + 🔺 Loop Scout.** Railway services `perps-bot` and `triangular-arb` stopped (`railway down`; empty shells left in the UI for manual deletion). Bounce Catcher ran the identical entry to Trail Blazer's paper bot and was REJECTED for durable edge (12 Jul audit) after being retire-flagged since 22 Jun; Loop Scout carried the 22-Jun retire verdict. Dashboard rows moved to RETIRED_ROWS (incl. the stale Gate-0 `perps-rsi-meanrev-lshadow`); `momo-bot` stays as the range-cluster representative; `yield-harvester-shadow` stays as the funding family's delta-neutral control.
- **Same-page sweep: every live-able strategy family now has its Lighter shadow twin (or an evidence-based reason not to).** Restarted 🎯 Perp Sniper as a proper Railway service (`perp-sniper-shadow`, Dockerfile.psniper — its Gate-0 shadow ran locally and went stale 10 Jul); already on: Tide Rider, Funding Farmer, Yield Harvester, Snap Back, Counterweight. NOT twinned, on evidence: Breakout Hunter FAILED its perp gate (`scripts/backtest_breakout_perp.py`: 1000d basket +74.6% spot but h2 -27.0% — the whole edge is the 2023-24 bull half; recent 150d -20.7% across the Lighter universe; funding drag 25pp on top) — which also FLAGS the spot bot itself (its "+20% core" was full-window only, the Trail Blazer failure shape); SwingDip core is flat-negative on the 4.5y replay; Trail Blazer's shadow would re-instate the twice-retired range-buy (stale Gate-0 lshadow row hidden). Freqtrade family bots (mum/dad/avo-maria/georgia) are not portable (third-party 5m strategies). Remaining port candidate if wanted: Two-Way Tide (regime-switch) — the only long/short trend family without a twin.
- **High-leverage operator search — BOTH candidate families REJECTED; nothing ships (doctrine held).** Asked to "redesign the day trader as a high-leverage operator". Candidate 1, intraday long/short breakout on the 8 tight-slippage Lighter majors (`scripts/backtest_leverage_operator.py`, hourly closed-bar Donchian break + ATR trail + time stop, honest leverage/liq/funding mechanics): 0/8 plain, 0/8 trend-gated, 0/8 long-only (fails mirror), 1/8 squeeze-gated — and that lone cell sits next to a -22% cliff and dies under 2x slip stress. Diagnostic worth keeping: FADING short-lookback breaks is catastrophic (mirror -43..-48%), so the information is real but whipsaw+slip bleed it faster than any tested exit captures it. Candidate 2, leverage on the VALIDATED funding-spread book (`scripts/backtest_overdrive.py`, intrabar replay): the short leg carries squeeze-blowup tail — a ~90% adverse intrabar excursion inside one 24h hold (liq even at 1x isolated), 5 liqs at 3x, 20 at 5x; adding the Farmer-standard 10% stop removes all liqs but kills the carry (h1 negative at every gear — stops whipsaw out of the mean-reverting positions that pay). STRUCTURAL conclusion: the funding edge is tail-risk compensation; levered you either eat the tail or overpay the insurance. Corollary recorded: Counterweight must stay unlevered and stop-free (both knobs now evidence-rejected).
- **TrendMomoV1 (crypto-trendmomo-4h) RETIRED — the audit's obvious loser, user-approved.** Core leg -29%/4.5y on the tagged replay (26.5% win), live bleeder (-$11.33). Dropped from run_all.sh (landed on main so freqtrade-bots redeploys without it) + dashboard RETIRED_ROWS (gate0). The fleet's spot lineup is now V4 trend / V5 day-trader / V6 swing / V7 breakout.
- **bear_bounce sleeve RETIRED from all four spot strategies (backtest-first, per repo rule).** The 07-03 "adaptive" downtrend half-stake bounce sleeve — copy-pasted into DayTraderV5Gated, SwingDipV1, MomoBreakoutV1, TrendMomoV1 — was replayed tag-attributed on 3.5–4.5y of Binance data (docker freqtrade, `~/freqtrade/user_data/config_bb_audit.json`): **negative in ALL FOUR carriers — 19 entries, −$7.27 aggregate, 26% win — and it never fired once in live paper** (zero `bear_bounce` rows in bot_trades since ship). Removed the entry legs + sleeve-only custom_stake/custom_exit branches + dead indicators; post-edit verification backtests show surviving legs unchanged (SwingDip dip_in_uptrend byte-identical 250 entries; DayTrader legs identical counts; MomoBreakout breakout +$201.8). Bots now stand down in downtrends, as originally validated. FLAGGED for separate decisions (not acted on): TrendMomoV1's core leg backtests −29%/4.5y (26.5% win) and DayTraderV5Gated's three surviving legs are all fee-bled negative on the same window (−27.6% total), while MomoBreakoutV1's core is the only positive spot core (+20%); ~/freqtrade local copies predate the sleeve and were left untouched.

## 2026-07-11
- **Funding Farmer: multi-factor POSITION SCANNER — picks the best risk-adjusted positions, not just the hottest funder.** Replaces the naive raw-|APR| sort (which spent slots on thin, high-vol, trending-against alts that run the 10% stop before funding pays — why it was break-even). Designed via a fan-out+judge+synthesize workflow (6 philosophies, adversarially judged) grounded in the real venue data surface, then PROVEN on a new portfolio backtest (`scripts/backtest_scanner.py`, 150d, 6-slot). Two layers: (1) BACKTESTED CORE — a realized-vol + adverse-trend VETO (skip stop-out traps, leave the slot empty) + an |APR|-backbone bounded-discount rank (downside-capped, never a vol divisor); (2) LIVE-ONLY overlay (shadow-validated) — cross-venue `_bench` sign-agreement + full-depth clip-slippage gate + book-imbalance tiebreak. Cheap funding prefilter → candle-scan top 15 (cached ~50min) → book-probe only top 5, sized to the ~21 weight/min governor (Railway ws is CDN-blocked so every book = 1 token). RESULT at the **default 0.40 gate**: net **+52%** ($21.6→$33.0/clip/150d), ret/DD **2.0→5.3**, maxDD **−43%**, FEWER trades (282 vs 336) — robust across both halves + OOS. HONEST: much of the edge is directional MEAN-REVERSION (regime-dependent), not extra funding — the vol veto is the durable piece; funding is thin. Gate-widening to 0.25 (+155% in-cache) is OPT-IN (`SCAN_ENTER=0.25`) and partly a flat-slippage artifact — shadow-validate first. `SCAN=off` restores legacy. Every entry logs scan evidence to `venue_orders`. Shipped core == backtested core (same veto/score/lookbacks). Adversarially reviewed (4-lens workflow, findings verified): 3 confirmed fixes — the live features were computed on the in-progress forming hourly bar (now dropped → closed-bar only, matching the backtest + no look-ahead); the feature cache is now keyed on the last CLOSED hour (a fresh vol spike is picked up next loop, not ~50min later); the top-15 prefilter divergence documented + env-tunable — plus 2 hardening (defensive best-first book sort for the Railway REST-fallback; scan wrapped so a bug can't crash stop-management). One "medium" (governor starvation) was verified a FALSE POSITIVE (scan runs after stop-management + degrades to no-entry + fail-safe flatten). Shadow-first — nothing armed.
- **Both-perp (self-custody) Yield Harvester hedge — BACKTESTED and REJECTED, not built.** Asked to take the delta-neutral Yield Harvester (`perps-funding-carry`) live on Lighter; it can't (perp-only venue has no same-coin hedge — the code hard-refuses a naked-perp funded run). Chosen self-custody alternative: hedge a hot alt's funding leg with a beta-weighted ETH/BTC perp. `scripts/backtest_carry_hedged.py` (150d real HL funding+price, reuses `.dirfund_cache.json`) compares directional vs major-hedge vs ideal spot-hedge on IDENTICAL signals. HONEST RESULT: the hedge is WORSE than the directional Funding Farmer it was meant to improve (+$5.5 β·ETH vs +$7.9 directional per $25 clip/150d). Structural, not tuning: the funding-receiving side's price P&L is already ~flat (little market-beta to strip), so the hedge mostly adds double slippage + its own funding drag, while the idiosyncratic alt risk it CAN'T hedge is the part that matters; higher gates go negative. BTC-hedge cuts drawdown (ret/DD 0.66->0.75) but at lower absolute profit + double the live surface. The only profitable delta-neutral structure remains the CEX-spot hedge (docs/cex_spot_hedge_scope.md). Verdict per repo rule (no ship without a proving backtest; cf Trail Blazer): DID NOT build the live bot. See memory both-perp-hedge-no-edge.
- **Funding Farmer (`perps-funding-lighter`): go-live prep — re-validated, shadow evidence checked, docs corrected.** Pivoted to taking the already-built DIRECTIONAL Funding Farmer live instead. Re-validated: `--once` runs clean (231 perps, no orders); proved `lighter_live` REFUSES to boot without BOTH `REAL_MONEY_KILL=DISARMED_I_UNDERSTAND` and `PERPS_FUNDING_LIGHTER_MAX_NOTIONAL`. Shadow evidence (Postgres): `perps-funding-lighter-lshadow` deployed + running, measured perp-leg slippage on traded coins is SUB-1bps (ETH 0.3–0.5, SOL 0.13, BTC 0.36, HYPE 0.92) — zero-fee + spread-gate thesis holds; only ~2 closed round-trips so P&L sample still thin (~net flat, as expected for ~break-even directional). Fixed GO_LIVE_LIGHTER.md env drift: the example still showed the pre-tuning `FUNDING_HARD_STOP=0.05` / `MAX_SPREAD_BPS=50` (a 5% stop LOSES) — corrected to the tuned defaults + a don't-lower warning, and added a dated shadow-readiness note. Keys/disarm/deposit remain the operator's — nothing armed. NOTE flagged: `crypto-trend-daily-lighter` (Tide Rider LIVE) is crashed/error ~11h (likely auth), separate from this work.

## 2026-07-10
- **Tide Rider on LIGHTER — new bot `lighter_trend_bot.py` (crypto-trend-daily, 1x long perp).** Re-expresses the Kraken-spot 50/200 golden-cross trend follower as a 1x LONG PERP on Lighter (venue layer; lighter_live auto-refused until disarmed). RE-VALIDATED with the funding drag (`scripts/backtest_tide_rider_perp.py`): basket +52% spot -> +40% perp over 2.7yr (funding drag -13pp, erases the down-trend protection) — viable but weaker than Kraken spot; the drag is MODELLED in shadow so the P&L is honest. Long-only golden(enter)/death(exit) cross + catastrophic 35% seatbelt. Adversarially reviewed: 4 fixes (funding accrual now runs in LIVE too so the ledger/win-rate include the drag; signal on CLOSED bars only — drop the forming candle; catastrophic-stop seatbelt fires even when candles fail; hl_paper daemon refused to avoid clobbering the live Freqtrade Tide Rider dashboard row). Dashboard shows it as "🌊 Tide Rider · SHADOW/LIVE" automatically.
- **Funding Farmer: risk defaults TUNED on a directional backtest (`scripts/backtest_directional_funding.py`).** Replays the exact directional logic on 150d of real HL funding+price (30 coins, intrabar stop fidelity). HONEST RESULT: the strategy is only ~BREAK-EVEN — funding capture is real (+$8/150d per $25 clip) but directional price risk nearly offsets it (-$6.5), as expected when funding compensates the risk it carries. Least-bad + most-robust config = WIDE stop + TIGHT take-profit (a tight 5% stop whipsaws out on noise and loses -$10.6; 10-12% stop breaks even). Tuned defaults: HARD_STOP 5%->10%, keep TAKE_PROFIT 4%, MIN_VOL $5M->$10M, MAX_SPREAD_BPS 50->20 (live Lighter shows hot+liquid candidates all <=3.5bps, so 20 is safe and excludes traps). Verdict: run tiny/shadow — the genuine edge needs the delta-neutral CEX-spot hedge (deferred), which keeps the funding WITHOUT the price risk.
- **Funding Farmer (Lighter directional funding) — new bot `lighter_funding_bot.py` (perps-funding-lighter).** "Unlock live harvesting" on Lighter only (no cross-venue hedge). A single perp-only venue can't be delta-neutral, so this captures funding DIRECTIONALLY: short positive-funding perps / long negative, collect Lighter's zero-fee funding, price risk bounded by a HARD STOP (not a hedge). Reuses the venue layer end-to-end (hl_paper|lighter_shadow|lighter_live) so lighter_live is auto-refused until REAL_MONEY_KILL is disarmed + a PERPS_FUNDING_LIGHTER_MAX_NOTIONAL cap is set. Exits: hard stop / take-profit / funding-flip / funding-decay / max-hold. Liquidity filter (vol) to avoid thin-book traps (WEN 870bps). Funding accrued in paper/shadow; live reads it from account equity. Self-test caught+fixed a CRITICAL inverted-stop-sign bug before ship (stop now fires on adverse moves). Dashboard: "💸 Funding Farmer". Adversarially reviewed. HONEST: directional (real price risk), shadow-first.
- **Yield Harvester (funding-carry): shadow EXECUTION path built (shadow-first, no live orders).** `funding_carry_bot.py` gains `_perp_leg_fill(...)` — in `VENUE=lighter_shadow` it MEASURES the real perp-leg slippage by walking the live Lighter book (mid-referenced, adverse-only, one `venue_orders` evidence row per fill); in hl_paper it models the flat PERP_FEE (zero behaviour change). Funded modes (testnet/live/unknown) are HARD-REFUSED via a fail-safe allowlist — a perp-only venue has no automated hedge, so a funded run would be a naked perp. Adversarially reviewed (workflow): 3 findings fixed — slippage referenced to book mid not the stale frozen mark; decay close-gate re-checks payback on MEASURED cost; allowlist (not blocklist) guard. First live read: liquid hot coins ~3–7bps round-trip @ $300 (ZEC/COIN/BB), thin/exotic ones are traps (WEN 870bps) → a live harvester needs a liquidity/slippage gate the shadow ledger will calibrate. See docs/lighter.md + memory funding-carry-structural-edge-lighter.
- **Yield Harvester (funding-carry): Lighter zero-fee backtest — REAL friction-bound edge confirmed.** `scripts/backtest_funding.py` replays the exact live bot logic on 180d of real HL hourly funding across 39 coins, sweeping the fee model HL(29bps)->Lighter+CEX-hedge(20bps)->both-perp(3bps)->0. Win rate climbs 28%->40%->81%->98% as fees fall — the signal is right, fees were the killer (OPPOSITE of Trail Blazer). Net/180d on deployed capital, 40% gate: HL +1.0%, Lighter+CEX-hedge +1.9% (~3.8%/yr market-neutral), both-perp +3.5%. Break-even friction ~39bps. Lowering ENTER_APR 40%->20% lifts net ~35% ON both-perp only; at 20bps-CEX-hedge a 15% gate goes NEGATIVE — so keep 40% gate unless hedging with a second Lighter perp. Edge concentrated (ME/JTO/INJ). Bot is still a paper simulator; live harvest needs a Lighter execution path (shadow-first). See memory funding-carry-structural-edge-lighter.
- **Trail Blazer: 200-EMA regime gate (REGIME_GATE_FRAC=0.97)** — only enters dips within 3% of the 200-EMA (a pullback in an uptrend), skipping falling-knife dips below trend (logged REGIME_SKIP). Backtest-tuned (scripts/backtest_regime.py): the loose -3% gate roughly HALVES the out-of-sample loss vs no gate (-7.7%->-3.3%) and trades more than the strict gate. Risk control, NOT an edge — the strategy is still net-negative even gated (see memory trail-blazer-no-durable-edge). Widening the universe was tested and REJECTED (made OOS worse -3.3%->-8.7%). Env-tunable; 0.0 disables.
- **live trade logging**: the live (lighter_live) order path now records every real open to venue_orders and every real close to venue_orders + paper_trades (venue='lighter', shadow=false, realized P&L, raw exchange response) — so the Lighter Live dashboard card shows trade history, win/loss and per-trade P&L exactly like the paper bots (previously only equity-based total P&L). Ledger writes are guarded separately so they can never mask an order failure. Also fixes a latent bug: live opens now set entry_ts (the 72h time-stop was inert on the live path). Applied to hyperliquid_momo_bot.py (Trail Blazer).
- **live bots no longer die silently (the 'stale' bug)**: when a lighter_live/testnet bot fails venue init (e.g. "private key does not match the one on Lighter"), venue_context now publishes an explicit status=error row with the reason before re-raising — so the dashboard card shows WHY the bot is down instead of just ageing into a stale flag. NEW `scripts/lighter_test_trade.py`: user-run order-path test — default places a POST-ONLY limit 50% below market (cannot fill) then cancels it (sign→submit→cancel proven, ~$0); `--fill` does a real ~$10 market round-trip.
- **live P&L measured from starting collateral, not the $1000 paper baseline** — a live Lighter bot funds from real collateral (e.g. $65), so the dashboard was showing an untraded live account as -$935 (equity - $1000). Fix: the live path records its starting equity once (persisted in bot_state `<bot>-lighter:live`) and publishes pnl_abs = equity - baseline (→ $0 until the first trade); the dashboard also never applies the $1000 paper baseline to `is_live_bot` rows. Applied to momo + perps bots.
- **venues/lighter_client.py — ws hardening**: Lighter's CDN 400s the order-book WebSocket upgrade from cloud (datacenter) IPs though its REST works from the same host, so the live bot spammed reconnect warnings. Now the ws presents a browser-like handshake (Origin + Chrome UA) as a best-effort, and after 3 quick retries goes QUIET (retries every 10 min, logs once) — orderbook() falls back to governed REST snapshots, which work fine for order placement. No functional impact either way.
- **Dockerfile.perpslive**: dedicated image for the bounce-catcher-live Lighter service (hyperliquid_perps_bot.py; same inert-until-disarmed pattern as Dockerfile.momolive).
- **dashboard: dedicated "⚡ Lighter Live" section** — real-money `<bot>-lighter` rows are pulled out of the paper grid into their own bordered, red-accented section at the top of the page (header shows live equity · P&L · bot count + a REAL MONEY tag), so live money is never lost among the paper cards. Render-tested. `perps-donchian-breakout-lighter` went live this session ($64.93, Trail Blazer on Lighter).
- **venues/lighter_client.py — live-path fix**: `SignerClient.__init__` builds an aiohttp `ApiClient` internally (→ `asyncio.get_running_loop()`), so constructing it in the synchronous `LighterClient.__init__` crashed the LIVE/testnet path with `RuntimeError: no running event loop` (never hit before because the signer path can't be tested without a key). Now the signer is constructed ON the client's loop thread via `run_coroutine_threadsafe`, same as the read-only ApiClient in `_build()`. Verified with a dummy key: construction now reaches auth (fails cleanly on key validation, not the loop). Shadow/paper paths unaffected.

## 2026-07-09
- **Dockerfile.momolive + trail-blazer-live service scaffold**: dedicated image for Trail Blazer's LIVE Lighter service (CMD baked in, funding-service pattern; select via RAILWAY_DOCKERFILE_PATH=Dockerfile.momolive). Deployed inert by design: VENUE=lighter_live with REAL_MONEY_KILL armed refuses to boot until the user supplies LIGHTER_API_PRIVATE_KEY and explicitly disarms — the user-only steps.
- **fleet_risk: venue-aware (no-miss-sync for go-live)** — the fleet risk light + signal bus counted directional exposure and read funding by BASE bot name, so once a bot goes live on Lighter (publishing as `<bot>-lighter`) the live book would be INVISIBLE to the fleet view and funding would be read from the wrong venue. Now `authoritative_row()` prefers the live Lighter row over the paper twin (counts real Lighter exposure, no double-count), the shadow/testnet cohort is surfaced as `info_only` (modelled, never moves the light), and the signal-bus funding prefers the Lighter-facing funding-carry row + tags `funding_source` (HL vs Lighter funding genuinely differ). Verified against the live DB: shows `shadow(info) 5L/0S` from the running shadow cohort. regime_oracle deliberately left on HL (majors' daily regime is venue-identical, nothing consumes it yet, and Lighter's candle REST is WAF-blocked — moving it would force the Lighter SDK into the shared freqtrade image for ~zero benefit); cross-exchange/triangular arb are inherently multi-venue and unchanged.
- **NEW bot `lighter_perp_sniper.py` (🎯 Perp Sniper) — Lighter-native new-listing sniper, SHADOW-FIRST**: replaces the spot Launch Sniper for the Lighter go-live (the spot sniper can't run on a fixed-market perps DEX). Detects freshly-listed perp markets by diffing the active market set against a persisted baseline (source of truth; `AnnouncementApi` text is context only), seeds the baseline on first run (never snipes the existing 215), then snipes new listings long with TP +15% / SL −10% / 6h max-hold + global cap, all through the venue layer (Lighter-only; defaults to `lighter_shadow`, refuses `hl_paper`). UNVALIDATED by design — runs shadow (modelled fills on live books → `venue_orders` ledger) until Eamon disarms the kill switch. Verified: seed cycle (198 active markets baselined, 0 sniped) + a simulated new-listing detection that shadow-snipes ADA/WIF crossing the real book (~3–4 bps entry slippage). Registered on the dashboard (EXPECTED + label); `railway.lightersniper.toml` deploys it from the base Dockerfile.
- **Dockerfile.funding**: COPY `paper_broker.py` + `venues/` — funding_carry now imports the venue layer, so the lean image needs them (validated by importing the bot with only the Dockerfile.funding file set present).
- **Lighter.xyz venue layer (Gate 0) — `venues/` package**: NEW `VenueClient` abstraction (place/cancel/amend, positions, balances, funding, orderbook, candles, ws) with `HyperliquidClient` (byte-identical passthrough of the old inline HL logic) + `LighterClient` (lighter-sdk 1.1.1). `VENUE` env selects `hl_paper` (default, unchanged) | `lighter_shadow` | `lighter_testnet` | `lighter_live`. Trail Blazer (`hyperliquid_momo_bot.py`), Bounce Catcher (`hyperliquid_perps_bot.py`) and Yield Harvester (`funding_carry_bot.py`) refactored onto it. **Parity proven**: HL candles byte-identical old-vs-new; one-cycle decision diff = 0 for Bounce Catcher, and only the 2 intended cap-clips for Trail Blazer. Both pilots ran one clean shadow cycle on LIVE Lighter mainnet books.
- **Trail Blazer global position cap (pre-live blocker)**: `hyperliquid_momo_bot.py` gains `MAX_OPEN_POSITIONS=8` + `MAX_NEW_PER_LOOP=2` gated on a live open count (the same pattern Bounce Catcher got Jul-5) — it was holding 15 concurrent longs alone vs the fleet budget of 20. Valid buys blocked by the cap log `CAP_SKIP`. In lighter modes the cap derives from the per-bot notional cap.
- **Adapter safety rails (`venues/safety.py`, `venues/governor.py`)**: `REAL_MONEY_KILL` ARMED by default — `lighter_live` refuses to start and flattens+halts mid-run unless set to `DISARMED_I_UNDERSTAND` (checked EVERY loop); per-bot notional caps from env (no growable code default); `LIGHTER_MAX_DAILY_LOSS` flatten-and-halt. Weighted TX-budget governor for the 60-req/min standard tier (order tx weight 6, exponential backoff on 429/405), ws-first market data with one shared reconnecting book cache.
- **Shadow mode (`venues/shadow.py`)**: `ShadowBroker` fills paper orders by crossing the LIVE Lighter book (walk asks for buys, bids for sells) and logs every order to the new `venue_orders` Postgres table (decision px vs modelled fill px, spread/slippage bps, raw response) — realizable-spread evidence for sizing. Never sends.
- **bot_pnl_store**: `paper_trades` gains `venue`/`shadow` columns; NEW `venue_orders` table + `publish_venue_order()`. All guarded (never raise into a loop).
- **dashboard: LIVE distinction + separate live P&L total** — venue-variant rows (`<bot>-lighter`/`-ltest`/`-lshadow`) are detected dynamically, badged (LIVE red / TESTNET amber / SHADOW blue), kept OUT of the paper crypto headline, and summed into their own **🔴 LIVE · Lighter** totals line + `/pnl.json` `meta.live_fleet`. Real-money live P&L folds into GRAND TOTAL; shadow stays out (modelled).
- **day-1 verifications → `docs/lighter.md`**: 215 mainnet markets, ZERO fees, $10/order min, ETH/ADA/HYPE listed (Cowork excerpt was wrong on ADA/HYPE), BLUR/ATOM/INJ/ORDI/TON unlisted, kBONK↔1000BONK / PEPE↔1000PEPE remaps, native cross-venue funding (lighter+HL+binance+bybit in one call), signer binaries verified on linux/amd64 AND Mac arm64, ws ~800ms. Scripts: `scripts/lighter_day1.py`, `scripts/lighter_testnet_smoke.py` (6/6 read-only pass), `scripts/lighter_accounts.py` (read-only sub-account mapper). `GO_LIVE_LIGHTER.md` = sub-account-per-bot user-only checklist. NO keys, NO funds, NO `lighter_live` deploy — that stays with Eamon.

## 2026-07-08
- **Dockerfile.dashboard**: COPY fleet_watchdog_svc.py into the image (follow-up — the explicit COPY list didn't include the new watchdog module, so /watchdog.json 500'd with ModuleNotFoundError while the guarded thread kept the service healthy).
- **fleet_watchdog_svc + dashboard: in-service fleet watchdog (C layer)** — daemon thread in pnl-dashboard probes its own /pnl.json every 5 min; transition-based alert emails via report_emailer (dormant until SMTP_* set; 30-min repeat gap; recovery notice on clear); warnings for open-positions>20 and daily loss<-$100; live state at `/watchdog.json` (read-only, no secrets). Complements the new GH-Actions `fleet-watchdog` workflow (external, ~30-min, single-issue transition alerts — first probe caught perps-funding-carry STALE + 58/20 exposure, issue #31). Read-only towards the fleet.
- **sniper Jul-7 −$29.06 root-caused from the paper_trades ledger (NOT old-inventory flush)**: 48 `delisted` churn closes −$11.56 — ALL opened Jul-7 itself, in 5 batches (03:29/04:44/06:47/12:06/18:55 UTC), the last three AFTER the wave-2 blocklist deployed 06:23 UTC, using tickers the curated set missed — plus one legitimate GROVE/EUR stop_loss −$17.50 (gate passed, stopped in 15min: strategy working). TOKENIZED_BASES extended with the 19 observed wave-3 leakers (RKLB/MRVL/SONY/HYUNDAI/TQQQ/EWZ/HIMS/ASTS/NBIS/XCU…). Marker in code: if a FOURTH wave leaks, stop extending the list and build the per-exchange listing-flood quarantine instead.
- **freqtrade_pnl_poller**: open_pos cap 6→12 — breakout-4h held 7, card listed 6; worse, fleet_risk.py counts longs from this list, so truncation silently under-reported fleet exposure to the Jul-14 enforcement review. (The "8 open / 6 listed" flag was this cap, NOT a max_open_trades breach.)
- **bot_pnl_store + dashboard: publisher-sent pnl_daily now real end-to-end** — bot_pnl gains a `pnl_daily` column (ADD COLUMN IF NOT EXISTS), publish() accepts it, and the card's "Today P&L (UTC)" prefers it over the server-side equity-curve delta — but only while the row was written today (UTC), so once-a-day publishers can't show yesterday's figure all day. Built for the alpaca anomaly: its +$10,673.77 "today" was the equity-delta vs a glitched Jul-6 88k snapshot (the −$19.9k anomaly's mirror), while its "lifetime −$5,365.91" was actually Alpaca's day P&L published into pnl_abs (fixed in the equities-momentum-alpaca repo: pnl_abs → lifetime vs $100k baseline, day P&L → pnl_daily).

## 2026-07-07
- **pair-web coverage (ledger + liquidity audit)**: perps bots (rsi-meanrev, donchian) universe 30→33 (+HYPE #4-by-volume/$359M-day with zero prior fleet coverage, +ZEC, +TAO); regime-switch whitelist 10→12 (+HYPE, +ZEC — two more liquid breakdown candidates for the armed short engine); regime-oracle universe → 16 to match; spot v5/v6/v7 24→29 (+TIA/ARB/WIF/BONK/PEPE — the mid-cap cluster already earning on the perps ledger: NEAR +$10.18/kBONK +$10.02/TIA +$4.87/WIF +$4.87/ARB +$4.00); v4 BNB/USD→ADA/USD (Kraken lists no BNB — pair was silently dropped at boot); sniper TOKENIZED_BASES extended with the second tokenized-equity wave (CSCO/DELL/ADBE/GLW/COHR/CRDO/SNDK/ADI/PLAY…, 0 wins ≈−$7 in ledger). Position caps unchanged everywhere — more signal candidates, same risk budget. Family bots deliberately untouched (era-zero measurement).
- **fleet_risk / regime_oracle (day-zero review catches)**: signal-bus snapshots now historized to bot_state_history (Jul-14 review needs them to judge L3); SUI added to the oracle universe (fleet holds it, oracle didn't cover it).
- **dashboard: UNIFORM CARDS** — every bot card now shows the same information set regardless of publisher quality: `Today P&L (UTC)` under Total (equity-curve delta since UTC midnight where a curve exists, else trades-closed-today), Win/Loss from the union ledger when a bot publishes NULLs (the perps bots), detailed `Last close` (pair · P&L · reason · age) for every bot, and open positions for bots whose book lives in bot_state (perps broker pos incl. mark-based uPnL, funding carries with net accrual). One cached ledger pass (`fetch_ledger_enrich`, 20s TTL); `/pnl.json` gains a per-bot `enrich` object for downstream consumers.
- **cross-bot layers 1-3 (ADVISORY)**: NEW `regime_oracle.py` — one shared per-major daily regime (EMA200+slope direction, ADX 17/11 hysteresis character, HL closed daily candles) published to bot_state `regime-oracle` + append-only `bot_state_history`, fail-safe TTL 2h. NEW `fleet_risk.py` — fleet-wide directional position count vs budgets (20L/12S) -> traffic light in `fleet-risk` incl. pair-pileup detection (the 26-position dip scar), plus scanner exhaust mirrored to `signal-bus` (funding APRs, cross-exchange dislocation, tri-arb depth, pulse mood). Wired into run_all.sh (30m/5m loops) + Dockerfile; `bot_pnl_store` gains `fetch_bot_pnl()` + `save_history()`. Publish-only week one; enforcement (entry veto / sizing) wired from the recorded history after ~7 days.
- **config_v7_momo (crypto-breakout-4h)**: max_open_trades 10→6 — held 10 correlated longs in one bounce with no cap (same risk class rsi-meanrev was capped for Jul-5).
- **RegimeSwitchV2**: per-pair per-4h regime diagnostic log (dir/trend/ADX/close/channels → verdict). Idle 6 days through two blind gate cuts; now the logs say why.
- **listing sniper**: DELISTING-RISK GATE — 'ghost' listings (junk intel AND first venue outside the top-15) skipped entirely; junk elsewhere quarter-stake (was half). Targets the 142/177 `delisted` exits (−$47 lifetime) while keeping the lottery tail reachable.
- **funding_carry_bot**: EXIT REBUILD (0W/28L — every close realized ≈ round-trip fees): decay alone no longer closes; closes only on flip persisting ≥1h, decay AFTER fee payback (net ≥ +$0.10), 14d expiry, or a −2%-of-notional bleed stop. NEW 6h persistence entry filter (|APR| must hold ≥ ENTER_APR for 6h — spike-and-revert funding never passes). hot_since persisted to bot_state.
- **Dockerfile.freqtrade**: COPY bot_learn.py — the learning brain has been dead in deploy since Jul-5 (run_all.sh runs it 2-hourly; the file was never in the image: `can't open file '/freqtrade/bot_learn.py'`).
- **OPTION-B ISOLATION**: family bots (mum/dad/avo-maria/georgia) removed from the freqtrade-bots container; each runs ONLY in its dedicated Railway service via run_all.sh `ONLY_BOT` mode (one bot + FT_POLLER_BOTS-scoped poller). Root cause fixed: the 4 services built from Dockerfile.freqtrade were EACH running all nine bots (fresh $1000, no volume) + a 9-name poller — five pollers race-writing bot_pnl (the Jul 5-7 "counter resets"/equity flapping; Jul-6 trade contamination). Poller _DEFAULT_BOTS back to the 5 originals; stale family comments fixed.

## 2026-07-06
- **funding_carry_bot**: raise ENTER_APR 20%→40%, EXIT_APR 8%→15%, lower MIN_DAY_VOLUME $5M→$2M, extend MAX_HOLD 7→14d — fix fee bleed on fast-decaying carries
- **funding_carry_bot**: raise MAX_POSITIONS 5→8, NOTIONAL $200→$300 — expand reach and earning capacity
- **SwingDipV1**: buy_rsi default 35→42 in strategy code (config key was silently ignored)
- **MomoBreakoutV1**: entry_lookback default 30→20 (shorter Donchian for more frequent signals)
- **RegimeSwitchV2**: ADX trend gate 20→17, chop gate 14→11 (admit weaker trends)

# Changelog

## 2026-07-07
- dashboard: **reset-proof "All-time Σ (ledger)" row on every bot card** — lifetime P&L + closed count straight from the durable `bot_trades` ledger, so a live-DB wipe can never again masquerade as "no trades" (July-6: Range Raider's card showed $1000/0 while the ledger held 8 real post-rework trades, 3W/5L −$2.47). If this row and Total P&L disagree, a reset happened.
- investigated Range Raider "no trades since rework": logs CLEAN (no errors/OOM/crash-loop); the regime-legs fix DID trade (8 closes 07-05→07-06 03:00); live counter wiped ~July-6 during the family-bot restructure deploys (exact mechanism aged out of the log window). Retracted the suspected mum/8089 port collision — regime-switch polls in its own container via FT_POLLER_BOTS.

Shared log of every change that affects a running bot, the dashboard, or deploy
behaviour. **Multiple Claude sessions work this repo in parallel** — this file is
how they stay in sync. If your diff touches bot code (`*.py`, `*.sh`, a strategy,
a config) you MUST add a dated entry here in the same commit (a CI check enforces
it). Newest first. Keep entries one line; link the commit if useful.

## 2026-07-05 (later)
- ⚡ Range Raider (`DayTraderV5Gated`): DIAGNOSED the "no trades since rework" — not a bug (logs show a clean load, no populate_indicators error); it was OVER-GATED: every mode needed a pullback AND an uptrend on the same 1h candle, a rare confluence, so it sat idle in this choppy market. FIX = two new regime-covering legs so it operates regularly: `trend_breakout` (ADX≥25 + close>EMA50 + break of 20-bar high — trades RISING phases; grounded in V7's Donchian edge; `confirm_trade_exit` vetoes the range-top sell so breakouts run) and `range_meanrev` (ADX<20 chop + buy the range low, no uptrend requirement — the engine for a choppy market; low-ADX gate is the research-backed guard against knife-catching). Both fee-band gated; range_meanrev half-stake + 2.0×ATR stop. Distinct tags so the brain measures each.

## 2026-07-05
- **NEW bot: `RegimeSwitchV2`** (⚖️ Two-Way Tide) — rebuilt `perps-regime-switch` from the fleet's combined lessons + evidenced public practice (AQR trend-following, Hyperliquid docs). The fleet's only dual-direction engine: 4h Donchian breakout LONG in up-regime / SHORT in down-regime (mirrors the proven V7 PF~1.8 edge to finally profit from a downtrend). Per-pair daily regime (EMA200+slope, ADX gate w/ hysteresis), structure-break exits (let winners run — not trailing/ROI cap), wide ATR disaster stop, inverse-vol sizing, low leverage, correlation-aware global cap, pulse half-stake, Postgres persist. UNVALIDATED (no OHLCV to backtest) — dry-run, robust-by-construction, brain-watched.
- `RegimeSwitchV2`: tuned for more frequency — Donchian 20/10→15/8, ADX gate 22/16→20/14, universe 2→10 liquid majors, max_open 3→6 (still <60% of book). Stays on fee-viable 4h (not 1h churn).
- dashboard: **trendy + descriptive bot names** (emoji + plain "what it does") replacing the technical labels.
- ci: deploy path-filter tracks `RegimeSwitchV[0-9]+.py` so future V-bumps auto-deploy the regime service.
- dashboard: **Total P&L on every bot card** — freqtrade bots publish `pnl_abs=None` (gain is `equity−$1000`), so they showed no P&L line; now computed from equity for all paper bots.
- learning: **brain now covers ALL bots** — `bot_learn.py` read only `bot_trades` (freqtrade); added `bot_pnl_store.fetch_paper_trades()` and merged it so perps + sniper closes (in `paper_trades`) are analysed too. The `reason` field splits into long/short enter_tag + exit_reason, giving the brain long-vs-short expectancy.
- day trader (`DayTraderV5Gated`): widen buy zone 0.22 → 0.37 (enter higher up the pullback for materially more fills; captured move thinner ~0.41×band, still fee-positive). Doubled live bounce eligibility 0→2 pairs.
- day trader (`DayTraderV5Gated`): loosen risk-off band gate 2.2% → 2.0% (more relief-rally pairs, still above fee floor).
- day trader: add third entry mode `bounce_pullback` — trades the relief rally in a risk-off regime when the pair's own 1h EMA50 is above price AND rising; half stake, fast exits. Fixes the "idle in a bounce" gap.
- perps `rsi-meanrev`: global position cap (`MAX_OPEN_POSITIONS=6`, `MAX_NEW_PER_LOOP=2`) gated on a live open count — was holding 14 longs (70% of book) in one correlated dip with no cap.
- ops: schedule the learning brain (`bot_learn.py`) every 2h in the freqtrade supervisor (`run_all.sh`) — it was never scheduled, so it never accumulated the 3 runs needed to promote a lesson to actionable.
- ops: add `CHANGELOG.md` + CI enforcement + `CLAUDE.md` session-coordination rules (this change).

## 2026-07-04
- Dashboard v2: health checks, per-mode (enter_tag) W/L/P&L rows, brain card, BTC regime chip, sparklines, pulse strip.
- sniper: throttle — stake 100→50, max-open 25→10, SL 50%→35%, max-hold 48h→24h, spread gate 500→250bps, depth 3×→5×, vol floor 25k, anti-chase 150%→100%.
- sniper intel (`listing_intel.py`): exchange announcement feeds + CoinGecko footprint, half-stake on junk listings.
- pulse: extend panic sizing to V6/V7 bounces; pulse history records funding APRs.
- brain v1.1: era awareness — hypotheses only from current-strategy-era trades.

## 2026-07-03
- Adaptive dual-mode strategies (V5/V6/V7/V8): BTC-4h 50/200 regime switch (risk-on pullbacks / risk-off sweep-reclaim bounces).
- Durable equity across redeploys: `/freqtrade/persist` volume + Postgres state for perps/sniper; freqtrade auto-deploy unfrozen (persistence makes redeploys safe).
- `perps-funding-carry` bot added (dry-run Hyperliquid funding-rate harvester).
- `market_pulse.py` news/social/funding feed → Postgres; `bot_learn.py` brain added.
- `perps-regime-switch` moved to Donchian-style entries; auto-deploys on its own file changes.

## Before 2026-07-03
See git history. Key milestones: fee-bleed + close fixes for perps (07-01), the
persistent-volume saga (07-01), day-trader fee-gate/trend-gate (07-01/02),
unified pnl-dashboard + Postgres publishing (06-19), multi-bot freqtrade
supervisor (06-18).
