## 2026-07-17 (n) — 🎫 Ticket Taker GO-LIVE build: divergence-only, the stranding bug, and the refusal lifted (operator: "do it now so we can go live")
- **Operator decision, on the record.** The taker takes 🌊 Tide Rider's vacated live slot, **divergence only** (Tide Rider's TRX flattened by the operator). The agent built and verified; **the operator flips `TT_VENUE` + `REAL_MONEY_KILL`** — never the agent.
- **🔴 THE BLOCKER, and it would have fired on the FIRST ticket.** `LighterClient` speaks **FLEET** symbols end to end: `positions()` runs every symbol through `from_lighter()`, and `market_close()` looks the coin up **in that dict** (`lighter_client.py:558`). This bot keys `marks`/`funding`/`meta` by the **venue-native** symbol from its own fetch. Live, `"1000BONK"` is looked up in a dict keyed `"kBONK"` → the arm **sees itself FLAT and re-opens every cycle**, while the exit pass never examines the real position — no stop, no max-hold. **MEASURED: `1000BONK` is a divergence ticket right now (short, gap 87.5pp) and the shadow arm holds it** — the first thing a divergence-only arm touches.
- **The half that strands it: `_resolve()` TOLERATES a native symbol.** So `market_OPEN` works while `market_CLOSE` misses its lookup and returns **`None` WITHOUT RAISING** — open succeeds, close silently no-ops, ledger books a close, `meta.pop()` runs, and the position **stays open on the venue untracked forever**. The existing guard caught only exceptions, so the block whose own comment read *"never book a close that did not happen"* **missed the exact case it was written for**. `None` is now a FAILED close at both call sites.
- **ROOT CAUSE — an asymmetry in `venues/symbol_map.py`.** `from_lighter()` is GENERIC (any `1000*` → `k*`); `to_lighter()` was a **hardcoded list of FOUR**, frozen at the 2026-07-09 market dump. Lighter now lists **SIX** 1000-markets, so `1000NOT`/`1000TOSHI` round-tripped to `kNOT`/`kTOSHI` and then to **themselves** — symbols the venue does not have. `to_lighter` now mirrors `from_lighter`. **VERIFIED across all 218 live markets: ZERO round-trip failures**; all 8 explicit entries unchanged (PEPE/SHIB/BONK/FLOKI raw-unit mults preserved); an unknown k-form still fails safe (`1000NEIRO` absent → skipped). Shared real-money file — behaviour-neutrality was proven, not assumed.
- **Converted at the venue BOUNDARY, not by normalising the bot.** `meta` + the broker snapshot are PERSISTED in native keys; changing the bot's key space would have stranded every open position on the shadow arm. One space, converted where the two meet.
- **DIVERGENCE-ONLY is fail-CLOSED and does not read the brain** (`allowed_lenses`, shipped in `afa6f7a`). `vetoed_lenses()` is fail-safe **OPEN** by design — a dark brain, stale payload, `load_state` blip or a dropped lens all collapse it to `set()`. Correct for a shadow book whose job is grading; catastrophic as the only thing between real money and three lenses rejected three times. Brain veto stays SENIOR on top; a hard assert sits at the last statement before money moves.
- **Two more real-money gates closed:** `TT_VENUE` is now **MANDATORY** (identity must never be inherited — same class as today's `VENUE` fix on the other two live bots; gated inside `main()` because four modules import this file). `apply_tuning()` is a **NO-OP in live**: the `taker.*` levers are the **SHADOW** lane, authored by `scout-tuner` (no live authority, gated on shadow tape) — going live would silently promote a shadow lane to a real-money lane. Only `live.*` levers may steer real money.
- **REFUSAL LIFTED — and what was traded away is written into the code, not just this log.** divergence's episode-deduped forward grade is **+0.004%/4h** (ehit4h CI **0.494–0.572**, straddling a coin flip) against a **6.68h** median hold; its realized **+4.23%/trade is 5–1000x** the brain's grade of the SAME signal at n=2934 — that gap is the luck; **$10.72 over 7 fills ≈ 3 fills of swing** at 100% WR. **The 30-day gate (WR>55% AND maxDD<15%) is NOT met: this is a deliberate exception, not a pass.** What replaced the refusal is guards, not permission.
- **A fixture had to be RE-POINTED, which is worth its own line:** the live suite asserted *"lighter_live must refuse"* — i.e. it asserted the presence of the very gate the operator lifted, so it would have demanded the code stay as it was. Re-pointed at the **identity** refusal (`TT_VENUE` unset), which no decision can lift. Same lesson as the 17-Jul "a test must not fail on GOOD NEWS".
- **VERIFIED:** `--selftest` + `--selftest-live` both green (all 10 live order-path checks); symbol round-trip fixture for all six 1000-markets; `TT_VENUE`-unset refuses; live `apply_tuning() == {}`; live `allowed_lenses == {divergence}`; **all four importers still grade ALL FOUR lenses with `TT_VENUE=lighter_live` in env**; born-dark green across 19 images.
- **STILL OPEN, operator's call:** *"divergence only" ≠ "shorts only"*. `short-divergence` n=7 (all TP, +$10.72); **`long-divergence` n=0** — and the live arm can take it (the shadow book holds one now, XPD). One-line change if the intent was the measured thing.

## 2026-07-17 (m) — INCIDENT: the shadow arm has been CRASH-LOOPING for an hour behind an "online" row — born-dark, one layer down
- **The Ticket Taker's shadow arm has never run. Not once.** `c5924e4` shipped `TT_VENUE=lighter_shadow` as the DEFAULT, which constructs `venues.lighter_client.LighterClient` → `import lighter`. **`Dockerfile.freqtrade` installs only `psycopg2-binary`** — it has never installed `lighter-sdk`. So from 05:35 UTC the taker died every 5 minutes on `venues.base.VenueError: lighter-sdk missing ... No module named 'lighter'`, caught live in the running container's logs.
- **MEASURED, not inferred:** the `-lshadow` row last published **05:48 UTC** and then sat stale for ~1h; `venue_orders` for this bot is **still ZERO**. The arm built to earn a go-live had not completed a single cycle.
- **THE FOSSIL.** `c5924e4` closed with "shadow arm healthy (1013.99, 18 closes)" and `09245dd` repeated it. Both were reading the **last publish of the OLD image** — a row that keeps its last-good values when the writer dies. A stale row is not a heartbeat, and `online` is not a health check. This is CLAUDE.md's own rule ("verify a NEW module by its OWN published output, never by 'it shipped'") failing in the exact way it was written to prevent.
- **WHY THE GUARD MISSED IT — the guard was right, its model was too small.** `audit_image_imports` models **repo-local** imports: it caught the missing `COPY venues/` (c5924e4's message credits it) and was satisfied. **`lighter` is a PIP dependency, invisible to it.** The guard verified the FILE and not the WHEEL. Same failure class as the four born-dark incidents, one layer down — and it is now **five in four days**. `requirements.txt` has always listed `lighter-sdk>=1.1.1`; no Dockerfile installs it by name, and this image never installs `requirements.txt` at all.
- **FIX: `Dockerfile.freqtrade` installs `lighter-sdk>=1.1.1` + `websockets>=12.0.0`.** Not optional — the taker's DEFAULT mode needs them. Deliberately NOT "fall back to PaperBroker when the SDK is missing": silent degradation to modelled fills is precisely the defect `c5924e4` set out to remove.
- **DETECTIVE CONTROL, because the hour of silence is the real finding.** A run-once process has no supervisor loop, so a crash published NOTHING and the row just aged — dead and broken look identical until someone greps the container. `_supervised()` now marks the row **ERROR** and re-raises (status-only; last good equity intact), the same reason `SafetyRails._publish_refusal` exists. Boot refusals (incl. the `lighter_live` evidence gate) pass through untouched — **a refusal is not a crash**. The fetch-fail skip path also gained a `heartbeat` (the funding bot's 12-Jul GO-GREEN rule, ported): **a skip is not a death.**
- **NEGATIVE FIXTURE, and it caught my own first draft.** Test 10 raises the incident's real error; the first version raised it inside `fetch_marks_and_funding()` — which `main()` CATCHES and returns from — so it passed against a supervisor that had never executed. The incident crashed at `LighterClient` construction, **uncaught**, propagating out of `main()` to `<module>`; the fixture now reproduces that shape. A fixture that cannot fail is the thing it is testing.
- **FOLLOW-UP (not done here):** extend `audit_image_imports` to third-party imports — map each image's `pip install` + `requirements.txt` against the non-stdlib imports its COPY'd files actually reach. That is the control that would have caught this at ship time instead of an hour later.

## 2026-07-17 (l) — 🎫 the Ticket Taker's ORDER PATH is built (still REFUSED); and the basis fix missed a THIRD book — the one that collects carry
- **THE ORDER PATH IS DONE — the half `09245dd` stopped short of, mirroring `lighter_funding_bot`.** That commit wired the signer + rails and deliberately left the orders, because every call site assumed a local broker (`broker.pos/.open/.close/.equity/.fees`) and refactoring a DEPLOYED loop mid-flight costs the very evidence we are waiting for. Now live reads **positions from the VENUE** (the account is the source of truth, not our memory — the 11-Jul ghost-position lesson), sends `market_open`/`market_close`, reconciles fills from the venue's own trade tape, and carries an equity guard, a daily-loss rail and a durable halt.
- **`lighter_live` STILL REFUSES, and that is the point.** Divergence remains the only lens the brain does not veto and it has **n=7 closes** (all `tp`, in a 28h window) against a fleet gate of 30-day WR>55% AND maxDD<15%. The path exists so the switch is one env var WHEN the evidence lands. **`venue_orders` for this bot is still ZERO** — not one of its 18 trades has touched a Lighter book; every fill it has ever booked was modelled. Lifting the refusal today would be the Trail Blazer mistake exactly.
- **THE BASIS FIX MISSED A THIRD BOOK — and it is the worst one to miss.** `93be95e` found two (`lighter_funding_spread_bot`, `lighter_family_bot`) and called the set complete; `31ec660` before it claimed five and fixed four. **`lighter_ticket_taker.py:361` was the third**: `drag = size * mark * rate * hours` — Lighter quotes per 8h, so it over-accrued **8x**. It survived both sweeps because it models carry INLINE rather than calling `funding_basis.to_hourly`, so a grep for the fixed call site could not see it. It is now the LAST self-modelled Lighter accrual in the fleet (verified: `funding_carry_bot:305` is Hyperliquid, hourly, correct — leave it alone).
- **MEASURED on the bot's own live state, not inferred.** All **5 open positions are `divergence`** — the one lens that COLLECTS carry and the ONLY one with a positive forward grade. Open `funding_paid` totalled **+0.4379 vs a true +0.0547** (artifact **+0.383**). The contamination lands exactly where it decides a go-live: `short-divergence` closes read **+4.48% / +5.12% / +4.50%** against a **+4% TP bar** — the excess above the bar IS the 8x-inflated carry credit, inflating the grade of the only lens that could earn this book real money. **The same shape as Counterweight, whose entire reported profit was this artifact.** Fixed FORWARD-ONLY; the 18 persisted ledger rows keep the scar.
- **The notional cap now lives on the RAIL that enforces it (`venues/safety.py: open_notional`).** `lighter_funding_bot` and `lighter_trend_bot` — **both real-money bots** — each carried their own copy, and the Taker's live path would have been the third. They were code-identical the day this moved, which is the argument and not the counter-argument: two definitions with **two different selftests** (the trend bot's never covered the short-at-own-entry case) is one edit from drifting, and the drift is a cap breach on real money (it breached on 15-Jul). Both bots re-export it under the original private name, so every call site is untouched and **their own selftests are the proof this is behaviour-neutral** — not a claim that it is.
- **The equity guard was MISSING from the live path, silently.** `LighterClient` only builds an `EquityGuard` when it has a signer, and `vet_account_read` returns the raw print **unvetted** when the guard is None. `09245dd` constructed the client without `guard_state_key`, so live would have run its daily-loss rail on UNVETTED equity — the exact 11-Jul failure where one dislocated print tripped the rail and the flatten sold into the dislocation for a real **-5.9%**. The funding bot gets this free via `venue_context()`; this bot builds its client directly, so it must pass it explicitly. It now does.
- **`--selftest-live` drives the live branch END-TO-END against a stub venue** (no network, no signer, no money), because a production-refused path is otherwise code no one has ever executed — which is how the fleet got a "live-ready" bot that would not have placed a single order (`c5924e4`). 9 checks: entry SENDS a real order + records the REAL fill; the notional cap is senior; the kill switch flattens the VENUE's book; daily loss CONFIRMS (debounce) then flattens + halts DURABLY; **a failed close books no phantom row and KEEPS the position**; live funding reaches the ledger but never modelled equity; **unreadable positions SKIP the cycle** (a phantom-empty set zeroes both the slot count AND the cap's input — "no positions" would read as "full book available" on top of real open risk); **a failed flatten reports the open book, not a green zero**; and the DEPLOYED shadow arm still trades on the true basis.
- **MY OWN UNIT TEST WAS NOT A DETECTOR, proved by mutation.** Re-introducing `rate * hours` in `main()` leaves `--selftest` **PASSING** — its basis assertions exercise `funding_basis` (which has its own selftest), not the CALL SITE. Only `--selftest-live` caught it. The block is kept as the worked arithmetic and now says so; the guard is the live selftest. A test that cannot fail on the bug is documentation wearing a test's clothes.
- **`TT_VENUE` deliberately KEEPS its default**, unlike `$VENUE` in (k). Same rule, different exposure: this bot has no service of its own (`run_all.sh` loops it inside the shared freqtrade-bots container with no env), so a mandatory var would kill the shadow arm collecting the evidence — and an unset var CANNOT produce a live taker, since live requires the literal `"lighter_live"`. **GO-LIVE PRECONDITION recorded in the code while it is cheap:** the live arm must be its OWN service with `TT_VENUE` explicit, or a lost var there double-writes `-lshadow` — (k)'s silent two-writers-one-row failure, rebuilt.
- Born-dark green (19 images); purity unchanged (8 violations / 5 files, none in the 4 files touched). Live-path additions are LIVE-ONLY by construction: the shadow arm's only behaviour change is the basis fix.

## 2026-07-17 (k) — the lighter_shadow fallback made the real-money bots' failure SILENT; VENUE is now mandatory for both
- **`ff5cd43` is RIGHT on the rule and it opened a hole — this closes it, it does not revert it.** Defaulting `VENUE` to `lighter_shadow` is correct ("the real money fallback has to be lighter") and the ordering was careful. But the two REAL-MONEY bots derive their IDENTITY from `VENUE`, and the change flipped their lost-var failure mode from **LOUD to SILENT**. Found by a 30-agent purity triage; every link then verified by hand against the code and the live board.
- **THE MECHANISM (Funding Farmer, real money).** `lighter_funding_bot.py:51` `BOT = "perps-funding-lighter"` + `venues/__init__.py:49` `_SUFFIX["lighter_shadow"] = "-lshadow"` = **`perps-funding-lighter-lshadow`** — which is EXACTLY `experiment_judge.py:62` `SHADOW_BOT`. Measured live: that row is **online, $1012.66, 39 closed**. So a lost `VENUE` on `trail-blazer-live` would: boot the LIVE service as a SHADOW book (simulated fills) while its **REAL positions ($67.22, 3 open, 23 closed) sit unmanaged on the venue**; DOUBLE-WRITE the judge's promotion arm (the judge is the only writer of `live.funding.*`); and **never page** — because the row looks healthy. Before `ff5cd43`, the same lost var gave the unsuffixed `perps-funding-lighter`, which publishes nothing → row goes stale → `STALE_AFTER` 15min → the watchdog PHONES. A detective control was traded for silence.
- **AND it silently disarmed Tide Rider's existing guard.** `lighter_trend_bot.py:172` refuses `hl_paper` as a daemon **because hl_paper was the default** — the comment says so. With the default moved, a lost `VENUE` sails straight past it into `crypto-trend-daily-lshadow` (**online, $999.77**), the row `tide-rider-lighter-shadow` already publishes. Two writers, one row, no page. The guard now covers only a case that can no longer happen.
- **THE IRONY, measured:** every SHADOW-only bot (`lighter_perp_sniper` / `_dislocation` / `_funding_spread`, family, momentum) carries a `setdefault("VENUE",...)` or a mode refusal. **`lighter_funding_bot.py` had NEITHER — the least-guarded file in the fleet was the one holding real money.** A lost Railway `VENUE` is not hypothetical: it happened 16-Jul, and it is why the others have setdefaults.
- **THE FIX: `VENUE` is now MANDATORY (no inherited default) for both real-money bots**, `--once` exempt for offline smokes. **Inert for production, verified:** all four services set it explicitly (`trail-blazer-live` + `tide-rider-lighter-live` = `lighter_live`; `funding-farmer-shadow` + `tide-rider-lighter-shadow` = `lighter_shadow`). It only converts the lost-var case back into a loud one. **NEGATIVE FIXTURE (a guard that never fires is not a guard): `VENUE` unset + daemon → exit 1 on BOTH bots; POSITIVE: `VENUE` set in either mode → no refusal; `--once` without `VENUE` → still smokes.** The deeper rule this encodes: a default is fine for a preference, never for an IDENTITY that decides whether real money moves.
- **UNRELATED, FLAGGED NOT TOUCHED:** the LIVE Tide Rider (`crypto-trend-daily-lighter`) is **`status: error`, equity $34.67, 1 open position** — its `REAL_MONEY_KILL` is **ARMED**, so `lighter_live` refuses to boot and the row error-loops. That is the kill switch doing its job, deliberately set, and disarming it is an operator-only action — but it means a real position is currently unmanaged. Contrast `trail-blazer-live`, which is `DISARMED_I_UNDERSTAND` and trading.


## 2026-07-17 (i) — 🛰️ market-context reads LIGHTER natively; the swap is behaviour-neutral, the DATASET is not
- **`market_context.py:44` was the LIGHTER-FIRST fleet's market context, sourced from HYPERLIQUID.** `audit_venue_purity` flagged `host:api.hyperliquid.xyz:44` as SHIPPED. All four quantities now come from Lighter: mark + OI from `/api/v1/orderBookDetails`, funding from `/api/v1/funding-rates` (lighter rows), premium from mark-vs-index. Key names and units unchanged — `{oi_ntl, funding_apr, premium_bps, mark}`. Audit drops **10 -> 9 violations / 7 -> 6 files**; `market_context` leaves the report, no new source appears.
- **THE BRIEF'S PREMISE WAS WRONG, AND IT MATTERED: this is NOT a display feed.** The brief said "only `pnl_dashboard.py` imports market_context — it is a DISPLAY feed", and asked that it be verified rather than trusted. **Measured: NOTHING imports the module — `pnl_dashboard.py` included.** Its only mentions are prose/attribution; it reads `fleet-alerts` + `coin-vetoes` over direct SQL, never the `market-context` key. The real consumers read the bot_state key: **`lighter_funding_bot.py:838` — the LIVE REAL-MONEY Funding Farmer** — and `lighter_dislocation_bot.py:279` (shadow). Had "display feed" been trusted, this swap would have been graded against the wrong blast radius.
- **Behaviour-neutral BY CONSTRUCTION, not by luck — verified in the consumer's own code.** `_mctx_slice` is written into the ledger row at `lighter_funding_bot.py:1137`, **after** `market_open()` has already executed; no branch anywhere reads it. It is a DATASET, not a gate. Confirmed end-to-end by running the live bot's real `_mctx_slice` against a real Lighter snapshot: BTC/ETH/SOL populate; an unlisted coin degrades to nulls, not a crash.
- **MEASURED HL-vs-Lighter divergence, 96 overlapping coins** (`mark` median **-6.9bps**, max |26bps| — same asset, same price). **`funding_apr` median diff -0.0044**, |apr| medians **0.1095 (HL) vs 0.1051 (LT)** — those ARE the two venues' documented resting defaults, which is the conversion validating itself. Converted via `funding_basis` (8h); a `24*365` here would have read **0.84** and every number would have stayed self-consistent and merely 8x wrong.
- **TWO FIELDS ARE MATERIALLY DIFFERENT — findings, not papered over.** (1) **`premium_bps`: a different QUANTITY.** Lighter publishes no premium field; it publishes the ingredients, and mark-vs-index is the fleet's own established Lighter premium (`lighter_market_scout.book_stats`, same formula) — so this is the venue's convention, not a proxy invented here. But HL's `premium` matches **NO** instantaneous formula (mark/mid/impact-vs-oracle each reproduce it on only **36-44 of 177** coins): HL publishes a SMOOTHED funding premium, ours is instantaneous. |prem| median **6.4bps (LT) vs 2.4bps (HL)**, ~2.7x. (2) **`oi_ntl`: a LEVEL break** — Lighter/HL notional ratio median **0.052** (p10 0.022, p90 0.117); Lighter is a ~20x smaller venue. `oi_chg_1h/24h` are ratios and cross cleanly.
- **EPOCH BREAK, recorded in the header because a joined dataset spans it.** `heat_mean_apr` measures **HL 0.1278 vs Lighter 0.2210 (1.73x)** on DIFFERENT universes (Lighter lists 105 books HL does not, incl. equity/commodity perps resting at 0.03504). `heat` / `premium_bps` / `oi_ntl` are **not comparable across 17-Jul** — `evaluate_evidence`'s decision+context dataset must split at the swap or it will fit the venue change, not the market. This is the one real cost of the swap, and it is paid in the dataset, not in a trade.
- **`open_interest` is BASE units — VERIFIED, not assumed.** The LT/HL notional ratio holds at ~0.03-0.12 across coins whose marks span 63354 (BTC) to 0.072 (DOGE); were the field quote-denominated that ratio would scale with mark and the two ends would differ by ~10^6. (BTC: 1745 * $63.4k = $110M OI — plausible; $1,745 of total BTC OI would be absurd.)
- **COVERAGE moved TOWARD the consumers, and is explicit.** HL 232 books vs Lighter 201 active, only 96 overlap. 136 HL-only coins go — the fleet does not trade them; both consumers are Lighter bots, so **105 Lighter books that previously resolved to an empty slice now carry context**. A book with no venue rate gets `funding_apr=None` (never 0.0, which would drag heat toward zero); no index gets `premium_bps=None`. A missing input reads as "no evidence".
- **BORN-DARK, 5th outing, caught again — and the first fix was the wrong one.** `Dockerfile.marketcontext` COPY'd only two files; the new `funding_basis` + `venues.symbol_map` imports would have crash-looped the service. The first fix (`COPY venues/symbol_map.py` as a PEP-420 namespace leaf, avoiding the package's real-money surface) **works at runtime but defeats the guard**, which models top-level names only: it registered `symbol_map` as a TOP-LEVEL module — the exact masking its own docstring warns against. Took the precedented path instead (`COPY venues/`, as 8 other images do) and DECLARED `fleet_tuning` + `paper_broker` in `BORN_DARK_OK` with reasons. Audit green across 19 images. **Guard gap logged, not silently worked around** — see the report.
- **`--selftest` added** (house style): contract keys/units, BASE-unit OI, mark-vs-index premium, fleet symbol namespace, inactive/junk rows skipped, coverage Nones, and a **NEGATIVE FIXTURE that fails loud if `funding_basis` is ever "simplified" back to `24*365`**. Its first draft asserted heat saw 3 rates when it saw 2 — **the code was right and the test was wrong**; the fixture that proved it is kept.

## 2026-07-17 (i) — 🧭 the regime oracle reads LIGHTER now; the swap is a no-op and the COVERAGE TRAP is a payload field
- **`regime_oracle.py` was the fleet's last HL-fed SIGNAL organ** (`audit_venue_purity.py`: `regime_oracle.py [SHIPPED] host:api.hyperliquid.xyz :56`). It now fetches Lighter 1d candles (`/api/v1/candles`, paged backward under the 500-bar cap) for its real `UNIVERSE`. The HL path is GONE, not disabled. Guard drops **regime_oracle.py out of the report entirely**; its selftest stays green (a concurrent session had already redesigned that block to assert the DETECTOR'S CAPABILITY rather than the fleet's illness — so it no longer fails on good news).
- **THE SWAP CHANGES NO DECISION — measured on the SHIPPED code, not inherited.** `study_lighter_vs_hl_equivalence.py` proved a claim about DATA ("Lighter candles -> same verdict 99.4% of 3,085 coin-days"); it could NOT prove a claim about the `fetch_daily()` I actually wrote. New `scripts/ab_regime_oracle_venue_swap.py` drives THIS module's real fetch against the verbatim old HL fetch: **today, 16/16 majors IDENTICAL** — and **SEED-INVARIANT** (both arms agree under hysteresis seed None/0/1, so no major sits on the ADX knife-edge today). 60-day rolling replay: **895/900 coin-days agree (99.4%)**, 12 of 15 replayable majors at 100%. It independently reproduces the study's NEAR 07-15/07-16 split **to the decimal** (HL LONG-window adx 13.0/13.4 vs Lighter chop-gated 12.0/12.3) from a DIFFERENT fetch path. The other 3: TAO 05-24 + 06-15, DOGE 05-22.
- **The replay OVERSTATES the live risk, and knowing why matters.** It runs TWO INDEPENDENT hysteresis chains, so one ADX-band straddle latches the arms apart for days. **Production has ONE chain** — `prev_trend` is read from the SHARED published state, so the swap INHERITS the HL-era trend instead of starting a rival. Live divergence <= the replay's. The 0.6% is not price error either: it is this oracle's OWN discontinuity at ADX 17/11, and neither venue is "wrong".
- **THE COVERAGE TRAP IS THE REAL FINDING, and it is now IMPOSSIBLE TO MISS.** Lighter lists some books later than HL, so a young book falls under the 203-bar (`EMA_SLOW+SLOPE_BARS`) gate while HL publishes — the study measured Lighter's **ZEC ungradeable on 115 of 200 replayed days**. A swap that silently drops a coin is a BUG: the fleet reads a smaller universe and never knows. Every ungradeable coin is now NAMED in the additive **`coverage.missing`** with its reason (`no-lighter-book` / `short-history(n<203)` / `fetch-failed:<Exc>`) and still lands in `errors` as before. **No verdict > a wrong verdict.**
- **Dormant, not absent — which is exactly why it needed a field and not a glance.** MEASURED today: **16/16 majors clear the gate; nothing is dropped.** But ZEC has **287 Lighter bars vs HL's 521** — clears by 84, and is STILL too young for the A/B to replay 60 days at all (it is the one major PART 3 could not score). A coin that clears TODAY and vanished YESTERDAY is invisible to a spot check.
- **Coverage collapse degrades SAFELY, by construction not by luck:** `fleet.read` needs an ABSOLUTE n>=4 long or short, so a universe thinned by missing books decays toward "mixed / transitional" — neutral — never toward a confident wrong direction. A dark venue publishes NOTHING (consumers keep last-good + TTL).
- **Behaviour-neutral by construction, asserted MECHANICALLY.** `classify()`, `wilder_adx()`, ADX 17/11, EMA 50/200, `SLOPE_BARS`, `LOOKBACK_DAYS`, the hysteresis, the 203-bar gate, `UNIVERSE`, `KEY`, `TTL_SEC` and every existing payload field are **byte-identical to HEAD** — the A/B asserts this itself, so it cannot silently vary two things (17-Jul lesson: the Stock Leaders A/B differed three ways and its headline pointed the opposite way to the truth). `coverage` is ADDITIVE; `params.source` -> `"lighter-1d"` because it must not lie about the tape.
- **Bar stamps stay MILLISECONDS on purpose.** Both venues stamp `t` in ms at the bar's OPEN, UTC-midnight aligned, so `asof` keeps its shape across the splice — `bot_learn._load_regime_history()` joins this key's ARCHIVE and the seam is the only real risk here (pre-swap rows HL-derived, post-swap Lighter-derived, same column; at 99.4% identical, nearly harmless).
- **Closed candles are now dropped by TIMESTAMP, not by `iloc[:-1]`.** Position-dropping assumes a partial bar always exists; an illiquid book that has not traded today publishes none, and the old rule would then eat a REAL closed day. This is the documented intent ("the partial today-candle is dropped") implemented faithfully, and it is what the study's replay actually measured.
- **The brief's premise is overstated — VERIFIED by grep, not assumed.** "The FLEET'S regime signal" reads no bots: consumers are `pnl_dashboard` (display), `fleet_respiration` (freshness only) and `bot_learn._load_regime_history()` (the history join). No bot trades off this key. The swap's risk is LONGITUDINAL, not live.
- **`pnl_dashboard` fixed in the same pass because this change perturbed it:** the regime-oracle vital counted **TOP-LEVEL PAYLOAD KEYS**, not majors — it displayed **"4 majors tracked" for a 16-major universe** (params/pairs/fleet/errors), and the additive `coverage` key would have silently moved it to 5. Now reads `len(pairs)` (correct: 16) and surfaces `coverage`/`MISSING` where the operator will actually see a dropped coin. Guarded for pre-swap payloads, which carry no `coverage`.
- **New `--selftest` on `regime_oracle.py`** (offline, stubbed venue): closed-bars-by-timestamp, ms stamps preserved, backward paging + termination on a short book, payload shape held, dark venue publishes nothing — and the NEGATIVE FIXTURE that matters: **a 100-bar book must be NAMED in `coverage.missing`, never silently dropped**. It also measures a thing worth recording: at `LOOKBACK_DAYS=300` ONE 500-bar page spans the window, so the real venue never pages — the loop is a safety net for a raised lookback or an under-served page, and is tested as such.
- **Verification:** regime_oracle selftest green; `audit_venue_purity` green (regime_oracle GONE) + its selftest green; `audit_image_imports` OK across 19 images (no new repo-local import — `urllib.parse` is stdlib; the image COPYs `regime_oracle.py` alone with no `venues/`, so importing `venues.lighter_client` would have shipped BORN-DARK); real run publishes **16/16 majors, "risk-off downtrend"**, coverage complete.
- **NOT fixed here, and stale as a result:** `audit_venue_purity.py`'s header still itemises the 15-violation/11-file census incl. `regime_oracle.py:56`. Live count is now **10 in 7 files** — this entry plus concurrent `market_context`/`market_pulse`/`venues` work. That header wants ONE reconciliation pass once the swaps land, by whoever lands last; three sessions editing it at once is how a census becomes fiction.

## 2026-07-17 (h) — the real-money FALLBACK was Hyperliquid (operator: "the real money fallback has to be lighter")
## 2026-07-17 (j) — ⚖️ Counterweight's backfill moves to LIGHTER (operator: "everything needs to be running off lighter")
- **The first venue-purity violation actually CLOSED, not just measured.** `lighter_funding_spread_bot.hl_backfill()` (api.hyperliquid.xyz, line 85) is gone; the ranking window now bootstraps from **Lighter's OWN settled series** (`/api/v1/fundings`). `audit_venue_purity`: **15 violations in 11 files -> 14 in 10.** Everything the three converging lines of evidence asked for: the equivalence study's §2 verdict ("LIGHTER IS BETTER — swap it, but as a BUG FIX"), the new BACKTEST-ON-LIGHTER-ONLY doctrine, and this session's own measurement that the **venue** (not the scale) was the contaminant.
- **UNITS, and why there is NO state migration.** The settled row is `{timestamp, rate PERCENT-PER-HOUR, direction long|short, value}` — an **UNSIGNED** rate plus a direction, unlike `/funding-rates` which IS signed. Converted once at the write site into the SAME per-period basis as the live `funding_map()` samples: `rate/100 * sign(direction) * HOURS_PER_PERIOD`, where `HOURS_PER_PERIOD = 24/periods_per_day('lighter') = 8` is **asked of `funding_basis`, never hardcoded**. VERIFIED against the live quote: **median ratio 1.00** across the majors (ETH/DOGE/LTC exactly 1.00; SOL 2.32 / XRP 1.39 are real rate movement between the settled mean and the current predicted rate, not basis). Because the new rows land in the basis the persisted `fund_hist` already uses, **the 93 samples/coin of live state stay valid — no migration, no second 8x.**
- **MEASURED cold-boot difference — the picks really do change.** Old (HL backfill): coverage 30/30, APRs **−0.9%..+1.9%** (HL *hourly* rates annualised with Lighter's H — wrong venue AND 8x low), LONG {OP −1%, SEI −1%, ADA −0%}. New (Lighter settled): coverage **25/30** (the 5 non-Lighter coins now get NOTHING instead of 78 rows of foreign funding — and `ctx.supports()` filters them anyway, so the window loses nothing it was entitled to), APRs **−12.1%..+10.7%** (real Lighter APRs), LONG {ADA −12%, AVAX −3%}, SHORT {NEAR/WIF/ENA/APT +11%}. It ranks Lighter's cross-section now, not Hyperliquid's.
- **INERT for the running book, by measurement.** The backfill only runs when a coin's coverage < `MIN_COVERAGE`(36h); the live instance has full coverage on all 25 rankable coins, so **a restart takes no backfill path at all**. This change bites only on a genuine COLD START — exactly the window `study_fundspread_basis_mixing.py` measured as broken. Preventive, not corrective.
- **Backtest-first is satisfied, not waived**: the factor is validated on precisely this tape (`backtest_xsect_funding_lighter.py`, 72h row 4/4 green, 3x friction OK). Born-dark audit green across 19 images for the new `venues.symbol_map` import; cold-boot smoked `--once` against real Lighter mainnet.

## 2026-07-17 (h) — the BASIS fix missed two books; ⚖️ Counterweight backtested ON LIGHTER at last (and it SURVIVES)
- **CORRECTION to (f): the accrual fix covered FOUR bots, not "five".** (f)'s own entry claims five bots moved to `funding_basis.to_hourly`. Four did (`lighter_trend`/`_momentum`/`_funding`/`_index`). **`lighter_funding_spread_bot.py` was touched but only its LOGGING constant `H` changed — the accrual was left**; `lighter_family_bot.py` was never touched at all. Both modelled Lighter carry as `rate * ... * dt_h`, right only for an HOURLY quote, so both **over-accrued 8x**. Fixed in this entry. A commit message is a claim, not evidence — this one was checked against the code and was wrong.
- **Counterweight's ENTIRE reported profit was the artifact.** MEASURED on its live state, not inferred: published equity **$1000.858 (+$0.858)**; `broker.equity()` 999.831, `fund_realized` -0.0651, `fund_open` +1.0923 -> corrected **$999.959 (-$0.04)**. Ledger cross-check closes to the cent: `sum(pnl_abs) -0.8730 = price -0.8079 + fund_realized -0.0651`. The book RECEIVES on both legs, so the 8x **FLATTERED** exactly the quantity it exists to harvest — on a go-live candidate whose shadow record IS the go-live evidence. The long-only family books PAY, so the same bug made them look **WORSE** than real: opposite sign, one cause. `accrued` reaches `account_value()` -> the **DAILY-LOSS RAIL** in both, so this was never cosmetic.
- **BORN-DARK, 4th outing, caught again.** `Dockerfile.familyshadow` did NOT COPY `funding_basis.py` and the new import is bare — shipping would have crash-looped `family-lighter-shadow` on boot. COPY added; audit green across 19 images.
- **⚖️ COUNTERWEIGHT, BACKTESTED ON LIGHTER AT LAST — the edge REPLICATES** (`scripts/backtest_xsect_funding_lighter.py`, 150d, 25 coins, 3585 hourly bars). `backtest_xsect_factor_lab.py` passed this factor and shipped the book — **on HYPERLIQUID data**, ranking and accruing HL funding start to finish. The live book ranks on LIGHTER funding and earns LIGHTER funding: **no backtest had ever tested the live signal.** Re-run on Lighter's OWN settled tape (`/api/v1/fundings`, signed via `direction`; never touches `/funding-rates`, so the 8x cannot leak in), holding scoring/book/gates/arithmetic identical and varying ONLY the venue. The shipped config (72h/K=5/reb 24h) **PASSES**: full **+13.6%** (HL claimed +13.7%), h1 **+5.6%** / h2 **+6.6%**, maxDD **10.1%**, mirror(price) **-16.4%**. The **whole 72h row is green (4/4)** — the HL lab's defining plateau, replicated. 3x friction holds: 15bps/side **+7.3% (+2.2/+3.7)**, green both halves.
- **But the plateau is NARROWER and the carry leg is THINNER.** 6/12 configs pass vs HL's 8/12; the 48h row collapses to 1/4 (HL: 3/4), so the plateau is the 72h ROW, not the 48-72 block — **do not run this book at 48h on Lighter**. Funding contributes **+4.5pp** of the +13.6% vs +6.8pp on HL: Lighter's funding cross-section is less extreme (floor band 3.5% apr, not 28%), so the book leans **more on its price/mean-reversion leg** than the HL lab implied. maxDD 10.1% also clears the 15% go-live gate — but a backtest is not a shadow record, and go-live stays a separate decision on the LIVE record.
- **CORRECTION, same day, to two things this entry originally overreached on.** (1) It said the Lighter re-test "**KILLED** the Funding Farmer and **VINDICATES** this one" — **withdrawn**: the Funding Farmer's verdict was itself revised hours later (its HARD_STOP, not its gate, was the defect — STOP 0.03 gives +$21.32, both halves positive, n=1911). The re-test replaces a HYPOTHESIS with a MEASUREMENT; it does not hand out death sentences. (2) **The +13.6% is a FLOOR, not an estimate.** The lab holds the HL lab's `SLIP=5bps/side` so exactly one variable (the venue) changes — but 5bps is an **ASSUMPTION**, and Lighter friction is now **MEASURED at 0.86bps/fill**. That is the *same* unmeasured assumption that manufactured the false "no edge" verdict on the Farmer. Real numbers are BETTER than these and the "3x stress" at 15bps is really ~17x measured — conservative in the direction that matters, but a re-run at 0.86bps is the obvious next measurement. The lesson cuts both ways: **an assumed friction can fake an edge as easily as it can hide one.**
- **DOCTRINE CONVERGENCE — and it makes the ranking fix mandatory, not optional.** The operator's 17-Jul rule ("backtest on Lighter ONLY — the venue we trade is the venue we measure") landed independently of this work, and `scripts/backtest_xsect_funding_lighter.py` IS that rule applied to Counterweight. The same rule condemns the bot's `hl_backfill()`: **`audit_venue_purity.py` flags `lighter_funding_spread_bot.py` line 85 (`api.hyperliquid.xyz`) as a SHIPPED violation.** So "stop ranking a Lighter book on a foreign venue's rates" is no longer a recommendation from this study — it is required, and the replacement tape (Lighter's own settled series) is the one this backtest just validated. Still backtest-first, still 21-Jul's call on the cutover.
- **The bug this investigation was OPENED for is real but INERT** (`scripts/study_fundspread_basis_mixing.py`). Reported: `fund_hist` merges HL HOURLY backfill rows with Lighter 8H live samples, corrupting the cross-section. Measured against live state: **every ranked coin's window is unit-PURE** — the 25 tradeable coins hold 93 Lighter samples and 0 HL rows (backfill aged out), and the only 5 HL-contaminated coins (GALA/INJ/RUNE/STX/W) are exactly the ones `ctx.supports()` filters out because they are not Lighter markets. Confirmed independently by a live `--once` smoke ("INJ not listed on lighter — skipping"). It bites only on a COLD START or a >36h outage, and `fund_hist` PERSISTS, so ordinary redeploys do not re-contaminate.
- **Two claims in the report REFUTED by measurement.** (1) *"Is `/funding-rates` `rate` unsigned like the settled series?"* — **NO, it is SIGNED**: 12 of 201 lighter rows are negative live (DATA -3.2e-04, TRX -4.64e-04, SOL -1.6e-05) and the row schema is exactly `[exchange, market_id, rate, symbol]` — there is no `direction` field to miss. The most-negative long leg fires correctly. (2) *"The mix ratio differs PER COIN"* — **no**: all 30 coins get a full 78-row backfill, so the mix is UNIFORM across the cross-section.
- **And the report's proposed FIX is refuted too — x8 makes the cold start WORSE.** Fidelity to the Lighter-only ranking, swept over every viable boot hour: at +48h **CONTROL 7.88/10 vs FIX 5.38/10, control closer in 16/16 boots, zero exceptions**; at +72h both 10.00 (HL aged out). Why, deterministically: a raw HL row (~1e-05) is ~8x SMALLER than a Lighter row (~9.6e-05), so under shipped code the foreign rows act as near-zero filler worth ~6% of the score — multiplying by 8 makes them EQUAL-weighted, handing **a full 1/3 of the ranking to Hyperliquid's cross-section**. **Unit-purity is not correctness.** No ranking change shipped: the honest fix is to stop ranking a Lighter book on a foreign venue's rates (drop HL rows once Lighter coverage >= MIN_COVERAGE, or accept a 36h cold-start blackout) — a strategy change, and now that the Lighter tape exists it can be tested properly. 21-Jul's call.
- **Incidental, doc-only:** `lighter_market_scout.py`'s `funding_aprs()` claims the benchmark "hyperliquid rows are hourly and are now 8x LOW". **False** — Lighter normalises ALL FOUR exchange rows to a common 8h basis (its HL row 1.0e-04 == 8x HL's native 1.2493e-05/hr, measured twice independently). The CODE is right; the COMMENT lies.
- **RECONCILED with the source study (`study_lighter_vs_hl_equivalence.py` §2c), because two studies disagreeing in the repo is worse than either being wrong.** It reported this bug first, and (i) its signedness answer was **already right** — the brief that reached this session was stale, and §2c had already measured `/funding-rates` as signed (105/684 rows across all four exchanges == 12/201 counting lighter rows only; both consistent); (ii) **its 8.0000 paired ratio is the better measurement** and its §2b method warning is correct — this session's own first naive pass manufactured an ADA ratio of **-167** in exactly the way §2b warns, so the 8x here rests on the STRUCTURAL proof instead (HL returns exactly 72 rows for a 72h window); (iii) its **mechanism is refuted** (uniform, not per-coin); (iv) **its proposed fix is the right one** — backfill from Lighter's OWN settled series, which drops the foreign-venue proxy, and the sweep shows that second half is the half that matters. Caveat recorded for whoever implements it: the settled series is **%/hr** and `/funding-rates` is an **8h fraction**, so "both are Lighter" is NOT yet "both are the same unit" — store TRUE APR and convert at BOTH write sites via `funding_basis`, or the merge just swaps one 8x for another.

## 2026-07-17 (g) — the fleet could not measure its own execution, and the organ built to do it was reporting DRIFT as slippage (operator: "measure the real fill cost")
- **THE ORGAN WAS LYING, and the number it lied about decides a live bot.** `implementation_shortfall._slip_bps` compared the LIVE arm's **AVERAGE entry price** against the SHADOW arm's **AVERAGE entry price**, per coin, over a **7-day window**. The two arms enter at DIFFERENT MOMENTS at DIFFERENT PRICES — so the difference of their averages measures **price DRIFT**, not execution. No averaging fixes it; the quantity has no execution meaning unless the fills are the same order. Live proof: **HYPE entry_slip −363.2bps beside exit +359.3bps** — a claim the live arm filled 3.6% BETTER than mark — with ETH 197.3 and LIT 326.1, while the organ's own aggregate said **gap −0.237pp / verdict clean**. Read as execution it said the live book bleeds 2-3% a round trip. **WITHDRAWN**; both keys are now always None. `gap_pp` (the paired per-trade read it IS entitled to) is untouched.
- **Its selftest pinned the BUG.** The fixture fed ONE synthetic trade per arm — where "live avg entry vs shadow avg entry" trivially IS the slippage — so it could not fail. Re-pinned to the withdrawal, plus the honest replacement's contract.
- **THE REAL FINDING: the live bot has NEVER recorded a fill price.** All three `lighter_funding_bot.publish_venue_order` calls passed **`px_decision=px, px_fill=px`** — the decision price echoed back — so `slippage_bps` is NULL on all 48 live order rows. The shadow twin (ShadowBroker walks the real book) reports **0.86bps/fill over n=158, spread 1.70bps**. CLAUDE.md has claimed since 15-Jul that the funding bot "records fill prices"; the code never did. The bot HAS the real fill (`venues.lighter_client.last_fill`, the 16-Jul FILL RECON + the `_real_exit` helper feeding `paper_trades`) — it just never handed it to the order ledger. **FIXED**: both live close paths now publish the decision mid and the REAL venue fill separately, with signed per-order slippage.
- **An echoed decision price now records NULL, never 0.0.** `_real_exit` falls back to the decision mid when the venue read fails, so `px_fill == px_decision` means NO reading — recording it as zero slippage is indistinguishable from a perfect fill. That confusion is the entire blind spot. New `_fetch_order_slip` reports `measurable` + `echoed_decision` per arm so "measured 0" and "never recorded" can never be conflated again.
- **WHY IT MATTERS — the whole Funding Farmer verdict hangs on this one unmeasured number.** Driving slip to zero isolates a **+6.18 bps/trade GROSS edge over 1,911 trades, both halves positive** (TP 0.04 / **STOP 0.10→0.03** / hold 72h at gate 0.05 TRUE — the only config that survives; TP 0.06 fails h2 at every slip, STOP 0.10 never passes). So the bot is **FRICTION-BOUND, not edgeless** — and the answer flips entirely on the round-trip cost: **backtest ASSUMED 10bps (loses −$18) / shadow MODELS 1.7bps (wins +$20, both halves) / live-vs-shadow gap implies ~25bps (loses badly)**. Three estimates, 14x apart, **not one of them measured**. The 5bps/fill that produced "no gate has an edge" was an assumption too.
- **NOT TUNED — deliberately.** `STOP 0.10→0.03` is cheap and supported, but shipping it now means tuning a bar while the dominant term is unmeasured, and there is no `hard_stop` lever in the fleet_tuning registry (the supported path is the shadow twin via the experiment judge, which needs a registry addition — real-money-adjacent, operator's call). Agenda 17 updated.
- **Verification:** the bot's `_slip_bps_of` and the pinned mirror agree IDENTICALLY across 7 cases incl. every degenerate one; selftests green (shortfall/basis/immune/board); born-dark audit OK across 19 images; new `_fetch_order_slip` exercised against the REAL ledger (shadow: 158 orders / 79 with slip / 0.86bps / measurable=true; live: 48 orders / 0 with slip / measurable=false — exactly the blind spot, now visible instead of invented).
## 2026-07-17 (f) — COMMIT (a) of 2: the funding BASIS fix, fleet-wide and BEHAVIOUR-NEUTRAL (operator: "implement universally to all bots")
- **`funding_basis.py` — one authority for funding BASIS per venue.** A quoted `rate` is a fraction per FUNDING PERIOD, and the period differs by venue. Lighter quotes per **8 HOURS**; every Lighter consumer annualised it as HOURLY (`rate*24*365`), so every apr the fleet computed, gated on, logged and stored was **8x TRUE**. VERIFIED against the SETTLED series `/api/v1/fundings`: ETH predicted 9.6e-05 -> published 84.1%, settled 10.51%, **ratio exactly 8.00**; DOGE and SPY likewise (SPY's floor: 28.0% published -> **3.50% TRUE**). Arithmetic closes: `9.6e-05*3*365*100 = 10.512`. (Lighter SETTLES hourly at rate/8 — the quote is the 8h convention; both facts hold.)
- **NOT a global replace — that would have broken four CORRECT sites.** The fleet knows both conventions and gets them right everywhere except the one venue holding real money. UNTOUCHED: `funding_carry_bot` + `market_context` (Hyperliquid, genuinely hourly), `market_pulse` (Binance, already `rate_8h*3*365`), every `scripts/backtest_*` (HL). An unknown venue now RAISES rather than defaulting — a silent default is the bug.
- **Why it was never "just a label".** `FUNDING_ENTER_APR=0.40` was born in `funding_carry_bot.py` against HYPERLIQUID data — hourly, so `24*365` is CORRECT there and 0.40 meant a TRUE 40% apr. It was ported to `lighter_funding_bot.py` as a bare constant. **The PORT is the bug**: the LIVE book has admitted at **~5% TRUE** ever since, on a bar no backtest ever supported. Measured today: the gate admits **130 of 214 books — 61% of the venue** — for a rule meant to harvest extreme carry.
- **BEHAVIOUR-NEUTRAL, and proven so.** The conversion AND every apr-denominated threshold were divided by the same 8 in this ONE commit: `FUNDING_ENTER_APR` 0.40->0.05, `FUNDING_EXIT_APR` 0.15->0.01875, `SCOUT_DIV_GAP` 300->37.5, `TT_DIV_GAP` 500->62.5, `MOMO_AB_VETO_APR` 1.5->0.1875, the incubator's base + gene grids, the judge's candidate queue, the scout-tuner's widen ladder, and **the fleet_tuning registry CLAMPS** (`taker.div_gap_pp` 300/700->37.5/87.5, `xp`/`live.funding.enter_apr` 0.25/0.60->0.03125/0.075 — miss those and the corrected values get clamped back to nonsense). Verified against the LIVE cross-section: **entry AND exit decisions bit-identical on 214/214 books, same 130 admitted.** Same trades, honest labels.
- **The other half nobody looks at: the ACCRUAL.** Five bots modelled carry as `accrued -= rate * notional * dt_h` — right only if the quote is hourly, so **8x over-accrued** on Lighter (now `funding_basis.to_hourly`, which is exactly the venue's rate/8 hourly settlement). Live equity was always honest (the VENUE charges the real thing) — but the modelled figure reaches the **per-trade ledger AND the win/loss call**, so a book that COLLECTS carry had its win rate inflated. **The live Funding Farmer's 82% WR is therefore suspect and must be recomputed** before anyone cites it (its +$5.15 equity comes from `account_value` and stands).
- **Born-dark guard EARNED ITS KEEP AGAIN.** `import funding_basis` is unguarded, and `scripts/audit_image_imports.py` caught **6 images missing the COPY — two of them LIVE real money** (`Dockerfile.fundinglighter`, `.trendlighter`). Unfixed, both live bots would have crash-looped on boot. COPY added to all six; audit now OK across 19 images.
- **Two defects the tests caught in my own work:** a rewrite produced `round((x or 0, 'lighter'), 1)` — a tuple to `round()`, which COMPILES and only dies at runtime; and every old-unit test FIXTURE (scout divergence, taker, tuner ladder, judge candidates, tuning registry, incubator grid) had to be re-denominated or it silently inverted its own meaning — the scout's MOMO ticket, 50pp, would have started clearing a 37.5 bar it was written to fail. Fixtures must be denominated in the same units as the threshold they exercise. Mutation-tested.
- **`funding_basis` stamp on the tape now reads 1.0 + `corrected: 2026-07-17`** (was 8.0). The key STAYS at 1.0 deliberately: a 60-day join spans both epochs and the divisor is the only thing that makes pre-fix rows readable.
- **Verification:** 19/19 fleet selftests green (tuning/judge/incubator/tuner/taker/replay/immune/proprioception/regen/board/risk/brain_stats/brain_replay/respiration/clock/shortfall/sentinel/scout/basis), born-dark audit OK, bit-identical decision proof on the live venue.
- **COMMIT (b) IS NOT THIS COMMIT.** Re-tuning the gate is operator-gated and backtest-first — and the artifact it needs does not exist yet: **ZERO backtests have ever run on Lighter funding data** (every one loads Hyperliquid). Neither 40% nor 5% TRUE is established *on the venue holding the money*, and Lighter's economics differ anyway (zero perp fee). Next: `scripts/backtest_lighter_funding.py` on the ~31d of settled rates `/api/v1/fundings` serves retroactively, then optimise. Mixing (a) and (b) is how a reporting fix becomes an 8x live entry change.
## 2026-07-17 (e) — 🏆 Stock Leaders RETIRED: it cannot promote at any size worth the slot (operator: "solution?")
- **THE ANSWER, and it is not the one the review was chasing.** The rule is NOT what failed: 86% of the loss that prompted this was a real market-wide crash (SNDK/MRVL/NBIS/MU printed -18.3/-18.7/-20.7/-10.1% on Yahoo over the same window) on **n=3 fills in 4 days**, well inside the strategy's own backtested envelope. Nothing was tuned. And **funding was never the problem either** — corrected for the 8x basis error found in (d), its picks paid **~8% TRUE apr against a ~52% breakeven**. Both of the stories this review told itself (carry drag, momentum-ranks-crowding) are dead; the second was refuted outright (Spearman -0.174, p=0.40).
- **What actually kills it needs no funding data at all: it cannot clear the fleet's own go-live gate.** maxDD **37-44% with ZERO funding modelled** (four independent harnesses; this file's own header says 36.9-44.3%) vs **maxDD < 15%** (CLAUDE.md Rules) — **2.5-3x over before a cent of carry**, and a one-path maxDD is downward-biased so the truth is worse.
- **The last hole closed — SIZING, the one lever ~10 tested designs never touched.** Drawdown scales **SUBLINEARLY** with deployment, so you cannot delever your way out: 90%->43.3% DD, 35%->25.5%, 20%->17.3%, and the **only** deployment clearing the gate is **15% ($30/slot) -> 6.7% CAGR / 13.9% maxDD = ~$67/yr** on the $1000 book. Strip the ~38% hindsight premium and it is **~$42/yr — at or BELOW risk-free cash, for a 14% drawdown**. Cutting size 6x only halves DD while cutting return 5x. (Harness calibrated against the workflow's control first: 37.9%/40.7% vs its 47.0%/43.0% — the first cut silently ran 1994-2026 off SPY's full grid and was thrown out.)
- **RETIRED.** Row hidden (`RETIRED_ROWS`, both `equities-momentum` and `-lshadow`); **ledger and state KEPT**. Retirement is a **code guard** (`MOMO_RETIRED`, default on) rather than `railway down`, because Railway auto-deploy RESURRECTS stopped services on git push (the perps-bot/momo-bot/triangular-arb trap). `SystemExit` re-raises through `_supervised()` so the container exits instead of crash-looping. **To un-retire: `MOMO_RETIRED=off` + drop the row from `RETIRED_ROWS`.**
- **What the epoch-2 A/B (c) was worth.** It ran ~1.5h and dies with the book — but fixing it was still right: v1 was reporting a number that pointed the OPPOSITE way to the truth, and an instrument that lies is worse than none. Its own postscript: `AB_VETO_APR` 1.5 = **18.75% TRUE**, ~3x tighter than breakeven — it vetoed affordable names and sat in cash. As configured it measured the wrong thing.
- **The two keepers from this whole thread outlive the bot:** the venue-wide funding cross-section + basis stamp (d), and agenda item 16 (every fleet APR is 8x true; the LIVE Funding Farmer's gate is denominated in it — untouched, backtest-first, operator's call; it is +$5.15 on 22 trades at 82% WR, so the 8x is a LABEL error, not an active loss).
## 2026-07-17 (d) — the scout stores the CARRY cross-section — and doing it found that every funding APR the fleet prints is 8x TRUE (operator: "fix the funding cross-section first")
- **THE FINDING (real money, NOT fixed here): Lighter's `/funding-rates` `rate` is an 8-HOUR fraction, and the whole fleet annualises it as HOURLY (`rate*24*365`). Every APR the fleet computes, prints, gates on, and stores is exactly 8x TRUE.** Verified against the SETTLED series `/api/v1/fundings` (whose `rate` is %/hr): ETH predicted `9.6e-05` -> published **84.1%**, settled **10.51%**, ratio **exactly 8.00**; DOGE and SPY also exactly 8.00 (SPY's floor: published 28.0%, settled **3.50%**). The arithmetic closes to the digit: `9.6e-05*3*365*100 = 10.512`. Correct conversion is `rate*3*365*100` (3 periods/day). Compare only STABLE names — a spiking predicted rate against a 48h settled mean gives noisy ratios (MU 12.1, MSTR 26.5) and proves nothing. **The 8x is a MONOTONIC rescale**, so rankings, the extremes ordering, and cross-venue divergence (all rows share the basis) are UNAFFECTED; what is wrong is every ABSOLUTE apr a human reads and any threshold meant to express a TRUE apr. **Deliberately NOT corrected:** the same conversion denominates the LIVE Funding Farmer's `FUNDING_ENTER_APR=0.40` (really ~5% true), plus `AB_VETO_APR` 1.5 and `DIV_GAP_PP` — re-denominating is an 8x live entry-behaviour change and goes backtest-first with the operator (agenda item 16). Shadow books that accrue their own funding (`lighter_momentum_bot`/`lighter_index_bot`: `accrued -= rate*size*px*dt_h`) over-accrue 8x in their ledgers; the live bots are charged by the VENUE so their realized P&L is honest — only their DECISIONS use the inflated number.
- **`lighter-market` now persists `funding`: the FULL per-symbol APR cross-section over every ACTIVE book (~201), historized beside `marks`.** Until now the only funding this organ kept was the top-8 |extremes| — a VIEW, not the data. That gap is why 4 of 5 strands of the Stock Leaders review could not resolve: with no per-symbol history the crowding question had to run on ONE snapshot at n=25, where most names tie on the floor band and |rho|>0.398 is needed for p<0.05 — unresolvable by construction. Same argument that widened `marks` from liquid-only on 16-Jul, and deliberately NOT liquid-gated (a book can fall under the floor while still held — the exact bug that fix repaired) and NOT kill-switched (an env flag on a DATA key is a born-dark switch waiting to be flipped). Honest about scope: `/api/v1/fundings` already serves ~31d of settled rates retroactively; what only this key gives is depth past that window plus the PREDICTED rate as seen at the decision instant.
- **`funding_basis` stamp rides with the data** (`true_apr_divisor: 8.0`) so the 60-day tape is self-correcting instead of 60 days of 8x-wrong carry asserted as fact. Pinned in the selftest — and mutation-tested: dropping the stamp, wronging the divisor, emptying `funding`, or having history drop it are all CAUGHT.
- **`historized()` extracted** from main()'s inline dict-comp so the selftest pins the REAL history projection. This organ's cross-sections exist only to be joined to a decision after the fact, so a key silently dropped there is the difference between evidence and nothing. Behaviour-identical (verified by equality/JSON/key-order/fuzz).
- **COST, measured not estimated:** ~3.0 KB/snapshot raw JSON, +47% on a 6.3 KB history row, ~52 MB at the 60-day retention floor, on a DB reaching ~150 MB at that steady state regardless. Those raw figures are conservative — jsonb TOAST-compresses this key well (~43 distinct values across ~201 symbols): ~1.5 KB and ~32 MB on disk. Cadence confirmed at 288 rows/day.
- **Verification:** 4 adversarial lenses (consumer-safety / live-blast-radius / born-dark-and-ops / does-it-close-the-gap) + a final gate. Consumer safety proven by a differential harness — all 8 `lighter-market` consumers produce byte-identical output with and without the key; none iterates keys or validates a schema. Live blast radius ZERO, demonstrated: neither live bot reads `lighter-market` by any path, neither writes `bot_state_history`, and they ship in different images so share no connection. Not born-dark: already `COPY`d (Dockerfile.freqtrade:34), looped by run_all.sh:152, zero imports added, `audit_image_imports.py` OK. The `does-it-close-the-gap` lens is what caught the 8x basis — the change was structurally fine and would have shipped a tape of confidently wrong numbers.
- **Stock Leaders postscript.** With the basis corrected, its picks paid **~7.7% true APR** against a **~52% breakeven** — funding was never its problem, and the "adverse selection" read published in (c) is **refuted** (Spearman -0.174, p=0.40; sign-unstable; untestable at n=25). What stands is unchanged and needs no funding data: **maxDD 37-44% with zero carry vs a 15% go-live gate**. Note the epoch-2 A/B's `AB_VETO_APR` 1.5 = **18.75% true** — ~3x tighter than breakeven, so it vetoes affordable names and sits in cash; as configured it measures the wrong thing. Agenda item 15 updated.
## 2026-07-17 (e) — 🕐 the clock's surviving options were never checked either: the MENU is withdrawn, not just the equities gate (operator: the refuters' doctrine argument "applies equally to scanners/heavy-jobs")
- **Correcting today's (b) entry, which is the point.** That entry withdrew the equities clock gate on the evidence and then wrote: "scanners/heavy-jobs (real resource rationale, no exit path) **remain the honest candidates**." That clause is wrong in the same way the thing it was correcting was wrong. All three options were authored by the same 16-Jul session, in one breath, with the same (zero) verification. **One of the three was finally checked against the real code and turned out to be fabricated. The other two did not survive scrutiny — they were never scrutinised.** They are survivors, not findings, and promoting them to "honest candidates" reproduced the exact unearned confidence that produced the fabrication, one line further down the same page. Struck: agenda item 11 no longer carries a pick-list.
- **The refutation had TWO grounds and only the empirical one got written down.** The one that got dropped is the one that generalizes: **wiring an advisory organ is the design decision the review exists to make.** `fleet_clock.py`'s own header states the contract — "nothing is FORCED to obey it until a consumer earns the wiring at a review (21-Jul agenda item 11)" — so the 17-Jul build violated the organ's stated terms before it ever reached the question of whether its premise was true. That argument does not care which option was picked; it applies to scanners and heavy-jobs verbatim. The empirical refutation was a lucky catch on top of a doctrine breach that would have stood regardless.
- **The refuted premise was still live in the organ's own docstring.** The (b) withdrawal cleaned the agenda item and left `fleet_clock.py` explaining its NYSE calendar "*because* the equity-perp books trade underlyings that only PRINT during NYSE hours; their perps trade 24/7 on a frozen index the rest of the time" — the facts are true, the "because" is the refuted inference, sitting in the first place the next reader looks. Fixed in place: the calendar STAYS (honest time math, pre-graded accurate to within one 5-min tick, DST-correct) and now says plainly that it does not come with a reason for anyone to obey it. A premise refuted in one file and left standing in another is how a dead idea gets rebuilt.
- **Item 11's question changed from "which consumer" to "does the clock get a consumer AT ALL."** "We built an organ" is not a reason to obey it; an advisory organ nobody obeys is a valid resting state, and a human reading the dashboard is already an honest use. The two struck options are restated as UNVERIFIED premises for the review to attack rather than inherit — explicitly including the restatement itself. **Scanners:** the claim "opens/closes are when dislocations and vol moves happen" was never tested, and note its shape — Gap Scout and Lighter Scout are CRYPTO scanners on 24/7 books, and the option keys their cadence to an EQUITY bell (the same family of unexamined assumption the equities premise turned out to be). It is cheaply checkable against data already collected: `gapscout-census` writes a history row on every episode open/close carrying a UTC `updated`, so hour-of-day episode density vs the `fleet-clock` transition log settles it with no new code and no shadow-days — with the honest caveat that epoch 2 only started 15-Jul and the record may be too thin to answer yet. **Heavy jobs:** no contended resource has ever been named; the sweeps compete for CPU with loops that are not on a deadline.
- **Nothing shipped to any bot. Doc/comment only; no consumer wired, no behaviour changed** — `fleet_clock` selftest green, still zero importers fleet-wide (verified by grep, not by assumption). The live bots are untouched by this round and were re-confirmed clock-free.
- **THEN THE SCANNER PREMISE WAS ACTUALLY MEASURED (operator: "may as well fix now") — NO SUPPORT FOUND.** New `clock_consumer_premise_check.py` (read-only, verdict + limitations in its header, re-runnable before the review). "Opens/closes are when dislocations and vol moves happen" is a DENSITY claim and the fleet already writes the tape: the Lighter Scout publishes a premium-stress cross-section every ~5 min, `bot_state_history` keeps 60 days. Over **827 samples / 69h / 3 NYSE sessions**, inside the clock's real ±15min window vs outside: `stress.med` **5.62 vs 5.62** (t=+0.06), `stress.p90` 15.59 vs 16.28 (t=−1.91), `stress.max` 67.65 vs 74.71 (t=−1.41), `oi_moves` 0.11 vs 0.06 (t=+0.98), tickets 17.06 vs 17.45 (t=−0.79). **Not one metric is higher in-window**; widening to any clock event (n=144) doesn't move it. Dislocation intensity at the bell is indistinguishable from any other moment on this venue. Kept honest as **"no support found", not "refuted"**: 3 sessions / n=36 in-window can't see an effect under ~1bps, and it measures intensity rather than the VALUE of acting in-window. Two metrics called INERT rather than cited: `prem_outliers` is a fixed top-8 list (length 8.00 both sides by construction — zero density signal), and `vol_surges`' "LOWER at t=−2.24" is an artifact of 0.00-vs-0.01 means (~8 surges in 791 samples), which cannot support a verdict in either direction.
- **The check caught TWO of my own claims from the entry above — which is the lesson eating its own tail, and the reason it got run instead of written.** (i) *"cheaply checkable via `gapscout-census`"* — **FALSE.** Epoch 2 has written **4 history rows total, spanning 12 minutes on 07-16**: two episodes, both CRO/USD on the same kraken→coinbaseexchange route (`scanner-cross-exchange-arb` = 2 paper_trades rows fleet-wide). No density exists to measure; **the Gap Scout half is NOT ANSWERABLE and stays untested** — the answer came from a different tape (`lighter-market`) than the one I'd confidently named. (ii) *"both are CRYPTO scanners on 24/7 books, keyed to an EQUITY bell"* — **FALSE**, and it stacked the deck against an option I had already struck: the Lighter Scout's ~215-book universe includes equity perps and its premium outliers are mostly equity names (SAMSUNGUSD, SKHYNIXUSD, ZHIPU, DRAM, ORCL). I argued confidently from an unverified claim about the scout's universe, inside the document warning against exactly that. Both corrected in item 11 with the measurement.
- **AND THE HEAVY-JOB PREMISE TOO — symmetry, because I'd measured one option and merely ASSERTED the other in the same breath.** "Heavy jobs running inside an event window cost something" has a prerequisite nobody stated, let alone checked: **something must actually CONTEND.** `run_all.sh` runs every organ in its own `( … ) &` subshell with its own sleep loop — independent processes sharing container CPU, not a sequential queue — and the canonical heavy job (the incubator) runs hourly in the same container as the scout, which loops `run; sleep 300`. A heavy job stealing CPU would stretch the scout's cycle. Over **831 intervals / 69h: p50 300.6s, p90 300.7s** vs a 300s nominal (p99 344.6s, stdev 20.1s, implied scout runtime 0.6s). 90% of cycles land within **0.7s** of nominal — the container is idle from the scout's vantage — and the slowest 10% spread FLAT across minute-of-hour buckets (worst 10 vs 7.0 expected), so the hourly incubator leaves no signature. **Nothing measurably contends; `heavy_ok` currently protects nothing.** Limits named in the script header: the scout is I/O-bound so this is an INDIRECT CPU probe (fair — a starved container would delay the `sleep 300` wake, and p90 says it doesn't — but a direct Railway CPU metric beats it); the incubator's phase drifts (`run; sleep 3600`) so minute-of-hour is weak corroboration, not the finding; contention could live on a resource this misses (network, DB pool, RAM) — name it, then measure that one. **My agenda line "no resource has been named as scarce" happened to survive measurement — luck, not method, and exactly why it got measured.** All three of item 11's original options are now measured or refuted rather than merely struck.
- **The scoped next test, if the review wants the scanner option alive:** the equity-perp SUBSET — `stress` is an aggregate over ~94 books, so a real bell effect confined to equity names could be washed out by the crypto majority. It needs the right bell per name (KRX/HKEX for the Korean/Chinese listings, not NYSE). Separately: a cadence boost for a Gap Scout booking 2 episodes in 2 days is aimed at the wrong bottleneck — the census is quiet enough that the board's growth rail is already widening its prefilter. **Still nothing wired; the review still picks.** Born-dark audit re-run OK across 19 images with the new script (inert in the one `COPY . .` image — no CMD path, no repo-local imports).

## 2026-07-17 (c) — 🏆 Stock Leaders' funding A/B was measuring the SEATBELT: epoch 2 makes the veto the only variable (operator: "constantly losing, review strategy and lets implement obvious changes")
- **The premise was checked first, and the RULE SURVIVES it — untouched.** The book is −$127.79 (−12.8%) on **3 closed trades in 4 days**, every one a catastrophic stop out of the single 13-Jul entry batch. Decomposed: **price ~−$110 (86%) / funding −$17.53 (14%)**. The price loss is not the rule's doing — SNDK/MRVL/NBIS/MU printed **−18.3% / −18.7% / −20.7% / −10.1% on Yahoo over the same window**: the marks were honest and the whole growth complex fell apart under it. Against this strategy's OWN backtested **44.3% maxDD**, a −12.8% week-one drawdown is well inside the envelope, and n=3 fills is not evidence — the same fills-not-hours lesson the incubator learned in (at) this week. Tuning a momentum rule on a 4-day momentum crash is the definition of overfitting; the obvious change was refused.
- **What IS structural: FUNDING ADVERSE SELECTION.** Live rates across the 25-name universe: 16 of 25 sit at Lighter's **28.0% baseline band** — but the rule's picks don't. **SNDK 967%, NBIS 687%, COIN 245%, MU 126%, HOOD 98% apr.** Ranking by 42d return ranks by CROWDEDNESS, and crowdedness *is* carry: 4 of 5 picks paid above baseline, two at 25–35× it. The justifying backtest (+43.7% CAGR / 44.3% DD, 15y, 10bps/switch) ran on **spot dailies and modelled no funding whatsoever**. Measured drag −$17.53 in 3.7d ≈ **2% of deployed capital**. Unlike the price P&L, this is a RATE measured across 5 names × continuous accrual — it converges far faster than 3 fills, and it is precisely the evidence this shadow book's docstring promised ("the shadow measures the real drag"). A +43.7% gross CAGR cannot pay a ~200% carry; even the 28% baseline eats most of it.
- **But the instrument built to measure that was measuring NOTHING.** The A/B funding-veto ledger differed from the real book in THREE variables at once: bootstrapped mid-cycle **at its own entry prices, in its own window**; carrying **no seatbelt** while the real book had one; and vetoing **only at rebalance**. It published `eq 829.30` against the real book's `872.21` — which reads as "the funding veto loses $43" — while `last_vetoed` sat **EMPTY: the veto had never once fired**. That $43 was the seatbelt. The 21-Jul review, the board, or a human would have drawn the exact opposite of the truth from it.
- **EPOCH 2 — the veto becomes the only variable.** The ledger is now SEEDED from the real book (identical legs/entries/accrual/marks plus a closed-book `ab_base`) so both ledgers start at the same equity **to the cent** — verified against REAL production state, delta `0.000000000000`. It MIRRORS the seatbelt and the rail flatten. The veto runs **CONTINUOUSLY**, because the spike is the entire cost: SNDK/NBIS entered affordable and only later printed 967%/687% (HOOD was observed moving **63% → 98% → 119% apr inside 20 minutes**) — an entry-only veto cannot see the carry that actually gets paid. Shared-leg entries inherit the real book's actual crossed fill and its mid-based sizing, so a 6bp spread can't masquerade as veto edge (verified `vs_real $0.00`). A veto-freed slot stays CASH to the next rebalance — back-filling mid-week would smuggle rotation cadence in as a second variable. Payload gains `epoch`/`vs_real`/`base`/`fund_paid`/`veto_exits`: `fund_paid` against the real book's `fund_realized+fund_open` attributes any edge to CARRY rather than price luck, and `veto_exits` explains a shorter `held` list that would otherwise reproduce the v1 mismatch. **Pre-epoch numbers are VOID.**
- **Two defects fixed en route.** (1) **Rebalance storm:** `(meta and not ab_meta)` gated the rotation — it re-fires EVERY loop for as long as the variant is flat while the real book holds, which is exactly what a fully-vetoed top-5 produces (SNDK 967% / NBIS 687% are one bad week from it). It would have silently demoted the validated WEEKLY rotation to ~6-hourly (bounded only by the Yahoo cache TTL) — and this bot's own sweep already found daily WORSE than weekly (maxDD 40.7 vs 36.9%, 176 vs 81 switches/yr). The one-time seed replaces it; the rebalance is the clock and nothing else. (2) **The daily-loss rail lost its BASELINE on every restart:** `day_start_equity` re-based to boot equity, so a redeploy part-way down a losing day re-anchored the 10% rail to the already-depressed number and it could no longer fire on that day's drawdown. The baseline now rides the persisted state (same UTC day) or the saved halt record. Same class as the 16-Jul `last_ts` fix.
- **LIVE-BOT RESIDUAL (standing audit rule — real money, NOT touched here):** `lighter_funding_bot` and `lighter_trend_bot` carry only the halt-record half of that rail fix, so they still re-base on a **PRE-halt** restart. Two adjacent suspicions were CLEARED in the same pass: `from_lighter` is identity, so the identically-printed rates (COIN/XAU 245.3, MU/CRCL 126.1) are Lighter's own discrete bands and not a symbol-map collision corrupting the live funding read; and the hourly-rate convention matches the live bot's `H = 24*365`. 21-Jul agenda item.
- **Verification:** epoch invariant against REAL production state (delta `0.000000000000`); 4 behaviour tests driving the SHIPPED `main()` through a fake venue (continuous veto exits a spiked leg; seatbelt mirrors both arms; no-veto/no-stop leaves the ledgers identical; storm trigger absent from executable code, checked against code lines not comments); shared-leg entry exactness at a 6bp crossed spread; live `--once` smoke against real Lighter mainnet. **The tests caught a real defect mid-build:** the first cut seeded AFTER the mark loop and so double-accrued one loop of funding — surfaced as a $0.01 epoch drift that had to be $0.00, fixed by seeding at the loop top. Ships on main; `perps-bot` (the recycled service that actually hosts Stock Leaders) verified by marker-grep per the trust-no-deploy-path rule.
## 2026-07-17 (d) — the guard goes into CI (the "PAT can't" was FALSE), and the "operator-gated" blockers were already closed
- **The born-dark guard now runs on every push/PR** — `changelog-check.yml` gained a `born-dark` job (the audit + its `--selftest`, whose negative fixture fails if the guard goes blind). Verified green in CI: run 29547436931, job "Born-dark guard (image imports)", 7s. It already exists for exactly this failure mode ("Parallel Claude sessions edit this repo… fail the build if…"), so that is where a guard against forgetting belongs — a rule that says *remember to run the audit* is the same control that failed three times in three days.
- **An inherited assumption was blocking it, and it was false.** Memory said the stored PAT lacks the `workflow` scope and `.github/workflows/*` must go via the GitHub web editor. TESTED rather than trusted: the push succeeded, the file is on origin/main, the job ran. Memory corrected with the evidence. This session has now been wrong-footed four times by inherited "you can't / it already works" claims (git-connected auto-deploy, gate0-push-rebuilds, "v3 shipped", and this) — **an inherited constraint is a hypothesis; testing costs one command.**
- **FLEET CURRENCY VERIFIED in the RUNNING containers (not from git) — every "operator-gated" item I had flagged was already done:** tide-rider-lighter-live (REAL MONEY) has fleet_tuning + the notional fix; trail-blazer-live (LIVE Funding Farmer) carries today's live-entry-path fixes; funding-farmer-shadow has fleet_tuning + apply_levers, so **the xp judge's ARM SKEW CLEARED** — it is genuinely RUNNING `enter-gate-0.30` with the lever applied and the clock restarted. All 13 other active services marker-grepped current; immune 0 sick. The "tide-rider frozen at 07-11, needs an operator restart" note was stale — fixed by the unfreeze round *before* I repeated it. Corrected in memory + agenda item 12, which can now grade the enactment→APPLICATION question it was created for.
- **Two comment-honesty fixes** (both mine, both caught by refuters, both docs-only): fleet_risk's blind-hold inherits a prior restriction across a cohort CHANGE (deliberate, restrict-only — but "a fresh cohort starts at 1.0" was only true with nothing to inherit); fleet_immune's detector will emit one expected, self-healing page in the ~2h after a deliberate `BRAIN_MULT_ENGINE=v2` is removed (payload ttl 26000s vs the brain's ~2h cycle).
## 2026-07-17 (c) — the BORN-DARK guard: the class that bit three times in three days now fails loudly at author time
- **Three incidents, one defect class.** `fleet_bus` (15-Jul — the L2 veto and L4 stake-mults never executed in the freqtrade image), `event_sentinel` (16-Jul — organ ran zero times), `brain_stats` (17-Jul — the brain ran its FROZEN v2 engine for a day). Every one: **the repo says a module exists, the image doesn't** — and the fleet's own (correct) `try/except ImportError` guards turned each into a silent degraded fallback instead of a crash. Nothing crashed; nothing paged; the only tells were a missing veto, a missing organ, and a `None` field.
- **NEW `scripts/audit_image_imports.py`** — dependency-free static analysis, milliseconds. Reconstructs each image's real file set (multi-source COPY, `--from=` skipped, `COPY venues/`, `COPY . .`), then walks the imports of EVERY python file in it — including inside COPY'd packages — plus every run path (CMD/ENTRYPOINT with continuations, any COPY'd `*.sh` incl. relative and `-m` invocations, and railway*.toml `startCommand`, which is how 4 services actually start). **Proven against all three incidents**: replayed at `967549a^` → flags `brain_stats` for its 3 importers; at `2850009^` → flags `event_sentinel`; and deleting `COPY fleet_bus.py` → flags all four `user_data/strategies/*.py` that import it inside `confirm_trade_entry` under try/except.
- **The first cut of this guard had the SAME shape as the bug it hunts, and the refuters caught it.** It never walked into COPY'd *directories*, so it was blind to `venues/` — the live bots' shared real-money surface — and to `user_data/strategies/`; it reported "OK — every repo-local import is COPY'd" over 11 real instances, and would have passed the fleet_bus incident documented three lines above the very COPY that fixed it. It also "passed" the base `Dockerfile` (`COPY . .`) by seeding *nothing* — clean by never looking. Rewritten package-aware, with a NEGATIVE fixture in the selftest (an image COPYing a package whose code imports a missing module MUST be a finding) — because "today's repo is clean" is a vacuous bar that cannot notice the guard stopped looking.
- **Deliberate omissions are now DECLARED, not invisible** (`BORN_DARK_OK`, 11 entries with reasons): the 9 shadow images that COPY `venues/` without `fleet_tuning` are *correct* — they sit outside the board's LIVE_ROWS, so the `live.clip_scale` lever (earned by the funding/trend bots' evidence) must not size them; `venues/__init__` falls back to env sizing. Both REAL-money images do copy it. Regime/trainer's `fleet_bus` likewise declared (retired service; backtests inert by design).
- **KILL-SWITCH BUG found while aligning the detector:** `BRAIN_MULT_ENGINE=V2` (any case/whitespace) was silently coerced to **v3** — the operator throws the kill switch and *nothing happens*, with no signal anywhere. Both `bot_learn` and `fleet_immune` now normalize the env identically (`.strip().lower()`) — they must agree or a typo'd switch silences the detector — and an unrecognized value now warns loudly instead of being swallowed.
- **Runtime backstop:** `fleet_immune` scans `brain-vitals` and pages when engine=v2 without a deliberate kill switch (env = INTENT, payload = REALITY, mismatch = the alive-but-sick class this organ exists for). Restrict-nothing: it pages, it never quarantines. Selftested incl. deliberate-v2-silent and stale-payload-silent (death ≠ sickness).
- **Honest gap:** NOT wired into CI — the PAT lacks workflow scope, so `changelog-check.yml` needs the GitHub web editor. A guard that depends on remembering is the control that already failed three times; that's the standing follow-up, flagged in CLAUDE.md.
## 2026-07-17 (b) — the review queue executed under delegated authority (operator: "assume control and fix the above") + the brain was SILENTLY running v2 in production
- **PRODUCTION BUG (found by data-validating IMB-24's gate): `brain_stats.py` was NEVER COPY'd into Dockerfile.freqtrade.** The deployed brain import-guarded its way to the frozen v2 engine from the day v3 "shipped" (16-Jul) — no decay weighting, no EB pooling, no Wilson bars, no episode grading ever ran in production; the tell was eps4h=None/n_syms=0 on all four lenses against n4h in the thousands. Same born-dark class as the sentinel COPY fixed in (a). Fix: COPY added; a SYSTEMATIC import-vs-COPY audit of every module in the Dockerfile found brain_stats the only remaining miss; and bot_learn now logs the fallback LOUDLY every run unless `BRAIN_MULT_ENGINE=v2` was set deliberately (IMB-29b — shipped hours before it proved its worth). Episode fields start accruing from this redeploy; IMB-24's veto-floor migration stays gated on ~4 days of REAL v3 data (agenda updated).
- **Fleet-clock consumer #1: ATTEMPTED, ADVERSARIALLY VERIFIED, AND WITHDRAWN — the agenda item's premise was false, and that finding is the deliverable.** Agenda item 11 offered "idle the equities books while NYSE is closed — the underlying doesn't print, a frozen index makes their marks noise… cheapest win, clearly correct." It isn't: **that describes a bot design this repo does not contain.** `lighter_index_bot.want_position()` and `lighter_momentum_bot.evaluate()` are pure functions of `closes`, and both `ref_closes()` fetch **Yahoo dailies of the real market** — neither signal has ever read a perp mark (marks are fill/seatbelt only). Both books already decide on real prints, so the gate adds ZERO signal quality: pure latency that (as built) deferred regime/rotation EXITS up to 65h across a weekend while the long pays ~28% APR funding. Both books' own labs have already MEASURED that cadence-coarsening hurts them (Index Rider: "monthly-eval Faber REJECTED, DD 26-29% vs 20-22%"; Stock Leaders: "monthly re-widens DD to 46.7%") — and it would have corrupted the funding-drag evidence these shadow books exist to produce, diverging them from their named IBKR/Alpaca control arms. **Both gates reverted; nothing shipped.** Two more traps recorded en route: the first cut wired `hyperliquid_momo_bot.py`, which is **Trail Blazer's 24/7 CRYPTO book** on the recycled momo-bot service, not Stock Leaders (names lie — check what a service RUNS); and Stock Leaders' universe holds BTC/ETH/XAU/XAG/WTI, so a whole-rotation gate pins 5 non-NYSE legs to the NYSE bell. Agenda item 11 rewritten with all of it — the equities option is WITHDRAWN, not deferred; scanners/heavy-jobs (real resource rationale, no exit path) remain the honest candidates, and any future equities attempt must argue FILL QUALITY and clear the existing 15y harness on both halves. The clock organ itself pre-graded HONEST (16-Jul transitions within one 5-min tick, DST-correct) — it is the wiring rationale that was wrong, not the clock.
- **dd-governor BLIND-HOLD (G1 amendment):** while the governor abstains (thin window, dd=None) a PRIOR clip restriction <1.0 is HELD instead of snapped to 1.0 — the same assert-nothing-must-not-release pattern as the board's blind-cohort hold; a fresh cohort with no prior restriction still starts at 1.0.
- **FLEET_RISK_MODE kill switch made TRUE (IMB-16):** advisory mode now publishes clip_scale=1.0 at the single point every clip consumer reads (taker/family never checked mode and would have kept down-scaling through a thrown kill switch); the governor's raw value stays visible as `clip_scale_raw` — which the blind-hold reads first, so the kill switch can't poison the hold memory. Header claim corrected.
- **bot_learn twin anchor (IMB-29a):** the venue_execution diagnosis now compares PER-TRADE means at the DIAG floor (twin_n≥10) — was absolute era-bucket pnl vs the twin's absolute lifetime whole-book pnl at n≥5 (differently-sized books made "venue problem" the default diagnosis; see live-vs-shadow-per-trade memory).
- **Review-state honesty:** EVBOARD_MODE=publish confirmed INERT (zero coded proposal consumers — the review item is a wiring design, not an env flip); proprioception day-1 snapshot recorded (organ alive: taker joint stance graded neutral Σ$0, gapscout HELPING, xp open); IMB-18/-20 deliberately deferred (tuner/scout are the sibling session's active surface tonight).
- **Verification:** 18-refuter adversarial verify across two rounds (3 lenses per fix). It refuted the clock gate 3/3 (withdrawn, above) and corrected IMB-29a; it did NOT refute the Dockerfile COPY, the blind-hold, the kill-switch honesty, or the loud fallbacks — those ship. brain_replay PASS with the bot_learn changes; every module selftest green (fleet_risk, proprioception, incubator, judge, board, tuning, brain_stats, scout_tuner). Ships on main; freqtrade-bots refreshed by `railway up` + marker-grep per the trust-no-deploy-path rule (the equities services need no deploy — their gates were reverted).
- **What this round says about the method:** three of the four defect fixes here were found by *validating data rather than reading code* (the brain's own eps4h=None told us v3 was never deployed), and the one DESIGN change was killed by refuters reading what the code actually consumes. The pattern worth keeping: a review item's stated rationale is a hypothesis, not a fact — check it against the code before building on it.
## 2026-07-17 (a) — fleet-wide UNFREEZE round + the sentinel that was born dark (operator: "unfreeze it and refresh and update with all our latest enhancements")
- **Every CLI-frozen service refreshed to main@e657fd6** and verified by marker-grep in the RUNNING container: tide-rider-lighter-live + trail-blazer-live (both REAL MONEY — pre-deploy equity/position snapshots, clean boots verified: equity guard restored, positions held, zero unexpected orders), tide-rider-lighter-shadow (refreshed in the same round so the live-vs-shadow pair stays same-code), funding-farmer-shadow, yield-harvester-shadow (now direction-prefixes its close reasons), market-context (**PR #57's paired per-coin divergence finally live after 5 days dark**), snap-back-shadow, perps-bot, family-lighter-shadow, freqtrade-bots.
- **Two auto-deploy assumptions proven FALSE in the same pass:** trail-blazer-live ("git-connected") did NOT pick up main — the LIVE Funding Farmer was missing the tranche's live-entry-path fixes until manually upped; and a gate0 push did NOT rebuild family-lighter-shadow (origin gate0 at e657fd6, container mtime 08:53). Deploy-path trust is dead: marker-grep the running image after every ship.
- **perps-bot identity corrected:** not a retired zombie — it hosts 🏆 Stock Leaders (equities-momentum-lshadow, held HOOD/MU) on a recycled service name. Checked before refreshing; names lie.
- **Event Sentinel was born DARK: the file was never in the image.** run_all.sh has looped `/freqtrade/event_sentinel.py` since 16-Jul but Dockerfile.freqtrade never COPY'd it; `|| true` ate the ENOENT every 10 minutes. One COPY line. Found because the post-refresh vitals sweep showed exactly one non-FRESH organ. The infra flavour of enacted-is-not-applied: shipped to git, looped by the runner, never in the artifact.
- **Post-round vitals: every organ FRESH/EVENT except the sentinel (fixed above).** xp-judge FRESH, phase running on enter-gate-0.30, held by the ARM-SKEW gate until the refreshed arm's receipted closes accumulate — the first receipted close ever landed minutes after the arm came up (`tuned:["xp.funding.enter_apr"], enter_apr:0.3`).
## 2026-07-17 (au) — the incubator was breeding a bot that does not exist; the lens veto gets one authority; proprioception stops grading on zero trades
- **The incubator honors the brain's LENS VETO.** The replay is deliberately veto-blind ("external bus state isn't in this tape") — right for a pure harness, and each CONSUMER is expected to correct for it. The tuner does (never widens a brain-vetoed lens); the incubator did not. Measured live 16-Jul: the brain grades **breakout (n4h=2241, avg4h −0.184%, hit 40.5%), dip (n=2110, −0.438%, 42.6%) and momentum (n=1178, −0.233%, 41.0%) negative at sample size**, so the live taker vetoes all three and only **divergence (n=2463, +0.041%, hit 55.2%)** can fill. **Ten of the tape's eleven closes came from lenses the live bot refuses to trade.** Fitness now scores only fillable lenses (`live_lenses`/`fresh_lens_fwd`, fail-safe OPEN + freshness-gated, mirroring the taker's own direction); the entry gene of a vetoed lens is dropped (`evolvable_genes`) while pleiotropic exit genes stay; payload gains `lenses_scored`/`lenses_vetoed`. Verified on the real tape: veto-blind top net **−$16.68 over 9 closes** → veto-aware **+$3.56 over 1 close**. The organ's true live-relevant evidence base is ONE trade.
- **The veto rule had two hand-copies and was about to get a third.** The taker's inline loop and `lighter_scout_tuner.vetoed_lenses` each defined it; the incubator — which breeds the very bars the veto decides are worthless — would have added a third. Extracted to `lighter_ticket_taker.vetoed_lenses()` as the single authority (the taker IS the behaviour); the tuner delegates, same `TT_LENS_VETO_MIN_N` env, identical semantics.
- **`conform()` — crash fix on the upgrade path.** The gene set is now dynamic (`reachable_genes` drops holds, `evolvable_genes` drops vetoed lenses, `DIV_GAP_PP` was added) and `breed()` indexes `a[g]` for every `g in genes` — so the **currently published elite, bred before the divergence gene existed, would KeyError the whole organ on its next cycle**. Stored genotypes are projected onto the current gene set (missing → module default, unknown → dropped, an off-grid allele actually in force is kept).
- **`fleet_proprioception.grade_taker` stops grading on zero trades.** Observed live: a **3.13h / 37-snapshot** taker episode returned `stance_net 0.0 / default_net 0.0 / delta_usd 0.0` and was stamped **`"graded"`** — nothing closed on either arm, recorded as a measurement. The floors (`MIN_EP_H`=0.75 → 45 minutes, `MIN_EP_SNAPS`=8) count time and ticks against a strategy whose positions live for hours. An empty sample is UNGRADEABLE, not neutral — and neutral is not free: it spends an episode toward `MIN_EPISODES` and lets a verdict form out of nothing. New `MIN_EP_CLOSES` (4) floor denominated in CLOSED TRADES (one fill swings ~$3.50 > `HURT_USD`/`HELP_USD` $3.00, so a single trade can carry a verdict), taken on the better-evidenced arm; new `too-few-trades` status, which `lever_verdicts` already ignores. Also gains **IMB-10 marked scoring** (closed + unrealized) — the last replay consumer still blind to deferral. Restrict-only and fail-safe by the organ's own contract: an ungraded episode restricts nothing AND earns nothing.
- **Verification:** proprioception selftest fixture reworked to RESOLVE trades rather than merely span time (N dip symbols → N closes on the stance arm; the default arm takes none — that IS the counterfactual), plus the live episode's exact shape (long + tick-rich + resolves nothing → `too-few-trades`), a thin-but-nonzero sample refused, and `lever_verdicts` ignoring the new status. Incubator selftest pins the REAL 16-Jul grades resolving to `{divergence}`, fail-safe open on empty/stale/unparseable, a negative lens UNDER the floor not vetoed, entry-gene-only dropping, lens-restricted `_marked`/`_pnl_usd`, and `conform` round-tripping a pre-divergence elite through `breed()`. Green across proprioception/taker/tuner/incubator/replay/judge/immune/regen/tuning/board/brain_stats.
- **Still open (21-Jul agenda).** `grade_taker` is itself veto-BLIND — it grades `taker.*` levers on fills the live taker won't make, the identical defect one file over. Not fixed here: it would silence taker verdicts almost entirely (only divergence is fillable and no divergence lever exists today), which is a call for the review, not a drive-by.

## 2026-07-17 (at) — the incubator breeds on ELEVEN trades: selection moved to the gametes, gates re-denominated in fills (operator: "an egg needs good sperm and good eggs to fertilise to produce good offspring... let's think genes and DNA")
- **The measurement first.** The incubator's own fitness function, run against the real tape (15-Jul 04:27Z → 16-Jul 23:03Z, 42.9h): **11 closed trades, 10 of them the same stop-loss**, closed net −$14.89; breakout 5/5 SL, dip 3/3 SL, momentum 2/2 SL, divergence 1 TP. A ~90-genotype GA — crossover, mutation, elite carry, both-halves champion gates — was searching a landscape made of eleven fills. Every genotype in the population scores NEGATIVE (best −$14.08). The organ's bottleneck was never its genetics; it is that no fitness signal exists yet. Marks only publish since 15-Jul, so the tape is 2.5 days old and cannot be older.
- **The gates were denominated below one fill — and were hours from opening.** At clip $50 / TP +4% / SL −3%, one trade flipping SL→TP swings **$3.50**; `EDGE_MARGIN` was $2.00 and `HALF_MARGIN` $1.00, so a single lucky fill cleared every anti-overfit bar. They looked strict and were not. Worse, `MIN_TAPE_HOURS=48` was the ONLY thing holding the champion gate shut and the tape crossed 48h at **17-Jul 04:27Z (14:27 Sydney) — the same afternoon**; with `PERSIST_CYCLES=3` hourly cycles the organ would have published a "stable" champion backed by ~12 trades by evening. Now: `MIN_CLOSES` (40) gates on **closed trades, checked first** — hours are not evidence, a 200h tape with 11 closes passed the old floor and was still noise — and both margins are denominated in TRADE SWINGS (`INCUBATOR_EDGE_TRADES`/`HALF_TRADES`; an explicit `INCUBATOR_*_MARGIN` env still wins). Verified on the live tape: `TENTATIVE — 11 closed trades < 40 min`.
- **Selection ran at BIRTH, not at the gametes (the operator's actual point).** `assess_champion` is rigorous and was decorative: the breeding elite were `scored[:ELITE_N]` ranked by raw net, with `both_halves_pos` only breaking exact float ties — i.e. never. So the very +$2.35/h1=+$0.01 noise genotype the champion gate REJECTS still bred, and elite feed forward into the next cycle's population (`prior['elite']`), compounding noise across generations. The gate protected the REPORT; nothing protected the GENE POOL. New `select_elite` filters gametes on MEASURABLE (≥`MIN_GT_CLOSES`=12 closes — you cannot select a genotype you cannot measure; same "starving lens earns its grading diet" logic the tuner runs) AND ROBUST (both halves ≥ HALF_MARGIN — the birth gate moved upstream). On the live tape: **0/20 genotypes viable**, and the old net-max winner is now refused as a gamete. Champion and elite are deliberately decoupled — the fittest is still assessed+published so its reason explains WHY, instead of the card going quiet.
- **Ranking on the point estimate IS the winner's curse** — this file's header said exactly that about the champion in July, then ranked the breeding pool on the point estimate anyway. `rank` now sorts on a one-sided 90% **t lower confidence bound** on total net (`net_lcb`; std error of a sum = sd·√n; t table df 1..30 converging to `brain_stats.Z80`, the fleet's existing convention — the normal approximation is far too generous at n≈11). One trade evidences no edge, ever. Replay gains an ADDITIVE per-lens `pnl_usd` (per-trade net $) — the mean alone cannot price uncertainty; no existing field moved.
- **IMB-10 reconciled + finished.** (as) added deferral-proof marked scoring inside `evaluate` concurrently; kept, lifted to a module-level `_marked` mirroring the tuner's. The missing half: the **MAX_HOLD grid still offered 48h/72h alleles on a 42.9h tape** — both unreachable, and the real tape shows ZERO hold-exits, so with 10-of-11 closes being stop-losses "never close anything" was the strongest gradient in the landscape and the grid supplied the allele to express it. New `reachable_genes` drops unreachable holds before selection can reward them (mirrors the tuner's `_skipped_holds`; never strips a grid bare). Verified: `dropped [48.0, 72.0] -> grid [24.0]`.
- **The gene pool was long-only and excluded the only lens not losing.** `taker.div_gap_pp` is in the fleet_tuning registry (300–700), in the taker's `TUNABLE`, and in the tuner's ladders — but `TAKER_GENES` omitted it, so the fleet's only SHORT lens was unevolvable. Added, with a grid that also explores **tightening** (600/700): the tuner's ladder only ever widens (500→300), so nothing in the fleet searched that direction. Divergence is also the only lens with a non-negative tape result (+$2.10 — on n=1, which is itself the point). Dashboard genotype card gained the `div` label (it was silently omitting the allele).
- **Verification:** incubator + replay selftests grown (reachable-holds incl. never-strip-bare, `_marked` deferral case, `net_lcb` zero-spread/one-trade/more-evidence properties, `t90` table + Z80 convergence, gamete selection refusing one-half and 3-trade genotypes, the closes floor shadowing every later bar, one-fill-cannot-clear pinned against `TRADE_SWING`, every allele of every gene registry-legal). Green across replay/incubator/tuner/proprioception/taker/tuning/brain_stats; dashboard parses. Exercised end-to-end on the live tape read-only.
- **NOT fixed — needs a decision (21-Jul agenda).** `fleet_proprioception.grade_taker` has the SAME `closed_net` deferral blindness (lines 299-300) with `MIN_EP_H=0.75` — it grades taker levers on **45-minute** windows against a 48h-hold strategy — and `HURT_USD`/`HELP_USD` of $3.00 sit **below the $3.50 single-fill swing**. It is the retrospective authority that can override the tuner's prospective gates (HELPING unlocks the expansion walk before the brain's ruling floor; HURTING blocks re-assertion), so a deferral artifact there steers the growth rail. Shadow-lane only (the live lane grades per-trade vs baselines, a different path) — no real money, not on fire. Changing verdict semantics on that organ is the operator's call.

## 2026-07-16 (ar) — judge residuals: relative fade, per-half floors, landed-write guard + the immune application invariant (operator: "all 4 go today, assume control")
- **Relative + rolling fade bar (`fade_check`).** The promotion bar is RELATIVE (+0.5pp vs live) but the release bar was ABSOLUTE (mean < 0, cumulative since promotion): a lever that cut live from +0.80%/trade to +0.10% destroyed the edge without inverting the sign and could stay on real money forever — and the cumulative mean converges toward its early value, so a LATE fade became mathematically unreachable. Now: both bars run on the ROLLING last FADE_N closes, and a second release path fires when the recent mean falls ≥ MARGIN_PP below the live arm's own PRE-promotion baseline (the paired window's live mean, stamped into state at PROMOTE as `promote_baseline`). Restrict-only: an extra release path can only pull a lever OFF real money. Fail-safe: missing baseline (old promotions / regen restores) → absolute bar only, byte-identical to the historical behaviour.
- **Per-half sample floors (`paired_eval`).** The full-window floors (30/10) said nothing about the halves, so ONE live trade in a half set that half's entire baseline — the doctrine's central noise filter degenerated into a noise amplifier on the exact comparison that moves real money. Each half now requires shadow ≥ min_closes//2 and live ≥ max(3, live_min//2); an even at-floor split still clears, a lopsided one holds with `why="hX under-powered"`. Derives from the same XPJ_* env knobs.
- **A dropped rail write is not an assert (`_asserted`).** `write_levers` returns None when nothing valid survived (unknown lever / out-of-lane / clamp reject / no DB) and every judge call site discarded it: a silently-dropped write left the judge counting days toward promotion on an arm running env defaults, and could stamp phase=promoted + push "PROMOTED to LIVE" while NO live lever was in force. All four sites now verify the write landed: idle-start refuses to start (and a registry-impossible candidate is marked INVALID and skipped, spending no judge slot, instead of retrying forever); running-re-assert failure HOLDS with the same semantics as ARM SKEW (no promote, no ABANDON aging, clock restarts on recovery, one urgent push per episode via sticky `assert_fail_notified`); a failed PROMOTE write stays RUNNING and retries — the promotion IS the write; a failed promoted-phase re-assert warns once (the TTL expiry to env defaults is already the fail-safe, but it must not be silent while the judge reports "promotion in force").
- **Immune application invariant (`application_sickness`).** The organ whose remit is alive-but-sick could see a lever whose VALUE is impossible but not one whose value is IGNORED — the fleet's largest fresh-but-wrong signal. New invariant on the receipt lanes only (xp.funding.*/live.funding.*, where the arms stamp extra.bars from inside apply_levers — unforgeable by a lever-blind image): a lever in force whose consumer closed ≥2 trades since first-seen (+15min loop grace, tracked in the organ's own `app_seen` map — the tuning payload carries no set-time) with ZERO matching receipts → SICK "enacted but not applied" → sick-list + phone. Deliberately NOT a quarantine: quarantining a lever the consumer provably ignores reverts nothing (that IS the sickness) and would feedback-loop on a healthy arm (quarantine → consumer runs defaults → receipts stop matching → sick forever). Fail-safe: zero closes = no evidence; dark ledger/tuning = nothing flagged.
- **Verification:** judge selftest grows to 12 pinned scenarios (all 9 prior unchanged + per-half floors, relative+rolling fade incl. the cumulative-miss case, asserted-write guard); immune selftest pins the invariant (first-sighting starts the clock, receiptless→sick, matching→healthy, wrong-value receipts→sick — the deaf-arm signature, floor, grace, non-receipt lanes never judged, expired levers forgotten). fleet_tuning + fleet_regen selftests green (payload shape additive).
## 2026-07-17 (as) — the balance audit's VERIFIED-REAL findings ship: 10 fixes across risk, judge, tuner, scanners + the live entry path (operator: "continue")
- **IMB-02 (dd governor, feeds the real-money up-gate).** A carried base now keeps its REAL venue in the cohort key (new `equity_venue` payload field) — one stale-flap of the hourly Tide Rider publisher no longer wipes the 7d dd sample series. And `dd_governor` ABSTAINS (dd=None) until the window spans `FLEET_DD_MIN_SPAN_SEC` (6h): a freshly-reset window used to hand the board's deliberately fail-closed dd leg a passing 0.0 — the erasure always landed on the RELEASE side, snapping clips 0.25→1.0 mid-drawdown.
- **IMB-03 + IMB-17 (LIVE entry path, rides next gate0 ff).** The coin-quality veto — the one consumed payload on the live-money entry path with NO freshness gate — now ages honestly: publisher stamps `updated+ttl_sec`, consumer discards a stale/unstamped set with a one-shot warning (fail-OPEN, its documented direction; legacy `ts` + env fallback cover the deploy-ordering window). And the live Funding Farmer finally READS the L2 light it was counted into: fresh + enforce + budget full → skip NEW directional longs only (shorts/exits untouched — the short budget's non-enforcement is deliberate, IMB-05 refuted); mirrors the trend bot's block; `FLEET_RISK_MODE=advisory` kill switch respected.
- **IMB-04.** `pulse_panic` in the family bot + all 4 freqtrade strategies now gates on the payload's own `updated+ttl_sec` — a fossil panic flag from a dead market_pulse kept halving every new entry forever (the 39h-fossil class, on an input the immune organ doesn't scan).
- **IMB-07 (judge; -06/-22 landed independently in (ar) above — reconciled, their richer relative-fade + env-derived floors kept).** The done-list AGES so the finite candidate universe can't permanently self-exhaust — but as FALLBACK-only retry (`pick_candidate`, pure + selftested): untried candidates always win, an aged ABANDONED/FADED/INVALID entry re-runs only when nothing untried remains (`XPJ_DONE_RETRY_D` 28d; legacy names stamped on migration; `done_at` persisted and stamped at all three verdict sites).
- **IMB-12.** `gapscout.extra_exchanges` hot-adds now REVERT: the venue set derives each scan from base EXCHANGES + the lever's current value, dropped venues prune `exchanges`/`markets_by_ex` and rebuild the symbol map — this was the one growth-rail lever whose promised TTL auto-revert was unreachable (one-way-until-redeploy).
- **IMB-10 + IMB-23.** Every tuner replay gate scores `closed_net + unrealized` (survivors marked at last tape prices) — a variant can no longer win a gate by deferring losses past the tape's end — and SWEEP_HOLD values the tape can't even reach are excluded from the grid; the incubator's funding baseline derives from the bot's own env names instead of hard-coded literals (an operator env drift no longer makes a no-op allele look like an experiment), dead `get_lever` dict removed.
- **The 21-refuter adversarial verify (3 lenses per fix) caught TWO real defects in this very tranche before it shipped — both repaired in the same commit:** (1) my coin-vetoes `ttl_sec=3600` was 6x SHORTER than the publisher's real 6-hourly quality cadence — on a perfectly healthy system the live veto would have been discarded ~83% of steady-state time (a tighten reflex weakened by its own freshness fix, plus a cry-wolf warning every 6h); repaired per the refuter's preferred shape: market_context re-stamps the payload's `updated` EVERY 5-min loop as a liveness attest (content still changes 6-hourly by design), so ttl 3600 genuinely means "12 missed loops = dead publisher"; the consumer's warn-once flag now re-arms on ANY fresh payload. (2) plain done-list aging would have STARVED incubator offspring permanently — the 3 statics sit ahead of offspring in pool order and rotate (~48d) slower than the 28d retry window, so an aged static would be re-selected ahead of every never-tried offspring forever; repaired as fallback-only retry (see the IMB-07 bullet above). Merge note: this tranche landed concurrently with (ar)'s independent judge work — the judge file was reconciled onto (ar)'s richer relative-fade/floors/landed-guard implementation with only the IMB-07 aging grafted on top; judge selftest green with both suites' cases. Also applied from the amendments: `_marked`/`_tape_span_h` regression pins in the tuner selftest, the incubator's bred-champion scoring gets the same deferral-proof marked net (dashboard-only today; a future consumer inherits no bias), and the dd-governor comment now records the deliberate ≤6h post-reset tighten-blind window (shadow-clip lane only) for the 21-Jul review. G1/G3/G4/G6/G7 survived all lenses outright.
- **Verification:** all module selftests green (fleet_risk incl. new dd-abstain cases, judge incl. rolling-fade + per-half-floor + fallback-retry cases, tuner incl. marked/span pins, incubator, gapscout, family, board, tuning, immune, both live-bot notional tests). Board/risk/judge/tuner/scanner/market_context changes ship on main; funding-bot + family/strategy consumer gates ride gate0.
## 2026-07-16 (ar) — verify round COMPLETE on the balance pass: F3 landed-guard repaired; 8 findings refuted; audit doc finalized
- **The adversarial verify finished (131 agents, 3 lenses per target).** Fixes F1-F2/F4-F8 survive 3/3. **F3 was REFUTED 2/3 and repaired the same evening:** the rail authors' unlocked fallback ignored `save_state`'s False — on a reads-OK/writes-FAILING DB (Postgres flipped read-only, conn reset between read and write) `write_levers`/`release_levers` returned a truthy payload that NEVER persisted, so the board's `live_write_ok` guard false-positived and the URGENT "LIVE clips x0.75" push could still claim a restriction that didn't land (one refuter reproduced it offline with a store stub). Both authors now honor the documented contract — None on a failed durable write — and the selftest pins the degraded-store case. Board-side guard logic unchanged (it was correct; its oracle was lying).
- **8 findings REFUTED by the verify pass (do-not-build), 4 contested:** recorded per-finding in `AUDIT_EXPAND_TIGHTEN_2026-07-16.md` (v3) so nobody re-litigates them from scratch; VERIFIED-real open items ranked in agenda item 14. gate0 was ff'd to the balance commit (live restart round — slot-anchor fix now on the live services); this F3 repair is author-side (freqtrade-bots container) and ships on main alone. NOTE the (aq) finding below: `tide-rider-lighter-live` runs a frozen 07-11 image with no fleet_tuning at all, so the clip lever is a silent no-op on that book until the operator restart round — the board's blind-hold/landed-guard machinery is unaffected, but enactment-vs-application verification (agenda 12) now covers the board's lever too.
## 2026-07-16 (aq) — the XP judge was grading an arm that cannot hear it: fail-closed on ARM SKEW (operator: "continue fixes")
- **The defect, on the one path that writes REAL MONEY.** `experiment_judge` was asserting `xp.funding.enter_apr=0.30` at a shadow arm with **no lever-reading code at all** — verified on the running service: `funding-farmer-shadow` → `apply_levers 0 · fleet_tuning 0 · get_lever 0`, and `fleet_tuning.py` → *No such file or directory*. Its gate is `ENTER_APR = os.environ.get("FUNDING_ENTER_APR", "0.40")` with the env **unset**, so it runs **0.40 — the same value the live arm runs**. The paired bar was therefore scoring 07-11-code-at-0.40 against 07-16-code-at-0.40 and attributing the residual (version skew + mark-vs-real fills) to a candidate that was never applied. A PROMOTE writes `live.funding.*`, which the live arm DOES consume. Ledger proof: `perps-funding-lighter-lshadow` = **30 closes, 0 receipts**.
- **Nothing was promoted** (`promoted_ts` null; floors at shadow 1/30, live 0/10) — a live defect caught before it cost anything, not a loss. The incubator had **6 more candidates queued** onto the same arm, each worth a 7–14d judge slot.
- **The missing invariant: nothing verified enactment→APPLICATION.** Every gate validated the DECISION to write a lever (registry clamp, lane binding, AUTHOR_LANES, TTL, paired bar, proprioception episodes); not one validated that a bot ever READ it. `blind_cycles` only catches a DARK ledger ("I can't see the data"), never a lying one ("the rows I see don't reflect my experiment") — precisely the alive-but-sick class `fleet_immune` exists for, which the judge sits outside.
- **The receipt already existed and had ZERO readers.** `lighter_funding_bot.py:493` stamps `extra={"bars": dict(_ACTIVE_BARS)}`, and `_ACTIVE_BARS` is populated **only inside `apply_levers()`** — a lever-blind image structurally *cannot* forge it, which makes a missing receipt **disproof, not silence**. `experiment_judge.py:22` has claimed *"Every close row carries extra.bars"* since 15-Jul; it was never wired. `fetch_paper_trades` never even SELECTed the column.
- **Fix (restrict-only — the gate can only ever BLOCK a promotion, never enable one):** `bot_pnl_store.fetch_paper_trades` now selects `extra` and passes it through (additive; `ADD COLUMN IF NOT EXISTS extra JSONB` runs in the same function before the SELECT, so all 7 consumers stay safe). New `ran_candidate(row, levers)` proof predicate. `paired_eval(cand_levers=…)` counts a SHADOW close only when its receipt matches the candidate — the **LIVE arm is deliberately NOT gated**: it is the control running env defaults and only 1 of its 14 rows carries a stamp, so gating it would starve the baseline and freeze the pipeline for good.
- **ARM SKEW is a distinct state, not a false negative.** Closes in-window but zero attributed → HOLD + one urgent push, and explicitly do NOT age toward `ABANDONED` (which would permanently retire a possibly-good candidate on a verdict about an experiment that never ran — `enter-gate-0.30` is the 11-Jul review's own item). On recovery the clock RESTARTS, so days accrued while deaf can't instantly trip `MAX_DAYS`. A stuck, loud queue beats a phantom promotion.
- **Verification:** the 8 existing judge selftest scenarios pass unchanged; new permanent cases pin that the OLD bar *promotes* on unattributed rows (`"shadow beats live by 4.90pp/trade over the window AND both halves"`) where the gated bar blocks, that an APPLYING arm still clears (not a brick wall), that a wrong-value receipt (0.4 vs candidate 0.3) is not proof, and that `cand_levers=None` is byte-identical to the historical bar. Found by a multi-agent adversarial audit; two of its findings were REFUTED by the verify pass and dropped (prop_fade is *not* inert — proprioception slices held stances every 24h; and the `extra.bars` evidence is *not* already in the DB — 1 of 14 live rows).
- **Operator-gated, NOT fixed here (real money, needs a restart round):** (1) `tide-rider-lighter-live` (REAL MONEY, frozen 07-11) has **no `fleet_tuning.py` at all** → the evidence board's only real-money lever, `live.clip_scale`, is a **silent no-op on one of the two live books** while the board records it as enacted. (2) `funding-farmer-shadow` needs a redeploy before ANY xp verdict is meaningful. Agenda item 12.
## 2026-07-16 (ap) — publisher gaps + honest organ vitals (operator: "can we address the above")
- **Launch Sniper published `pnl_abs` but never `equity`**, so `bot_equity_history` stored NULL and its go-live gate read `dd —` / `age —` forever (the gate filters `equity IS NOT NULL`). Now publishes the fleet's paper basis: $1,000 start + realized + open unrealized (the same `unrealized` added when realized-only readings hid 85 bleeding positions). The age gate honestly counts the equity track record we actually have (0/30d), **not** uptime — deliberate, not a bug.
- **Yield Harvester closed with BARE reasons** (`flip`/`decay_paid`/`max_hold`/`bleed_stop`/`delisted`), which `split_reason` leaves untagged **by design** — the 14-Jul fix, after treating exit paths as entry modes cost the brain 92 runs proposing to "tighten the 'flip' entry gates". So the fix belongs at the PUBLISHER, not the parser: both close sites now prefix the direction the bot already stores in `pos["side"]`. Verified against the real parser — `short_decay_paid` → tag `short` / exit `decay_paid`, so multi-underscore exits survive intact and the brain can finally grade the two legs apart. `pos["side"]` was already dereferenced unconditionally at both sites, so no new KeyError surface.
- **`fleet-tuning` + `xp-queue` sat permanently DARK on a calm fleet** — which is how operators learn to ignore DARK. Both are ON-ACTION writers, confirmed in source: `write_levers()` has `if not out: return None`, and the incubator's only `xp-queue` save is guarded by `if props:`. A quiet `fleet-tuning` means "levers auto-reverted" = the healthy resting state. Marked EVENT (silence is an answer). Liveness is not lost: `scout-tuner` + `strategy-incubator` are unconditional per-run heartbeats and stay TTL'd; neither key is watchdog-critical, so no pager change. Live after deploy: `fleet-tuning EVENT · 1 active levers · 0 lighter-live`, `xp-queue EVENT · 6 candidates queued`.
## 2026-07-16 (ao) — evidence-board review fixes + EXPAND↔TIGHTEN rebalance (operator: "merge and commit all fixes, ensure the expand vs tighten is balanced for real money and all other bots too")
- **THE IMBALANCE, measured then removed (LIVE lane, `evidence_board.synthesize_live`).** Tighten had five reachable reflexes; expand had ZERO reachable ones. DB ground truth: the up-ladder required EVERY live row ≥30 LIFETIME closes AND lifetime-positive — Tide Rider is a position HOLDER (**0 closes ever, by design**; the judge excludes it for the same reason) and the Funding Farmer sat lifetime −$0.30 despite a +$2.38 measured week. Lifetime anchors also made the down reflex a RATCHET (a healed book stayed "hurt" forever). Both directions now anchor on the SAME 7d realized ledger window (new read-only `bot_pnl_store.fetch_realized_window`): TIGHTEN = 7d ≤ −$10, lifetime ≤ −$20 backstop, dark-window fallback = the OLD lifetime rule (tighten never weakens on missing data); EXPAND = whole cohort fresh + non-harming (lifetime bleed ≥ −$1) AND ≥1 row PROVES the edge (≥30 lifetime closes, ≥5 window closes, positive week) — dark window fails CLOSED. The roles split honestly: the active trader proves edge, the structural holder proves non-harm. Verified read-only against the live DB: the gate is now REACHABLE (~2 weeks of normal funding-bot trading) but not satisfied today (light red + 14/30 closes) — earned, not gifted. All other gates unchanged (green light, shallow dd, calm venue, 24h cooldown, top-step-needs-measured-HELPING, hurting-release, divergence reflex, frozen-row fail-closed).
- **Fossil-brain lens warns (restrict-side fail-safe gap).** `synthesize()` freshness-gated `lighter-market` and `fleet-risk` but read the lens feed RAW while the expand side gated it at 26000s — a dead brain's last payload kept firing `lens-negative` warns (phone) + lens-veto proposals indefinitely. Both sides now share `LENS_FRESH_S`; `inputs_fresh.lens_forward` reports real freshness (was `bool(lf)` presence).
- **The live lever's honesty + leash.** A lapse/hurting release is now an EXPLICIT WITHDRAWAL — new `fleet_tuning.release_levers` (author can only release its OWN levers; locked read-modify-write) removes the lever instantly instead of waiting up to 2h for TTL while the phone claimed "back to x1.0" — a true removal, not a 1.0 overwrite, so proprioception sees a clean `released` episode end instead of a phantom no-op stance it would have to grade; the URGENT push and the ladder/cooldown memory only advance when the rail write actually LANDED (failed write = log + retry, never a false "protection applied" push on the money channel); the live lever carries per-entry ttl 1800s (a dead board reverts real clips in ~30min, not 2h) via new `fleet_tuning.write_levers` per-entry `ttl_sec` that can only SHORTEN the call-level TTL; after any release the up-ladder waits 1h before re-asserting (anti-flap — a gate oscillating at threshold can't strobe URGENT pushes); the growth-rail step push lists only the board's own gapscout levers (was the whole merged dict incl. other authors' lanes).
- **Notification memory:** full `seen {key: first_seen}` map persisted (prior payloads only kept the top-20 DISPLAYED items — anything ranked 21+ lost first_seen and re-notified as "new" after the gap); `synthesize()` dropped its unused `prior_keys` param and its docstring no longer claims condition-ENTER firing it never had.
- **The audit graded the fix itself and three amendments shipped in the same pass:** (a) BLIND-HOLD — the release trigger fired on ANY assert-nothing, so a vanished bot_pnl row would have instantly withdrawn an in-force 0.75 restriction (worse-than-TTL); now the DOWN reflex runs on whatever rows ARE visible (plus the rows-free divergence alert — partial cohort never disarms tighten), and a blind cohort/dark window HOLDS a restriction (re-asserted each cycle) — only measured healing or a reasoned hurting-grade releases it; expansions still release on any lapse (fail-closed both directions). (b) HOLDER BAR — Tide Rider's tighten bar had silently doubled to the $20 backstop (the 7d window is realized-closes-only and it closes nothing by design); a 0-window-close row now keeps the full -$10 bar on its mark-anchored lifetime. (c) LENS_FRESH_S now shared by both board callsites (the expand side had it hardcoded). Plus: growth-step memory only advances on a landed write.
- **Fleet-wide balance findings beyond the board (audit output):** 37 imbalances catalogued — 7 shipped now: the three above + brain `urgent` flag re-earned every run (a sticky flag from a v3 run kept fast-pathing straight through the `BRAIN_MULT_ENGINE=v2` kill switch — one line, brain_replay still PASSES), proprioception now observes only consumer-FOLLOWED levers (`observed_active`: quarantined / live-hurting levers stop generating episodes — a hurting verdict could previously heal itself on default-arm trades it mislabeled as scaled, then resume steering real money), and the live slot budget anchors to the operator's env clip (floor(cap/scaled_clip) meant a DOWN-scale minted position slots — a tighten action expanding a budget; venues/, needs the next gate0 ff to reach live services). The other 30 (none freeze-class): `AUDIT_EXPAND_TIGHTEN_2026-07-16.md` + agenda item 14.
- **Balance elsewhere confirmed, not changed:** shadow lanes are two-sided by design (tuner widens winners/starving lenses ↔ brain lens-veto + immune quarantine; gapscout widen-ladder ↔ TTL revert; judge promotion ↔ fade-watch); family/freqtrade books are restrict-only on stake BY DOCTRINE (brain authority ≤1.0 unchanged — their expand path IS the promotion pipeline). Multi-agent adversarial audit (10 subsystem mappers → synthesis → per-fix doctrine/money-safety/effectiveness refuters) run against the working tree.
- **Verification:** evidence_board + fleet_tuning + fleet_proprioception + fleet_immune + both live bots' notional selftests green (live-lane tests now mirror the REAL cohort shape — that exact shape earning step 1 IS the regression test; blind-hold, partial-cohort tighten, mark-bar and backstop paths all pinned); brain_replay PASSES with the urgent fix; read-only smoke of the full decision path against live fleet data. Board/rail/store/brain changes ship on main (freqtrade-bots container); the venues/ slot-anchor fix rides the next deliberate gate0 ff.
## 2026-07-16 (af) — dashboard: the EXPERIMENT bots now show their go-live gates (operator: "the experiment bots dont show go live metrics")
- **Root cause: the render suppressed them.** `render()` only emitted gate chips `if stg in ("ready","proving")`, so every `EXPERIMENT_BASES` book — Ticket Taker, **Snap Back**, Perp Sniper, Launch Sniper — displayed NO progress-to-live. Snap Back is the STANDING next go-live candidate (memory: lighter-go-live-decision), and you couldn't see how far off it was. `fetch_gate_metrics` was fine (it already unions both ledgers); the data existed and was simply never rendered.
- **Fix:** `("ready","proving","experiment")`. Stage classification is deliberately UNCHANGED — experiment books stay in the experiment section and can never auto-promote to "ready" on gates alone; this only makes their distance from the bar visible. `control`/`scanner` stay off by design (a reference book isn't on the promotion track).
- **Verified against the live DB first** (in-network), then rendered offline against each bot's REAL metrics: Snap Back `1/30 trades ✗ · WR 100% ✓ · dd -2.0% ✓ · age 5/30d ✗`; Ticket Taker `8/30 ✗ · WR 62% ✓ · dd -1% ✓ · age 2/30d ✗`; Perp Sniper `0/30 ✗ · age 6/30d ✗`; Launch Sniper `356/30 ✓ · WR 11% ✗`. All four have the `bot_equity_history` the dd/age gates need. Empty/dark bot renders all-✗ without crashing.
- **Also hardened `fetch_gate_metrics`**: its paper_trades leg did NOT exclude `side='skip'` / NULL pnl. Today those land under a separate id (`event-listing-sniper-skips`, 545 rows) so it only dropped a phantom gate row — but had skips ever been written under a real bot id they'd have silently inflated n30 and cratered its win rate. Same guard as fetch_bot_quality now.
- **Honest gap surfaced, not hidden:** Launch Sniper shows `dd —` / `age —` because its `bot_equity_history` rows carry NULL equity (the gate filters `equity IS NOT NULL`). A publisher-side gap, visible rather than faked.
## 2026-07-16 (ae) — dashboard: ALL bots (live + shadow) now share the same metrics (operator: "some bots have metrics others don't")
- **Root cause: two ledgers, one read.** `fetch_bot_quality()` computed win rate / profit factor / expectancy / last-close / 24h-count / per-mode breakdown `FROM bot_trades` on every query — the freqtrade era only. Every Lighter bot and scanner records to `paper_trades` and rendered BLANK. Worst of it: **both LIVE real-money rows write to paper_trades**, so the two books with actual money showed the LEAST detail while retired Kraken paper rows showed full stats.
- **Unified in SQL** (`UNION ALL` → `GROUP BY bot`), never client-side — `wr`/`pf`/`exp` are RATIOS, recomputable only from summed gross-win/gross-loss. Ledger differences handled: `pnl_abs` vs `profit_abs`, `closed_at`(TEXT, `pg_input_is_valid`-guarded) vs `close_ts`, `is_open IS NOT TRUE` (the column is nullable — `= FALSE` silently dropped closed-by-omission rows), and paper_trades' `side='skip'` rows (the sniper's gate-rejection LOGS, not trades) which would have poisoned every metric. `enter_tag` derived from `reason` mirroring `split_reason` — NOT the `tag` column (Gap Scout writes a venue ROUTE there). Keys stay fully-suffixed row ids: normalizing would merge a live book with its shadow twin. `COUNT(DISTINCT src)` logs any bot appearing in both ledgers (none today) — a card blending dead Kraken history with a live Lighter book is the one merge failure that looks plausible and is wrong. Output shape byte-identical, so no consumer breaks.
- **VALIDATED against the live DB before deploy** (in-network via `railway ssh`): **24 bots now carry metrics, up from ~7**. `perps-funding-lighter-lighter` (LIVE Funding Farmer) goes blank → `n=14 wr=86% pnl=+2.38`. Ticket Taker's lens tags resolve correctly (`long-breakout`, `short-divergence`, `long-dip`). `src=1` for every bot (no blending).
- **Two latent bugs fixed in the same change, because unification ARMS them:** (1) the `STOCKS` health-check skip compared BASE names against venue-SUFFIXED rows so it never matched — now `venue_variant(b)[0] in STOCKS`; (2) the flat `n24 > 15` over-trading bar was written for the slow freqtrade books — Launch Sniper closes **19/day by design** and would have banner-spammed every load. Now `OVERTRADE_LIMIT` per bot CLASS (Snap Back/snipers 60, Ticket Taker/Funding Farmer/Counterweight 40, default 15).
- **Honest remaining gaps (not papered over):** Tide Rider (`crypto-trend-daily-lighter`) shows blank because it has **zero closed trades** — 0 ledger rows, verified; it populates on its first close. Launch Sniper + Yield Harvester show `↳ ?` tags because their publishers write a bare `reason` with no entry attribution — a PUBLISHER fix (prefix `long_`), deliberately NOT fixed by loosening the parser (that regex gate cost the brain 92 wasted runs). Gap Scout renders on its optimistic-fill basis — not comparable to the rest of the fleet.
## 2026-07-16 (an) — workflow fix: the GitHub failure emails were a RETIRED service (operator: "im getting github failed notifications")
- **Every real deploy succeeded** — the last two Railway Redeploy runs went red only because the workflow still tried `railway up --service perps-regime-switch`, a service decommissioned in the 12/13-Jul retirement ("Service not found" ×3 → exit 1 → failure email), triggered on any `bot_pnl_store.py` change (both 16-Jul deferred-fix merges touched it). freqtrade-bots, pnl-dashboard, and funding-carry all logged "Deploy complete" in the same runs — the fixes ARE live.
- railway-redeploy.yml: perps-regime-switch block + path filters removed (comment records why); railway-logs.yml manual-dispatch default swapped to a living service. No runtime code in this change.
## 2026-07-16 (am) — the LAST deferred items ship: real live fills, the merge-race lock, and every remaining agenda-13 leftover (operator: "how do we get the remaining tasks done" under standing blanket approval)
Nothing on agenda item 12 waits on anyone now except review-grading. The two "can't do blind" blockers both fell to verification-from-here:
- **REAL LIVE FILL PRICES (13-vii — the last live-money measurement gap).** The blocker was the venue's fill-response shape; resolved by verifying against the REAL lighter-sdk (installed + inspected: `SignerClient.create_auth_token_with_expiry` returns (token, err); `OrderApi.trades(sort_by, limit, authorization, market_id, account_index)` -> Trade rows carrying price/size/ask_account_id/bid_account_id/timestamp). NEW `LighterClient.last_fill(coin, is_ask, since_ts)` — a READ-ONLY auth-token trades query returning the size-weighted average of OUR fills since the order went in; None on any failure. Both live bots record exits through it (`_real_exit`, all 5 close sites: funding normal/stop_blind/flatten, trend close_long/flatten) with the decision price as the honest fallback — the order path itself is UNTOUCHED. Entries were already real (the manage pass rebuilds meta entry from the venue's `avg_entry_price`). The implementation-shortfall tracker's live-vs-shadow premise now actually holds.
- **MERGE-RACE LOCK (13-v).** `bot_pnl_store.locked_state_update(key, fn)` — a Postgres session advisory lock (hashtext(key)) serializing read->merge->write on ONE bot_state key. `fleet_tuning.write_levers` uses it with the old unlocked path as the explicit fallback, so a lock failure degrades to exactly yesterday's behavior — the shared-infra-risk objection is gone. Three authors on independent timers can no longer drop each other's just-written levers.
- **Remaining agenda-12 leftovers, all shipped:** sniper ghost-skip AGE-OUT now covers the gate-PASS branch (a deterministically-skipped candidate no longer re-pends forever burning API calls); partial+final exits carry the reason IN the trade_id (same-second legs no longer overwrite each other in the durable ledger); tuner scout-diet ladders take their DEFAULTS from the scout's OWN env + only enact notches strictly WIDER (an operator-widened scout can never be tightened by its own tuner); bars+exits moving in one cycle now require a JOINT both-halves replay (interaction unproven -> exits dropped that cycle); funding-accrual clocks persist across restarts in all five accruing books (family per-book last_accrue + fundspread/index/momentum/carry last_ts; gap bounded 48h) — the systematic undercount of the drag/credit these books exist to measure is closed; Snap Back + Counterweight get the sniper's `VENUE` setdefault (a lost Railway var can no longer silently boot them as hl_paper).
- **Verification:** all 23 module selftests pass; every touched file compile-checked; the SDK call-chain for last_fill verified against the installed lighter-sdk models rather than assumed. Ships main -> gate0 ff (both live bots changed — live restart round; the fill recon is measurement-only but rides the same restart).
## 2026-07-16 (al) — deferred-fix round: operator gave blanket approval ("get around me having to do anything") — agenda-13 items SHIPPED
The 16-Jul audit deferred items needing a trading-logic change or an operator decision; the operator approved all of it and asked for zero remaining operator steps. Also confirmed: NO dashboard deploy step existed to remove — railway-redeploy.yml already auto-deploys pnl-dashboard on main pushes touching it (verified green on the audit merge: "Deployed: freqtrade-bots,pnl-dashboard"); the "manual railway up" changelog notes were stale.
- **ZOMBIE GUARD fleet-wide (agenda 13-i).** The sniper's 2-Jul delist give-up ported to all six exposed bots — Ticket Taker, Perp Sniper, Yield Harvester, family bot (in-list unpriceable + ORPHANS dropped from the coin list), Index Rider (same two paths), Snap Back (per-coin unmanageable sweep, gated on a LIVE reference feed so a whole-feed outage can never flatten the book). Design shared everywhere: a held position whose book/reference stays unpriceable for DELIST_GIVEUP_H (env per bot; 6h default, carry 24h) force-closes at the LAST KNOWN mark with exit reason `delisted`, freeing the slot and putting an honest row on the ledger. Clocks reset the moment a price reappears; carry uses modelled close costs (the live book is gone by definition).
- **Replay mark universe (13-ii):** the scout's history `marks` now cover ALL active books (~215) instead of liquid-only (~130) — replayed positions whose book dropped below the volume floor mid-hold could never exit and were valued at ENTRY, an optimistic bias in `closed_net`, the exact metric the tuner and incubator accept on. The taker-vs-replay delist give-up asymmetry is documented in the replay's honest-divergences list.
- **Per-bot clips live again on Lighter (13-iii, code not env):** `VenueContext.order_usd(..., own=True)` — Snap Back ($10) and Counterweight ($20/leg) keep their BACKTESTED per-bot clips instead of being silently overridden to the global $30 (Snap Back ran 3× its documented size). The funding/trend twins deliberately keep mirroring live sizing (control arm); live mode unchanged (global anchor + growth-rail lever).
- **Census double-count (13-iv):** a stage-1 sighting above the prefilter now refreshes an open episode's `last_seen` — a persisting gap crowded out of the stage-2 book budget (or restored after a restart) no longer sweeps "stale" and re-books as a NEW episode. Booking itself stays depth-confirmed in stage 2.
- **bot_state_history retention (13-vi):** first retention anywhere — `prune_history()` (default keep 60d, env `BOT_STATE_HISTORY_KEEP_DAYS`; every consumer reads days) runs on every ~200th `save_history` write + deterministically at boot via cleanup_legacy_bots.
- **Smaller audit items:** listing_intel K-prefix lookup is raw-symbol-first (KAVA/KAITO/KERNEL no longer mangled to AVA/AITO/ERNEL; kSHIB-style fallback kept — behaviorally tested); Perp Sniper finally publishes closed/wins/losses (ledger-seeded — the dashboard row had NO W/L record; dead `realized_seeded` var replaced with the seeding it promised); Snap Back's own MAX_OPEN cap stays senior to floor(cap/clip).
- **Still deferred with reasons:** live fill prices are decision mids (needs venue fill-response parsing verified against the REAL signer response — not writable blind from here; the one remaining agenda-13 code item touching the live order path), fleet_tuning merge race (bounded: expired levers cannot resurrect; a lock adds shared-infra risk exceeding the harm), sniper ghost-skip age-out + partial/final trade_id collision + joint tuner replay + scout-diet absolute notches + funding-accrual restart gap (all measurement-grade; queued for the 21-Jul review).
- **Verification:** all 23 module selftests pass; every touched bot compile-checked; K-prefix and census behaviors covered by test. Ships main → gate0 ff (shadow-bot restart round; live bots untouched this round).
## 2026-07-16 (ak) — FLEET-WIDE BUG AUDIT round 2 (operator: "have all bugs been fixed… check all organs, scanners and bots"): 6 parallel adversarial reviews → 19 fixes
Answer to the question first: every bug FOUND in the 14–16 Jul sessions was already fixed and deployed (verified in code + gate0 at main HEAD + all 18 then-existing selftests green). But six fresh adversarial audits (live-money, 15-Jul organs, core intelligence, scanners, shadow bots, dashboard/watchdog) found a NEW layer. Everything on a money or pager path shipped here; trading-logic/operator-decision items are DEFERRED to agenda item 13 (with the full list).
- **HIGH — daily-loss flatten ran exactly ONCE (`lighter_funding_bot.py`, `lighter_trend_bot.py`, LIVE).** A single failed close at the halt transition (rate-limit storm, venue blip) left that REAL position with NO stop until the UTC day rolled (up to ~22h) — the kill-switch path retries every loop; daily-loss didn't. Now `_flatten_all("daily_loss")` retries every halted loop (idempotent once flat; skipped when the kill switch just flattened).
- **HIGH — the judge's promotion bar was weaker than documented (`experiment_judge.py`, LIVE-reaching).** The 0.5pp margin only gated the FULL window; each half needed any edge > 0 — so h1 +1.8pp / h2 +0.01pp (pure noise) promoted live levers. Each half now clears the same margin (the doc'd bar); selftest carries the exact lucky-half repro.
- **HIGH — `fleet_risk` could still crash on list-shaped `held`.** The 15-Jul `held_items()` hotfix missed the SHADOW-tally loop (`held.values()` at ~line 362): the exact shape that killed the organ on 15-Jul would kill it again — light/governor/budget-veto all dark. Now list-tolerant at every site.
- **MEDIUM live-money hardening:** `_open_notional`'s last-resort fallback priced an untracked position at the CURRENT (possibly down-scaled) clip — now the venue's own entry notional (`abs(sz)*entry`), closing the residual cap-breach window; the phantom `_selftest_notional()` both docstrings cited NOW EXISTS (exact 16-Jul breach scenario, both bots, `--selftest`). Board `synthesize_live`: up-scale now requires FRESH live rows (`updated_at` ≤ 2h10m — a frozen row's lifetime-positive pnl kept earning up-scales) and `_fresh` honors each payload's OWN `ttl_sec` (a 40-min-stale fleet-risk with ttl 900 gated real money through the fixed 2700s window). Judge: promoted-phase ledger blackout was fail-OPEN (levers re-asserted forever, fade-watch blind) — now tolerates `XPJ_BLIND_MAX_CYCLES` (24) then stops asserting (expiry = env defaults); `_needs_reset` hardened (levers must be a non-empty dict, unmapped-lever spec resets, corrupt numeric fields degrade via `_num`); `save_history` snapshots FULL state so a fleet_regen repair is no longer a memory wipe.
- **MEDIUM — the ~18h divergence blind window.** The board trusts a `live-shadow-gap` alert for 6h but market_context's 24h dedup only re-stamped `ts` daily: a PERSISTING divergence had the down-scale reflex blind ~18h of every 24. `_alert` now refreshes `last_seen` (+msg) on every dedup-hit; board + immune age off `max(ts, last_seen)` (a still-confirmed alert is not a fossil).
- **MEDIUM — AUTHOR→LANE BINDING (`fleet_tuning.py`).** "The judge is the ONLY writer of `live.funding.*`" was convention — any author could write any enact-lane lever. `AUTHOR_LANES` + a live-prefix ownership rule now enforce it (board → `live.clip_scale` only; judge → `live.funding.*` only; tuner/unbound authors → never live). Docs de-drifted to match what ships (fleet_tuning docstring, board header, CLAUDE.md).
- **MEDIUM — phone-spam cluster (4 organs + watchdog).** `last_push` was dropped by the first `save_state` every cycle (immune/regen/respiration/shortfall) → NOTIFY_GAP was ~2 cycles; immune sick-ids embedded live numbers → every cycle a "new" sickness (page every ~30 min forever) — ids now digit-normalized; respiration paged urgent HYPOXIA in a loop on ONE failed batch read → per-key fallback, a fully-blind read publishes state="blind" (never a diagnosis), and hypoxia needs a 2nd consecutive cycle to page; watchdog transition dedup keyed on strings with live numbers ("freshest=312s") → digit-normalized keys + the 30-min gap now applies to every alert (flap-proof); "halted" (the NORMAL daily-loss state) no longer pages as NOT ONLINE (warning instead).
- **MEDIUM — /vitals could die of one bad payload (and take the pager with it).** One malformed organ payload (string ttl_sec, list-shaped state, alert without ts) killed the whole vitals surface, and the watchdog read the error payload as "no problems" — organ-death detection silently OFF (the alive-but-sick class again). Per-organ isolation → a loud red ERROR row; the watchdog now treats an unreadable vitals payload as a PROBLEM and pages critical ERROR organs like DARK ones; `fetch_flow_vitals` guarded; `_iso_dt` normalizes tz-naive stamps (was a TypeError that vanished the whole Autonomy card).
- **Dashboard honesty + the operator's ask ("no genotype list on the pnl"):** the incubator's genotype LEADERBOARD (elite top-5: net, h1/h2, genes, ⭐ champion) now renders under the 🧬 Incubator row on the main page; Autonomy card rows degrade per-row (one sick organ can't hide the stack); `$None`/`None%`/`phase None` leaks fixed everywhere (`_v` dashes missing values); missing SpO2 renders grey "—" not a green 0%; fleet-tuning + xp-queue graded EVENT on /vitals (event-written keys sat permanently DARK in a quiet fleet); active-lever counts (incl. the LIVE flag) now exclude EXPIRED levers (a reverted live lever displayed as active for hours).
- **Scout tuner — zero-evidence widen (the "both-halves passes on empty halves" class).** A starving lens whose tickets were unreachable at EVERY notch walked to ladder MAX (not_worse passed 0.0-vs-0.0 trivially) — the widest taker bar enacted on zero replay fills. A notch now only enacts when the replay actually FILLS at it AND not-worse holds; unreachable lenses correctly get NO widen (repro in the selftest).
- **Verification:** all 20 module selftests pass (18 existing + the 2 new live-bot notional tests); dashboard changes smoke-tested against the audit's exact crash payloads (malformed ttl/ts/list-state, naive timestamps, string spo2, junk elite entries, all-dark). Deferred findings (delisted-book zombie positions ×6 bots, replay mark-universe bias, census double-count, per-bot clip envs dead on Lighter, bot_state_history retention, live fill prices = decision mids, K-prefix mangling, merge race) → agenda item 13 with rationale. Ships main → gate0 ff (both live bots changed — live restart round).
## 2026-07-16 (aj) — 🦾 the REAL-MONEY BOTS consume verdicts: central get_lever hurting-revert (operator: "And with the new rule, real money bots too")
- **The latency window this closes:** when proprioception grades a live lever HURTING, the board releases the clip lever on its next 10-min cycle and the judge fades a promotion on its next hourly cycle — but until then (and until the lever's ~2h TTL) the LIVE BOTS kept consuming the measured-bad value. Now they don't: **`fleet_tuning.get_lever` reverts a lighter-live lever with a fresh HURTING verdict to the caller's env default AT THE CONSUMER** — the funding bot's `apply_levers` re-reads every loop and both live bots' clip re-reads every loop via `venues/VenueContext.order_usd`, so the revert lands within one bot loop of the verdict. Same one-hook-protects-every-consumer pattern as the immune quarantine, and it composes with it (quarantine checked first, then the hurting-revert).
- **Scoped and safe by construction:** live lane ONLY — shadow lanes keep TTL semantics (their authors already stop re-asserting on hurting; an instant consumer-side revert there would distort the learning dynamics the 21-Jul review grades). Fail-safe OPEN (dark/stale organ reverts nothing — a dead sense must not steer). Restrict-only: the hook can only ever hand back the operator's own default, never a widened value. Zero bot-code changes — per the 16-Jul audit rule the live-bot lever path was audited in this pass: both consumers route through `get_lever`, so the hook covers the entire real-money lever surface (`live.clip_scale` + `live.funding.*`).
- **Selftested:** hurting live lever → operator default; hurting shadow lever → unaffected (lane-scoped); verdict cleared → the asserted lever applies again; dark organ → no revert. CONTRACTS row updated (LIVE BOTS now listed as direct consumers). Ships main → gate0 ff — `fleet_tuning.py` rides in the live images, so a live-service restart round carries the hook.
## 2026-07-16 (ai) — 🦾 proprioception CONSUMER SUPPORT: first-class bus signal + incubator consumption (operator: "It needs consumer support")
- **The gap:** the organ's verdicts were consumed by three hand-wired readers (tuner/board/judge), each with its own inline `load_state` + freshness check — no supported client accessor, no off-Railway visibility, and the incubator was still proposing funding genes the live lane had just measured bad. Consumer support closes all three.
- **`fleet_bus.lever_outcome(lever)`** — the ONE supported accessor for any strategy/bot (the same client the family bot + freqtrade strategies already import for stake mults + the risk light): returns `helping|hurting|neutral|insufficient|None` under the standard fail-safe contract (dark/stale/absent organ, unknown lever, unknown verdict value → `None` = no evidence; backtests inert). fleet_bus gains its first `__main__` selftest (fresh/unknown/stale/absent, no DB).
- **`/bus.json` now serves `fleet-proprioception`** (live payload + `?hours=` history) beside the brain keys — the outcome grades are readable off-Railway by gate0 services, the 21-Jul review, and ad-hoc consumers, same contract as every other bus key (read-only, no auth, no secrets).
- **Incubator consumes verdicts (restrict-only):** `funding_proposals` skips any gene whose LIVE counterpart lever (`xp.funding.X` → `live.funding.X`) is currently graded HURTING — a 7-day judge slot is never spent re-proposing a knob the live lane just measured bad; the skip logs, releases when the verdict clears, and a dark organ skips nothing. Selftested (hurting gene skipped, siblings unaffected, empty/absent = no-op).
- **Dashboard CONTRACTS row updated** to the full consumer set (tuner hurting-skip + helping-walk · board items/clip-gates/ladder-discount · judge early-fade · incubator gene-skip · fleet_bus.lever_outcome · /bus.json). All selftests green (fleet_bus, incubator, tuner, board, judge, proprioception, immune) + dashboard compile.
## 2026-07-16 (ah) — 🦾 the LIVE lane LEARNS: paired episode grading + restrict-first consumption on real money (operator: "The live lane needs to learn")
- **(af)/(ag) left the live lane record-only** — episodes were kept for the review but produced no verdicts and nothing consumed them. Per the operator's mandate the live lane now closes its own act → sense → adjust loop, with the authority lines untouched: the judge remains the ONLY writer of `live.funding.*`, the board's clip ladder keeps every existing gate, and go-live/sizing caps stay operator-only.
- **Grading (`grade_live` v2):** a live episode is graded on per-trade `pnl_pct` (size-invariant — the judge's own lesson) against TWO baselines: the books' own pre-episode window AND the shadow twins over the SAME window (identical signals; the only difference is the lever + real fills). `bad` requires being worse than EVERY available baseline by ≥0.25pp (`PROP_LIVE_MARGIN_PP`) — real money is never blamed on one noisy comparison; `good` is symmetric; thin data (n<5 during, no usable baseline) stays `recorded`. **Attribution split by author:** `live.clip_scale` (board) and `live.funding.*` (judge) now grade as separate groups — the board's movement is never blamed on the judge's. Verdicts need ≥2 same-direction episodes (`basis: live-paired`).
- **Consumption — restrict-first on real money:** ① **board (`synthesize_live` + `live_clip_grade`):** a HURTING clip grade RELEASES the asserted lever (reverts to operator env sizing, urgent push via the existing dedicated channel) and silently blocks any up-start; the DOWN reflex stays senior to the grade. ② **judge (`prop_fade`):** a fresh HURTING verdict on a promoted lever releases the promotion EARLY — before the absolute fade bar (live mean <0 at n≥15) is reached; the judge stays the only writer, proprioception is evidence in. ③ **the single live EARN:** the clip ladder's TOP step (1.5) now REQUIRES a measured HELPING grade at 1.25 — **fail-CLOSED** (a dark/stale/neutral sense caps the ladder at 1.25; the 15-Jul audit-fix principle: a real-money up-scale never proceeds on missing evidence).
- **Selftests:** proprioception (bad/flat/good vs both baselines, the never-blame-on-one-noisy-comparison rule, funding-group isolation, live verdict floors, twin mapping), board (hurting releases with a restrict item / blocks a fresh start / hurt-row down-scale stays senior; top step refused on dark grade, granted on helping; grade sourcing), judge (prop_fade fires on a promoted lever's hurting, ignores unrelated/helping/stale/absent) — all green. Agenda item 12 extended with the live-lane grading checklist (incl. "would the old aggregate-only ladder have stepped where the measured gate refused?").
## 2026-07-16 (ag) — 🦾 proprioception EXPAND side: HELPING verdicts now EARN (operator: "implement the expanding side of things now so the July 21 can review both sides")
- **(af) shipped the sense with only the restrict reflex wired** (hurting-skip); the expand side was parked for the review. Per the operator's instruction it ships now, so the 21-Jul review grades a week of BOTH directions instead of one. Every earn stays inside the existing gates — fleet_tuning registry clamps + TTL, per-notch replay margins, the brain's veto senior, zero real-money lanes.
- **Three HELPING consumers:** ① **taker conviction bars** (`lighter_scout_tuner.desired_taker_bars`): a lever whose graded episodes measured net-POSITIVE (n≥2, ≥+$3 counterfactual — an evidence bar independent of the brain's) unlocks the winner-style expansion walk BEFORE the lens reaches the brain's n4h≥75 ruling floor; every notch still requires the improve-both-halves margin, and a brain-vetoed lens is never widened, helping or not. ② **scout grading diet** (`desired_scout_levers`): a HELPING diet lever walks one notch DEEPER while its lens is under the floor (dip 0.15→0.20, top-n 9→12; advisory tickets only; still released at the floor). ③ **Gap Scout widen ladder** (`evidence_board.gapscout_bar_scale` + `widen_step(bar_scale)`): a HELPING gapscout verdict discounts the quiet-hour bars ×0.75 (env `EVBOARD_GAPSCOUT_HELP_SCALE`, hard 12h floor `EVBOARD_WIDEN_BAR_MIN_H`) — ladder VALUES unchanged, only how long the board waits; the 🌱 item says when the discount is in force.
- **The live lane earns NOTHING** — `live.*` has no helping consumer anywhere; the experiment judge's paired bar remains the only road to real money. Fail-safe is now symmetric and explicit: a dark/stale proprioception restricts nothing AND earns nothing (`helping_levers` mirrors `hurting_levers`).
- **Selftests:** tuner (helping-unlock walk on a non-starving/non-winner lens, losing-tape refusal, veto-stays-senior, deeper-diet notches, un-helped levers hold notch 1), board (bar discount steps, 12h floor, scale sourcing incl. taker-helping ≠ gapscout-helping, stale⇒1.0), proprioception (helping hook symmetry) — all green; agenda item 12 extended with the both-sides grading checklist.
## 2026-07-16 (af) — 🦾 PROPRIOCEPTION: the autonomy stack grades its OWN enactments (operator: "advance, enhance and improve the autonomous organ")
- **The gap:** every autonomous actuator was gated PROSPECTIVELY — the scout tuner replays the tape that MOTIVATED a widening (in-sample), the board widens on census quiet-hours, the judge runs a paired bar. Once a lever was enacted, NOTHING looked back: no organ measured whether the fleet actually did better during the hours a lever was in force than it would have on the operator's defaults. The growth rail could act; it could not feel the consequence of its own action (the 15-Jul cap breach was only found by a human audit). Proprioception closes the act → sense → adjust loop.
- **NEW `fleet_proprioception.py`** (freqtrade-bots container, ~15 min): tracks lever EPISODES per lane-group stance (all taker.* bars = one joint stance — they replay together so they grade together; each scout.* diet lever its own; gapscout.* one; live.*/xp.* record-only). A stance change/expiry/release closes the episode (release END is backdated to the lever's own `expires` so a dead author doesn't inflate the window); stances held >24h are SLICED daily so grades accrue. Grades are OUT-OF-SAMPLE by construction (the deciding organ tuned on tape BEFORE the episode; the grade replays the tape recorded DURING it): **taker** = the true counterfactual $ (env defaults vs episode bars through the taker's real code via `lighter_ticket_replay`), **scout** = grading throughput (lens n4h delta — the diet levers exist to buy grades), **gapscout** = census activity (quiet_hours reset inside the window), **live/xp** = RECORDED ONLY — the judge's paired bar + fade-watch stay the sole real-money authority. Per-lever verdicts helping/hurting/neutral with anti-noise floors (n≥2 graded episodes AND ±$3; episodes under 45 min or 8 snapshots refuse to grade; joint stances share blame — conservative in the restrict direction; HURTING exists only on the taker lane, the one lane with a $ counterfactual).
- **First consumer (restrict-only — the always-allowed direction): the scout tuner.** `apply_proprioception` drops any would-be enactment whose lever carries a fresh HURTING verdict — the tuner stops repeating a movement that measured net-negative in reality, even while in-sample replay still likes it. Fail-safe: a dark/stale/absent proprioception drops NOTHING (a dead sense must not paralyze the body; levers stay bounded + TTL'd regardless). Expand-side consumption (helping earns anything) deliberately UNWIRED until the 21-Jul review rules — agenda item 12 written with the grading checklist.
- **Surfaced everywhere the operator triages:** evidence board 🦾 items (`synthesize_proprioception`: hurting = warn/restrict + names the auto-skip, helping = expand evidence, stale organ emits nothing), immune organ invariants (episode end<start, unknown verdict values — sickness that would mislead the tuner's skip), dashboard /vitals organ row + 🤖 Autonomy card "Outcomes" row + honest CONTRACTS row.
- **Verified:** new organ selftest (episode lifecycle incl. backdated release + daily slice, replay counterfactual win/lose/too-short, scout throughput, gapscout activity, live record-only, verdict floors + joint blame, fail-safe consumer hook) + tuner/board/immune selftests extended and green + dashboard card/vital smoke-tested crash-safe against realistic/empty/no-counts/malformed/stale payloads. **Doctrine intact:** no new expand authority, no live-money surface change (the live bots' code is untouched; live/xp lanes are record-only here), publishes-then-earns like every organ before it.
## 2026-07-16 (ae) — 🕐 fleet clock v2: market OPEN/CLOSE events (operator: "when any market opens or closes we are able to allocate resources")
- **The circadian organ now knows EVENTS, not just zones.** v1 published fuzzy crypto liquidity sessions (asia/eu/us/off + thin/weekend); nothing in the fleet knew that NYSE — the market behind the Index Rider 📊 / Stock Leaders 🏆 equity perps — opens and closes, or WHEN. Their underlyings only print during NYSE regular hours; the perps trade 24/7 against a frozen index the rest of the time, and no organ could time resource allocation around any open/close.
- **NEW in `fleet-clock`:** (i) `markets.nyse` — a real calendar: zoneinfo DST (13:30 vs 14:30 UTC opens handled), 2026-27 holidays incl. observed dates (3-Jul-26, 24-Dec-27…), 13:00-ET early closes (27-Nov, 24-Dec-26), `calendar_ok=false` degrade on unknown years, sessions-only degrade if tzdata is missing; (ii) `events` / `next_event` — the next open+close of EVERY market (NYSE + the three crypto sessions) with absolute `at_utc` (authoritative; the sampled `in_min` is display-only); (iii) `event_window` (±15 min pre/post, env `CLOCK_EVENT_PRE/POST_MIN`) with `window_events` naming what's opening/closing; (iv) **`heavy_ok` is now event-aware** — thin AND outside every event window (an open/close is exactly when resources belong on the market, not the sweep); (v) history writes on TRANSITIONS only (session change / thin flip / NYSE open↔close) — `fleet-clock` history is now an open/close event log instead of a 30-min heartbeat.
- **Cadence 30 min → 5 min** (`CLOCK_INTERVAL_SEC:-300`, ttl 900) so sampled window flags stay honest. Selftest extended: DST both sides, holiday + Monday reopen, early close, weekend, event ordering, ±window membership, thin+window→heavy_ok=false, 2028 calendar degrade — plus every v1 assert unchanged (existing consumers: none; dashboard renders old and new payloads).
- **Dashboard:** Autonomy-card clock row + /vitals now show NYSE open/closed + the next event with live relative time (`nyse open in 25m`), smoke-tested against rich/v1/empty payloads; honest CONTRACTS row added (published, unconsumed). **Doctrine intact:** advisory/publish-first — nobody is wired to obey it; candidate first consumers (equities books idle while NYSE closed · scanner cadence boost in event windows · heavy jobs on heavy_ok) queued as **agenda item 11** for the 21-Jul review.
## 2026-07-16 (ad) — dashboard: dedicated 🧬 Incubator / Reproduction card (operator: "implement the incubator in the pnl")
- **NEW `incubator_card()`** on the main page (mirrors evidence_board_card, wired via `brain_html`): the genotype-breeding stack made fully visible beyond the one-line Autonomy summary. Shows the CHAMPION with its anti-overfit confidence (tentative/candidate/stable, colour-coded — only STABLE is trusted) + net vs default + streak + both-halves; the ELITE LEADERBOARD (top 5 bred genotypes, replay-scored, each with net/h1/h2, a ✓both marker, and a compact genotype like `brk0.95 dip0.05 momo5.0 tp0.04 sl-0.04 hold48.0`); and the funding-candidate count proposed → judge. Live at ship time: fittest -$2.84 vs default -$8.41 (beats it by ~$5.57) but held TENTATIVE (22h tape < 48h min) — the anti-overfit gate visibly doing its job.
- Fail-silent (empty/dark hides the card; malformed payload renders nothing) and smoke-tested against realistic / stable-champion / empty / malformed payloads (no crash, no None/`$None` leaks) before deploy — the dashboard is the operator's only view. Deploys via `railway up --service pnl-dashboard`.
## 2026-07-16 (ac) — HOTFIX: experiment judge crashed every cycle since the name-based refactor (surfaced within minutes by the new dashboard visibility)
- **The new /vitals surfacing immediately paid off:** the moment the growth-rail organs became visible, `xp-judge` showed DARK (>9h stale) while its sibling loops were FRESH. Investigation found it CRASHING every cycle with `KeyError: 'levers'` at `run_once` line 291.
- **Root cause:** the 15-Jul name-based candidate refactor (commit a214606) didn't handle MIGRATION from the old index-based state. The live `xp-judge` state was written by the old code (`cand_idx`, no `current`/`spec`), so post-refactor `spec={}` while `phase="running"`, and `cand = spec if spec.get("name")==current` collapsed to `{}` → `cand["levers"]` threw. The judge's run_once path was NOT covered by the selftest (only its pure functions were) — exactly the gap I flagged when refactoring, and the reason the audit rule (verify runtime, don't trust selftests) exists.
- **Fix:** new pure `_needs_reset(phase, current, spec)` guard — a running/promoted phase with no valid spec resets to idle and re-selects from the pool cleanly instead of KeyError. Selftested (the exact crash case + mismatch + valid + idle). The judge recovers on next deploy: resets to idle, re-starts candidate `enter-gate-0.30` with the proper spec format.
- **No real money touched:** a dead judge wrote no `live.funding.*` levers, so the live bots ran on env defaults throughout (safe). Ships main → gate0 ff.
## 2026-07-16 (ab) — dashboard: the 11 headless growth-rail organs become VISIBLE (operator: "not many things we added last night appear on the dashboard")
- **Root cause:** everything built 15-Jul is the intelligence/autonomy layer — organs that publish to bot_state and act ON the bots, but place no trades, so they produce no P&L row. Correct by design, BUT the dashboard render (which predates them) surfaced none of them: 11 of 12 organ keys had ZERO refs in pnl_dashboard.py (only evidence-board rendered). They were running headless.
- **`/vitals` now grades all 11** (ORGAN_SPECS + `_organ_vital` branches): fleet-tuning, scout-tuner, strategy-incubator, xp-queue, xp-judge, fleet-immune, fleet-regen, fleet-respiration, fleet-clock, impl-shortfall, gapscout-census — each with a one-line vital sign and correct FRESH/LATE/DARK grading against its own ttl. Only `fleet-immune` + `fleet-respiration` are watchdog-critical for now (a DARK immune/lungs = flying blind); the other 9 non-critical to avoid a pager storm, promote per-organ once cadence is proven.
- **NEW 🤖 Autonomy / Growth Rail card** on the main page (mirrors the evidence-board card): active levers (LIVE subset flagged), tuner enactments, incubator champion + confidence, XP queue depth, judge phase+candidate, immune sick/quarantine/pruned, regen repaired/needs-operator, respiration SpO2, clock session, live-vs-shadow shortfall verdict+gap, Gap Scout census. Colour-coded by danger (red=sick/needs-operator/slipping, gold=quarantine/thin/enacted, green=healthy/promoted/ahead); a dark organ omits its row, an all-dark fleet hides the card, any error renders nothing.
- **Designed via a mapping workflow, implemented under tight control, smoke-tested** against all-dark / realistic / stale / missing-field / null payloads (no crash, no `$None` leaks) BEFORE deploy — this is the operator's only view of the fleet. Read-only display; no live-money surface is writable. Deploys via `railway up --service pnl-dashboard` (main pushes don't build it).
## 2026-07-16 (aa) — AUDIT FIXES: a CRITICAL real-money notional-cap breach + 5 more (two parallel adversarial code reviews of the 15-Jul organs)
Two subagents ran a correctness bug-scan + an integration-synchronicity check over everything built 15-Jul. Synchronicity came back CLEAN (21 levers, one author each, no collisions, no dead reads, deploy wiring complete, immune-quarantine loop correct). The bug-scan found one critical defect and five lesser ones — all fixed here.
- **CRITICAL — live notional-cap breach (`lighter_funding_bot.py`, `lighter_trend_bot.py`).** The entry gate estimated open exposure as `open_now * order_usd` — correct only when every position was opened at the CURRENT clip. The 15-Jul live growth-rail made the clip variable: a `live.clip_scale` DOWN-scale (0.75) shrinks `order_usd`, which RAISES `max_open=floor(cap/clip)`, and with held positions still sized at the larger clip the understated estimate let a new entry breach the operator's hard notional cap (worked example: cap $150, 5×$30 held = at cap, down-scale → a 6th $22.50 entry allowed → $172.50, 15% over; up-then-down reaches ~2×). Ironically triggered by the risk-OFF down-scale, and it contradicted the "cannot add exposure" claim made when the live lane shipped. **Fix:** new `_open_notional()` measures REAL deployed notional — each held position at its OWN clip (`meta['clip']`, or `size*entry` fallback) + this loop's opens at the current clip. Unit-tested against the exact breach scenario on both bots (now correctly blocks the 6th).
- **MEDIUM — `pnl_pct` on the wrong clip (`lighter_funding_bot.py`).** `pnl_pct = pnl / order_usd` used the CLOSE-time clip; a position opened at $30 and closed after an up-scale to $37.50 reported per-trade return understated ~20% — and only the LIVE arm scales, so it skewed the shadow→live promotion judge (and the shortfall tracker). **Fix:** the entry clip is now stamped in `meta['clip']` and passed to `_record_close` at all close sites (both bots).
- **LOW ×3:** (`fleet_regen.py`) `is_clean` reused `immune.organ_invariants`, which freshness-gates — so an AGED history snapshot's corrupt content was never inspected, defanging tier-2 self-repair in its own 3–24h window; now it stamps a fresh `updated` for the content probe (tested: aged `phase=haywire` now detected). (`evidence_board.py`) the live UP-scale gate fail-OPENED on a missing `fleet_dd_7d`; now fails CLOSED (real-money default). (`bot_pnl_store.py`) `fetch_bot_pnl` omitted `pnl_pct`, so the immune organ's NaN/absurd-`pnl_pct` sickness check silently never fired on real rows; column added.
- **ACKNOWLEDGED, not fixed (LOW):** the immune `fleet-alerts` prune races `market_context`'s append; harm is a dropped fresh alert that re-fires next cycle (no data loss, both writers use the identical shape). Left as-is with its owning comment — a proper atomic-append fix exceeds the harm. Also low/by-design from the sync audit: `evidence-review` is operator/scheduler-authored (no in-repo writer); `-lshadow` Dockerfiles omit `fleet_tuning.py` (safe — live-only gate; a footgun ONLY if a shadow were promoted to live without adding the COPY).
Ships main → gate0 ff (both live bots changed — live restart round).
## 2026-07-15 (z) — 📏 implementation-shortfall tracker: is the LIVE book slipping vs shadow, and where? (operator question on the funding live-vs-shadow gap)
- **The finding that prompted it:** the operator read the live Funding Farmer as "much worse" than its shadow. By `pnl_abs` it looks that way (+$1.66 shadow vs −$1.39 live) — but those are DIFFERENT-SIZED books ($1000 paper vs ~$61 real). By the fair metric (return PER TRADE) the LIVE bot is actually AHEAD: +0.80%/trade vs shadow +0.62%, higher win rate (86% vs 71%). Its equity dip is two tail losses (avg −6.4%, one −10% hard-stop) on a tiny concentrated book at n=14 — variance made visible by book size, not signal decay. This is exactly why the judge compares per-trade %, never equity.
- **NEW `implementation_shortfall.py`** — the continuous tracker behind market_context's threshold alert. Pairs live vs shadow per-coin per-trade `pnl_pct` (same coins, same window; the only difference is execution — live=real fills, shadow=mark), publishes the weighted gap + per-coin breakdown + a rolling verdict (clean / live-ahead / live-slipping / insufficient) + history, and DECOMPOSES the gap into ENTRY-slip and EXIT-slip bps once fill prices are on the ledger — so "slipping on exits" gets a clean yes/no. Sustained (`SUSTAIN` cycles) slipping → phone. Board surfaces the verdict (dedicated-push, no double-send). Selftested incl. long/short entry+exit decomposition.
- **`lighter_funding_bot.py` now records `side` + `entry_price` + `exit_price`** on every close (both arms) — live's real fills vs shadow's mark fills are the raw material for the entry/exit decomposition, which starts accruing now (the metric answers "is it exits?" cleanly once ~a day of paired closes lands). Ships main → gate0 ff (funding restart round).
## 2026-07-15 (y) — bloodstream batched reads + incubator anti-overfit (operator: "improve the bloodstream" + honest critique of what we built)
- **BLOODSTREAM — `bot_pnl_store.fetch_states(keys)`**: one query returns many bot_state keys instead of N round-trips. Wired into the organs that read a fistful of bus keys per cycle: evidence board (~8→1), fleet_immune (5→1), fleet_respiration (4→1). Same fail-safe contract (batch {} on failure → callers fall back to load_state/defaults). Cuts Postgres round-trips fleet-wide — the blood carries more per beat, and fewer beats means less contention (the thing that causes the slow-cycle stale-flags).
- **INCUBATOR ANTI-OVERFIT** (the flaw the first live cycle exposed: "fittest" genotype scored +$2.35 but h1 was +$0.01 — all edge in one half = noise). A genotype is now only a trusted CHAMPION when it clears `assess_champion`: tape spans ≥48h (was scoring on ~7h), EACH half ≥ +$1 (both-halves BY MARGIN, not just >0), beats the DEFAULT genome by ≥$2, AND stays champion for 3 consecutive cycles (persistence). Until then the leaderboard is published but flagged `tentative (noise-risk)` — trusted by nothing. Funding offspring still only reach live through the judge's paired bar regardless. Selftested: the +$0.01-half case is now correctly REJECTED as noise; short-tape/thin-edge/streak-reset all covered.
- Held the line (per the loop-frequency discussion): cadences stay matched to each signal's refresh rate, NOT maximized — faster polling invites venue WAF/rate-limit bans, hammers the single Postgres, and self-defeats into MORE staleness.
## 2026-07-15 (x) — market pulse: 30-min history (operator: "more data to better inform") + its freshness made readable
- **`market_pulse.py` appends history every ~30 min instead of hourly** (`PULSE_HIST_GAP_SEC` 3300→1740), cap 200→400 (`PULSE_HIST_CAP`) so the ~8-day window holds at the doubled resolution. `latest` still refreshes every ~10-min loop; the brain's mood/panic-conditioned analysis and the board now read twice the history points.
- **Pulse now carries the fleet freshness contract** (`updated`+`ttl_sec`) — it never had one, so `fleet_respiration` couldn't read its age and FALSE-FLAGGED it as a dead oxygen feed (SpO2 dinged on first boot). Fixed at the source; respiration's market-pulse limit tightened 2h→30min to match the real refresh. Now every organ (respiration, immune, fleet_bus.is_fresh) can see pulse's age like any other feed.
- Note recorded for the 21-Jul review: loop cadence is matched to each signal's natural refresh rate, NOT maximized — faster polling past the signal rate invites venue rate-limits/WAF bans (Lighter has blocked Railway REST before), hammers the single Postgres, and is self-defeating (slower cycles → more staleness). Pulse at 30-min history / 10-min refresh fits fear-greed + funding cadence.
## 2026-07-15 (w) — FOUR organs: 🩹 regeneration, 🧬 reproduction, 🕐 circadian, 🫁 respiration (operator: "build all of the above" + "what is the oxygen / what processes it")
- **🩹 `fleet_regen.py` — REGENERATION (self-repair tier 2).** The immune organ recognizes + quarantines; regen RESTORES. When a STATEFUL organ (xp-judge phase machine, scout-tuner, gapscout-census) gets wedged on fresh-but-wrong content the immune organ flagged SICK, regen restores its last-good `bot_state_history` snapshot (age-bounded to 24h) or a registered safe baseline. Content-only, carrying the snapshot's ORIGINAL age + a `_regen` marker, so a restored payload reads as exactly the aged snapshot it is (fail-safe-stale until the organ republishes) — never asserts old data as current. Reuses the immune invariants for "clean". Never touches live/account state or the ledgers. Phone-pushes repairs.
- **🧬 `strategy_incubator.py` — REPRODUCTION.** Breeds whole GENOTYPES (crossover of the elite + single-allele mutation, deterministic, on-grid). TWO substrates with the safety line between them: TAKER genotypes are scored INSTANTLY by replay over the recorded tape (shadow-only $1k book — a leaderboard, zero live reach); FUNDING genotypes can reach the live bot, so they are only PROPOSED to `xp-queue` and the experiment judge runs each through the IDENTICAL paired bar (≥7d/≥30 closes/both-halves) — no offspring gets a shortcut a human-written candidate wouldn't. Recombines within `fleet_tuning` registry bounds ONLY; genuinely new trading LOGIC stays a human+backtest job (invention ≠ adaptation).
- **Experiment judge now consumes the queue** — refactored from index-based to NAME-based candidate tracking (`done`/`current`/`spec`) with a pure `candidate_pool()` (static CANDIDATES + admitted xp-queue offspring; an offspring carrying an unknown lever is rejected) + `next_candidate()`. The promotion bar itself is UNTOUCHED. Selftested.
- **🕐 `fleet_clock.py` — CIRCADIAN.** One shared sense of time: trading session (asia/eu/us/off, overlaps modelled), thin-liquidity (weekend / off-seam / deep-night), heavy-job window. Advisory/publish-first.
- **🫁 `fleet_respiration.py` — RESPIRATION (answers "what is the oxygen").** OXYGEN = fresh market data (money is food — slow; data is oxygen — minutes). MOLECULE = mark/index/bid-ask/funding/vol/OI. LUNGS = the venue-fetch layer; MEMBRANE = the equity guard + freshness filters. The gap it fills: the watchdog catches organ DEATH, the immune organ catches SICKNESS, neither measures the SUPPLY. Respiration computes blood-oxygen SATURATION (weighted fraction of data feeds breathing fresh) and phone-alerts on a HYPOXIA transition — the fleet-wide data-starvation a per-organ check misses (exactly tonight's family-bot WS-drop, writ fleet-wide). 
- All four selftested; ships main → gate0 ff (one restart round for all four). Board already surfaces immune 🛡️; these publish for the /vitals + board to pick up.
## 2026-07-15 (v) — 🛡️ IMMUNE + SELF-REPAIR organ: covering "alive but sick" (operator's "what self-repairs / what filters" framing, made concrete by the same-evening false-down-scale incident)
- **The gap:** the whole fleet's safety model treats failure as DEATH — every fail-safe contract says "stale/silent → ignore." That leaves ALIVE-BUT-SICK uncovered: an organ publishing FRESH, in-TTL, trusted data that is WRONG. It bit real money hours earlier (the 39h fossil driving a false live down-scale). The watchdog is a pain receptor (screams at DARK organs); nothing filtered the blood or recognized fresh-but-wrong.
- **NEW `fleet_immune.py`** (freqtrade-bots container, ~15 min). **FILTRATION (kidney/liver):** prunes the `fleet-alerts` feed of age-stale fossils (the feed count-caps at 50 but never pruned by AGE — that's why the fossil survived) and known-toxic ANTIBODY matches (seeded with the retired whole-book "P&L gap +X%" pattern). Safe by construction — only removes stale data. **ADAPTIVE IMMUNITY:** scans fresh payloads for invariant violations (scout n_liquid>n_books or stress.med<0; lens-forward n4h<0 or hit4h∉[0,1]; census negatives; xp-judge unknown phase; bot_pnl NaN/inf/absurd-pnl_pct; a fleet-tuning lever outside its registry bounds), and — the one active remediation — QUARANTINES a sick lever. `fleet_tuning.get_lever` now consults `fleet-immune.quarantined_levers` centrally (one hook protects EVERY lever consumer) and returns the operator default for a quarantined lever = revert to the operator's own value = the safe direction. Phone-pushes NEW sickness (the watchdog's blind spot). Fail-safe OPEN: a stale/dead immune payload quarantines nothing (a dead immune system must not paralyze the body; levers stay bounded + TTL'd regardless). Board surfaces sick findings as a 🛡️ warn item.
- **Explicitly restrict/clean only** — never opens positions, never widens, never an expand action. Selftested: age+antibody filtration, lever-out-of-bounds detection, organ invariants with death≠sickness (a STALE organ's impossible content is NOT flagged — that's the watchdog's job), bot-row NaN/inf/absurd, and the central quarantine hook in fleet_tuning. Ships main → gate0 ff.
## 2026-07-15 (u) — HOTFIX: false live down-scale on a stale artifact + the "Proposed (shadow)" mislabel (operator caught the confusing push)
- **The bug the operator saw:** a phone push read "💰 LIVE clips x0.75 … Proposed (shadow)" — contradictory, because the live lane ENACTS, it doesn't propose. Ground truth from the log (`LIVE clip_scale: None -> 0.75`): the board DID enact the down-scale, but (a) the generic notify template mislabeled the enacted live action with the shadow-proposal wording AND double-pushed it (the dedicated "LIVE clips x0.75" push also fired), and (b) — worse — it fired on a **39h-stale, retired-check artifact**: the `live-shadow-gap` alert in the feed is the diagnosed "+5.4%" whole-book-ratio fossil from 13-Jul that the paired per-coin check replaced on 15-Jul (f). The 48h freshness window let a fossil cut real money.
- **Fix:** live down-scale now requires a divergence alert within `EVBOARD_LIVE_GAP_FRESH_H` (default 6h) — a genuine execution divergence re-fires within market_context's cadence; stale/retired signals age out. Generic notify loop now SKIPS dedicated-push keys (`board:live-clip-scale`, `board:gapscout-quiet`) so live/growth actions aren't double-sent or mislabeled, and expand items say "Suggests:" (never "Proposed (shadow)"). Net effect next cycle: no fresh divergence → `desired_live=None` → the 0.75 lever expires → live bots return to the operator's env sizing (the small live books were never in danger — down-scale is the safe direction — but it was wrong).
- Selftest reproduces the exact fossil (39h `live-shadow-gap` +5.4% → asserts no down-scale) and confirms a FRESH alert still cuts. This is the "alive but sick" failure class (fresh-looking but wrong data driving an action) caught live — motivating the immune/self-repair organ (next).
## 2026-07-15 (t) — 🧪⚖️ EXPERIMENT JUDGE: shadow wins now carry across to the real-money bots — through the bar that separates edge from luck (user: "positive changes made to shadow bots… get carried across to the real money bots… so that they also carry the edge to a win")
- **NEW `experiment_judge.py`** (freqtrade-bots container, hourly). The Funding Farmer's `-lshadow` twin becomes a managed EXPERIMENT ARM: the judge runs ONE candidate at a time via bounded `xp.funding.*` levers (new lane `lighter-xp`, zero real money) — first in the queue is the enter-gate widening to 0.30 that the 11-Jul scanner review explicitly parked as "opt-in, shadow-validate first". Every close row now stamps `extra.bars` (the exact bars that arm was running) so attribution is ledger-grade. **Promotion bar (the anti-Trail-Blazer clause):** ≥7 days, ≥30 shadow closes and ≥10 live closes in the SAME window, shadow positive in its own right, AND beating the live arm's per-trade pnl_pct by ≥0.5pp on the full window AND on both halves. Only then does the judge write the `live.funding.*` counterpart (it is the ONLY writer of that prefix) — and both arms run the same bars again, restoring the control-arm function. **Fade-watch:** if the live arm goes negative over n≥15 since promotion, the judge stops re-asserting and every lever expires back to env defaults; cooldown, next candidate. Abandon at 14 days without clearing. Phone pushes per transition (urgent for PROMOTE/FADE). Tide Rider deliberately excluded — ~0 closes/week can never fill a paired bar; its edge stays backtest-validated.
- **`lighter_funding_bot.py`**: arm-aware `apply_levers(mode)` at loop top — shadow consumes `xp.*`, live consumes `live.funding.*`, env defaults rule when no lever is live (recomputed from env each loop, so expiry reverts cleanly; `ENTER_GATE` recomputed). `_record_close` stamps `extra.bars` on every ledger row. Board surfaces the judge's phase as a 🧪 expand item.
- Selftests: promote / lucky-half reject / margin gate / floors / shadow-positive-in-own-right / fade / registry-and-live-twin mapping — plus the full organ sweep re-run green. First cycle behavior: judge starts candidate 'enter-gate-0.30' on the shadow arm within the hour; earliest possible promotion ~22-Jul (after the 21-Jul review inspects the bar — agenda item 10(v)). Ships via main → gate0 ff (funding service restart round).
## 2026-07-15 (s) — 💰 the growth rail reaches the LIVE bots (user: "i want evidence and scanning for live bot changes also and for it to implement also… the live one which actually has money")
- **New `lighter-live` lane, deliberately ONE lever:** `live.clip_scale` [0.5–1.5] — a bounded multiplier on the operator's env clip (`LIGHTER_ORDER_USD`), consumed in `venues/VenueContext.order_usd()` for `lighter_live` mode only and re-read every loop by Tide Rider + Funding Farmer (new entries only; open positions untouched). **It cannot add exposure:** SafetyRails' notional cap is operator-only, divides the open-position budget by the scaled clip, AND re-checks at order time — the lever reshapes clip size within the operator's own risk boundary.
- **The author is the evidence board (`synthesize_live`), and sizing UP must be EARNED on every gate at once:** every live row ≥30 closes AND net positive, fleet light green, 7d drawdown > −2%, venue stress calm — then ONE ladder step (1.0→1.25→1.5) per 24h cooldown, re-asserted per cycle, TTL auto-revert to 1.0 the moment any gate fails. **Sizing DOWN needs no permission:** a live row at ≤ −$10 or a live-vs-shadow divergence alert scales clips ×0.75 immediately. Every change (including release back to 1.0) pushes URGENT 💰 to the phone and lands as a severity-action board item. Kill switch: drop `lighter-live` from `FLEET_TUNING_ENACT_LANES`.
- **First decision is a no-op by design:** at ship time Tide Rider has 0 closes (−$0.08) and Funding Farmer 13 (−$0.39) — neither hurt enough to scale down nor proven enough to scale up, so no lever is asserted and live sizing stays exactly the operator's env until the books earn it. Go-live itself (keys, dry_run, notional caps, top-ups) remains operator-only forever — this lane tunes ALREADY-live sizing inside the operator's caps, nothing else.
- Board writes are now ONE combined `write_levers` call per cycle (gapscout + live) so merge semantics never drop the board's own levers; selftests cover the earn-ladder, cooldown hold, ladder ceiling, instant down on hurt/divergence, red-light/hot-venue/missing-row refusals, and registry bounds. Ships via main → **gate0 fast-forward (deliberate live-service restart round)** so the live bots actually carry the consumer code.
## 2026-07-15 (r) — ⚖️ evidence board gets its OTHER EYE: expand-direction synthesis (user: "it's so restrict heavy … completely one sided")
- **`synthesize_expand()`** — the board now authors POSITIVE evidence with the same machinery it uses for warnings, every item `direction: expand`: 🏆 **winner lenses** (brain-graded positive at the n4h≥75 ruling floor — the exact inverse of the lens-veto rule; points at the tuner's replay-gated expansion + review promotion), 🌱 **tuner enactments** surfaced on the triage board (fresh `scout-tuner` state), 🚀 **promotion watch** — shadow books passing the provisional expectancy screen (n≥30 closed, ≥+$10; deliberately NOT the WR>55% rule the sniper's fat-tail book falsifies — agenda item 5's gate rules at the review, go-live stays operator-only), and 🔓 **restrictions that never bind** (venue stress med ≤ half the taker veto bar → calibration item; the board now questions its own restrict side).
- **Expand items push to the phone** like warnings do (default priority, seedling tag, same 6h/key gap): missing the cream is as real a failure as missing a risk. Items and proposals carry `direction` end-to-end; feed inputs stay fail-safe (stale lens-forward/tuner/market state contributes nothing).
- Selftested (winner/loser/tiny-n classification, stale-feed suppression, live-row exclusion, small-n exclusion, direction tagging) + full board selftest green.
## 2026-07-15 (q) — 🧠🔧 SCOUT TUNER: the Lighter loop becomes self-tuning (user: "make the brain elite… it needs the freedom, with the brain's support, to act on things, skyrocket the learning")
- **The gap this closes:** the Lighter loop (scout 🛰️ → taker 🎫 → brain 🧠) could only learn and RESTRICT — the brain grades every ticket counterfactually and the taker vetoes negative lenses, but every WIDENING needed the operator to ship env changes. Live proof of the cost, caught tonight: the **dip lens is the fleet's best grader (hit4h 92%, avg4h +1.08%) and its most starved (n4h=25 vs ~250 for every other lens)** — the cream was exactly what the fixed bars were leaving unlearned.
- **NEW `lighter_scout_tuner.py`** (freqtrade-bots container, hourly, stateless — every cycle recomputes desired bars from env DEFAULTS + today's tape, so auto-revert is the resting state): ① **starving lens** (tickets seen, zero fills, under the brain's n4h≥75 ruling floor) → widen its taker conviction bar one ladder notch at a time, each notch replay-gated NOT-WORSE on both halves of the tape through the taker's real code (`lighter_ticket_replay`); ② **winner lens** (brain-graded positive at the floor — the taker-veto rule inverted) → expand its bar while each notch IMPROVES the replayed net on both halves by ≥$0.50 — the capture-the-cream rule; ③ **exit-ladder sweep** — agenda item 2's TP/SL/max-hold grid auto-run with anti-overfit floors (≥10 baseline closes, winner beats baseline ≥$2 total AND both halves); ④ **grading diet** — a lens under the ruling floor gets its SCOUT emission bar held one notch wider (dip range 0.10→0.15, brk 0.90→0.87, momo 3.0→2.5, top-n 6→9): advisory tickets only, more brain grades per hour, zero trading surface, released the moment the floor is reached. Never widens a brain-vetoed lens; fail-safe NEUTRAL when lens-forward is missing/stale; skips below 60 tape snapshots. Phone push (🧠 tag) on every enactment change; publishes bot_state `scout-tuner` + history.
- **fleet_tuning v2:** per-lever `expires` (levers die individually) + MERGED writes (board and tuner share the key without clobbering) + 11 new registered levers on two new zero-real-money lanes: `lighter-scout` (emission bars, top-n) and `lighter-taker` (conviction bars + TP/SL/hold, bounds = the agenda's own sweep grids). Scout + taker consume levers each cycle (`apply_tuning()` — env defaults stay authoritative when no lever is live); the taker also publishes its effective `bars` in extra so the dashboard shows what was in force at any time.
- **Doctrine:** replay-first IS backtest-first — no taker-bar change ships without beating/matching the tape on both halves; live books have no levers (NO_REAL_MONEY untouched); the brain's veto stays senior to every expansion. Selftests: ladders/veto/win-widen/lose-reject/floor-release/anti-overfit/registry-bounds + all six sibling organs re-run green. Dry-run against the real 89-snapshot tape behaved exactly to spec (sweep refused at 5 closes; fail-safe neutral without the brain feed).
## 2026-07-15 (p) — Gap Scout EPOCH 2 + the fleet's GROWTH RAIL (user: "implement now… the scanner needs to be able to implement opening or altering a strategy or widening something too so growth can happen fleet wide")
- **🔀 Gap Scout epoch 2 — the honest counter (agenda item 10-A shipped).** The unit of account is now the EPISODE: one booking per (symbol, buy-venue, sell-venue) gap episode, re-armed only after the gap closes below −10bps or falls silent 15min (the v1 odometer re-booked persisting gaps every ~19s — the whole +$5,225). Bookable = BOTH legs carry ≥$5k of REAL liquidity within 1% of their own book mids (`ARB_MIN_TOUCH_DEPTH`) — measured from the Stage-2 order books because live probes proved bulk tickers can't do it (CBX/Gemini `fetch_tickers` carry NO volume and NO bid/ask, only `last`; a bulk-ticker floor of either kind disqualified 2 of 3 venues outright in the smoke test). Balances reset to $0 with the odometer parked in `extra.legacy`; every booking writes a ledger row (`trade_id` prefixed `e2:`) carrying a **capacity curve** (the same books repriced at $250/$1k/$5k/$25k). First live scan on the new rules: 135 candidates, 15 hollow books rejected by the touch floor, best depth −1.5% → nothing books, correctly.
- **Gauge fixed at the source:** `liquid_top_pct` (the bus's `xexchange_dislocation_pct`) is now DEPTH-CONFIRMED — real top-of-book mids of the best major candidate (+2 book fetches when the budget missed it), None when no major clears the prefilter; the stage-1 reference number stays as `liquid_ref_pct` diagnostics. Kills the DOT/USDT class (dead CBX book, $2.8k/day, 4.5h-stale last = the 15-Jul "1% majors dislocation"). NEW cross-quote gauge: majors' USD-vs-USDT/USDC books normalized through the LIVE stablecoin rate (never par) → `xquote_top_pct` + census. NEW `gapscout-census` bot_state key: episodes open/closed, day counters, `quiet_hours`, >5% artifact band top-3, depth-rejected count, venues, effective prefilter.
- **🌱 fleet_tuning.py — the GROWTH RAIL (new organ contract).** Every prior autonomous actuator was restrict-only; per the user's mandate the fleet can now WIDEN itself: a whitelisted, hard-bounded, TTL'd lever registry (unknown names dropped, values clamped, levers expire back to defaults unless re-asserted — auto-revert is the resting state). Lanes gated by `FLEET_TUNING_ENACT_LANES` (default `paper-scanner` only); real-money lanes are structurally impossible (no such lane exists in the registry; NO_REAL_MONEY unchanged). Registered today: `gapscout.prefilter_gap` [0.10%,0.30%], `gapscout.max_book_fetches` [10,60], `gapscout.extra_exchanges` ⊆ {kucoin,gateio,mexc,bitget,htx}.
- **⚖️ Evidence board gains the ENACT tier (expand direction).** New widen-when-quiet ladder off the census's `quiet_hours`: 24h → prefilter 0.15% + 45 books; 48h → 0.10% + 60 books + hot-add kucoin,gateio; 96h → +mexc. Enactments re-asserted per 10-min cycle while the evidence holds, phone-notified on each step increase (`seedling` tag), surfaced in the board payload (`growth_step`, `enacted`) and proposals now carry `direction: restrict|expand`. Restrict proposals stay SHADOW (EVBOARD_MODE unchanged); the scanner consumes levers each scan and can hot-add venues mid-flight (init failures blacklisted, ≤6 venues, `ARB_EXCHANGES` env unchanged as the default set).
- **Selftests extended and green** (fleet_tuning clamp/lane/freshness; scanner episode machine book-once/hold/re-arm/stale-sweep, capacity thin-out, cross-quote live-rate-vs-par, touch-depth floor incl. the hollow-book case; board ladder monotonicity + every ladder value verified in-registry/in-bounds). Live `--once` smoke test ran the full pipeline against Kraken/CBX/Gemini. Scanner + freqtrade images both ship `fleet_tuning.py`. Agenda item 10 annotated with what shipped and what the review grades (episode census, enactment log, first trading-lane lever candidacy).
## 2026-07-15 (o) — Gap Scout's "+$5k" answered → agenda item 10: honest counter, THEN widen the net (operator question; docs only)
- **Forensics on `pnl_abs +$5,225` (gross +$6,995):** it's an odometer, not a book — bookings only when modelled edge ≥ 0 (losses impossible), and a persisting gap re-books every ~19s scan (~190×/hour). The counter restarted at $0 with the 14-Jul persistence deploy, accrued the whole figure in ≲30h, then froze all 15-Jul (zero PAPER-FILL lines in every retrievable window, best depth ≈ −1%, balance bit-identical across deployments) — the burst-then-flat signature of a few milked stale-book/collision episodes. Artifact caught live: the bus's "1.0% majors dislocation" (DOT/USDT) was CBX's dead book ($2.8k/day, last trade 4½h stale) vs Kraken's live mid — Stage-1 `last`-fallback lets dead books manufacture gaps; the DISLOC_BASES fix bounded the BASE, not the BOOK. Pre-ledger attribution unrecoverable (upsert row + ephemeral CSV + purged logs). Verdict: nothing real is being missed on the CEX legs (0.66–1.0% combined taker fees; docstring expects flat; no CEX execution in the fleet) — the useful exhaust (Lighter premium, majors gauge) is already on the bus.
- **Agenda item 10 written (the operator's widening ask included):** A. honesty fixes — episode dedup, fresh-bid/ask + book-volume floors (booking AND the `liquid_top_pct` gauge), balance epoch reset once ledger-backed (per-fill ledger itself shipped in (j)). B. widen the DETECTION net, publish/census-only, sequenced strictly after A: second-tier venues (KuCoin/Gate/MEXC/Bitget/HTX, region-probe first), cross-quote USD↔USDT via live USDT/USD reference, multi-size capacity census ($250–$25k), mine the >5% band with a base-identity check (listings/halts/depegs → listing-intel, not arb), prefilter/book-budget knobs if scan time allows. Anti-widenings named (par-USDT, softer haircuts, bigger clips, looser MIN_NET_EDGE — fiction inflators). Decision shapes (a)–(d) at the review, judged on episodes/day by class from the new ledger.
- Main only, no code, no service semantics change; gate0 deliberately not fast-forwarded (docs don't bounce live services — the (h) precedent).

## 2026-07-15 (n) — operator notes on 🎯 Launch Sniper: EWY dropped (shipped), 03–05 UTC session note (agenda item 9)
- **`"EWY", "EWYG"` join `TOKENIZED_BASES`** (`listing_sniper.py`; operator: "consider dropping EWY/USDT from event-listing-sniper's whitelist" — the sniper has no whitelist, the skip-set is the drop mechanism). Ledger evidence: EWY (iShares South Korea ETF token — sibling of already-blocked EWZ/EWT) FLAP-lists — the venue delists/relists it, each reappearance reads as a brand-new pair, and the sniper re-bought it 9 times 4→13-Jul with every close `delisted` within ~2h (8/9 red, net −$1.9); EWYG rode the same section. Restrict-only, main-only (sniper deploys from main), gate0 untouched. No backtest exists for new listings; the ledger is the evidence (3-Jul/7-Jul precedent). If flapping pairs recur beyond tokenized tickers, the structural fix is delist→relist quarantine, not more curation.
- **03–05 UTC "best session" → agenda item 9, NOT a sizing change.** The raw claim checks out (+$350.06 on 39 in-block closes vs −$106.66 on the other 300; WR 23% vs 8.7%) but ~4 payoff events carry the whole block (ANSEM +$274 alone) and the median in-block trade is still −$0.10 — sizing up on 4 lottery tickets is the Trail Blazer lucky-window lesson. Parked behind an evidence bar (≥30 in-block payoff events, both-halves, hour-vs-exchange confound check) for the 21-Jul review.

## 2026-07-15 (m) — HEALTH TAB v2: /vitals — the organism's own checkup (user: "upgrade and advance the health tab and integrate with evidence and brain and scanner")
- **NEW `/vitals` tab + `/vitals.json` (no-auth machine feed)**, linked "health →" from the main page. Three layers no surface showed together: **ORGAN VITALS** — all 12 intelligence organs (risk light, scout, evidence board, pulse, brain + its 3 output keys, bus, oracle, alert feed, review) with freshness graded against each organ's OWN ttl (FRESH ≤ ttl / LATE ≤ 3× / DARK beyond; event-driven feeds exempt) plus a one-line vital sign each; **CONSUMPTION MATRIX** — the 15-Jul consumption audit as a permanent live surface (signal → publisher → consumers → mode → contract freshness), including the honest rows ("evidence proposals → NOTHING yet — shadow", "regime/listing intel → published, unconsumed by design"); **FLOW VITALS** — 24h write counts across every durable store (paper closes, sniper skips, freqtrade closes, equity samples, venue orders + pnl_pct-NULL data-quality count).
- **Watchdog gains ORGAN-DEATH detection** (`fleet_watchdog_svc.py` polls /vitals.json): a critical organ (scout/risk/brain/pulse/board) going DARK — >3× its own ttl — becomes a watchdog problem → transition push to the phone. This closes the 5-7 Jul scar class for good: the brain died silently for TWO DAYS because the watchdog only watched bots; now the organs have a pulse monitor with teeth.
- Shakedown finding fixed before ship: brain + pulse read DARK because their payloads carry no timestamp — freshness truth is the bot_state TABLE's updated_at; `_states_with_ts` uses it as fallback. Live verify: 12/12 organs correct (10 FRESH, 2 EVENT, 0 DARK). `/health` (bare liveness ping) untouched.

## 2026-07-15 (l) — EVIDENCE BOARD v2: the board becomes an organ (user: "make the evidence board more advanced… an even more integrated system"; FREEZE LIFTED by user the same evening)
- **NEW ⚖️ `evidence_board.py`** (freqtrade-bots container, 10-min loop): the evidence board graduates from passive banner to live organ. Per-key **lifecycle + scoring** (severity × recency-decay × persistence × mechanical corroboration across independent feeds); **synthesis** of cross-feed items no single publisher can see (venue stress ≥ taker veto bar, lens-forward crossing the n4h≥75 ruling floor, lenses NEGATIVE at the floor, drawdown-governor proximity, budget crowding with effective-bets context, coin-veto FLAP detection); **auto-verdicts** for mechanical items (milestones stale after 24h, dislocations fold into the census unless escalating, veto records age out at 48h) with **manual `evidence-review` verdicts always senior**; **phone notify** for new warn/action items (watchdog's NTFY channel, 6h/key gap); and **restrict-only proposals in SHADOW mode** — each names an existing lever (lens veto / clip scale / stress veto / budget), consumed by NOTHING until a review flips `EVBOARD_MODE` (the earn-your-wiring path the L2 light walked). Doctrine unchanged and explicit in the header: never opens positions, never loosens, opportunity flow stays scout→Ticket Taker→brain. Selftested (scoring monotonicity, trend, auto-verdict rules, manual-wins merge, synthesis incl. stale-feed refusal, flap detection); first live cycle: 12 items, 6 synthesized — it immediately authored `board:lens-negative:momentum` (n4h=90, avg4h −1.05%) and `board:veto-flap:ADA` (3 changes in 7d), both previously un-itemized.
- **Dashboard:** new ⚖️ Evidence board card (scores, trend arrows, verdicts, shadow proposals, mode badge) beside the brain card; the 🔔 banner now also honors the organ's auto-verdicts (manual review stays senior in both directions).
- **Freeze lifted** (user: "take off the freeze as I believe this could be a breakthrough") — recorded in CLAUDE.md + agenda header; the 21-Jul review stands, and grading the board's shadow week is now on its agenda (§8). Dip-lens bar NOT changed: the replay sweep (0.05/0.10/0.15) was IDENTICAL on today's tape — the knob doesn't bind yet; re-sweep on the week's tape per agenda item 2.

## 2026-07-15 (k) — /history grows a window selector + max-DD captions (stash salvage part 2; user: "implement your changes even though the freeze is on")
- **`/history?hours=24|168|720|all`** (default 7d unchanged) with the time axis thinned to ≤500 points on long windows; each cohort header (Total/Live/Shadow/Paper/Scanner) now carries its **max peak-to-trough drawdown** for the selected window. Dashboard-only — the review week reads this page, and 7d-fixed made longer arcs invisible. Pure-logic selftested offline (DD math edge cases, window parsing, selector render). Deployed via `railway up` (see (j) note: main pushes don't build pnl-dashboard — memory corrected).

## 2026-07-15 (j) — 7-Jul stash salvage: the three measurement pieces that shouldn't wait for the review (user-approved "anything obviously positive"; measurement only, no strategy logic)
The 7-Jul "pre-OptionB" stash was excavated: most of it is superseded (brain reads paper_trades, /periods unions ledgers, Gap Scout balance persistence) or moot (Trail Blazer/Bounce Catcher/triangular-arb retired; Kraken strategies dormant), and its pair-blacklist actuator lost to Option B (the L4 stake mults) — left for the 21-Jul review. Three evidence pieces were still missing and now accrue a week of data before the review:
- **`paper_trades` schema WIDENED for learning** (`bot_pnl_store.py` — revives the never-deployed 7-Jul b82c5aa design): `side/tag/entry_price/exit_price/size/extra(JSONB)`, all optional, legacy callers unchanged. `side='skip'` is reserved for gate-rejection logs; `fetch_paper_trades` now excludes it so skips can never reach the brain/analyzer, and `/periods` (`fetch_period_pnl`) filters it too. Verified END-TO-END against the fleet Postgres (ALTER+insert+JSONB round-trip+skip-guard; 706 real rows intact; test row removed).
- **🎯 Launch Sniper: the gate's false-negative half is finally durable** (`listing_sniper.py`): every research-gate rejection mirrors to Postgres under `event-listing-sniper-skips` (side='skip', microstructure in extra) — sniper_skips.csv died with each redeploy, so "did the gate dodge rugs or skip winners?" was unanswerable. Closes are also enriched: entry/exit price, size, intel tag, entry-time spread/depth/volume, peak %, hold minutes → feeds the agenda-item-5 go-live-gate restatement, which will judge this book on expectancy.
- **🔀 Gap Scout: booked fills become auditable** (`cross_exchange_arb.py`): per-fill ledger rows (REAL basis, `long-xarb_paper-fill` per the tag contract, route in tag, tops-of-book as entry/exit) + closed/wins/losses published and restored from state/ledger — the dashboard showed `trades=0 0W/0L` forever and the 14-Jul review flagged the cumulative balance as unauditable. `/trades.json?source=paper` also serves the widened columns now.
- NOT salvaged: /history window selector (cosmetic — post-freeze), pair-blacklist emitter + strategy gates (strategy logic; design superseded by L4 mults; review decides if pair-level throttling rides the mult machinery).

## 2026-07-15 (i) — watchdog alerts go to the operator's PHONE (user-requested; infra only, no strategy logic)
- **`fleet_watchdog_svc.py` gains an ntfy.sh push channel** (user: "send to my phone instead of email"). Auth-free pub/sub: the unguessable topic in `NTFY_TOPIC` (pnl-dashboard service env) is the only secret; the phone's ntfy app subscribes to the same topic — no account, no SMTP, no new credentials anywhere. Same transition-based semantics (🚨 alert / updated ≥30-min gap / ✅ recovery): phone push sends first (priority `urgent` for alerts, `default` for recovery), email second if SMTP is ever also configured; either delivery marks the transition sent. Titles are ASCII-sanitized (urllib headers are latin-1; emoji ride as ntfy tags), bodies stay UTF-8. `/watchdog.json` now reports `push_armed` beside `email_armed`. Selftest covers URL/headers/sanitization, unconfigured→False, network-failure→False-never-raises. SMTP remains the (dormant) path for report_emailer's scheduled P&L mails — push replaces email for WATCHDOG alerts only.

## 2026-07-15 (h) — evidence registry: Index Pilot macro-gate REJECTED scripts enter git (docs/hygiene, no runtime)
- **`scripts/backtest_index_pilot_macro{,_long}.py` committed with explicit VERDICT headers** (12-Jul REJECTED on both the short HL window and the 2022-inclusive long window). The validation doctrine says script headers ARE the don't-re-test registry — these two sat untracked on one machine, so the registry had a hole a future session could fall into (rebuilding the SPY/QQQ macro gate). Headers point at `memory/index-pilot-lighter-rejected.md`; this closed the LAST open Index Pilot flavor.
- **`.gitignore`: `scripts/.*.json`** — the hidden offline-rerun backtest caches (~10MB across 6 files) stop cluttering `git status`. `.claude/settings.local.json` stays tracked on purpose (#55/#56 mirror decision — noted in the ignore file so nobody "fixes" it).
- No runtime surface: nothing deploys, nothing restarts. Landed on main only; gate0 not fast-forwarded (no reason to bounce the live services for docs). Freeze exception logged in the agenda §8.

## 2026-07-15 (g) — HOTFIX: review-reminder.yml was invalid YAML (the source of the GitHub failure emails)
- `review-reminder.yml`'s issue body was a multi-line double-quoted string with column-0 lines, which terminated the `run: |` block scalar early — GitHub could not parse the workflow's `name:`, so it raised a FAILED run (and a failure email) on EVERY push (~13 today). Nothing in the fleet was affected; the noise was purely the unparseable file. Rebuilt the body with `printf` (each line a quoted arg whose indentation sits OUTSIDE the quotes) so the YAML is valid and the rendered issue body has no stray spaces. Verified: pyyaml parses it, `push` is not a trigger (so no more per-push runs), body renders clean. The workflow itself is kept — the MCP scheduler trigger still needs a claude.ai connector approval, so this remains the durable 21-Jul review reminder.

## 2026-07-15 (f) — PR #34's evidence changes land on main (user-approved; supersedes the stale gate0 draft)
- **Funding Farmer live-vs-shadow divergence: PAIRED per-coin per-trade** (`market_context.py`, authored 14-Jul in PR #34, cherry-picked 44b2f38). Replaces the whole-book equity-ratio check whose 13-Jul "+5.4%" alert was an artifact (a ~$64 live book divided against the $1k shadow moves ~16x per dollar; live holds only the top slots). New: trade-count-weighted mean of per-trade `pnl_pct` gaps on coins BOTH books closed in 7d (`seen_at`-filtered), ≥3 overlapping coins, alert >1.5pp/trade. This is agenda item 4 (live-vs-shadow execution quality) running a week EARLY — the 21-Jul review reads data instead of writing a spec.
- **Snap Back census `count_enter`** (`lighter_dislocation_bot.py`): per-coin count of prints clearing `ENTER_BPS` (150bps) — the tradeable-density number its promotion review needs.
- PR #34 itself closed as superseded: its third change (vercel.json) duplicated the richer one already on main since 14-Jul (c), and merging into gate0 would have broken the gate0⊂main fast-forward invariant. Landed on main + gate0 fast-forwarded instead. Freeze exception logged (measurement only, no strategy logic).

## 2026-07-15 (e) — operator-queue sweep (user: "attend to all of my queue"; freeze-exception logged in the agenda)
- **🧭 Trail Blazer STOPPED via retirement guard (`hyperliquid_momo_bot.py`).** Retired 11/12-Jul on user sign-off but its momo-bot Railway service kept running (16+ ledger closes after retirement). The service auto-deploys from main and restartPolicy=always would crash-loop an exit, so the guard boots INERT: no data, no trades, no publishes — the stale bot_pnl row gets boot-pruned and stays gone. Ledger history untouched. Resurrect deliberately via `MOMO_RETIRED_OVERRIDE=run`; deleting the idle service remains a console click.
- **NEW `.github/workflows/db-backup.yml` — the fleet's Postgres is now backed up nightly (04:07 AEST).** Every ledger and all brain memory live in one Railway Postgres with an unverified backup story; the workflow pg_dumps it via the Railway CLI's variable injection (account-token pattern from railway-volume.yml — NO new secrets), prefers `DATABASE_PUBLIC_URL` (runners can't reach railway.internal), masks the URL, uploads a custom-format dump as a 30-day artifact on the private repo. Restore: `pg_restore --no-owner --dbname <url> bot_fleet.dump`.
- **NEW `.github/workflows/review-reminder.yml`** — opens a GitHub issue at 09:00 AEST Tue 21-Jul so the scheduled review can't be missed (session cron dies with the container; the MCP scheduler needs an interactive approval). Yearly-recurring cron — the agenda's item 8 says delete the workflow at the review.
- Agenda operator checklist swept + freeze-exception log updated (FLEET_REVIEW_AGENDA_2026-07-21.md). Still operator-only: the scheduler approval click, SMTP vars for the (built, dormant) report_emailer, and hunting the off-repo equities-regime-ibkr publisher host.

## 2026-07-15 (d) — consumption-audit gap fixes: every counted bot now checks what counts it (user-approved: "fix all of the above")
- **🌊 Tide Rider LIVE consumes the fleet long-budget veto (`lighter_trend_bot.py`).** The consumption audit found the only real-money LONG book was counted BY the light but never CHECKED it. Same contract as the family bot/taker (15-Jul audit-fix pattern): fresh payload + mode=enforce + budget full → skip NEW golden-cross entries that cycle; exits/stops/daily-loss rails untouched; missing/stale state fails OPEN; central kill switch `FLEET_RISK_MODE=advisory`. NOTE: the light is red 23/20 at ship time — Tide Rider pauses new entries until the fleet book shrinks, which is the veto doing its job.
- **👨‍👩‍👧‍👦 family books consume the drawdown governor.** `clip_scale` was taker-only ("gate0 advisory until ported") — ported: new-entry stakes scale ×1.0/0.5/0.25 with the same clamp + fail-open semantics, logged when active. Applies at entry sizing only.
- **🎯 Launch Sniper closes are finally learnable:** `reason="long-listing_"+exit` (was raw exit — 337 closes permanently '(untagged)', invisible to per-sleeve learning). New closes bucket under enter_tag `long-listing`; history unchanged.
- **🎯 Perp Sniper joins the exposure view:** publishes `held` ({sym: L/S} by DIRECTION_LONG) in extra — it was `sym_uncovered` in the new concentration block.

## 2026-07-15 (c) — HOTFIX: exposure harvest crashed fleet_risk on the live trend bot's row
- **`held` publishes in two shapes and the (b) exposure harvest assumed one.** Funding/family books publish `extra.held` as a DICT ({coin: 'L'/'S'/tag}) but the LIVE Tide Rider (and the equities ports) publish a LIST (`sorted(meta.keys())`); `held.items()` threw AttributeError on `crypto-trend-daily-lighter`'s row, killing every fleet_risk cycle post-deploy — the light froze (last publish 06:09Z) and consumers correctly failed OPEN, i.e. the long-budget veto went dark on the running fleet. Caught ~25 min in by the post-merge verification poller + the consumption-matrix audit. Fix: `held_items()` normalizes both shapes (list entries side-default long — correct for the long-only publishers of that shape), used in both harvest loops AND the pre-existing PERPS longs/shorts derivation (same latent hazard). Selftest covers dict/list/None + a direct simulation of the crashing row.

## 2026-07-15 (b) — what successful systems have: exposure view + replay harness (user-approved)
- **fleet_risk EXPOSURE VIEW (`exposure_concentration`) — the light counts positions, this says how many BETS they are.** From the ecosystem gap review ("you are 23× long one factor and nothing says so"): the same directional cohort the light counts now also publishes `exposure` — per-symbol pileup (top-8), distinct symbols, **effective independent-bet count (1/Herfindahl)**, crypto-vs-equity-perp cluster split (curated `FLEET_EQUITY_BASES`, env-extensible; ambiguous tickers stay crypto), and `sym_uncovered` (positions whose publisher exposes no symbol detail — the view only claims what it can see). Symbol harvest now ALSO reads `extra.held` (gate0 family books + live Funding Farmer), so those books join `pair_concentration` for the first time. Advisory publish-only (payload + lean history mirror); budgets/light/veto semantics untouched. `--selftest` covers pileup math (4×SOL+1×ETH → eff_n 1.5), clusters, coverage honesty.
- **NEW 🎬 `lighter_ticket_replay.py` — replay the recorded scout tape through the Ticket Taker's REAL code.** The Lighter-native fleet validated by forward-shadow only (days per rule tweak) while the scout was already recording the needed tape (5-min `lighter-market` history with marks since 15-Jul). The harness feeds that history through the taker's own `incredible()` bars and `exit_reason()` TP/SL/max-hold (imported, not reimplemented — it cannot drift from the bot), models fills at snapshot marks with the same 4bps fees, replays the stress veto, and reports per-lens seen/taken/closed/WR/net/exits + unrealized tail. Env-tunable variants (`TT_TP=0.06 python3 lighter_ticket_replay.py`) judge a bar change against recorded reality in seconds. Honest divergences documented in-file: no funding accrual (tape has no rates — long lenses read rich, divergence shorts poor), fixed clips (no ranges), no bus vetoes. Reads Postgres or the public `/bus.json`; writes nothing. Selftest (tp/sl/hold, dip bar, stress veto, one-per-lens, fee math) + first live-tape run green (19 snapshots: divergence TP +$2.10, momentum SL −$1.60, 6 open).

## 2026-07-15 — learning-loop wiring: point the brain at the living fleet (user-approved)
- **Evidence & learning assessment** (`EVIDENCE_AND_LEARNING_REVIEW_2026-07-15.md`): 120 brain runs, ZERO premature throttles (floors/streaks behaving), but the 14-Jul pivot broke the L4 mult loop on both ends — the only consumers were the stopped Kraken strategies, and the family bot's closes dropped the strategy tag so all seven books bucketed as enter-tag `long`. 10/16 ACTIONABLE hypotheses prosecuted retired bots (rows never leave the fetch window → the streak-retire path could never fire); Trail Blazer's paper engine STILL trades (16 closes since 14-Jul — stopping the momo service stays operator-only). Lens-forward was empty purely because scout `marks` began publishing 04:27 today.
- **Family bot rejoins the learning loop (`lighter_family_bot.py`, gate0 cross-merge needed to deploy).** Closes now publish `long-<tag hyphenated>_<exit>` (`ledger_reason`; hyphens because the ledger parser splits at the FIRST underscore — taker precedent 14-Jul (g)) so the brain buckets per strategy tag, and entries apply `fleet_bus.stake_multiplier(b.bot_id, ledger_tag(tag))` — the same helper derives ledger tag AND lookup key, so buckets and lookups can't drift. Reduce-only, clamped [0.5,1.0], fail-neutral, logged when <1.0; NO-OP at deploy (published table empty). `fleet_bus.py` added to Dockerfile.familyshadow. No entry/exit logic touched.
- **Brain LIVENESS filter (`bot_learn.py`).** Hypotheses/diagnoses/multipliers generate only for living bots (not in `cleanup_legacy_bots.LEGACY_BOTS`, ≥1 ledger close in 7d); scorecards keep printing for everyone flagged `[inactive/retired]`; existing dead-bot entries decay via the normal 3-run retire path. `_epoch` now parses the sniper's `'... UTC'` timestamps (liveness + drift joins). Verified with a 3-run fixture: identical losing evidence in a live/stale/retired bot → hypotheses only for the live one (candidate→ACTIONABLE at run 3).
- **Ledger tag contract extracted (`bot_pnl_store.split_reason`)** — the one parser the taker's and family bot's composers round-trip against (unit-tested: 36 tag/exit combos + legacy/funding/sniper edges). `/bus.json` now also serves `brain-lens-forward` (live + history) beside the other brain keys; run_all.sh taker comment corrected to `<side>-<lens>_<exit>`.

## 2026-07-14 (h) — the missing risk leg: measured risk, not counted (user-approved)
- **Fleet DRAWDOWN GOVERNOR (`fleet_risk.dd_governor`).** The fleet had per-bot seatbelts but nothing that said "the whole book is bleeding — shrink". fleet_risk now sums equity over the same authoritative rows the light counts, keeps a 7-day rolling sample window inside its own state (≥30-min gaps, redeploy-safe), and publishes `fleet_equity` / `fleet_dd_7d` / **`clip_scale`** (1.0 → 0.5 past −5% 7d drawdown → 0.25 past −10%; env `FLEET_DD_HALF`/`FLEET_DD_QUARTER`) — state + history. Ticket Taker CONSUMES it (clips scale down, fail-safe neutral on stale state); advisory for the gate0 books until ported. 7-case unit suite + end-to-end drive (seeded −10% → half clips, historized).
- **Ticket Taker: CONSTANT-RISK sizing (`vol_clip`).** Fixed $50 clips carried ~5x different risk across books; now clip = `TT_RISK_USD`(1.5) / expected adverse move (half the daily range, floored 0.5%), bounded $20–$80, fallback $50 when a book has no range data. Per-position clip stored in meta so ledger `pnl_pct`/fees use the real size; the brain's per-lens grading is on pnl_pct so sizing doesn't distort it. Calm books (BTC ~2% range) size up to the cap; wild alts size down to the floor. Selftested across the curve.

## 2026-07-14 (g) — divergence lens + stress veto (user-approved same evening)
- **Ticket Taker learns SHORTS + the FUNDING-DIVERGENCE lens (the strategy nobody in the fleet traded).** Scout gains `tickets.divergence`: when Lighter's funding diverges ≥300pp APR (`SCOUT_DIV_GAP`) from the binance/bybit/HL median for the same liquid symbol, ticket the RECEIVING side (positive gap → short, negative → long), skipping entries that fight the premium (no shorting already-cheap books). Ticket Taker takes them at ≥500pp (`TT_DIV_GAP`) with full SHORT support: signed funding accrual (a short under a positive rate is CREDITED — the lens's whole thesis), side-aware exits, and `PaperBroker`'s signed positions. Live at ship time: 6 divergence tickets, top HOOD +1118.7pp.
- **Stress veto:** when the scout's venue-wide |premium| median ≥15bps (`TT_STRESS_VETO_BPS`; normal tape ~6), Ticket Taker takes NO new entries that cycle (exits keep running) — a whole-venue dislocation means marks are unreliable and every book is one bet.
- **Per-lens brain grading fixed before it mattered:** the ledger's reason parser splits on the FIRST underscore, so the old `long_<lens>_<exit>` tags would have collapsed every lens into one "long" bucket for the brain. New format `<side>-<lens>_<exit>` → enter_tags like `long-breakout` / `short-divergence`; no closes had been recorded yet, so no history was mis-tagged. fleet_risk's short-detection (`"short" in tag`) works with the new tags unchanged.

## 2026-07-14 (f) — KRAKEN RETIREMENT + the scanner gets its trader
- **Kraken bots RETIRED (user instruction: "let's just focus on lighter — less clutter").** The 4 spot originals no longer launch from run_all.sh (main-mode poller retired with them) and all 8 Kraken paper rows (spot four + family four) move to RETIRED_ROWS + boot-prune; their bases move EXPECTED→VARIANT_ONLY so the **-lshadow twins keep displaying as the fleet** and Tide Rider's LIVE row is untouched (retirement matches exact row names — verified against fetch_rows' filter). The main container keeps every fleet organ (pulse/brain/oracle/risk/scout/cleanup/ticket-taker). OPERATOR-ONLY: stop the 4 family Railway services (ONLY_BOT mum/dad/avo-maria/georgia — left functional so they don't crash-loop meanwhile). fleet_risk's `authoritative_row` chain is now live > -lshadow > paper (fresh rows only): the light tracks the Lighter fleet — a documented semantic change (shadow books are modelled capital, but they ARE the managed fleet; the pileup scar applies to any cohort trading one beta).
- **NEW 🎫 Ticket Taker (`lighter_ticket_taker.py`, row `lighter-ticket-taker-lshadow`) — the scanner's designated trader (user ask: "when a scanner finds something incredible, the designated bot absorbs the finding and makes a trade").** A $1,000 SHADOW book that takes only the HIGH-CONVICTION subset of the scout's tickets (bars: breakout range≥0.95 & ≥$1M; dip range≤0.05; momentum ≥+5% & ≥$2M — env-tunable), fills modelled at Lighter mark via the existing PaperBroker (fees), hourly funding drag accrued from the venue's own feed, exits TP+4%/SL−3%/48h max-hold, one new position per lens per cycle, 6 max, $50 clips. **Every close is tagged `long_<lens>_<exit>` into the durable paper_trades ledger, so the brain grades each lens on real forward returns** — the scanner→bot→brain loop that decides which lens has an edge. UNVALIDATED by design (lens rules need forward data — collecting it is this book's job); no live path exists; state persists in bot_state across redeploys; counted in the fleet light (fleet_risk-shaped open_pos). Selftests (bars/exits/accounting incl. funding) + live one-shot green; fleet_risk chain verified against a synthetic lshadow/live/ticket-taker fleet (no double-counting).

## 2026-07-14 (e)
- **Lighter Scout: per-strategy TICKETS (user ask: "a scanner per bot so each bot has an edge").** One scanner engine, per-bot-family lenses — not eight services: `strategy_tickets()` hunts each family's setup across ALL liquid Lighter books instead of a fixed whitelist and publishes ranked candidates in the scout payload (`tickets.breakout` → Breakout Hunter/Dad: at the daily high on volume, not trading rich, longs not paying >100% APR; `tickets.dip` → Dip Buyer/Avo Maria: hard bounded flush to the lows, tagged `trend: unknown_v1` — no candle history keylessly; `tickets.momentum` → Stock Leaders/momo: strongest liquid movers, funding-sane). HONEST FRAME: a scanner amplifies a validated edge, it cannot conjure one — and the brain's diagnosis layer shows entries are NOT every bot's lever (georgia/intraday were exit_too_tight, trend-daily fee-scale). So tickets are ADVISORY + HISTORIZED: the weekly review grades each lens's forward returns against the bots' actual entries, and a lens only earns a consumer feed by that evidence. Live print: 6 breakout / 2 dip / 4 momentum tickets over 88 liquid books; selftest covers lens picks + rich/knife/illiquid exclusions.

## 2026-07-14 (d) — Lighter-first sweep (user-directed)
- **PR #33 integrated onto current main** (the other Jul-14 session's brain L4 + diagnosis + enforcement wiring + backtested bot fixes) — conflicts resolved keeping BOTH sides: enforce mode + venue-aware counting AND the staleness filter + Lighter mirrors; `authoritative_row` now also requires freshness; the bus funding preference (Lighter > shadow > HL) considers fresh rows only; `/bus.json` and `/disloc.json` both kept.
- **Laptop stock bots RETIRED on user instruction (Lighter-first cut).** `equities-momentum-alpaca` + `equities-regime-ibkr` → RETIRED_ROWS + boot-prune; their Lighter ports (Stock Leaders / Index Rider `-lshadow` books) are the bots now. Also resolves the broker-scale distortion ($100k/$250k equity rows). NOTE: the laptop processes run outside this repo — the operator must stop the cron/processes; until then their rows re-upsert harmlessly hidden.
- **Gap Scout signal + persistence fixes (review action item).** The bus dislocation is now `liquid_top_pct` — the same max restricted to unambiguous majors (`DISLOC_BASES`, default BTC/ETH/SOL/XRP/ADA/DOGE/LTC/LINK/DOT/AVAX) — instead of the symbol-collision max that printed 4.6% junk; raw kept in extra for diagnostics. Paper balances (real + gross basis) now persist via bot_state across redeploys (the 25-Jun sniper bug class).
- **Brain visible from outside.** `/bus.json` now serves `learning-brain`, `brain-stake-mults`, `brain-diagnosis` (+ history) — the brain died silently for two days once (5–7 Jul); its outputs are now on the same no-auth surface as the bus. Audited bot_learn v2 read/write paths: guarded end-to-end, ran end-to-end from a no-DB sandbox via the HTTP fallback (121 trades, 1 diagnosis).
- **NEW 🛰️ Lighter Scout (`lighter_market_scout.py`) — the venue-wide market map (user ask: a wider Lighter scanner feeding the brain).** Two keyless calls per run (orderBookDetails + funding-rates) over ALL ~215 books → bot_state `lighter-market` (+history, on `/bus.json`): premium stress (med/p90/max |bps| over ≥$100k books), top liquid premium outliers, **liquid-floored Lighter-native funding extremes** (fixes the tiny-coin-noise flaw of the old bus funding signal), **cross-venue funding divergence** (Lighter APR vs median of binance/bybit/HL — the carry force seen BEFORE entry; live print: DATA +925pp, XPT −591pp), volume surges (≥3×), OI moves (≥25%), listing/delisting diffs. ADVISORY/publish-only per doctrine; fail-silent; looped 5-min in run_all.sh. Selftest + live-verified (198 books, 88 liquid, stress med 6.0bps).

## 2026-07-14 (c)
- **Pipeline-hiccup sweep (user-requested audit follow-up).** Three read/write defects found while auditing what could inhibit collecting/optimising: (1) **market_pulse history window was ~33h, not the intended ~7d** — the 200-entry cap assumed hourly appends but the pulse loops every ~10 min; history now appends at most hourly (`append_hourly`, gap 55 min), so the brain's mood-conditioned analysis gets ~8 days again. (2) **fleet watchdog's position warning was apples-to-oranges** — it summed open_trades over shadows/funding/stocks/scanner rows (64 on a healthy day) against the 20-position directional budget, warning forever; it now counts the directional cohort only (paper + real-live trading rows; gross kept in the snapshot line). (3) **paper_trades had no read endpoint** (funding bots, sniper, live Lighter closes were invisible to outside reviews) — `/trades.json?source=paper` now serves it. Also pruned the retired-bot LABELS dead code (rows deleted + RETIRED_ROWS filters republishes). FLAGGED, needs an owner decision (bots live outside this repo): equities-momentum-alpaca publishes raw $100k-scale Alpaca paper equity and equities-regime-ibkr raw ~$250k IBKR paper equity — 100-250x every other book, swamping cross-bot P&L comparisons and tripping the watchdog daily-loss alert; fix at source (publish vs a recorded baseline, like the Jul-10 live-Lighter fix) or normalize dashboard-side.
- **Dead bots removed at the source (user-approved).** `cleanup_legacy_bots.py` allow-list extended with the dashboard's RETIRED_ROWS set (Bounce Catcher + lshadow, Trail Blazer paper/live/lshadow, regime-switch + lshadow, Loop Scout, trendmomo-4h) and now runs `--apply` once on every freqtrade-bots boot (new COPY in Dockerfile.freqtrade + run_all.sh one-shot) — deletes the frozen `bot_pnl` snapshot rows ONLY; trade ledgers, venue_orders and bot_state history are kept (Manage-panel delete semantics), and a revived bot's row reappears on its next publish. `fleet_risk.py` counting lists pruned to the living cohort (PERPS_LS_BOTS now empty — the whole perps L/S cohort is retired). Belt (staleness filter) + braces (rows gone).
- **fleet_risk: GHOST-EXPOSURE fix — stale bot_pnl rows no longer count.** Post-merge verification of `/bus.json` caught the live light pinned at RED 32L/0S for hours while the real fleet held ~10 longs: `fleet_risk` reads the raw `bot_pnl` table (not the dashboard's filtered view), and rows outlive their bots — decommissioned Bounce Catcher (`perps-rsi-meanrev`, stopped 12 Jul) froze holding 6 longs, Trail Blazer's undisplayed paper row held 16 more. Rows older than 30 min (`STALE_ROW_SEC`) are now ignored in exposure counts AND all signal-bus mirrors (a frozen scanner row must not republish last week's dislocation). Unit-driven with synthetic fresh/ghost/stale rows. PREREQUISITE for the RED-veto enforcement wiring — a ghost-inflated light would throttle live bots for positions nobody holds. Review doc's "current state" stamp corrected (the GREEN 10L/0S figure was read off filtered `/pnl.json`).
- **Cross-bot advisory review (the one the 7-Jul design scheduled) — verdicts + evidence in `CROSS_BOT_ADVISORY_REVIEW_2026-07-14.md`.** Reconstructed the advisory week from the `/trades.json` ledger (121 trades, Jul-03→13; `bot_state_history` is unreachable off-Railway — itself a finding, fixed below). **L2 (fleet-risk light): evidence supports wiring the RED veto** — entries taken at 13+ ledger-visible fleet positions won 18% (n=44, −$18.04, worst bucket); peak visible concurrency 20 with two perps books uncounted; a RED(20L) veto would have cost ~nothing while capping the 26-position-scar tail. Enforcement wiring is bot logic → its own change, not this PR. **L3 (signal bus): do NOT wire dislocation/funding as published** — `xexchange_dislocation_pct` printed 4.64% at review time (a thin-pair/symbol-collision artifact under the 5% ceiling; corroborated by Gap Scout's +$6,095 "real"-basis balance on an engine whose docstring expects flat), and hottest-funding is permanently-extreme tiny-coin noise. Pulse: all 8 entries in the visible window fired during panic and 7 lost *despite* the half-stake gate (n=8 — keep logging, no entry gate yet per doctrine). Next review Jul-21.
- **Gap Scout learns the venue we actually trade: LIGHTER PREMIUM on the signal bus (advisory).** `cross_exchange_arb.py` now polls Lighter's public `orderBookDetails` (one keyless call, all books; guarded fail-silent — Lighter's WAF has blocked other REST from Railway) and publishes the venue premium `mark/index − 1` in bps: per-book for the family watchlist (`BTC,ETH,SOL,SPY,QQQ,XAU`, env `LIGHTER_WATCH`) + median/max |premium| across every active ≥$100k/day book (the venue-stress gauge; floor env `LIGHTER_MIN_QVOL`). `fleet_risk.py` mirrors to `signal-bus` as `lighter_prem_bps` / `lighter_venue_stress_bps` (historized like the rest). Live probe: majors −5…+9bps, RKLB +26bps, SKHYNIX +50bps. Why: taker entries pay the premium, persistent premium leads funding drag (the momo funding-veto A/B measures the trailing version), and fleet-wide |premium| widening is Lighter-specific stress. PUBLISH-ONLY until a review earns a consumer. Pure math in `lighter_premiums()` + selftest. NOTE for gate0: main's `fleet_risk.py` predates the venue-aware `authoritative_row()` — port the mirror to whichever copy the service runs.
- **dashboard `/bus.json`**: read-only, no-auth (same contract as `/pulse.json`) — live `fleet-risk` + `signal-bus` states plus their `bot_state_history` snapshots (`?hours=`, default 24, max 200), so advisory reviews read evidence instead of reconstructing from the trades ledger.
- **`vercel.json`: git deployments disabled.** A Vercel project was connected to this repo and error-failed EVERY commit's status check with "No python entrypoint found" (all recent deployments incl. main production; the fleet deploys on Railway — there is nothing for Vercel to build). `github.enabled=false` + `git.deploymentEnabled=false` stop the doomed deploys and the red X on every PR. Fuller cleanup (operator-only): disconnect the repo in the Vercel project settings.

## 2026-07-14 (b)
- **Brain DIAGNOSIS layer — it can now tell an entry problem from an exit problem from a fee/regime/venue problem.** Born from the dashboard's four "tighten the X entry gates" proposals: two were plumbing artifacts (fixed earlier today — exit reasons masquerading as entry tags), and the deeper flaw was that the brain had ONE prose template for every negative bucket. `bot_learn.diagnose()` now classifies each negative (bot, enter_tag) bucket at n≥10 into **exit_too_tight / venue_execution / fee_bleed / regime_timing / entry_quality / mixed_unclear** using: exit-path loss splits (ledger), **post-exit drift from public Kraken 1h candles** (did price reclaim entry within 24h of the losing exit? — the mechanized form of the manual replay behind the 13-Jul stop widening), fee-scale tests (median loser vs round-trip friction), a **regime-oracle-history join** (new `bot_pnl_store.fetch_state_history`; snapshots every 30 min since 7-Jul), and the venue A/B table. Proposals now name the lever; "tighten the entry gates" is only earned by confirmed entry_quality. Publishes bot_state `brain-diagnosis` (+history); hypothesis keys unchanged so existing streaks carry over. **Validated against known ground truth** (full-July replay, live candles): intraday/georgia trailing-stop bleed → exit_too_tight with 79–100% reclaim (the manual 13-Jul replay found 77–89%); crypto-breakout-4h breakout → entry_quality (22% reclaim, negative fwd — the tide-gate finding); freqtrade-dad → mixed_unclear on murky 11-trade evidence (declines to call it); trend-daily probes → fee-scale, "signals are not the lever" (the CONTROL verdict). 6-scenario unit suite green. Advisory-only: humans + dashboard consume it; nothing trades on diagnoses directly. Drift work bounded (once-per-pair candle cache, ≤120 evaluations/run); all evidence fail-soft — a missing source just removes its rule from contention.

## 2026-07-14
- **Fleet metrics/strategy review + THE CONSUMPTION WIRING — the Jul-14 enforcement review the 07-07 design scheduled** (full write-up: `FLEET_REVIEW_2026-07-14.md`). The audit found every intelligence organ built, publishing, and consumed by NOBODY (oracle/risk-light/signal-bus: zero readers; the brain's per-tag analysis ended as prose). W/L-ratio triage corrected first: sniper (31W/296L but **+$205.70**) and funding-carry (4W/30L but **+$7.25**, Lighter shadow 5W/3L +$8.92) are positive-expectancy KEEPs; the real bleeders were the breakout carriers, mum, and the DayTrader carriers (already fixed 13-Jul).
- **MomoBreakoutV1: BTC-TIDE GATE (backtest-first).** Live bleed: crypto-breakout-4h + freqtrade-dad both 2W/9L (~-$8 each) in 3–13 Jul — 11 pairs broke their own 200-EMA and died in a fear tape (market-wide fakeout wave, invisible to the per-pair filter). `scripts/backtest_momo_tide_gate.py` (Kraken 4h, 30 pairs, 20-Apr→14-Jul, 0.26%/side): as-deployed 121 trades PF 0.79 Σ−94.0% (reproduces the live July bleed); **+ BTC>4h-EMA200 gate: 74 trades PF 1.43 Σ+79.1%**, catastrophe stops 6→1; breadth-gate and half-size variants dominated. Shipped the hard gate via the BTC informative pair (deterministic in `freqtrade backtesting`). Honest caveat: one 3-month window — but the exact bleeding regime, and the validated core (+$201.59/603) earned everything in up-tides.
- **freqtrade-mum (TrendMomoV1): whitelist curated 15→10 (backtest-first).** `scripts/backtest_trendmomo_pairs.py` (exact strategy rules, Kraken 1d, 2yr): DOT/ATOM/NEAR/LTC/AVAX structurally negative (PF 0.08–0.71, Σ−313%) while the kept ten are positive (Σ+863%; BTC 1.79 / ETH 3.03 / XRP 3.75). Same precedent as V4's majors curation. Also softens the 12-Jul "core −29%/4.5y" flag: recent-2yr core is strongly positive; the 4.5y number was the 2022 bear + the trendless five.
- **bot_learn v2 — L4 META-LABELING SHIPPED (the brain's first output bots trade on).** The brain now computes NUMERIC, REDUCE-ONLY per-(bot, enter_tag) stake multipliers from the era-filtered ledger — 0.5× (n≥30, negative, wr<25%), 0.75× (n≥30 wr<40%, or 15≤n<30 wr<25%) — STREAK-GATED (must recur 3 consecutive runs to publish; a recovered tag un-throttles immediately; v1 never sizes up) → bot_state `brain-stake-mults` (updated+ttl_sec freshness contract). Plus a **venue A/B report** every run (paper vs -lshadow/-lighter twins → lessons + state `venue_ab`) so the 13-Jul shadow books feed learning, not just the dashboard. ERA_START updated for the 13/14-Jul strategy changes. Live-data dry run: 121 trades, 9 A/B pairs, 0 mults published (correct — nothing clears the floor yet; the machinery is armed).
- **NEW `fleet_bus.py` — the strategies' read-side bus client (one file owns the fail-safe contract).** `stake_multiplier(bot, tag)` (clamped [0.5,1.0]) + `long_entries_blocked()` + `is_fresh()`; neutral on missing/stale/error; inert in backtests (no DATABASE_URL). All four active strategies consume it: brain multiplier in `custom_stake_amount`, and the **L2 fleet-risk long-budget veto ENFORCED in `confirm_trade_entry`** (side-specific long count — a blown short budget can never freeze long-only spot bots; fail-safe OPEN; checked before DayTrader's throttle so vetoes don't burn candle slots). Central kill switch: `FLEET_RISK_MODE=advisory`. Current book at ship time: 10/20 longs, green, veto idle. `fleet_risk.py` also gained the gate0 venue-aware counting main had missed (09-Jul entry described it; main never got it) + publishes `mode`.
- **L3: sniper intel on the bus.** `listing_sniper.py` publishes classifications to bot_state `listing-intel` every cycle (open book by class, ghost-skip tally, per-position class/venue) — scanner knowledge visible to brain/dashboard instead of dying in-process.
- **Config hygiene:** `bot_name` was stale copy-paste in all 8 active configs ("trend_golden_cross" on four different bots) — set to the real dashboard bot IDs; this is the identity fleet_bus uses for multiplier lookups. Wiring tests: 7-scenario fleet_bus/brain suite green (freshness, clamps, streak-gating, un-throttle, side-specific veto, venue A/B); fleet_risk simulated against the live pnl snapshot end-to-end.

## 2026-07-12
- **Fleet strategy audit → the cross-sectional factor programme → ⚖️ Counterweight shipped (shadow).** The audit found the fleet saturated with duplicated time-series signals: `perps-rsi-meanrev` and `perps-donchian-breakout` had CONVERGED on the identical entry (bottom 15% of the 20-candle range; 33 same-pair-same-day entries in 30d), four spot bots carried a copy-pasted bear_bounce sleeve, and NOTHING ranked coins against each other. Five cross-sectional factors were pushed through the validation doctrine on 150d real HL hourly data (both-halves + mirror + plateau + slip stress): momentum REJECTED (2/40, isolated — `scripts/backtest_xsect_momentum.py`), short-term reversal REJECTED (1/16), low-vol REJECTED (1/12) (`scripts/backtest_xsect_factor_lab.py`), funding-SLOPE rank REJECTED (0/16 — slope is entry timing, not a book; `scripts/backtest_xsect_slope.py`). FUNDING-LEVEL rank PASSED with a real plateau (8/12 green in both halves, whole 72h row, mirrors negative, survives 3x slippage). Shipped as **`lighter_funding_spread_bot.py` (⚖️ Counterweight, row `perps-funding-spread-lshadow`, service `counterweight-shadow`)**: rank 30-coin universe by 72h mean funding, LONG bottom-5 / SHORT top-5, $20 clips ($200 gross), 24h rebalance, market-neutral, funding accrued hourly, HL-backfilled warm boot, no per-position stop (backtest fidelity; 5% daily rail only), REFUSES lighter_live in v1. Publishes `ff_overlap` every loop to quantify kinship with the live Funding Farmer (booted 7/10 — watch it before citing this as a diversifier).
- **Decommissioned (user-approved): 🪃 Bounce Catcher + 🔺 Loop Scout.** Railway services `perps-bot` and `triangular-arb` stopped (`railway down`; empty shells left in the UI for manual deletion). Bounce Catcher ran the identical entry to Trail Blazer's paper bot and was REJECTED for durable edge (12 Jul audit) after being retire-flagged since 22 Jun; Loop Scout carried the 22-Jun retire verdict. Dashboard rows moved to RETIRED_ROWS (incl. the stale Gate-0 `perps-rsi-meanrev-lshadow`); `momo-bot` stays as the range-cluster representative; `yield-harvester-shadow` stays as the funding family's delta-neutral control.

## 2026-07-12
- **Fleet strategy audit → the cross-sectional factor programme → ⚖️ Counterweight shipped (shadow).** The audit found the fleet saturated with duplicated time-series signals: `perps-rsi-meanrev` and `perps-donchian-breakout` had CONVERGED on the identical entry (bottom 15% of the 20-candle range; 33 same-pair-same-day entries in 30d), four spot bots carried a copy-pasted bear_bounce sleeve, and NOTHING ranked coins against each other. Five cross-sectional factors were pushed through the validation doctrine on 150d real HL hourly data (both-halves + mirror + plateau + slip stress): momentum REJECTED (2/40, isolated — `scripts/backtest_xsect_momentum.py`), short-term reversal REJECTED (1/16), low-vol REJECTED (1/12) (`scripts/backtest_xsect_factor_lab.py`), funding-SLOPE rank REJECTED (0/16 — slope is entry timing, not a book; `scripts/backtest_xsect_slope.py`). FUNDING-LEVEL rank PASSED with a real plateau (8/12 green in both halves, whole 72h row, mirrors negative, survives 3x slippage). Shipped as **`lighter_funding_spread_bot.py` (⚖️ Counterweight, row `perps-funding-spread-lshadow`, service `counterweight-shadow`)**: rank 30-coin universe by 72h mean funding, LONG bottom-5 / SHORT top-5, $20 clips ($200 gross), 24h rebalance, market-neutral, funding accrued hourly, HL-backfilled warm boot, no per-position stop (backtest fidelity; 5% daily rail only), REFUSES lighter_live in v1. Publishes `ff_overlap` every loop to quantify kinship with the live Funding Farmer (booted 7/10 — watch it before citing this as a diversifier).
- **Decommissioned (user-approved): 🪃 Bounce Catcher + 🔺 Loop Scout.** Railway services `perps-bot` and `triangular-arb` stopped (`railway down`; empty shells left in the UI for manual deletion). Bounce Catcher ran the identical entry to Trail Blazer's paper bot and was REJECTED for durable edge (12 Jul audit) after being retire-flagged since 22 Jun; Loop Scout carried the 22-Jun retire verdict. Dashboard rows moved to RETIRED_ROWS (incl. the stale Gate-0 `perps-rsi-meanrev-lshadow`); `momo-bot` stays as the range-cluster representative; `yield-harvester-shadow` stays as the funding family's delta-neutral control.
- **Same-page sweep: every live-able strategy family now has its Lighter shadow twin (or an evidence-based reason not to).** Restarted 🎯 Perp Sniper as a proper Railway service (`perp-sniper-shadow`, Dockerfile.psniper — its Gate-0 shadow ran locally and went stale 10 Jul); already on: Tide Rider, Funding Farmer, Yield Harvester, Snap Back, Counterweight. NOT twinned, on evidence: Breakout Hunter FAILED its perp gate (`scripts/backtest_breakout_perp.py`: 1000d basket +74.6% spot but h2 -27.0% — the whole edge is the 2023-24 bull half; recent 150d -20.7% across the Lighter universe; funding drag 25pp on top) — which also FLAGS the spot bot itself (its "+20% core" was full-window only, the Trail Blazer failure shape); SwingDip core is flat-negative on the 4.5y replay; Trail Blazer's shadow would re-instate the twice-retired range-buy (stale Gate-0 lshadow row hidden). Freqtrade family bots (mum/dad/avo-maria/georgia) are not portable (third-party 5m strategies). Remaining port candidate if wanted: Two-Way Tide (regime-switch) — the only long/short trend family without a twin.
- **High-leverage operator search — BOTH candidate families REJECTED; nothing ships (doctrine held).** Asked to "redesign the day trader as a high-leverage operator". Candidate 1, intraday long/short breakout on the 8 tight-slippage Lighter majors (`scripts/backtest_leverage_operator.py`, hourly closed-bar Donchian break + ATR trail + time stop, honest leverage/liq/funding mechanics): 0/8 plain, 0/8 trend-gated, 0/8 long-only (fails mirror), 1/8 squeeze-gated — and that lone cell sits next to a -22% cliff and dies under 2x slip stress. Diagnostic worth keeping: FADING short-lookback breaks is catastrophic (mirror -43..-48%), so the information is real but whipsaw+slip bleed it faster than any tested exit captures it. Candidate 2, leverage on the VALIDATED funding-spread book (`scripts/backtest_overdrive.py`, intrabar replay): the short leg carries squeeze-blowup tail — a ~90% adverse intrabar excursion inside one 24h hold (liq even at 1x isolated), 5 liqs at 3x, 20 at 5x; adding the Farmer-standard 10% stop removes all liqs but kills the carry (h1 negative at every gear — stops whipsaw out of the mean-reverting positions that pay). STRUCTURAL conclusion: the funding edge is tail-risk compensation; levered you either eat the tail or overpay the insurance. Corollary recorded: Counterweight must stay unlevered and stop-free (both knobs now evidence-rejected).
- **TrendMomoV1 (crypto-trendmomo-4h) RETIRED — the audit's obvious loser, user-approved.** Core leg -29%/4.5y on the tagged replay (26.5% win), live bleeder (-$11.33). Dropped from run_all.sh (landed on main so freqtrade-bots redeploys without it) + dashboard RETIRED_ROWS (gate0). The fleet's spot lineup is now V4 trend / V5 day-trader / V6 swing / V7 breakout.
- **bear_bounce sleeve RETIRED from all four spot strategies (backtest-first, per repo rule).** The 07-03 "adaptive" downtrend half-stake bounce sleeve — copy-pasted into DayTraderV5Gated, SwingDipV1, MomoBreakoutV1, TrendMomoV1 — was replayed tag-attributed on 3.5–4.5y of Binance data (docker freqtrade, `~/freqtrade/user_data/config_bb_audit.json`): **negative in ALL FOUR carriers — 19 entries, −$7.27 aggregate, 26% win — and it never fired once in live paper** (zero `bear_bounce` rows in bot_trades since ship). Removed the entry legs + sleeve-only custom_stake/custom_exit branches + dead indicators; post-edit verification backtests show surviving legs unchanged (SwingDip dip_in_uptrend byte-identical 250 entries; DayTrader legs identical counts; MomoBreakout breakout +$201.8). Bots now stand down in downtrends, as originally validated. FLAGGED for separate decisions (not acted on): TrendMomoV1's core leg backtests −29%/4.5y (26.5% win) and DayTraderV5Gated's three surviving legs are all fee-bled negative on the same window (−27.6% total), while MomoBreakoutV1's core is the only positive spot core (+20%); ~/freqtrade local copies predate the sleeve and were left untouched.

## 2026-07-11
- **Funding Farmer: multi-factor POSITION SCANNER — picks the best risk-adjusted positions, not just the hottest funder.** Replaces the naive raw-|APR| sort (which spent slots on thin, high-vol, trending-against alts that run the 10% stop before funding pays — why it was break-even). Designed via a fan-out+judge+synthesize workflow (6 philosophies, adversarially judged) grounded in the real venue data surface, then PROVEN on a new portfolio backtest (`scripts/backtest_scanner.py`, 150d, 6-slot). Two layers: (1) BACKTESTED CORE — a realized-vol + adverse-trend VETO (skip stop-out traps, leave the slot empty) + an |APR|-backbone bounded-discount rank (downside-capped, never a vol divisor); (2) LIVE-ONLY overlay (shadow-validated) — cross-venue `_bench` sign-agreement + full-depth clip-slippage gate + book-imbalance tiebreak. Cheap funding prefilter → candle-scan top 15 (cached ~50min) → book-probe only top 5, sized to the ~21 weight/min governor (Railway ws is CDN-blocked so every book = 1 token). RESULT at the **default 0.40 gate**: net **+52%** ($21.6→$33.0/clip/150d), ret/DD **2.0→5.3**, maxDD **−43%**, FEWER trades (282 vs 336) — robust across both halves + OOS. HONEST: much of the edge is directional MEAN-REVERSION (regime-dependent), not extra funding — the vol veto is the durable piece; funding is thin. Gate-widening to 0.25 (+155% in-cache) is OPT-IN (`SCAN_ENTER=0.25`) and partly a flat-slippage artifact — shadow-validate first. `SCAN=off` restores legacy. Every entry logs scan evidence to `venue_orders`. Shipped core == backtested core (same veto/score/lookbacks). Adversarially reviewed (4-lens workflow, findings verified): 3 confirmed fixes — the live features were computed on the in-progress forming hourly bar (now dropped → closed-bar only, matching the backtest + no look-ahead); the feature cache is now keyed on the last CLOSED hour (a fresh vol spike is picked up next loop, not ~50min later); the top-15 prefilter divergence documented + env-tunable — plus 2 hardening (defensive best-first book sort for the Railway REST-fallback; scan wrapped so a bug can't crash stop-management). One "medium" (governor starvation) was verified a FALSE POSITIVE (scan runs after stop-management + degrades to no-entry + fail-safe flatten). Shadow-first — nothing armed.
- **Both-perp (self-custody) Yield Harvester hedge — BACKTESTED and REJECTED, not built.** Asked to take the delta-neutral Yield Harvester (`perps-funding-carry`) live on Lighter; it can't (perp-only venue has no same-coin hedge — the code hard-refuses a naked-perp funded run). Chosen self-custody alternative: hedge a hot alt's funding leg with a beta-weighted ETH/BTC perp. `scripts/backtest_carry_hedged.py` (150d real HL funding+price, reuses `.dirfund_cache.json`) compares directional vs major-hedge vs ideal spot-hedge on IDENTICAL signals. HONEST RESULT: the hedge is WORSE than the directional Funding Farmer it was meant to improve (+$5.5 β·ETH vs +$7.9 directional per $25 clip/150d). Structural, not tuning: the funding-receiving side's price P&L is already ~flat (little market-beta to strip), so the hedge mostly adds double slippage + its own funding drag, while the idiosyncratic alt risk it CAN'T hedge is the part that matters; higher gates go negative. BTC-hedge cuts drawdown (ret/DD 0.66->0.75) but at lower absolute profit + double the live surface. The only profitable delta-neutral structure remains the CEX-spot hedge (docs/cex_spot_hedge_scope.md). Verdict per repo rule (no ship without a proving backtest; cf Trail Blazer): DID NOT build the live bot. See memory both-perp-hedge-no-edge.
- **Funding Farmer (`perps-funding-lighter`): go-live prep — re-validated, shadow evidence checked, docs corrected.** Pivoted to taking the already-built DIRECTIONAL Funding Farmer live instead. Re-validated: `--once` runs clean (231 perps, no orders); proved `lighter_live` REFUSES to boot without BOTH `REAL_MONEY_KILL=DISARMED_I_UNDERSTAND` and `PERPS_FUNDING_LIGHTER_MAX_NOTIONAL`. Shadow evidence (Postgres): `perps-funding-lighter-lshadow` deployed + running, measured perp-leg slippage on traded coins is SUB-1bps (ETH 0.3–0.5, SOL 0.13, BTC 0.36, HYPE 0.92) — zero-fee + spread-gate thesis holds; only ~2 closed round-trips so P&L sample still thin (~net flat, as expected for ~break-even directional). Fixed GO_LIVE_LIGHTER.md env drift: the example still showed the pre-tuning `FUNDING_HARD_STOP=0.05` / `MAX_SPREAD_BPS=50` (a 5% stop LOSES) — corrected to the tuned defaults + a don't-lower warning, and added a dated shadow-readiness note. Keys/disarm/deposit remain the operator's — nothing armed. NOTE flagged: `crypto-trend-daily-lighter` (Tide Rider LIVE) is crashed/error ~11h (likely auth), separate from this work.

## 2026-07-10
- **Tide Rider on LIGHTER — new bot `lighter_trend_bot.py` (crypto-trend-daily, 1x long perp).** Re-expresses the Kraken-spot 50/200 golden-cross trend follower as a 1x LONG PERP on Lighter (venue layer; lighter_live auto-refused until disarmed). RE-VALIDATED with the funding drag (`scripts/backtest_tide_rider_perp.py`): basket +52% spot -> +40% perp over 2.7yr (funding drag -13pp, erases the down-trend protection) — viable but weaker than Kraken spot; the drag is MODELLED in shadow so the P&L is honest. Long-only golden(enter)/death(exit) cross + catastrophic 35% seatbelt. Adversarially reviewed: 4 fixes (funding accrual now runs in LIVE too so the ledger/win-rate include the drag; signal on CLOSED bars only — drop the forming candle; catastrophic-stop seatbelt fires even when candles fail; hl_paper daemon refused to avoid clobbering the live Freqtrade Tide Rider dashboard row). Dashboard shows it as "🌊 Tide Rider · SHADOW/LIVE" automatically.
- **Funding Farmer: risk defaults TUNED on a directional backtest (`scripts/backtest_directional_funding.py`).** Replays the exact directional logic on 150d of real HL funding+price (30 coins, intrabar stop fidelity). HONEST RESULT: the strategy is only ~BREAK-EVEN — funding capture is real (+$8/150d per $25 clip) but directional price risk nearly offsets it (-$6.5), as expected when funding compensates the risk it carries. Least-bad + most-robust config = WIDE stop + TIGHT take-profit (a tight 5% stop whipsaws out on noise and loses -$10.6; 10-12% stop breaks even). Tuned defaults: HARD_STOP 5%->10%, keep TAKE_PROFIT 4%, MIN_VOL $5M->$10M, MAX_SPREAD_BPS 50->20 (live Lighter shows hot+liquid candidates all <=3.5bps, so 20 is safe and excludes traps). Verdict: run tiny/shadow — the genuine edge needs the delta-neutral CEX-spot hedge (deferred), which keeps the funding WITHOUT the price risk.
- **Funding Farmer (Lighter directional funding) — new bot `lighter_funding_bot.py` (perps-funding-lighter).** "Unlock live harvesting" on Lighter only (no cross-venue hedge). A single perp-only venue can't be delta-neutral, so this captures funding DIRECTIONALLY: short positive-funding perps / long negative, collect Lighter's zero-fee funding, price risk bounded by a HARD STOP (not a hedge). Reuses the venue layer end-to-end (hl_paper|lighter_shadow|lighter_live) so lighter_live is auto-refused until REAL_MONEY_KILL is disarmed + a PERPS_FUNDING_LIGHTER_MAX_NOTIONAL cap is set. Exits: hard stop / take-profit / funding-flip / funding-decay / max-hold. Liquidity filter (vol) to avoid thin-book traps (WEN 870bps). Funding accrued in paper/shadow; live reads it from account equity. Self-test caught+fixed a CRITICAL inverted-stop-sign bug before ship (stop now fires on adverse moves). Dashboard: "💸 Funding Farmer". Adversarially reviewed. HONEST: directional (real price risk), shadow-first.
- **Yield Harvester (funding-carry): shadow EXECUTION path built (shadow-first, no live orders).** `funding_carry_bot.py` gains `_perp_leg_fill(...)` — in `VENUE=lighter_shadow` it MEASURES the real perp-leg slippage by walking the live Lighter book (mid-referenced, adverse-only, one `venue_orders` evidence row per fill); in hl_paper it models the flat PERP_FEE (zero behaviour change). Funded modes (testnet/live/unknown) are HARD-REFUSED via a fail-safe allowlist — a perp-only venue has no automated hedge, so a funded run would be a naked perp. Adversarially reviewed (workflow): 3 findings fixed — slippage referenced to book mid not the stale frozen mark; decay close-gate re-checks payback on MEASURED cost; allowlist (not blocklist) guard. First live read: liquid hot coins ~3–7bps round-trip @ $300 (ZEC/COIN/BB), thin/exotic ones are traps (WEN 870bps) → a live harvester needs a liquidity/slippage gate the shadow ledger will calibrate. See docs/lighter.md + memory funding-carry-structural-edge-lighter.
- **Yield Harvester (funding-carry): Lighter zero-fee backtest — REAL friction-bound edge confirmed.** `scripts/backtest_funding.py` replays the exact live bot logic on 180d of real HL hourly funding across 39 coins, sweeping the fee model HL(29bps)->Lighter+CEX-hedge(20bps)->both-perp(3bps)->0. Win rate climbs 28%->40%->81%->98% as fees fall — the signal is right, fees were the killer (OPPOSITE of Trail Blazer). Net/180d on deployed capital, 40% gate: HL +1.0%, Lighter+CEX-hedge +1.9% (~3.8%/yr market-neutral), both-perp +3.5%. Break-even friction ~39bps. Lowering ENTER_APR 40%->20% lifts net ~35% ON both-perp only; at 20bps-CEX-hedge a 15% gate goes NEGATIVE — so keep 40% gate unless hedging with a second Lighter perp. Edge concentrated (ME/JTO/INJ). Bot is still a paper simulator; live harvest needs a Lighter execution path (shadow-first). See memory funding-carry-structural-edge-lighter.
- **Trail Blazer: 200-EMA regime gate (REGIME_GATE_FRAC=0.97)** — only enters dips within 3% of the 200-EMA (a pullback in an uptrend), skipping falling-knife dips below trend (logged REGIME_SKIP). Backtest-tuned (scripts/backtest_regime.py): the loose -3% gate roughly HALVES the out-of-sample loss vs no gate (-7.7%->-3.3%) and trades more than the strict gate. Risk control, NOT an edge — the strategy is still net-negative even gated (see memory trail-blazer-no-durable-edge). Widening the universe was tested and REJECTED (made OOS worse -3.3%->-8.7%). Env-tunable; 0.0 disables.
- **live trade logging**: the live (lighter_live) order path now records every real open to venue_orders and every real close to venue_orders + paper_trades (venue='lighter', shadow=false, realized P&L, raw exchange response) — so the Lighter Live dashboard card shows trade history, win/loss and per-trade P&L exactly like the paper bots (previously only equity-based total P&L). Ledger writes are guarded separately so they can never mask an order failure. Also fixes a latent bug: live opens now set entry_ts (the 72h time-stop was inert on the live path). Applied to hyperliquid_momo_bot.py (Trail Blazer).
- **live bots no longer die silently (the 'stale' bug)**: when a lighter_live/testnet bot fails venue init (e.g. "private key does not match the one on Lighter"), venue_context now publishes an explicit status=error row with the reason before re-raising — so the dashboard card shows WHY the bot is down instead of just ageing into a stale flag. NEW `scripts/lighter_test_trade.py`: user-run order-path test — default places a POST-ONLY limit 50% below market (cannot fill) then cancels it (sign→submit→cancel proven, ~$0); `--fill` does a real ~$10 market round-trip.
- **live P&L measured from starting collateral, not the $1000 paper baseline** — a live Lighter bot funds from real collateral (e.g. $65), so the dashboard was showing an untraded live account as -$935 (equity - $1000). Fix: the live path records its starting equity once (persisted in bot_state `<bot>-lighter:live`) and publishes pnl_abs = equity - baseline (→ $0 until the first trade); the dashboard also never applies the $1000 paper baseline to `is_live_bot` rows. Applied to momo + perps bots.
- **venues/lighter_client.py — ws hardening**: Lighter's CDN 400s the order-book WebSocket upgrade from cloud (datacenter) IPs though its REST works from the same host, so the live bot spammed reconnect warnings. Now the ws presents a browser-like handshake (Origin + Chrome UA) as a best-effort, and after 3 quick retries goes QUIET (retries every 10 min, logs once) — orderbook() falls back to governed REST snapshots, which work fine for order placement. No functional impact either way.
- **Dockerfile.perpslive**: dedicated image for the bounce-catcher-live Lighter service (hyperliquid_perps_bot.py; same inert-until-disarmed pattern as Dockerfile.momolive).
- **dashboard: dedicated "⚡ Lighter Live" section** — real-money `<bot>-lighter` rows are pulled out of the paper grid into their own bordered, red-accented section at the top of the page (header shows live equity · P&L · bot count + a REAL MONEY tag), so live money is never lost among the paper cards. Render-tested. `perps-donchian-breakout-lighter` went live this session ($64.93, Trail Blazer on Lighter).
- **venues/lighter_client.py — live-path fix**: `SignerClient.__init__` builds an aiohttp `ApiClient` internally (→ `asyncio.get_running_loop()`), so constructing it in the synchronous `LighterClient.__init__` crashed the LIVE/testnet path with `RuntimeError: no running event loop` (never hit before because the signer path can't be tested without a key). Now the signer is constructed ON the client's loop thread via `run_coroutine_threadsafe`, same as the read-only ApiClient in `_build()`. Verified with a dummy key: construction now reaches auth (fails cleanly on key validation, not the loop). Shadow/paper paths unaffected.

## 2026-07-09
- **Dockerfile.momolive + trail-blazer-live service scaffold**: dedicated image for Trail Blazer's LIVE Lighter service (CMD baked in, funding-service pattern; select via RAILWAY_DOCKERFILE_PATH=Dockerfile.momolive). Deployed inert by design: VENUE=lighter_live with REAL_MONEY_KILL armed refuses to boot until the user supplies LIGHTER_API_PRIVATE_KEY and explicitly disarms — the user-only steps.
- **fleet_risk: venue-aware (no-miss-sync for go-live)** — the fleet risk light + signal bus counted directional exposure and read funding by BASE bot name, so once a bot goes live on Lighter (publishing as `<bot>-lighter`) the live book would be INVISIBLE to the fleet view and funding would be read from the wrong venue. Now `authoritative_row()` prefers the live Lighter row over the paper twin (counts real Lighter exposure, no double-count), the shadow/testnet cohort is surfaced as `info_only` (modelled, never moves the light), and the signal-bus funding prefers the Lighter-facing funding-carry row + tags `funding_source` (HL vs Lighter funding genuinely differ). Verified against the live DB: shows `shadow(info) 5L/0S` from the running shadow cohort. regime_oracle deliberately left on HL (majors' daily regime is venue-identical, nothing consumes it yet, and Lighter's candle REST is WAF-blocked — moving it would force the Lighter SDK into the shared freqtrade image for ~zero benefit); cross-exchange/triangular arb are inherently multi-venue and unchanged.
- **NEW bot `lighter_perp_sniper.py` (🎯 Perp Sniper) — Lighter-native new-listing sniper, SHADOW-FIRST**: replaces the spot Launch Sniper for the Lighter go-live (the spot sniper can't run on a fixed-market perps DEX). Detects freshly-listed perp markets by diffing the active market set against a persisted baseline (source of truth; `AnnouncementApi` text is context only), seeds the baseline on first run (never snipes the existing 215), then snipes new listings long with TP +15% / SL −10% / 6h max-hold + global cap, all through the venue layer (Lighter-only; defaults to `lighter_shadow`, refuses `hl_paper`). UNVALIDATED by design — runs shadow (modelled fills on live books → `venue_orders` ledger) until Eamon disarms the kill switch. Verified: seed cycle (198 active markets baselined, 0 sniped) + a simulated new-listing detection that shadow-snipes ADA/WIF crossing the real book (~3–4 bps entry slippage). Registered on the dashboard (EXPECTED + label); `railway.lightersniper.toml` deploys it from the base Dockerfile.
- **Dockerfile.funding**: COPY `paper_broker.py` + `venues/` — funding_carry now imports the venue layer, so the lean image needs them (validated by importing the bot with only the Dockerfile.funding file set present).
- **Lighter.xyz venue layer (Gate 0) — `venues/` package**: NEW `VenueClient` abstraction (place/cancel/amend, positions, balances, funding, orderbook, candles, ws) with `HyperliquidClient` (byte-identical passthrough of the old inline HL logic) + `LighterClient` (lighter-sdk 1.1.1). `VENUE` env selects `hl_paper` (default, unchanged) | `lighter_shadow` | `lighter_testnet` | `lighter_live`. Trail Blazer (`hyperliquid_momo_bot.py`), Bounce Catcher (`hyperliquid_perps_bot.py`) and Yield Harvester (`funding_carry_bot.py`) refactored onto it. **Parity proven**: HL candles byte-identical old-vs-new; one-cycle decision diff = 0 for Bounce Catcher, and only the 2 intended cap-clips for Trail Blazer. Both pilots ran one clean shadow cycle on LIVE Lighter mainnet books.
- **Trail Blazer global position cap (pre-live blocker)**: `hyperliquid_momo_bot.py` gains `MAX_OPEN_POSITIONS=8` + `MAX_NEW_PER_LOOP=2` gated on a live open count (the same pattern Bounce Catcher got Jul-5) — it was holding 15 concurrent longs alone vs the fleet budget of 20. Valid buys blocked by the cap log `CAP_SKIP`. In lighter modes the cap derives from the per-bot notional cap.
- **Adapter safety rails (`venues/safety.py`, `venues/governor.py`)**: `REAL_MONEY_KILL` ARMED by default — `lighter_live` refuses to start and flattens+halts mid-run unless set to `DISARMED_I_UNDERSTAND` (checked EVERY loop); per-bot notional caps from env (no growable code default); `LIGHTER_MAX_DAILY_LOSS` flatten-and-halt. Weighted TX-budget governor for the 60-req/min standard tier (order tx weight 6, exponential backoff on 429/405), ws-first market data with one shared reconnecting book cache.
- **Shadow mode (`venues/shadow.py`)**: `ShadowBroker` fills paper orders by crossing the LIVE Lighter book (walk asks for buys, bids for sells) and logs every order to the new `venue_orders` Postgres table (decision px vs modelled fill px, spread/slippage bps, raw response) — realizable-spread evidence for sizing. Never sends.
- **bot_pnl_store**: `paper_trades` gains `venue`/`shadow` columns; NEW `venue_orders` table + `publish_venue_order()`. All guarded (never raise into a loop).
- **dashboard: LIVE distinction + separate live P&L total** — venue-variant rows (`<bot>-lighter`/`-ltest`/`-lshadow`) are detected dynamically, badged (LIVE red / TESTNET amber / SHADOW blue), kept OUT of the paper crypto headline, and summed into their own **🔴 LIVE · Lighter** totals line + `/pnl.json` `meta.live_fleet`. Real-money live P&L folds into GRAND TOTAL; shadow stays out (modelled).
- **day-1 verifications → `docs/lighter.md`**: 215 mainnet markets, ZERO fees, $10/order min, ETH/ADA/HYPE listed (Cowork excerpt was wrong on ADA/HYPE), BLUR/ATOM/INJ/ORDI/TON unlisted, kBONK↔1000BONK / PEPE↔1000PEPE remaps, native cross-venue funding (lighter+HL+binance+bybit in one call), signer binaries verified on linux/amd64 AND Mac arm64, ws ~800ms. Scripts: `scripts/lighter_day1.py`, `scripts/lighter_testnet_smoke.py` (6/6 read-only pass), `scripts/lighter_accounts.py` (read-only sub-account mapper). `GO_LIVE_LIGHTER.md` = sub-account-per-bot user-only checklist. NO keys, NO funds, NO `lighter_live` deploy — that stays with Eamon.

## 2026-07-08
- **Dockerfile.dashboard**: COPY fleet_watchdog_svc.py into the image (follow-up — the explicit COPY list didn't include the new watchdog module, so /watchdog.json 500'd with ModuleNotFoundError while the guarded thread kept the service healthy).
- **fleet_watchdog_svc + dashboard: in-service fleet watchdog (C layer)** — daemon thread in pnl-dashboard probes its own /pnl.json every 5 min; transition-based alert emails via report_emailer (dormant until SMTP_* set; 30-min repeat gap; recovery notice on clear); warnings for open-positions>20 and daily loss<-$100; live state at `/watchdog.json` (read-only, no secrets). Complements the new GH-Actions `fleet-watchdog` workflow (external, ~30-min, single-issue transition alerts — first probe caught perps-funding-carry STALE + 58/20 exposure, issue #31). Read-only towards the fleet.
- **sniper Jul-7 −$29.06 root-caused from the paper_trades ledger (NOT old-inventory flush)**: 48 `delisted` churn closes −$11.56 — ALL opened Jul-7 itself, in 5 batches (03:29/04:44/06:47/12:06/18:55 UTC), the last three AFTER the wave-2 blocklist deployed 06:23 UTC, using tickers the curated set missed — plus one legitimate GROVE/EUR stop_loss −$17.50 (gate passed, stopped in 15min: strategy working). TOKENIZED_BASES extended with the 19 observed wave-3 leakers (RKLB/MRVL/SONY/HYUNDAI/TQQQ/EWZ/HIMS/ASTS/NBIS/XCU…). Marker in code: if a FOURTH wave leaks, stop extending the list and build the per-exchange listing-flood quarantine instead.
- **freqtrade_pnl_poller**: open_pos cap 6→12 — breakout-4h held 7, card listed 6; worse, fleet_risk.py counts longs from this list, so truncation silently under-reported fleet exposure to the Jul-14 enforcement review. (The "8 open / 6 listed" flag was this cap, NOT a max_open_trades breach.)
- **bot_pnl_store + dashboard: publisher-sent pnl_daily now real end-to-end** — bot_pnl gains a `pnl_daily` column (ADD COLUMN IF NOT EXISTS), publish() accepts it, and the card's "Today P&L (UTC)" prefers it over the server-side equity-curve delta — but only while the row was written today (UTC), so once-a-day publishers can't show yesterday's figure all day. Built for the alpaca anomaly: its +$10,673.77 "today" was the equity-delta vs a glitched Jul-6 88k snapshot (the −$19.9k anomaly's mirror), while its "lifetime −$5,365.91" was actually Alpaca's day P&L published into pnl_abs (fixed in the equities-momentum-alpaca repo: pnl_abs → lifetime vs $100k baseline, day P&L → pnl_daily).

## 2026-07-07
- **pair-web coverage (ledger + liquidity audit)**: perps bots (rsi-meanrev, donchian) universe 30→33 (+HYPE #4-by-volume/$359M-day with zero prior fleet coverage, +ZEC, +TAO); regime-switch whitelist 10→12 (+HYPE, +ZEC — two more liquid breakdown candidates for the armed short engine); regime-oracle universe → 16 to match; spot v5/v6/v7 24→29 (+TIA/ARB/WIF/BONK/PEPE — the mid-cap cluster already earning on the perps ledger: NEAR +$10.18/kBONK +$10.02/TIA +$4.87/WIF +$4.87/ARB +$4.00); v4 BNB/USD→ADA/USD (Kraken lists no BNB — pair was silently dropped at boot); sniper TOKENIZED_BASES extended with the second tokenized-equity wave (CSCO/DELL/ADBE/GLW/COHR/CRDO/SNDK/ADI/PLAY…, 0 wins ≈−$7 in ledger). Position caps unchanged everywhere — more signal candidates, same risk budget. Family bots deliberately untouched (era-zero measurement).
- **fleet_risk / regime_oracle (day-zero review catches)**: signal-bus snapshots now historized to bot_state_history (Jul-14 review needs them to judge L3); SUI added to the oracle universe (fleet holds it, oracle didn't cover it).
- **dashboard: UNIFORM CARDS** — every bot card now shows the same information set regardless of publisher quality: `Today P&L (UTC)` under Total (equity-curve delta since UTC midnight where a curve exists, else trades-closed-today), Win/Loss from the union ledger when a bot publishes NULLs (the perps bots), detailed `Last close` (pair · P&L · reason · age) for every bot, and open positions for bots whose book lives in bot_state (perps broker pos incl. mark-based uPnL, funding carries with net accrual). One cached ledger pass (`fetch_ledger_enrich`, 20s TTL); `/pnl.json` gains a per-bot `enrich` object for downstream consumers.
- **cross-bot layers 1-3 (ADVISORY)**: NEW `regime_oracle.py` — one shared per-major daily regime (EMA200+slope direction, ADX 17/11 hysteresis character, HL closed daily candles) published to bot_state `regime-oracle` + append-only `bot_state_history`, fail-safe TTL 2h. NEW `fleet_risk.py` — fleet-wide directional position count vs budgets (20L/12S) -> traffic light in `fleet-risk` incl. pair-pileup detection (the 26-position dip scar), plus scanner exhaust mirrored to `signal-bus` (funding APRs, cross-exchange dislocation, tri-arb depth, pulse mood). Wired into run_all.sh (30m/5m loops) + Dockerfile; `bot_pnl_store` gains `fetch_bot_pnl()` + `save_history()`. Publish-only week one; enforcement (entry veto / sizing) wired from the recorded history after ~7 days.
- **config_v7_momo (crypto-breakout-4h)**: max_open_trades 10→6 — held 10 correlated longs in one bounce with no cap (same risk class rsi-meanrev was capped for Jul-5).
- **RegimeSwitchV2**: per-pair per-4h regime diagnostic log (dir/trend/ADX/close/channels → verdict). Idle 6 days through two blind gate cuts; now the logs say why.
- **listing sniper**: DELISTING-RISK GATE — 'ghost' listings (junk intel AND first venue outside the top-15) skipped entirely; junk elsewhere quarter-stake (was half). Targets the 142/177 `delisted` exits (−$47 lifetime) while keeping the lottery tail reachable.
- **funding_carry_bot**: EXIT REBUILD (0W/28L — every close realized ≈ round-trip fees): decay alone no longer closes; closes only on flip persisting ≥1h, decay AFTER fee payback (net ≥ +$0.10), 14d expiry, or a −2%-of-notional bleed stop. NEW 6h persistence entry filter (|APR| must hold ≥ ENTER_APR for 6h — spike-and-revert funding never passes). hot_since persisted to bot_state.
- **Dockerfile.freqtrade**: COPY bot_learn.py — the learning brain has been dead in deploy since Jul-5 (run_all.sh runs it 2-hourly; the file was never in the image: `can't open file '/freqtrade/bot_learn.py'`).
- **OPTION-B ISOLATION**: family bots (mum/dad/avo-maria/georgia) removed from the freqtrade-bots container; each runs ONLY in its dedicated Railway service via run_all.sh `ONLY_BOT` mode (one bot + FT_POLLER_BOTS-scoped poller). Root cause fixed: the 4 services built from Dockerfile.freqtrade were EACH running all nine bots (fresh $1000, no volume) + a 9-name poller — five pollers race-writing bot_pnl (the Jul 5-7 "counter resets"/equity flapping; Jul-6 trade contamination). Poller _DEFAULT_BOTS back to the 5 originals; stale family comments fixed.

## 2026-07-06
- **funding_carry_bot**: raise ENTER_APR 20%→40%, EXIT_APR 8%→15%, lower MIN_DAY_VOLUME $5M→$2M, extend MAX_HOLD 7→14d — fix fee bleed on fast-decaying carries
- **funding_carry_bot**: raise MAX_POSITIONS 5→8, NOTIONAL $200→$300 — expand reach and earning capacity
- **SwingDipV1**: buy_rsi default 35→42 in strategy code (config key was silently ignored)
- **MomoBreakoutV1**: entry_lookback default 30→20 (shorter Donchian for more frequent signals)
- **RegimeSwitchV2**: ADX trend gate 20→17, chop gate 14→11 (admit weaker trends)

# Changelog

## 2026-07-07
- dashboard: **reset-proof "All-time Σ (ledger)" row on every bot card** — lifetime P&L + closed count straight from the durable `bot_trades` ledger, so a live-DB wipe can never again masquerade as "no trades" (July-6: Range Raider's card showed $1000/0 while the ledger held 8 real post-rework trades, 3W/5L −$2.47). If this row and Total P&L disagree, a reset happened.
- investigated Range Raider "no trades since rework": logs CLEAN (no errors/OOM/crash-loop); the regime-legs fix DID trade (8 closes 07-05→07-06 03:00); live counter wiped ~July-6 during the family-bot restructure deploys (exact mechanism aged out of the log window). Retracted the suspected mum/8089 port collision — regime-switch polls in its own container via FT_POLLER_BOTS.

Shared log of every change that affects a running bot, the dashboard, or deploy
behaviour. **Multiple Claude sessions work this repo in parallel** — this file is
how they stay in sync. If your diff touches bot code (`*.py`, `*.sh`, a strategy,
a config) you MUST add a dated entry here in the same commit (a CI check enforces
it). Newest first. Keep entries one line; link the commit if useful.

## 2026-07-05 (later)
- ⚡ Range Raider (`DayTraderV5Gated`): DIAGNOSED the "no trades since rework" — not a bug (logs show a clean load, no populate_indicators error); it was OVER-GATED: every mode needed a pullback AND an uptrend on the same 1h candle, a rare confluence, so it sat idle in this choppy market. FIX = two new regime-covering legs so it operates regularly: `trend_breakout` (ADX≥25 + close>EMA50 + break of 20-bar high — trades RISING phases; grounded in V7's Donchian edge; `confirm_trade_exit` vetoes the range-top sell so breakouts run) and `range_meanrev` (ADX<20 chop + buy the range low, no uptrend requirement — the engine for a choppy market; low-ADX gate is the research-backed guard against knife-catching). Both fee-band gated; range_meanrev half-stake + 2.0×ATR stop. Distinct tags so the brain measures each.

## 2026-07-05
- **NEW bot: `RegimeSwitchV2`** (⚖️ Two-Way Tide) — rebuilt `perps-regime-switch` from the fleet's combined lessons + evidenced public practice (AQR trend-following, Hyperliquid docs). The fleet's only dual-direction engine: 4h Donchian breakout LONG in up-regime / SHORT in down-regime (mirrors the proven V7 PF~1.8 edge to finally profit from a downtrend). Per-pair daily regime (EMA200+slope, ADX gate w/ hysteresis), structure-break exits (let winners run — not trailing/ROI cap), wide ATR disaster stop, inverse-vol sizing, low leverage, correlation-aware global cap, pulse half-stake, Postgres persist. UNVALIDATED (no OHLCV to backtest) — dry-run, robust-by-construction, brain-watched.
- `RegimeSwitchV2`: tuned for more frequency — Donchian 20/10→15/8, ADX gate 22/16→20/14, universe 2→10 liquid majors, max_open 3→6 (still <60% of book). Stays on fee-viable 4h (not 1h churn).
- dashboard: **trendy + descriptive bot names** (emoji + plain "what it does") replacing the technical labels.
- ci: deploy path-filter tracks `RegimeSwitchV[0-9]+.py` so future V-bumps auto-deploy the regime service.
- dashboard: **Total P&L on every bot card** — freqtrade bots publish `pnl_abs=None` (gain is `equity−$1000`), so they showed no P&L line; now computed from equity for all paper bots.
- learning: **brain now covers ALL bots** — `bot_learn.py` read only `bot_trades` (freqtrade); added `bot_pnl_store.fetch_paper_trades()` and merged it so perps + sniper closes (in `paper_trades`) are analysed too. The `reason` field splits into long/short enter_tag + exit_reason, giving the brain long-vs-short expectancy.
- day trader (`DayTraderV5Gated`): widen buy zone 0.22 → 0.37 (enter higher up the pullback for materially more fills; captured move thinner ~0.41×band, still fee-positive). Doubled live bounce eligibility 0→2 pairs.
- day trader (`DayTraderV5Gated`): loosen risk-off band gate 2.2% → 2.0% (more relief-rally pairs, still above fee floor).
- day trader: add third entry mode `bounce_pullback` — trades the relief rally in a risk-off regime when the pair's own 1h EMA50 is above price AND rising; half stake, fast exits. Fixes the "idle in a bounce" gap.
- perps `rsi-meanrev`: global position cap (`MAX_OPEN_POSITIONS=6`, `MAX_NEW_PER_LOOP=2`) gated on a live open count — was holding 14 longs (70% of book) in one correlated dip with no cap.
- ops: schedule the learning brain (`bot_learn.py`) every 2h in the freqtrade supervisor (`run_all.sh`) — it was never scheduled, so it never accumulated the 3 runs needed to promote a lesson to actionable.
- ops: add `CHANGELOG.md` + CI enforcement + `CLAUDE.md` session-coordination rules (this change).

## 2026-07-04
- Dashboard v2: health checks, per-mode (enter_tag) W/L/P&L rows, brain card, BTC regime chip, sparklines, pulse strip.
- sniper: throttle — stake 100→50, max-open 25→10, SL 50%→35%, max-hold 48h→24h, spread gate 500→250bps, depth 3×→5×, vol floor 25k, anti-chase 150%→100%.
- sniper intel (`listing_intel.py`): exchange announcement feeds + CoinGecko footprint, half-stake on junk listings.
- pulse: extend panic sizing to V6/V7 bounces; pulse history records funding APRs.
- brain v1.1: era awareness — hypotheses only from current-strategy-era trades.

## 2026-07-03
- Adaptive dual-mode strategies (V5/V6/V7/V8): BTC-4h 50/200 regime switch (risk-on pullbacks / risk-off sweep-reclaim bounces).
- Durable equity across redeploys: `/freqtrade/persist` volume + Postgres state for perps/sniper; freqtrade auto-deploy unfrozen (persistence makes redeploys safe).
- `perps-funding-carry` bot added (dry-run Hyperliquid funding-rate harvester).
- `market_pulse.py` news/social/funding feed → Postgres; `bot_learn.py` brain added.
- `perps-regime-switch` moved to Donchian-style entries; auto-deploys on its own file changes.

## Before 2026-07-03
See git history. Key milestones: fee-bleed + close fixes for perps (07-01), the
persistent-volume saga (07-01), day-trader fee-gate/trend-gate (07-01/02),
unified pnl-dashboard + Postgres publishing (06-19), multi-bot freqtrade
supervisor (06-18).
