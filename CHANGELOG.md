## 2026-07-16 (an) — workflow fix: the GitHub failure emails were a RETIRED service (operator: "im getting github failed notifications")
- **Every real deploy succeeded** — the last two Railway Redeploy runs went red only because the workflow still tried `railway up --service perps-regime-switch`, a service decommissioned in the 12/13-Jul retirement ("Service not found" ×3 → exit 1 → failure email), triggered on any `bot_pnl_store.py` change (both 16-Jul deferred-fix merges touched it). freqtrade-bots, pnl-dashboard, and funding-carry all logged "Deploy complete" in the same runs — the fixes ARE live.
- railway-redeploy.yml: perps-regime-switch block + path filters removed (comment records why); railway-logs.yml manual-dispatch default swapped to a living service. No runtime code in this change.
