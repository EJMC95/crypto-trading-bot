# Fleet daily — 16 Aug 2026 (Sat), 14:49 AEST · afternoon edition

Feed generated **16-Aug 14:49 AEST** (04:49Z). All 18 rows fresh —
`feed_stale: false`, `n_stale: 0`. Liveness read before content (I1). Shadow
books are paper; two rows are real money. Not financial advice.

> **This supersedes the 09:54 brief**, preserved at
> `daily_pnl_2026-08-16.superseded-0954.md` — nothing was overwritten. That
> edition's growth item (🧘 Douglas realising 3.5× its declared stop) was
> **fixed and deployed today**; §5 is what the fleet looks like after.

---

## 1 · Live money — both rows healthy, nothing to action

| Row | Equity | P&L | Open | Closes | Age |
|---|---|---|---|---|---|
| 💸 `perps-funding-lighter-lighter` (Farmer) | **$197.53** | +$6.00 | 1 (XAU short) | 123 | 151 s |
| 🙏 `freqtrade-avo-maria-lighter` (Avo LIVE) | **$62.76** | −$0.03 | 4 (ADA/BTC/LTC/TRX) | 0 | 107 s |

Live total **$260.29 equity, +$5.97**. Risk light green, `clip_scale` 1.0,
respiration healthy, brain healthy, no quarantined levers.

**Both rows gained a margin block today and it now reads off the live book.**
Farmer: leverage 0.1532×, cross, nearest liquidation `dist_frac` **6.35 —
i.e. 635% away**, not 6.35%. Avo: 0.999×, and `liq_unknown: [ADA, BTC, LTC,
TRX]` is *correct*, not a data gap — at 1× a long has no liquidation price.

**Verdict: real money is safe.**

---

## 2 · Fleet

**Today (16-Aug UTC, part-day): +$0.63 on 6 trades.** ⚖️ Counterweight +0.48,
🎫 Taker +0.40, 🏛️ Albanese −0.25.

Yesterday closed at **−$27.64**, of which 🧘 Douglas was **−$26.48** — and
§3 explains why that number is not what it looks like. Trailing 7d −$40.72;
August MTD −$86.52. Shadow fleet 16 rows, +$21.40 aggregate.

Roster is **18 rows**, down one: 🧙 `book-schwager` was retired today by
another session (I17, undecidable-by-tail).

---

## 3 · Today's fixes — the frozen-mark class, closed fleet-wide

The headline of the day is a correctness class, not a P&L move. **Four books
were pricing themselves off a mark captured at container boot and never
refreshed**, because `funding_map()[coin]["mark"]` is
`markets[sym]["last"]` and `_load_markets()` runs once at client
construction. `venues/marks.py` had forbidden exactly this on a risk path
since it was written; three books did it anyway.

Measured against the venue's own tape: 🧘 Douglas booked an entry at
**0.017707 when ROBO's entire 12:00 bar was [0.014507, 0.014938]** — 21%
above the whole hour's high. Its real move was **−6.7%, inside its own 7.03%
stop**; it booked −23.2%. **So yesterday's −$26.48 is largely an accounting
artifact, and Douglas's two closes should be read as void rather than as a
−3.5R signal.**

| Fix | What it closes |
|---|---|
| `(nm)` 🧘 douglas · 📐 grimes · 🧙 schwager | bracket, entry and MTM all price off the live book; exits leave the funding gate; blind stops published |
| `(nw)` 🎸 Barnes | 10 open `xsect` legs were marking off the frozen price; `price_basis` now stamped so `(nf)`'s attribution stays splittable |
| `(nn)` 🎯 sniper | `(ne)`'s surge telemetry moved back to module level — **CI had been red on every push** over a 0.1pp coverage floor |
| `(os)` 🛡️ immune organ | a 540s boot fuse longer than the gap between deploys meant it **never ran for 50 minutes** |
| `(ou)` 7 sibling organs | judge, tuner, proprioception, regen, radar, allocation, incubator, impl-shortfall — same starvation, all recovered |

**Verified live, not assumed.** All 18 containers `CURRENT`; Barnes shows
`.get("mark")` ×0 with `stop_marks` wired; the three price books publish
`stops_blind`; ⚖️ Schwager's `open_pnl` moved 4.43 → 3.92 → 3.82 under an
unchanged build stamp, which the frozen mark made *impossible*. All nine
starved organs went 4/9 → 9/9 fresh within three minutes.

**One real-money deploy**, on your instruction: the margin fix reached both
live rows (Farmer `5f75543e27ec → d73e8938ad8d`, Avo `c317eb48e37b →
3ef2c7dee9e8`). Pre-flight found it carried **more than telemetry** — `(oc)`
removes Avo's fallback to the shared clip dial. Measured before shipping: no
`live.*` levers in force, `clip_scale` 1.0 raw, so both paths resolve to 1.0
and it is a no-op at today's state.

**Two things I got wrong, corrected on the record.** I twice claimed
Schwager's four open positions carried phantom entries; measured, **all four
sit inside the venue's real bar** (−0.30%..+0.24% of the close). The rule I
had missed: the frozen mark's error *grows with container uptime*, and those
four opened at their container's first boot. And I ran `git reset --hard` on
a tree three sessions share — both peers verified nothing of theirs was lost,
but that was luck plus their commit discipline, not care on my part.

---

## 4 · Claims and gate distance

`fleet_allocation` (advisory): three books hold a measured claim, all funding
— 🌾 carry **0.1492%**/trade (n=101), 💸 Farmer live 0.0291% (n=123), Farmer
shadow 0.0268% (n=180). Every directional book reads **0.0** across 10 books
and 362 closes.

`golive-readiness`: **`ready: []`**. 🙏 Avo's shadow arm is the fleet's only
`on_track` book — 4/6 bars, t=2.17, ETA **2026-11-07**, binding bar is closes
(n=8). 🔮 Georgia also reads 4/6 but is `undecidable`: t=0.18 at n=106.

---

## 5 · One growth item — CHECKED AND REFUSED WITH EVIDENCE

**[CORRECTED 15:10 AEST, after this brief was first sent.]** The item I filed
here — *"🌾 carry's claim drives the fleet's largest capital move on a sample
its own era rejects"* — **was wrong in the half that matters, and I am
correcting it rather than leaving it to be acted on.**

The premise was right: the organ does rank on the pooled all-time claim, carry
does read `claim: 0.1492%` at n=101 with `claim_era: null` at n_era=10, and it
does publish a **$10,072 target**. What I asserted and had not checked is that
this *moves capital*. It does not.

**`(lx)`, 13-Aug, already fixed the acting half** — and it separates the two
questions I had conflated:

* **Ranking stays pooled, deliberately.** `(kc)` measured that era-scoping the
  ranked claim leaves **zero claimants on 15 of 15 days** — it turns the organ
  off. So that is a measured refusal, not an oversight.
* **Expansion is priced on the book's own era.**
  `fleet_bus.allocation_scale` caps at flat unless
  `era_supports_expansion(row)`, which demands a REAL positive `claim_era`
  (bool excluded, junk types declined, NaN already false). One-directional:
  it can only ever make a book smaller, never larger, never below the probe
  floor, and it reverses itself unaided the moment the era sample earns a
  positive bound.

**Verified on today's live payload, not assumed:**

| book | target | raw scale | `claim_era` | effective |
|---|---|---|---|---|
| 🌾 carry | $10,072 | 4.00 | `None` | **1.00** |
| 💸 Farmer shadow | $2,015 | 2.01 | 0.0 | **1.00** |
| ⚖️ Counterweight | $250 | 0.25 | 0.0 | **0.25** (a reduction, allowed) |

No book is scaled above flat. Guarded by
`tests/autonomy/test_allocation_consumer.py` (11 tests green), which pins the
exact carry case and the `0.0`-vs-`None` distinction.

**So there is no growth item on offer today, and that is the honest output.**
What I checked: the allocation ranking (already correct by measurement), the
gate horizon (nobody `ready`, 🙏 Avo shadow `on_track` and needing sample not
repair), the immune organ's Parliament flag (transitional, §6), and the
frozen-mark class (closed fleet-wide, §3). A refusal with evidence beats an
invented option.

**The transferable error:** I read `target_usd` as an instruction and never
traced it to the consumer. The payload published `claim_era: null` directly
beside the claim — the information was there and I did not join it up. Same
shape as the day's other findings, and this time it was mine.

## 6 · Housekeeping

- **🏛️ Parliament flagged sick — transitional, not a fault.** 5 restarts
  counted against a counter now at 9. Its own health reads `stalled: []`,
  cycles climbing. Measured: **all 6 of yesterday's restarts correlate with a
  freqtrade-bots deploy** (−18s..+4.5m), so there is no crash loop — it is
  deploy churn. `(oi)` now discriminates the two by build stamp, and it is
  demonstrably working: across two immune runs the counter went 8 → 9 while
  the death count stayed at **5**. The 5 were accumulated while the immune
  organ itself was down with no build baseline; they drain out of the 24h
  window.
- **Deploy storm:** 71 commits to main today from several concurrent sessions,
  freqtrade-bots redeploying every few minutes. That is what starved the
  organs in §3, and it is the fleet's real operating hazard right now.
- **Suite green:** 1644 passed / 2 skipped on the desk, exit 0 on a clean
  checkout of `origin/main`; CI green on the current tip.

**Carried:** carry's era-vs-all-time claim (§5); 🙏 Avo shadow needs sample
only; 🧘 Douglas's clock restarts from today on correct prices.
