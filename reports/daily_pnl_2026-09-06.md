# Fleet daily — 6 Sep 2026

**Snapshot:** `/pnl.json` `generated_at` **Sat 6 Sep 21:12 AEST** (freshest row
13s, `feed_stale: false`, `n_stale: 0` across all 16 rows). Organs read from the
same pull; every one inside its TTL. Repo synced, `HEAD == origin/main ==
4bd3d17`, nothing new since (yc) — the last commit is 5 Sep 02:55 AEST.

> **A second brief already existed for today.** An earlier run wrote
> `daily_pnl_2026-09-06.md` at **21:08** off a 19:39 snapshot. It has **not**
> been overwritten — it is preserved at
> `reports/daily_pnl_2026-09-06.superseded-2108.md`. This brief is an
> independent read of a **90-minute-fresher** pull, and it reaches a different
> growth item. Where the two disagree on a number, the reasons are noted below.

---

## 1. Real money — both rows healthy, nothing halted

| | 🙏 avo-maria LIVE | 👩 mum LIVE |
|---|---|---|
| equity | **$413.32** | **$569.18** |
| P&L | **+$99.16 (+26.06%)** | **+$48.76 (+9.37%)** |
| today realised | — (no closes) | **−$5.26** |
| open / closed | 2 / 14 | **0** / 89 |
| W–L | 9–5 | 66–23 |
| age | 20s ✅ | 34s ✅ |
| status | `online` | `online` |
| build | `cdefd4383ee1` / n=18 | `cdefd4383ee1` / n=18 — same stamp |

**Live book: $982.50 equity, +$147.92.** No halt flag, no `flatten_incomplete`,
no stale write on either row. `fleet_risk` **green on both cohorts** (live 2 of
20 longs, shadow 10 of 26); respiration SpO₂ **1.00 healthy**; brain v3 run 884,
healthy, zero urgent.

**Live execution is running slightly in our favour** — `impl-shortfall` measures
live slip at **−1.46 bps** over 77 measurable orders against shadow's +3.12 bps.
(Its headline verdict reads `xp-contaminated`, which is the judge's experiment
running, not a fault.)

**avo's +$4.10 since the 19:39 brief is mark-to-market, not a close.** She has
had no realised close in **43.2h**; her two open legs are MON and PUMP, gross
$335 on $413 equity = **0.81× leverage** against a configured `gross_x` of 2.0.
Headroom reads `unliquidatable` and `stop_reachable: true`. Nothing to do.

**mum is flat, and that is her gate working.** `scan`: universe 104,
`rsi_med 54.9 · rsi_min 35.2` against a **36.0** bar, `no_signal 103 ·
uptrend_blocked 1`. The regime oracle reads **risk-on uptrend, 11 long / 0
short** — she is a dip buyer and there are no dips. This is (ya)'s finding,
unchanged. Her `rsi_bar: 36.0` on the row confirms **(yc)'s revert is live**.

*Note on one number:* the 21:08 brief showed mum "+$1.98 today" from the row's
`pnl_daily` field; `periods.json` — the source this brief is specified to use —
books her UTC-6-Sep realised at **−$5.26**. Her last close was **7.9h ago**, so
both readings describe a settled day, not a moving one; the −$5.26 is the
realised-ledger figure and follows **+$29.92 on 5 Sep**.

### The one immune flag — still watch, don't act, and here is the field to watch

`fleet-immune` lists exactly one sickness:

> `freqtrade-mum-lighter` — *protective stop is DEAD at gross 5.0 (ceiling 4.17)*

She holds **zero positions** (`margin.n: 0`, `headroom.reason: "flat"`,
`stop_reachable_held: null`), so the ceiling is computed off the **universe-worst
maintenance margin** (0.20) rather than a basket anybody holds — a trigger
satisfied structurally, not measured (I7). `mmf_clip` is **on** (`ref: 0.06`).

**It becomes a real question the moment she opens.** The field that answers it is
`extra.leverage.stop_reachable_held`, which is `null` today and will be a boolean
on her first fill. If it comes back `false`, that is a live finding, not a
universe artefact.

---

## 2. Fleet — +$36.64 today on 67 trades

**Fleet P&L +$312.74** (14 shadow books +$164.82 · live +$147.92).
**7-day realised +$229.58.**

| today (UTC 6 Sep, partial) | |
|---|---|
| 🎫 ticket-taker | **+45.47** |
| ⚖️ counterweight | +4.28 |
| 🪁 kelly | +2.46 |
| 🔮 georgia | +2.35 |
| 🏛️ albanese | +1.75 |
| 👩 mum shadow | +1.41 |
| 🔮 georgia-v3 | +0.17 |
| 🏛️ turnbull | −0.67 |
| 👩 **mum LIVE** | **−5.26** |
| 📦 bezos | **−15.31** |

Yesterday: **+$70.97** on 130 trades (taker +35.72, mum LIVE +29.92).
📦 bezos is the only book losing consistently across both days (−15.31, −7.16).

---

## 3. Claims — six books have one, ten do not

`fleet-allocation` (advisory; moves nothing) ranks on the era lower bound
`max(0, mean − t·SE)`:

| book | claim (era) | n era | mean %/trade |
|---|---|---|---|
| 🙏 avo shadow | **0.0082** | 29 | +1.76% |
| 🎫 taker | **0.0064** | 181 | +0.89% |
| 🙏 avo LIVE | **0.0044** | 14 | +2.36% |
| 👩 mum shadow | 0.0026 | 89 | +0.81% |
| 👩 mum LIVE | 0.0014 | 89 | +0.40% |
| 🌾 carry | 0.0013 | 26 | +0.30% |

Ten books hold **no** claim, including 🪁 kelly (n=565, mean −0.16%), ⚖️
counterweight (n=153, −1.32%), 🎯 sniper (−0.73%) and 📦 bezos (−0.87%).
**Today's biggest winner is also the fleet's best-claimed book** — for once the
day's leader is not luck.

---

## 4. Gate distance — 🎫 the taker is READY, 🙏 avo shadow is one close away

`golive-readiness` (published 6 h ago, inside its 12 h TTL):

* **🎫 `lighter-ticket-taker-lshadow` — 6 of 6 bars, `ready: true`, `fails: []`.**
  Era n=177, **t=2.60**, halves **+25.74 / +119.38**, window 36.7d, MTM maxDD
  **5.42%** against the 15% bar. All-time n=273, t=2.68, and cluster-robust
  **t=2.66 on n_eff 268** — so this is not batching. The only READY book in the
  fleet.
* **🙏 `freqtrade-avo-maria-lshadow` — 5 of 6. The single failure is `n 29 < 30`.**
  t=2.46, halves +5.43/+19.12, maxDD 1.48%. Horizon **1.8 days (ETA 7 Sep)**.
  One close.
* 👩 mum shadow — 5 of 6, fails only `window 11.8d < 30d`; t=2.63, both halves
  positive. On track, ~18 days.
* 👩 mum LIVE — 3 of 6: `window 8.6d`, `t 1.97` (just under), and **`halves
  +76.45 / −5.16` — her second half is negative.** Worth naming: she wins 74.2%
  but her payoff is **0.514** (avg win $3.36 vs avg loss $6.54), so her
  break-even hit rate is 66.1% and her margin over it is only **7.3pp**. A
  high-win-rate book with thin headroom (I15).
* 🙏 avo LIVE — 3 of 6; horizon says **~99 days at her measured 0.27 closes/day**,
  beyond the 90-day window. Flagged, not actioned: her shadow twin decides first.

**Nothing moved backwards yesterday.** The taker held READY; avo's shadow went
from needing 2 closes to needing 1.

---

## 5. One growth item — the judge's mum lane is inert **by a name**, and its
## current experiment expires in ~2 hours

**Class: correctness. This is the fleet's only path to a promoted real-money
improvement on 👩 mum, and it is not running.**

The experiment judge reports `phase: running`, `serial_lane: mum`, current
candidate **`mum-vel-12-20`**, with both levers open on the bus since 5 Sep
03:06 UTC and **expiring 6 Sep 13:21 UTC = 23:21 AEST tonight**:

```
xp.mum.vel_lo = 12.0    xp.mum.vel_hi = 20.0    set_by: experiment-judge
```

**The shadow arm is not running them.** Her own row publishes the band it is
actually enforcing:

```
"vel_band": [-999.0, 999.0]   "vel_read": 105   "vel_in_band": 105   "vel_blocked": 0
```

That is the env default, untouched, blocking 0 of 105 reads — after ~34 hours.

**The mechanism, confirmed in the code, not inferred:**

`lighter_family_bot.py:3239` builds the lever prefix from the row id:

```python
apply_book_levers(b.s, f"xp.{b.bot_id.split('-', 1)[-1]}.")
```

`bot_id` is `strat.bot + "-lshadow"` (`:2506`) and mum's strategy is constructed
as `OversoldRebound("freqtrade-mum", ...)` (`:1941`). So:

```
"freqtrade-mum-lshadow".split("-", 1)[-1]  ->  "mum-lshadow"
prefix -> "xp.mum-lshadow."      the judge writes -> "xp.mum."
```

`fleet_tuning.LEVERS` has no `xp.mum-lshadow.*` entry, and `get_lever` returns
the caller's default for an unregistered name at `fleet_tuning.py:1152`. Checked
directly: `LEVERS.get("xp.mum-lshadow.vel_lo") -> None`. **The `lighter-xp` lane
is enabled**, so the lane switch is not the cause — the *name* is. The live arm
is unaffected: `lighter_avo_live_bot.py:2772` uses `live.{_PFX.lower()}.` =
`live.mum.`, which resolves.

**Four things corroborate it, and one of them is the judge's own verdict:**
1. `audit_code_currency`: **every container CURRENT at HEAD** — the arm carries
   the (xl) velocity code, it just never receives the values.
2. The last verdict, 4 Sep, on the previous candidate `mum-rsi-32`:
   **`VOIDED-NEVER-APPLIED`** — *"0/4 shadow closes carry a receipt … the arm is
   not running this experiment."* Same lane, same mechanism, 37.7h burned.
3. `pairs.mum` reads `phase: "idle"`, `candidate: null` — *"no candidate in this
   pair's queue (`xp.mum.*`)"* — while the top-level judge says `running`. The
   pair cannot see its own experiment.
4. `tests/autonomy/test_mum_judge_lane.py:99` calls
   `apply_book_levers(s, "xp.mum.")` with the correct prefix **hard-coded**, so
   it exercises the function and never the call site. It stays green while the
   caller passes a different string — a substring test, not a wiring test.

**Why it matters in money terms, stated honestly:** this buys no edge by itself.
What it buys is that mum's judge lane can *produce* evidence at all. Her pair is
otherwise healthy and well-powered — live 5.79 closes/day, shadow 6.36, MDE
1.21pp against a 0.5pp margin, stamps 30/30 on both arms — and its
`eta_judgeable` is **13 Sep**. Every candidate that runs under the malformed
prefix voids after ~34h and consumes that window for nothing. Two have now.

**The fix is one string** (derive the prefix from the strategy's own bare name
rather than by splitting the suffixed row id), plus a test asserting the **call
site's** prefix rather than the function's. Shadow-lane, no real-money surface
touched. **I propose it; I have executed nothing** — this brief is read-only.

**Timing:** the levers expire **23:21 AEST tonight**. Nothing breaks if they
lapse — the arm is already at defaults — but the judge will record a third void.

### What I checked and did NOT propose

* **Acting on the mum stop-dead page** — refused above: she is flat, the read is
  the universe-worst tier, `mmf_clip` is on.
* **Anything aimed at 🙏 avo** — she is one close from the gate. Leave her alone.
* **A 📦 bezos retirement** — `underpowered` at n=32, ETA 2.4 days. Retiring on a
  thin sample is what I17-as-amended forbids. Re-read when decidable.
* **A ⚖️ counterweight call** — its pre-registered read is 1 Oct or n≥60 fresh
  on-class closes, whichever first. Not due.
* **Anything on 🎫 the taker's READY status** — a promotion here means re-arming
  a real-money row retired 13 Aug on its own record. That is Eamon's act alone,
  and its bracket levers have moved inside the graded window ((hm): freeze the
  bars first).

---

*14 shadow books are $1,000 paper each; 🙏 avo-maria and 👩 mum are real money.
This brief proposes and never executes — go-live and every real-money change
stay Eamon's explicit act. Not financial advice.*
