# Fleet daily — 20 Aug 2026 (Thu), 08:30 AEST

**Real money is safe.** Both live rows online, fresh, unhalted, low leverage, no
liquidation proximity. **The day's finding is not a P&L one:** 👩 mum was retired
16.6 hours ago and **is still trading** — it booked **+$14.12 today** and is the
only one of 40 `RETIRED_ROWS` still served by the feed.

Feed: `generated_at` 2026-08-19T22:30:39Z (= 20-Aug 08:30 AEST), `feed_stale`
false, **0 of 19 rows stale**, freshest 5s. `/watchdog.json` and `/trades.json`
could **not** be fetched this run (DNS failures from the runner, 5 attempts) —
recorded, not worked around; two checks below are open because of it.

---

## 1 · Live money (real)

| Row | Equity | P&L | Day (UTC 19-Aug) | Open | Lev | Age | Status |
|---|---|---|---|---|---|---|---|
| 💸 Funding Farmer `perps-funding-lighter-lighter` | **$195.53** | +$4.01 | +$0.08 | 3 (BTC/SOL/XAU, all short) | 0.47× | 172s | online |
| 🙏 Avo Maria `freqtrade-avo-maria-lighter` | **$63.09** | +$0.30 | +$0.32 | 3 (QQQ/SPY/NVDA, all long) | 0.37× | 118s | online |

Combined live equity **$258.62**, live P&L **+$4.31**. Live pair verified against
`meta.live_fleet.live_bots` — matches the task file.

- **Nothing stale, nothing halted, no outsized loss.** Farmer's nearest liquidation
  is XAU at **6.06× away** (`dist_frac`); Avo reports `liq_unknown` on all three
  equity legs, which is its normal state for non-crypto perps, not a fault.
- Farmer running `cap_usd` 150 / `clip_usd` 30 / `max_open` 5, gate `enter_apr`
  0.05, `capital_adjust` 130.39. Avo at `cap_usd` 200 / `clip_usd` 15.77, sides
  long-only, 3 coins under `coin_veto` (XLM, DOGE, NEAR — all ≥50% 30d stop rate).
- Organs quiet: immune `sick: []`, **no quarantined levers**, risk light **green**
  (`mode: enforce`, `clip_scale` 1.0, 7d fleet DD 0.0), respiration SpO₂ 1.0.
  Implementation shortfall reads `xp-contaminated` (n_overlap 5) — a judge-arm
  artifact, not a slip signal; slip streak 0.

---

## 2 · Fleet day — UTC 19-Aug (= Sydney 19-Aug 10:00 → 20-Aug 08:30)

**Realized −$14.06 across 97 trades** — and **97 trades is ~3.7× the prior day's
26**, which is itself the story (see §5).

| Book | Day | Note |
|---|---|---|
| 🧘 book-douglas | **−15.76** | 21 closes, t −2.21; the day's worst line |
| 👩 **freqtrade-mum** | **+15.62** | **RETIRED 19-Aug — should not have traded at all** |
| 🔮 freqtrade-georgia | +11.10 | the fleet's best-placed book (§4) |
| 🎫 lighter-ticket-taker | −10.28 | shadow arm |
| 🛢️ band-garrett | −6.46 | |
| 🧭 nav-cook | −5.71 | 24 closes on a gate 3.3× loose (§5) |
| 🪁 band-kelly | −4.12 | 9 closes, first full day |
| 🎯 perp-sniper | +3.02 | |
| others (6) | −$2.6 combined | Farmer +0.08, Avo +0.32, turnbull +0.17 |

**Ex-mum the fleet day is −$29.68.** The headline −$14.06 is flattered by
$15.62 booked by a book the fleet retired yesterday.

---

## 3 · Claims (I16) — who actually has evidence

`fleet_allocation`, sample **`all-time-pooled`**, 19 books. Only **3 of 19** carry
a lower bound > 0:

| Book | n | mean %/trade | claim (lower bound) | target |
|---|---|---|---|---|
| 🌾 perps-funding-carry | 101 | +0.254 | **0.1492%** | $11,741 |
| 🔮 freqtrade-georgia | 138 | +0.162 | 0.0229% | $2,016 |
| 💸 Funding Farmer (live) | 133 | +0.271 | 0.0129% | $1,243 |

**The number that matters more: `n_with_era_claim` is 0 in BOTH classes.** Every
claim above is all-time-pooled; not one book holds a claim on its *current* policy
era. Carry's is the sharpest case — pooled n=101 gives the fleet's largest claim,
while its **in-era sample is n=10 at t=−4.48** and the go-live grader calls its
horizon `unreachable`. Read the carry claim as history, not as a live edge.

Yesterday's biggest movers were all **zero-claim** books (douglas, mum, taker,
garrett, nav-cook, kelly). Nothing in §2 is evidence in either direction.

---

## 4 · Gate distance — the forward metric

`golive_readiness`, updated 22:24Z, **`ready: []`** (no book at the gate).

| Book | bars | binding | horizon |
|---|---|---|---|
| 🔮 **freqtrade-georgia** | **5 / 6** | **t 1.50 < 2.0** — the only failing bar | `on_track` |
| 🙏 avo-maria (shadow) | 3 / 6 | window 23.7d < 30d, n 14 < 30 | `on_track` |
| 💸 Farmer (live) | 3 / 6 | window 26.6d < 30d, t 0.31 | `undecidable` |

**Georgia is the fleet's nearest book and moved the right way yesterday** (+$11.10,
n_era 126, mean +0.172%, maxDD 0.60%). It needs `t` alone. Avo's shadow arm clears
its window ~26-Aug and needs 16 more closes.

Decision docket (valid, 7d): **⚖️ Counterweight** and **🎯 perp-sniper** both
`unreachable`, stuck ≥13.4d, asking keep-or-retire — operator calls, unchanged
since 6-Aug. Counterweight's own pre-registered review date is ~28-Aug.

---

## 5 · ONE GROWTH ITEM — **a retirement that reached neither consumer**

**Correctness. Owner: operator (a deploy act; this brief is read-only).**

👩 mum was retired at **19-Aug 15:52 AEST** (commit `6b1f938`, (rd)) with all
three halves in main — `RETIRED_BOOKS` guard, `RETIRED_ROWS` hide, `LEGACY_BOTS`
prune, pinned by `tests/autonomy/test_mum_retired.py`. **16.6 hours later neither
consumer is enforcing it**, and both were the *point* of the commit:

- **The writer is still trading it.** `family-lighter-shadow` publishes
  `freqtrade-mum-lshadow` fresh (age 5s) with a *live scan census* —
  `scanned 23 / no_signal 23`. A book dropped by `live_strategies()` publishes
  nothing at all, so mum is still in the running roster. Its **4 open positions
  did not FREEZE as (rd) declared** — closes went 3 → 7, booking **+$15.62**
  (last: NVDA, 22:11:49Z). Container stamp `644317c0a910/15` is **UNRESOLVED by
  `audit_code_currency` even at `--depth 900`** — no answer, per its own contract.
- **The reader is still serving it.** Verified programmatically: of the **40**
  entries in `RETIRED_ROWS` at HEAD, **exactly one leaks into live `/pnl.json`** —
  mum. Every earlier retirement (barnes, dad, the four PMs, intraday, swing-daily,
  breakout-4h, schwager, index-rider, tide-rider, snap-back) is correctly absent.

**These two facts do not fit one stale checkout, which is why this needs a stamp
readback rather than a guess.** `6b1f938` is an **ancestor** of `73bb764` (the (rj)
nav-cook registrations), the reader plainly *has* `73bb764` — nav-cook's row is
visible, and `73bb764`'s own tree contains the mum entry — so a single checkout
cannot show nav-cook *and* mum. I could not close this from outside: the
`/watchdog.json` boot stamp, which (rn) built the hourly reader-currency check
around, was unreachable from this runner all five attempts.

**What it costs:** mum is $1,000 of paper — no real money at risk. The cost is
**evidence integrity**: a retired book's closes keep entering the ledger, its
+$15.62 flatters the fleet's daily line by 111% of the reported loss (§2), and the
freeze precedent set by (mr)/(nf)/(lo) was silently not honoured.

**Suggested operator action:** redeploy `family-lighter-shadow` and `pnl-dashboard`,
then verify by the `extra.build` + `extra.build_n` readback on the rows — never by
a green run ((ml)). Confirm `MUM_RETIRED_OVERRIDE` is unset on the family service
while there (it would revert the writer but *not* the row hide, so it does not
explain what is observed).

### Also carried, smaller, and probably just in flight

🧭 **nav-cook is `BEHIND-OWN` by 1 commit** — `374f3d0`, this morning's (ro) fix
for the confirm window that shipped **3.3× looser** than the gate it was graded on.
Pushed 07:49 AEST, 41 minutes before this snapshot, so the auto-deploy may simply
not have landed yet. Until it does the book keeps entering on a 180s confirm where
its study required 600s — that is what produced yesterday's 24 closes at a 5.5-minute
median hold, 24 of 24 `converged`, against a 4h horizon that has never been reached.
**Verify by stamp** (`ddc83aa0120e/14` = pre-fix); if it has not moved by mid-morning
it is the same class as the mum item above, not a slow deploy.

---

*Shadow books are $1,000 paper each. The two live rows are real money. Go-live and
every real-money change stay explicit operator acts — this brief proposes, never
executes. Not financial advice.*
