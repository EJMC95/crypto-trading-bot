# Evidence Review — 2026-08-17

_Reviewed 2026-08-17 07:40 AEST (Sydney) · 2026-08-16T21:40:14+00:00 UTC._

## Verdicts

| Key | Status | Why |
|-----|--------|-----|
| census:50 | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:0G | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:APEX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:AVAX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:BIO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:CHIP | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:EIGEN | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:GMX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:KAITO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:MU | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:NEAR | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:RESOLV | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKHYNIXUSD | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKY | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SNDK | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STABLE | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STBL | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:WTI | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:ZORA | stale | dislocation census publisher `lighter-dislocation-lshadow` is 286.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| factor-sample:18 | active | joined decision+context dataset at 555 closes (51% win), bucket 18 |
| veto:NEAR,SNDK | resolved | NEAR,SNDK no longer vetoed |
| veto:SNDK | resolved | SNDK no longer vetoed |

## New evidence

- 🎫 shadow lens 'short-divergence' at n=120 (≥10): net $+0.79, WR 35%, t=0.05 — noise
- 🎫 shadow lens 'long-breakoutup' at n=36 (≥10): net $+8.18, WR 42%, t=0.69 — noise
- 🎫 shadow lens 'long-divergence' at n=20 (≥10): net $+2.44, WR 45%, t=0.31 — noise
- 🎫 shadow lens 'long-dip' at n=13 (≥10): net $-9.15, WR 15%, t=-2.74 — significant
- 🎫 shadow lens 'long-breakout' at n=10 (≥10): net $+7.72, WR 80%, t=1.73 — noise
- 💰 LIVE perps-funding-lighter-lighter: n=124, net $+7.23 — by lens [('short', 117, 6.75), ('long', 7, 0.48)]
- 🚦 go-live gates (CANONICAL grader golive_readiness — bars: window, closes, mean, t, halves, maxdd; win rate reported, NOT a bar; retired, already-live and live-twin rows excluded): NO new candidate
- 🔭 gate horizon (computed at trajectory, (ks)): no projectable candidate · undecidable@trend: freqtrade-georgia-lshadow, pm-albanese-lshadow, pm-turnbull-lshadow; unreachable@trend: band-barnes-lshadow, band-garrett-lshadow, lighter-perp-sniper-lshadow, lighter-ticket-taker-lshadow, perps-funding-carry-lshadow, perps-funding-spread-lshadow
- 🚦 fleet-risk light green — longs 11/20, shorts 2/12 (gross 13); 7d DD -0.13%, clip_scale 1.0
- 📏 Farmer live-vs-shadow per-trade gap +0.017pp (live -0.162% n=51, shadow -0.179% n=78) — no divergence
- 🧬 Farmer arms AGREE: live e981e11820fc vs shadow e981e11820fc (n=16)
- 🧬 Avo arms differ on FILE SET, not necessarily code: live 4d5c491c01ef (n=17) vs shadow 082797c61510 (n=15) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-avo-maria-lshadow differs from the repo on FILE SET, not necessarily code: container 082797c61510 (n=15) vs repo 39f464133444 (n=16) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 freqtrade-avo-maria-lighter stamp differs from the repo tree: container 4d5c491c01ef vs repo 6ad858544386 (both n=17, so not a file-set difference) — UNCLASSIFIED. A shared-module change moves every image's stamp without changing any bot's logic, so this is NOT yet a finding: run `scripts/audit_code_currency.py`, whose own-entry-file verdict is the only one that means the container is stale [REAL MONEY row]
- 🧬 perps-funding-lighter-lighter differs from the repo on FILE SET, not necessarily code: container e981e11820fc (n=16) vs repo 9015cce0be29 (n=17) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 perps-funding-lighter-lshadow differs from the repo on FILE SET, not necessarily code: container e981e11820fc (n=16) vs repo 9015cce0be29 (n=17) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy

## Summary

22 alert keys reviewed: 1 active, 2 resolved, 19 stale. No divergence and no drawdown-governor trigger. 16 new-evidence items scanned.

---

# HUMAN LAYER — 17-Aug, 08:07 AEST (Sydney)

Mechanical run clean: `payload.errors` empty, all 22 alert keys verified, every
organ key fresh against its own TTL except the two declared-dead ones
(`gapscout-census`, `listing-intel` — both retired publishers, failing safe).
`test_review_currency.py` green, so this review graded against the live standard.

## ⚠️ ACTION — two things need you

### 1 · CI HAS BEEN RED ON MAIN SINCE 16-Aug 07:31Z — I FIXED IT, IT NEEDS A PUSH

Yesterday's report flagged red CI on the coverage floor; that one cleared, and
main went red again **14 minutes later** on a different cause and stayed red
through HEAD. Nobody's green/red signal has meant anything since.

    FAILED test_enforced_audit_guard[scripts/audit_boot_stagger.py]
      KeyError: 'tolerance_src'

A **hard crash**, not a verdict, in an `ENFORCED_AUDITS` member — so it gates
every push. And it is invisible on a laptop: the tolerance was assigned *below*
`assess()`'s `if not gaps` early return, and `gaps` is only empty when the
deploy-cadence lookup returns nothing. Your `gh` has full scope and returns ~58
deploys; **CI's `GITHUB_TOKEN` has no `actions: read`**, so in CI every row took
the early return and lost the key. The crashing block is the one `(pe)` added to
*declare which regime produced the verdicts*.

**Fixed and committed as `(pg)`** — tolerance hoisted above the cadence check so
every row carries it by construction; 2 regression tests that fail on the exact
pre-fix code **and nothing else**. Full suite green in the CI regime
(`env -u DATABASE_URL`).

> **RESOLVED — pushed on the operator's instruction, and CI is GREEN.**
> `11a082f` (pg) ran **success**, ending the ~24h red streak. `(pf)` and `(pg)`
> reached the remote via a concurrent session's push from the shared worktree
> (the shared-tree hazard, benign this time); `(ph)`/`(pi)` pushed directly as
> `3603ef6`. Both `(pf)` books verified in the LIVE payload: 🌾 carry
> `build=6adee0dea8fb` and 🎸 Barnes `build=202ea6058575` now publish
> `caps.crypto_only: True`.

### 2 · TWO `unreachable` VERDICTS ARE MEASURING A POLICY THE BOOKS NO LONGER RUN

The go-live table hands you this:

| book | era grade | horizon |
|---|---|---|
| 🌾 `perps-funding-carry-lshadow` | n=10, **t=−4.48**, −$15.45 | `unreachable` |
| 🎸 `band-barnes-lshadow` (carry sleeve) | n=9, **t=−4.47**, −$1.45 | `unreachable` |

Split by instrument class, reproducing the grader's own sample exactly
(n=10 and n=9 matched before I believed the split):

```
🌾 carry   CRYPTO      n=1   −$0.49  (KAITO)
           NON-CRYPTO  n=9  −$14.96  SKHYNIXUSD/SPCX/WTI   t=−3.78
🎸 Barnes  CRYPTO      n=0
           NON-CRYPTO  n=9   −$1.45  SKHYNIXUSD/SPCX/WTI   t=−4.21
```

**9 of 10, and 9 of 9.** `(lk)`/`(lv)` gave both books a crypto-only screen on
13-Aug. Neither has opened a position since, so the graded sample is frozen on
the removed population and *cannot* update. **These are not "losing books" — they
are starved books whose last recorded trades were taken under a rule you already
deleted.** Do not read `unreachable` here as evidence to retire on.

**The decision this actually surfaces is the one yesterday escalated** (three
books on one cell), not a per-book retirement. See OPTIONS below.

I shipped the *measurement* half as `(pf)`: 🌾 carry and 🎸 Barnes now publish
`caps.crypto_only`, so this split is derivable from the payload instead of by
hand. Publish-only, no trade changes, main-only under `(mm)`.

## What I did this run

| | |
|---|---|
| `(pf)` | 🌾 carry + 🎸 Barnes publish `caps.crypto_only`. New guard `test_class_screen_declared.py` — finds the population by AST (module-level `ALLOW_NONCRYPTO`), so a **new** book with a screen is covered the day it lands; requires the value to be *derived* from that switch; reads dict keys by AST not substring. **6/6 mutations red.** 🎯 sniper declared in `PARTIAL_SCREEN_OK` (its listing source is deliberately unscreened, so `crypto_only: true` would be a false declaration). |
| `(pg)` | The CI crash above. **2 regression tests, faithful-revert verified.** |

**Why the class, not just two edits:** every book *born* with the screen (🧮 Hull,
🏦 Rich Dad, 🧘 Douglas, 📐 Grimes) publishes `caps.crypto_only`; every book that
had it *retrofitted* published nothing. `(lz)` had already added `min_vol` to
both books for exactly this reason — *"unpublished, the collision is
undetectable"* — and stopped one field short of the other half of the same gate.

**What I did NOT do, because it is the tempting move:** neither book's era moved.
CLAUDE.md names *"a widened universe"* as ordinary tuning that does not reset the
clock, and a narrowing is the same act mirrored. The blessed precedent is
`(jg)`/`(ki)` on ⚖️ Counterweight — **report** the class split beside the pooled
grade, don't re-cut the sample.

## Yesterday's work, graded against today's data

| `(nk)` sleeve-aware grading | ✅ **holds.** 🎸 Barnes sits at `below floor n9`, every other book byte-identical. |
| `(nf)` xsect wind-down | ✅ **RESOLVED** — yesterday's dated check ("if Barnes is not at 0 xsect positions after 05:00Z, the wind-down is broken"). Ten xsect legs closed 16-Aug 04:59Z; `positions: {}`. Carry sleeve's clock unaffected, as designed. |
| `(om)` Grimes fixed-universe gate | ✅ **did exactly what it claimed, and the direction is the honest one.** Under the fixed set the gate **re-closed**: keltner `t +0.53 → −0.37` (n 99→129), failtest −1.43, pullback −0.79 — all three `open: false`. `gate_drift` reads **`ungraded: []`** (graded 18 / traded 8 / overlap 8), so the hazard it published is currently benign — the book cannot enter a coin its gate never tested. |
| `(oy)` allocation payload | ✅ **working, and it is load-bearing today.** 🌾 carry ranks #1 on the all-time pooled ledger (`target_usd 10,466`) but `n_era=10 < MIN_N`, so `claim_era` is `None` and **`scale_effective` is 1.0** — the `(lx)` era gate declined the expansion. The headline number reaches no consumer. |
| `(os)`/`(ou)`/`(pa)`/`(pe)` boot stagger | ✅ organ-side: every organ key fresh vs its TTL, `golive-readiness` 4.1h against a 12h TTL. ❌ audit-side: see ACTION 1. |
| `(nl)` funding census | ✅ and it is what answers today's supply question from the payload. |

**One number from yesterday I could not reproduce, and it changes an option.**
Yesterday put the carry cell at **29.8% occupied** and flagged the difference
against CLAUDE.md's 6.6%. The canonical detector says **5.93%** (9,662 snapshots,
33.7 days) — close to CLAUDE.md, not to yesterday. The cause is the **6h
persistence gate**, which these books actually require:

```
carry cell (≥20% TRUE, ≥$2M, crypto)   persist 6h:  5.9% occupied, median 0, max 2, 3 coins
                                       persist 0h: 17.3% occupied, median 0, max 5, 14 coins
```

Yesterday's thin-tier proposal was sized on the no-persistence basis. Re-measured
with persistence applied (same tape, same construction):

| cell | occupied | median | max | distinct |
|---|---|---|---|---|
| carry `≥20%, ≥$2M` — **3 books** | **5.9%** | 0 | 2 | 3 |
| thin tier `≥5%, $0.1–2M` — 🛢️ alone | 53.3% | 10 | 30 | 75 |
| thin sub-band `[5%,20%)` | 53.3% | 10 | 27 | 73 |
| thin sub-band `≥20%` | **28.7%** | **0** | 4 | 24 |

So the ≥20% thin sub-band is **~5× better supplied than the carry cell — a real
improvement — but its median is 0, not the ~4 that "98.6% occupied" implied.**
I could not reproduce 98.6% on any basis I tried; different window/definition.
The *direction* is robust on both bases; the *magnitude* is not. **Moving 🏦 Rich
Dad there would not make it quickly decidable.**

## Carried priorities

1. **Long-budget Steps 2/3** — untouched. Not binding today: longs 11/20, shorts
   2/12, gross 13. No book is being refused a slot by the budget.
2. ~~🧲 Snap Back keep-or-retire~~ — **DROP.** Retired `(jh)`; row hidden + pruned,
   clean in the integrity table both days. Seen clean twice as required.
3. **🌾 carry keep-or-wait** — now has a much better-evidenced footing (ACTION 2).
   Still a late-Aug operator call; still do not tune it.
4. ~~⚖️ Counterweight early revert~~ — **DROP.** Post-revert sample is accruing
   cleanly: 24 closes since 4-Aug, and its crypto-only subset reads **n=20,
   +$0.95, +0.312%/trade**. The `(jg)`/`(ki)` decomposition still reproduces:
   non-crypto n=21 carries **−$36.48** of a −$31.16 book while 70 crypto legs
   read **+$5.32**. Pre-registered ~28-Aug decision stands, unchanged.
5. **Allocation re-weight** — still open, still yours. Claims table below.
6. **MTM re-grade under `(ia)`/`(iz)`** — maxDD is computing for every book
   (🌾 1.6%, ⚖️ 4.1%, 🎫 2.1%), so the reader is alive. No book is near the 15%
   bar; nothing to decide yet.

## Fleet state

- **Every row fresh** (max age 0.08h). **Ledger integrity:** one finding, 🌾 carry,
  7 historical overlaps — **most recent 446h ago**, nothing to stop, permanent.
- **Code currency: clean.** No `BEHIND-OWN`. Three `DEFERRED` (both Farmer arms +
  Avo live) are marker-gated and working as designed.
- **Live money:** 💸 Farmer $197.36 (+$5.84, 1 open XAU short); 🙏 Avo LIVE $62.73,
  4 longs ≈ 100% of collateral, **0 closes in 4 days** — early, nothing to act on.
  Live-vs-shadow gap **+0.017pp** (live −0.162% n=51 vs shadow −0.179% n=78): no
  divergence, and the sign favours live.
- **Go-live: READY none.** Zero projectable candidates — every graded book is
  `undecidable` or `unreachable`.
- **Allocation claims:** only 3 books have any claim at all (🌾 0.0015, 💸 live
  0.00025, 💸 shadow 0.00023); **15 of 18 sit at the 25% probe floor.** The
  directional cohort still measures zero claims, as it has since 1-Aug.
- **The whole ≥20% carry cohort holds nothing**, and their censuses agree on why:
  `scanned 225, cold ~204, thin ~21, eligible 0, noncrypto 0`. **The binding gate
  is the 20% APR bar** — not volume, not the class screen (which sees nothing
  because the APR gate runs first).

## OPTIONS TO OPTIMISE

**Nothing lever-shaped is on offer today, and that is a finding rather than an
omission.** What I checked and why each is refused:

1. **Push the two commits.** The only free win on the table. `(pg)` restores CI
   as a signal for every session; `(pf)` makes the ACTION-2 split readable from
   the payload. Expectancy cost: **none** — neither changes a trade.

2. **Consolidate the ≥20% carry cell — 3 books on 3 coins. YOUR CALL, and the
   fleet's largest structural inefficiency.** Measured, 33.7 days: the cell is
   occupied **5.9%** of the time, max **2** coins at once, **3 distinct coins**
   (KAITO 483 snapshots, XMR 71, PAXG 38). 🌾 carry, 🎸 Barnes and 🏦 Rich Dad all
   gate there; all three hold nothing; two are being graded `unreachable` on
   trades they can no longer take, and 🏦 has **0 closes in 4 days**. Meanwhile
   🛢️ Garrett covers a tier occupied **53.3%** with a median of **10** candidates
   — alone, capped at 6, and refusing a real high-APR tail.
   *Expectancy cost of doing nothing:* three books' worth of rows, clocks and
   attention producing ~0 evidence. *Cost of acting:* re-banding resets a book's
   `(hm)` clock, and 🎸 is birth-frozen to 4-Sep. **The trap yesterday named still
   binds: a book at `≥20%` in the thin tier without narrowing 🛢️ to `[5%,20%)`
   is a strict subset running second — the I18 failure that killed 🎸's `extreme`
   sleeve. Both edges move in one commit or neither.** And size it on the
   persistence-applied numbers above (median 0), not yesterday's.

3. **Lower the 20% APR bar — REFUSED, and it is an identity, not a preference.**
   It is the one lever that would unstarve three books at once. The bar is
   *derived* from unit economics: a ~30bps round trip must be repaid inside the
   hold, which is what puts 🏦 Rich Dad's payback-velocity bar at ~21.9% and
   carry's at 20%. Below it the funding does not repay the round trip. Already
   measured and refused 3-Aug (20%→10% needs the rate to hold 254 of 336h to
   break even). **Turnover bought with expectancy — I19.**

4. **Capacity: no headroom anywhere.** The only book at cap is ⚖️ Counterweight
   (10 legs = k=5×2), and it is at **−$30.99 MTM** — `(hs)`/I7 requires a
   capacity widening to read MTM P&L and **fail closed**, so it is refused. Every
   other book is starved, not capped: 🌾 0/12, 🎸 0/4, 🏦 0/6, 🎯 0/4, 📐 0/2,
   🧮 2/4. **Adding slots to books with no candidates buys nothing.**

5. **🛢️ Garrett's cap (6, with 27 eligible refused) — still refused, unchanged
   from yesterday, and the reason is unchanged: no claim (n=10, `claim = 0.0`).**
   Its census settles it at n≥30, ~27-Aug. Re-proposing it today would be the
   circle this job exists to break.

6. **📐 Grimes — watch, do not touch.** All three setups now closed on the fixed
   universe. `(om)` explicitly declared that a gate staying shut is a
   keep-or-retire call and *never* a bar-lowering session. It is **one day** into
   the fixed-universe regime; the honest action is to let it run.

**Dated checks:** ⚖️ Counterweight ~28-Aug · 🛢️ Garrett n≥30 ~27-Aug (and the cap
question with it) · 🌾 carry late-Aug · 🏦 Rich Dad gradeable ~12-Sep · 🎸 Barnes
birth-freeze lifts 4-Sep.

## Noticed — and now FIXED (operator: "fix all of the above")

- **`audit_boot_stagger` depends on live external state** → **investigated, and
  the real finding was worse than the one I filed.** The FAIL condition is
  contract-based, not rate-based, so cadence variation is *intended*. But the
  audit could **never reach a cadence in CI at all**: `gh run list` needs
  `actions: read` (CI's token lacks it) and `git log origin/main` needs history
  (`actions/checkout@v4` defaults to depth 1, detached HEAD). It returned
  "nothing asserted", exit 0 — an `ENFORCED_AUDITS` member structurally unable
  to fail the build it gates. Fixed as **`(pi)`**: `HEAD` fallback in the git
  tier + `fetch-depth: 0` in `tests.yml`. **Measured before enabling** on the
  burstiest window this repo has (60 commits, median gap 2.3 min, min 0.0):
  nothing fails. 3 mutations red.
- **`audit_book_overlap` treats `crypto_only` as a global flag** → fixed as
  **`(ph)`**. Measured at `--gate 0.20 --allow-noncrypto`: it called 🌾 carry,
  🎸 Barnes and 🏦 Rich Dad rivals for a **14-coin** supply their own screens
  let them reach **3** of — a collision quoted 4.7× too deep. Now annotates
  reach per book and qualifies the verdict; three-valued, so an unpublished
  screen prints `class screen UNPUBLISHED` and changes no count (which names
  who is next: 🛢️ Garrett and both 💸 Farmer arms). **This file had no test of
  any kind** despite CLAUDE.md naming it as I20's enforcement —
  `test_book_overlap_gate.py` now covers it, 6 mutations red.

## ✏️ CORRECTION to this report

**The XAU ×2 "REAL MONEY IN THE STACK" item above was WRONG.** I listed it as
"still present, second sighting". It is not: `a9e3267` fixed it on 16-Aug
(`ARM_PAIRS` + `same_book`), and today's run puts XAU under *"same coin on both
arms of ONE book — by design, reported, never a finding"*. I carried it forward
from yesterday's report instead of re-running the detector — the exact
anti-drift failure this review's own rules open with. Running it is what found
the real `(ph)` defect.

The live same-side concentration finding today is **LINK ×3** across three
genuinely different books (💸 Farmer shadow, ⚖️ Counterweight, 🛢️ Garrett). A
test now pins that a third book joining an arm pair is still a finding.

## Still open — and still yours

**Consolidating the ≥20% carry cell (3 books, 3 coins) is NOT fixed and I did
not touch it deliberately.** Re-banding a book changes what you commissioned and
resets its `(hm)` clock; 🎸 Barnes is birth-frozen to 4-Sep. That is a
commissioning decision, not a defect. The measurement is in OPTIONS above, now
with persistence applied — say the word and I'll implement it as one commit
moving both edges together.

## Follow-on — `(pj)`: the last UNPUBLISHED class screens

`(ph)`'s three-valued output named who was left, so I finished the sweep.
💸 Farmer live + shadow and 🛢️ Garrett (all one file) now publish
`crypto_only: **False**` — and checking that value was the job. A first grep
said "no screen"; the module actually threads `is_crypto` into its census,
**counts** non-crypto and leaves it **eligible** ("reporting a class is not
screening it", selftest-pinned). The asymmetry against its three siblings is
declared and was **measured refused** 16-Aug (live n=12, t=−0.44). So these rows
genuinely are rivals for the whole supply, and an UNKNOWN under-informs exactly
as a wrong `True` would overstate.

**It also caught a false positive in `(pf)`'s own guard, hours old.** I claimed
in a comment that future drift here would be caught, then verified rather than
asserted — and it reddened for the *wrong* reason ("publishes no declaration")
while the declaration sat three lines away. This fleet has **two publish
shapes** and `living_gates` reads both; my guard knew only `caps`. Fixed to read
either, both tests now run off the same collector, **7/7 mutations red**.

Live state: 🛢️ Garrett `CURRENT`, `crypto_only=False`, label cleared. The Farmer
**pair** stays `DEFERRED` — `(hi)` deliberately puts both arms on one marker gate
so the judge's control arm can't drift from the live one. Clears on the next
`[deploy-live-farmer]`. Entry corrected in place when I'd written "three rows".

## The carry-cell consolidation — MEASURED AND REFUSED, and what shipped instead

Asked to fix it, I measured the specific design first. **It is a step back, with
a number.**

🛢️ Garrett is a **ranked selector** (`sort` on `|apr|`, cap 6), so an apr ceiling
does not trim its marginal candidates — it removes the ones it picks *first*.
Live, in-band, 40 candidates:

| rank | coin | \|TRUE apr\| |
|---|---|---:|
| 1 | H100 | 2085.8% |
| 2 | KAITO | 145.4% |
| 3 | SKHYNIXUSD | 144.5% |
| 4 | ROBO | 97.2% |
| 5 | FOGO | 63.9% |
| 6 | PENDLE | 53.4% |

**All 6 of the top 6 — its whole cap — are ≥20%.** Capping it there removes
exactly what it takes, every cycle. And this is the fleet's only funding book
filling its slots (6/6 held, 27 eligible refused), being asked to hand its top of
book to 🏦 Rich Dad — **zero closes** — into a sub-band whose median is **0**.
Garrett's own claim is `0.0` on n=10, so the tail can't be argued either way.
That is an unpriced expectancy cost on the one book that works: **I19 refuses
it.**

**The honest restatement:** the cell holds at most 2 coins at once and 3 books
sit in it. That is *how many books*, not *which bands* — an I17 keep-or-retire
call, and yours.

**What shipped instead — `(pl)`.** I20 says "run the supply check before
minting"; it was a command a human had to remember, with the cell typed by hand.
Nobody ran it, which is how three books landed there. `audit_book_overlap` now
derives cells from the books' **own published gates** and reports collisions by
default, no arguments — possible only now that every funding book publishes its
whole gate `(lz)`/`(mh)`/`(pf)`/`(pj)`/`(pk)`. Declared collisions carry the
measurement, a reason and an owner; **an undeclared one exits non-zero, so a
fourth book minted here fails.**

Two defects in my own first run, both found by running it rather than reasoning:
it **fused two tiers over an empty sliver** (carry ∩ Farmer = `≥20%, ≥$10M`,
measured **0 snapshots / 0 coins over 9,693** — reproducing `(ly)` independently),
and it **counted the Farmer's two arms as two books**. Edges are now material,
arms collapsed. 9 mutations red.

## The keep-or-retire call — MADE: 🎸 Barnes retired `(pm)`

`(pl)` established the question was *how many books*, not *which bands*. The
decisive measurement, on the book's own ledger — every episode its living carry
sleeve ever opened, against 🌾 carry's holdings at that instant:

**0 of 9 independent.** Three distinct coins (SKHYNIXUSD, SPCX, WTI), every one
also traded by carry. In its entire life it produced not one observation the
fleet did not already have. That is `(lv)`'s `extreme`-sleeve finding —
*"offered nothing carry hadn't claimed"* — reproduced on the **realised ledger**,
about the sleeve that was actually trading.

Why retire rather than tune: minted as a **three-sleeve super-book whose thesis
was the COMBINATION**, and two sleeves were already retired ((ly), (nf)) — what
remained was 🌾 carry's own gate at a smaller cap (4 vs 12). All 9 closes are the
class removed 13-Aug; nothing opened since; allocation claim 0.0. It was
**birth-frozen until 4-Sep so it could not be tuned even in principle**. And
retiring it **cost zero coverage** — carry covers the identical cell with 3× the
capacity.

Why this book and not another on the cell: 🌾 carry is the one the others copied,
with the largest cap and the only real history. 🏦 Rich Dad is **four days old**
with a genuinely different rule set (effective ~21.9% payback bar, its own
exits) and a ~12-Sep gradeable date — retiring it now would judge a book before
its record starts, the trap `(nu)` names on 🧙 Schwager.

**A third undecidability class.** I17's original is the slow clock (🌊/📊: too few
closes ever). `(oj)`'s is the fat tail (🧙: enough closes, a `t` the distribution
can't support). This one is **redundancy** — normal trading rate, every
observation a duplicate. Its `n` looks like evidence and isn't.

Verified live: the row is **pruned and gone from `bot_pnl`**, the container is
idling, and the cell reads `DECLARED: 2 books share a cell`. 8 mutations red —
including one my own new test caught that review had missed (`FUNDING_BOOKS`
still listed the retired book, which would have kept a corpse contending for
supply). Reversible via `BARNES_RETIRED_OVERRIDE=run`.

**Optional, not done:** stopping the Railway service `band-barnes-shadow` is
tidiness a push resurrects — the code guard is the durable half, per the 🧙
Schwager precedent.

## Final state — all shipped and verified

| | |
|---|---|
| main | `d0643a6`, **CI green** (was red ~24h) |
| `(pf)` | live in containers — 🌾 carry + 🎸 Barnes publish `caps.crypto_only: True` |
| `(pg)` | `11a082f` ran **success** — the red streak broken |
| `(ph)` | verdict no longer overstates; `test_book_overlap_gate.py`, 6 mutations red |
| `(pi)` | CI now reaches a real cadence, **asserted in the same job** rather than reasoned — verified against a real shallow clone + CI-scoped token (`n=0, unavailable` → reds) |
| `(pj)` | Farmer/Garrett publish `crypto_only: False`; the `(pf)` guard now reads both publish shapes, 7/7 mutations red |

| `(pl)` | carry-cell re-band **refused with the measurement**; automatic cell-collision detection shipped, 9 mutations red |

| `(pm)` | 🎸 Barnes **RETIRED** — 0 of 9 episodes independent; both halves + guard + doctrine, 8 mutations red; row pruned live |

main `a64d58f`, CI green on every push. The carry cell is **3 books → 2**, and
the remaining pair is declared with an owner and a ~12-Sep decision point. Every funding book's class screen is now
either **published** or **deliberately deferred behind a marker gate** — none is
silently unknown.

Other sessions' working-tree changes untouched throughout.

_Times Sydney (AEST, UTC+10). Fleet internals UTC. Report not committed._
