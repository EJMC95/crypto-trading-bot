# Evidence Review — 2026-08-26

_Reviewed 2026-08-26 13:41 AEST (Sydney) · 2026-08-26T03:41:26+00:00 UTC._

## Verdicts

| Key | Status | Why |
|-----|--------|-----|
| census:50 | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:0G | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:APEX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:AVAX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:BIO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:CHIP | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:EIGEN | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:GMX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:KAITO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:MU | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:NEAR | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:RESOLV | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKHYNIXUSD | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKY | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SNDK | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STABLE | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STBL | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:WTI | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:ZORA | stale | dislocation census publisher `lighter-dislocation-lshadow` is 508.5h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| factor-sample:21 | active | joined decision+context dataset at 643 closes (49% win), bucket 21 |
| veto:XLM | active | stop rate 4/5 >= 50% (30d) |

## New evidence

- 🎫 shadow lens 'short-divergence' at n=131 (≥10): net $-8.13, WR 34%, t=-0.48 — noise
- 🎫 shadow lens 'long-breakoutup' at n=82 (≥10): net $+57.48, WR 52%, t=2.28 — significant
- 🎫 shadow lens 'long-divergence' at n=20 (≥10): net $+2.44, WR 45%, t=0.31 — noise
- 🎫 shadow lens 'long-dip' at n=13 (≥10): net $-9.15, WR 15%, t=-2.74 — significant
- 🎫 shadow lens 'long-breakout' at n=10 (≥10): net $+7.72, WR 80%, t=1.73 — noise
- 💰 LIVE freqtrade-avo-maria-lighter: n=4, net $+0.51 — by lens [('long-dip-in-uptrend', 4, 0.51)]
- 💰 LIVE freqtrade-georgia-lighter: n=47, net $-20.70 — by lens [('long-trend-breakout', 30, 5.17), ('long-range-on', 16, -25.03), ('long', 1, -0.84)]
- 🚦 go-live gates (CANONICAL grader golive_readiness — bars: window, closes, mean, t, halves, maxdd; win rate reported, NOT a bar; retired, already-live and live-twin rows excluded): NO new candidate
- 🔭 gate horizon (computed at trajectory, (ks)): lighter-ticket-taker-lshadow → 2026-10-25 (t) FLOOR:halves · undecidable@trend: pm-albanese-lshadow, pm-turnbull-lshadow; unreachable@trend: band-garrett-lshadow, band-kelly-lshadow, book-douglas-lshadow, lighter-perp-sniper-lshadow, nav-cook-lshadow, perps-funding-carry-lshadow, perps-funding-lighter-lshadow, perps-funding-spread-lshadow
- 🚦 fleet-risk light green — longs 10/20, shorts 5/12 (gross 15); 7d DD -0.65%, clip_scale 1.0
- 📏 freqtrade-avo-maria-lighter live-vs-shadow per-trade gap +0.546pp (live +0.822% n=4, shadow +0.276% n=10) — no divergence
- 📏 freqtrade-georgia-lighter live-vs-shadow per-trade gap -0.236pp (live -0.082% n=47, shadow +0.154% n=103) — no divergence
- 📏 freqtrade-mum-lighter live-vs-shadow: insufficient paired closes (live n=0, shadow n=6) — no verdict
- 🧬 freqtrade-avo-maria-lighter arms differ on FILE SET, not necessarily code: live 76cb7b7c207e (n=17) vs shadow b3d122b3aa44 (n=15) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-georgia-lighter arms differ on FILE SET, not necessarily code: live d88b3a330ded (n=17) vs shadow b3d122b3aa44 (n=15) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-mum-lighter arms differ on FILE SET, not necessarily code: live d88b3a330ded (n=17) vs shadow bebe9f2b35fe (n=15) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-avo-maria-lshadow differs from the repo on FILE SET, not necessarily code: container b3d122b3aa44 (n=15) vs repo 01098cd58872 (n=16) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 freqtrade-georgia-lshadow differs from the repo on FILE SET, not necessarily code: container b3d122b3aa44 (n=15) vs repo 01098cd58872 (n=16) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 freqtrade-mum-lshadow differs from the repo on FILE SET, not necessarily code: container bebe9f2b35fe (n=15) vs repo 01098cd58872 (n=16) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 freqtrade-avo-maria-lighter stamp differs from the repo tree: container 76cb7b7c207e vs repo d88b3a330ded (both n=17, so not a file-set difference) — UNCLASSIFIED. A shared-module change moves every image's stamp without changing any bot's logic, so this is NOT yet a finding: run `scripts/audit_code_currency.py`, whose own-entry-file verdict is the only one that means the container is stale [REAL MONEY row]
- 🧬 freqtrade-georgia-lighter matches the repo tree: d88b3a330ded (n=17)
- 🧬 freqtrade-mum-lighter matches the repo tree: d88b3a330ded (n=17)

## Summary

21 alert keys reviewed: 2 active, 0 resolved, 19 stale. No divergence and no drawdown-governor trigger. 22 new-evidence items scanned.

_An earlier report for this day was preserved at `evidence_review_2026-08-26.superseded-1210.md`._

---

# HUMAN LAYER — daily review, 2nd pass, Wed 26 Aug 2026, ~13:45 Sydney (AEST, UTC+10)

**This is the second run of this job today.** The 10:36 pass is preserved at
`evidence_review_2026-08-26.superseded-1133.md` and its conclusions still hold —
I have not re-litigated them. This layer covers only **what an hour of fleet
time and one more pair of eyes turned up that the morning pass did not**, plus a
re-verification of the two real-money entries that landed in between.

Graded against HEAD `08bbd4f`, rebased onto `origin/main` twice during the run
because a concurrent session was merging live-money work throughout.

## ⚠️ ACTION — one push, and it is smaller than this morning's

**Nothing is on fire.** No book is newly READY, the DD governor has not
triggered, the fleet light is green, `payload.errors` is empty, and
`audit_code_currency` returns **OK** across every stamped container.

This morning's action is **DISCHARGED** — its three fixes reached `origin/main`
via the concurrent session's merge. Two new commits from this run are local:

    git log --oneline origin/main..HEAD     # 2 commits
    git push origin HEAD:main

Both are correctness-only. They change **no bot's behaviour and no trade** —
they change what this report tells you about real money.

## THE FINDING — the class closed this morning was still open in this script

The 10:36 pass fixed `winners_docket.py`: halt/flatten EVENTS ($0.00 P&L, no
entry price) were being graded as trades on the two real-money books. Correct
fix, mutation-verified. **But the class was not closed** — and the rule is that
a fix closes a class or it is not finished.

`scripts/evidence_review.py` — *the script that writes this report* — had **zero
references to a phantom filter**. `golive_readiness` has had one since (th);
`winners_docket` gained one this morning; the reviewer itself never did. Both of
its live-money sections counted the same 13 rows as trades.

The two graders were disagreeing **on the same page of the same report**:

| section | before | after | truth |
|---|---|---|---|
| 💰 LIVE avo-maria-lighter | **n=13**, lens `('long', 9, $0.00)` | **n=4** | go-live grader said n=4 all along |
| 💰 LIVE georgia-lighter | n=51, lens `('long', 5, −0.84)` | n=47, `('long', 1, −0.84)` | 4 halts + 1 real trade were one bucket |
| 📏 avo live-vs-shadow gap | **−0.023pp** (live +0.253% n=13) | **+0.546pp** (live +0.822% n=4) | ~24× wrong, and it inverted the story |

The 9 phantom rows are all `long_daily_loss` on 23–24 Aug, all $0.00, all
entry-price-less. **The direction matters more than the magnitude.** Zeros do
not merely dilute a mean — they *shrink the sample variance*, so a divergence
detector fed nine of them is biased toward the all-clear it exists to be able to
withhold. The one instrument meant to catch a live book drifting from its
control was structurally reluctant to say so. Its "no divergence" verdict was
right this time by luck, not by measurement.

**How it is fixed:** by identity import of `golive_readiness.is_phantom_close`
through a thin SQL-row adapter (`sql_row_is_phantom`) — never a re-typed SQL
predicate, because a second copy of that signature is a second rule and the two
would drift on exactly the rows that matter.

**The mutation round is why this entry is worth reading.** The first round
killed 2 of 3 and left **M2 alive**: deleting the phantom filter from the
💰 LIVE section left the whole suite green. The filter was correct and
**unreachable from any test** — on the one section of this report that names
real money. Same lesson the band-kelly dispersion counter taught this morning,
six hours apart. Fixed by extracting `live_lens_rollup()` so a test can reach
it, plus an **AST** wiring check that `scan_new_evidence` still routes through
it (a substring check would pass against a re-inlined copy — the incident text
contains the name too).

Second round: **4 of 4 meaningful mutations RED**, including the wiring bypass
and a re-typed-signature drift. The 5th reported "survivor" is my own
construction error — `len(x) + 0 if x else 0` is semantically identical to
`len(x)`, so nothing could have caught it. Saying so rather than quietly
dropping it.

Counterfactuals pinned both ways: a real **scratch trade** (entry price, $0.00)
must survive, and a real **forced-flatten loss** on the same `long_daily_loss`
reason must survive. The signature is `$0.00 AND no entry price`, never the
reason string.

## Grading the work that landed since 10:36 (mandate item 1)

Two entries, both real money, both by the concurrent session. **Both HELD** on
the live payloads:

| entry | claim | verdict against the live row |
|---|---|---|
| **(tn)** avo's fixed-dollar rails resized for the funded book | daily-loss backstop 6 → 32 | **HELD** — `leverage.halt.abs_usd: 32.0`, `binding: "abs"`, row fresh |
| **(tm)** georgia's live arm runs her own exit policy | breakout veto + trailing ratchet on the live host | **HELD** — her live row now publishes `strategy: "daytrader-15m"`, her own ROI ladder, `stoploss −0.05`, `scan_order: "diversified"` |
| **(tm)** mum's census names the uptrend block | `uptrend_blocked` reachable on the live host | **HELD** — see below; I nearly filed this as a defect and it is the design working |

### The near-miss I want on the record

👩 mum's live census reads `rsi_min 21.9` against her `rsi_bar 25.0` — a coin
**below her entry bar** — beside `verdicts {no_signal: 22, noncrypto_ungated: 1}`
and **no `uptrend_blocked` bucket at all**. That is exactly the shape (tm) says
it fixed, so it looked like a claim that had not landed on real money.

It has landed. Reading the live host's actual gate order settles it: the census
buckets *before* the mark and the oracle gate, so `noncrypto_ungated` can only
fire for a coin that **did** produce an entry signal. Her one sub-bar coin
signalled, cleared the NOT-uptrend half, and was then refused by the
fail-closed per-asset oracle gate — one of `IWM`/`WTI`/`XCU`, the three her
`ungraded` list names. Every one of the 23 coins is accounted for.

**The operational reading is the interesting part, and it is not an action:**
mum's only qualifying signal today was on a book her own oracle cannot grade.
She is live, 0 open, 0 closes ever, ~1 day old, `days_to_gate_obs 29.3` (inside
the I22 60-day bar). Signal-starved, not mis-wired. Watch.

### Two more things that looked like findings and are not

* **The three live rows are on two different builds** — avo on `76cb7b7c207e`,
  georgia and mum on `d88b3a330ded`, all three the same variant host. That is
  the **marker discipline working exactly as (te) describes it**: the deploy
  commit is `2d4444c [deploy-live-georgia] [deploy-live-mum]`, and avo's
  service was deliberately not restarted. A fix to one book did not restart the
  other two.
* **`audit_code_currency` reported 5 UNRESOLVED rows** — "no answer", which the
  guard's own header says is not a pass. It survived `--depth 160`, which ruled
  out the window and pointed at the ugly cause (a deploy from an uncommitted
  worktree). **It was my stale tree**: the stamps came from a commit I had not
  fetched. After rebasing: **OK**, one `DEFERRED` row behind its marker gate.
  Checking before reporting is the whole point; this one was two rebases away
  from being a false alarm on real money.

## Fleet state (deltas from 10:36 only)

* **fleet-risk GREEN** — 7d DD −0.82%, `clip_scale 1.0`, long budget nowhere
  near binding. REACH is not today's constraint.
* **go-live: READY none.** 🎫 the taker `on_track 2026-10-24` (binding bar `t`).
  🙏 avo-lshadow now n=17 / **29.5d** — it crosses the 30-day window bar within
  hours, leaving `closes` as its binding bar.
* **Winners' docket: 51 buckets** (56 → 54 → 51 as the filters landed).
  `lighter-ticket-taker-lshadow [exit:hold]` **still PROVEN** (n=63, t=3.71,
  +1.633%/trade). **`freqtrade-avo-maria-lighter` is correctly gone from the
  list** — this morning's fix holding, verified rather than assumed.
* **Ledger integrity: exit 0**, the one known historical failure (🌾 carry's 7
  same-pair overlaps, most recent 666h ago, all pre-era). Not new.
* **🎫 taker is now 5 of 6 slots** (was 4 this morning), `lens_veto:
  ["dip","divergence"]` correctly held. Its `caps: None` is **not** the (gn)
  stale-container signature — the taker publishes `max_open`, not `caps`.
* **🌾 carry's cap is being walked live**: `max_positions` read **16** and then
  **18** minutes apart, with 17 held. Not over-cap — a lever mid-walk. Recorded
  because a single read of a moving lever is how a baseline document goes stale.

## The two watch-items from 10:36, unchanged

1. **No shadow arm has closed a trade** — avo-lshadow 15.5h, georgia-lshadow
   19.7h, mum-lshadow 19.3h. Still the real thing to watch.
2. **The judge is still frozen `policy_unstamped` on all three pairs, and is
   still right to be.** Live arms stamp `extra.policy` on 100% of closes
   (avo 13/13, georgia 51/51); shadow arms on **0**, because every shadow close
   predates the (ti) stamp. **Self-resolves on the first shadow close** — which
   is item 1. The judge is the fleet's only designed path to more real money,
   so a quiet shadow fleet is what is actually holding it shut.

## Carried priorities

| # | item | status |
|---|---|---|
| 1 | Long-budget Steps 2/3 | still owed, still not time-critical (budget slack) |
| 2 | 🧲 Snap Back retirement | **DROPPED** — clean twice, as the morning pass directed |
| 3 | 🌾 carry keep-or-wait | **DO NOT TUNE.** `eligible: 0` of 228 scanned, `waiting: 3`, `next: APT` in 5.4h. Supply-bound, not slot-bound — third consecutive confirmation. Late-Aug operator call stands. |
| 4 | ⚖️ Counterweight | pre-registered keep-or-retire **~28-Aug — two days out**. era n=121, mean −1.655%, t=−1.75; on-screen n=100 reads −1.21. |
| 5 | Allocation re-weight | advisory, unchanged. The morning caveat still governs the decision: `n_with_era_claim` is **0 in both classes**. |
| 6 | MTM re-grade | **discharged** — maxDD computed for every book, all under the 15% bar. |

## OPTIONS TO OPTIMISE

**Honest headline: one correctness win banked, and nothing else is on offer that
does not cost expectancy. The morning pass checked capacity, emission and the
growth rail and found them already optimal or already handled; an hour later
that has not changed, and re-proposing them would be the circling this job
exists to stop.**

### 1. BANKED — the phantom filter (needs the push)
The reviewer's two live-money sections now agree with the go-live grader about
the same ledger. **Expectancy cost: none** — no trade changes. Value: the
divergence detector on three real-money books can now actually say "diverging",
and 🙏 Avo's live arm is reported at its true **+0.822%/trade over n=4** rather
than a phantom-diluted +0.253% over n=13.

### 2. REFUSED — 🎫 taker capacity, again, and now with a closer number
It is at **5 of 6**, up from 4. Tempting, and still refused: the binding
constraint is **supply**, not slots. `dip` (t=−2.66) and `divergence` (t=−1.36)
are correctly vetoed, and the morning's `(hd)` cap-binding test on scout
emission was **negative on every lens** (dip 8, breakout 3, momentum 6,
divergence 11 against a cap of 12 — nothing truncated). Raising `max_open`
buys zero trades. **Revisit if it reaches 6 of 6 with a `breakout` ticket
unfilled** — that is the first moment slots would actually bind.

### 3. REFUSED — 🌾 carry capacity. The cap is already moving and it is the wrong knob.
17 held, cap walking 16→18, `eligible: 0` of 228 scanned. A cap raise admits
nobody. The binding gate is supply at the 20% TRUE bar, which (px)/(qx) already
measured and which no reachable lever moves without paying expectancy.

### 4. NO ACTION — 🧮 Hull and 🏦 Rich Dad both sit 6/6 with zero closes ever
Unchanged from this morning and refused on the same I7 grounds: an always-at-cap
book satisfies `open >= cap` on every cycle, so "at cap" observes nothing. Both
are env-only by design (single-policy clock). Revisit when either posts a close
a cap actually refused.

### 5. THE REAL CONSTRAINT TODAY IS NOT A LEVER
Two of the fleet's three forward paths are blocked by **quiet shadow books**,
not by any knob: the judge cannot wake without a shadow close, and 🙏
avo-lshadow needs closes rather than time. Nothing in the growth rail addresses
that, and manufacturing turnover to fix it is precisely the trade `(hl)` killed
25 of 30 candidates for. **Stated as a refusal with evidence, not an omission.**

---

_Mechanical sections above are the script's; this layer is the judgement pass.
Report not committed, per the task. The two fixes ARE committed locally
(`31544ae`, `08bbd4f`) and are **unpushed** — this job does not push._
