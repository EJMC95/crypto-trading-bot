# Evidence Review — 2026-09-10

_Reviewed 2026-09-10 11:35 AEST (Sydney) · 2026-09-10T01:35:04+00:00 UTC._

## ⚠️ ACTION — needs an operator decision

- 🚦 go-live gates (CANONICAL grader golive_readiness — bars: window, closes, mean, t, halves, maxdd; win rate reported, NOT a bar; retired, already-live and live-twin rows excluded): lighter-ticket-taker-lshadow (n=204, 40.4d, mean +0.970%, t=2.25, halves +48.15/+83.73, WR 49.0% (reported, not a bar), maxDD 4.6% (mtm)) [era 2026-07-30T11:09:46+00:00: 204 of 345 closes count]

## Verdicts

| Key | Status | Why |
|-----|--------|-----|
| census:50 | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:0G | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:APEX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:AVAX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:BIO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:CHIP | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:EIGEN | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:GMX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:KAITO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:MU | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:NEAR | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:RESOLV | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKHYNIXUSD | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKY | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SNDK | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STABLE | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STBL | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:WTI | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:ZORA | stale | dislocation census publisher `lighter-dislocation-lshadow` is 866.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| factor-sample:23 | active | joined decision+context dataset at 704 closes (50% win), bucket 23 |
| stale-live:freqtrade-avo-maria-lighter | resolved | RECOVERED — freqtrade-avo-maria-lighter ($431.30 real) is publishing now (0 min old, limit 20); worst publish gap in the last 7d was 0.44h |
| stale-live:freqtrade-mum-lighter | resolved | RECOVERED — freqtrade-mum-lighter ($532.55 real) is publishing now (1 min old, limit 20); worst publish gap in the last 7d was 0.44h |
| veto:AI | active | measured slip 33.93bps > 15 (n=5) |

## New evidence

- 🎫 shadow lens 'long-breakoutup' at n=169 (≥10): net $+153.95, WR 53%, t=2.81 — significant
- 🎫 shadow lens 'short-divergence' at n=131 (≥10): net $-8.13, WR 34%, t=-0.48 — noise
- 🎫 shadow lens 'long-divergence' at n=20 (≥10): net $+2.44, WR 45%, t=0.31 — noise
- 🎫 shadow lens 'long-dip' at n=13 (≥10): net $-9.15, WR 15%, t=-2.74 — significant
- 🎫 shadow lens 'long-breakout' at n=10 (≥10): net $+7.72, WR 80%, t=1.73 — noise
- 💰 LIVE freqtrade-avo-maria-lighter: n=18, net $+120.99 — by lens [('long-dip-in-uptrend', 18, 120.99)]
- 💰 LIVE freqtrade-mum-lighter: n=115, net $+46.85 — by lens [('long-oversold-rebound', 113, 33.89), ('long', 2, 12.96)]
- 🚦 go-live gates (CANONICAL grader golive_readiness — bars: window, closes, mean, t, halves, maxdd; win rate reported, NOT a bar; retired, already-live and live-twin rows excluded): lighter-ticket-taker-lshadow (n=204, 40.4d, mean +0.970%, t=2.25, halves +48.15/+83.73, WR 49.0% (reported, not a bar), maxDD 4.6% (mtm)) [era 2026-07-30T11:09:46+00:00: 204 of 345 closes count]
- 🔭 gate horizon (computed at trajectory, (ks)): pm-turnbull-lshadow → 2026-10-29 (t) · unprojectable@trend: perps-funding-carry-lshadow; unreachable@trend: band-kelly-lshadow, book-bezos-lshadow, perps-funding-spread-lshadow
- 🚦 fleet-risk light yellow — longs 15/20, shorts 0/12 (gross 15) [longs ENFORCED per cohort: live 9/20, shadow 14/26]; 7d DD -3.45%, clip_scale 1.0
- 📏 freqtrade-avo-maria-lighter live-vs-shadow per-trade gap -0.775pp (live +4.058% n=12, shadow +4.833% n=15) — no divergence
- 📏 freqtrade-mum-lighter live-vs-shadow per-trade gap -0.171pp (live +0.203% n=115, shadow +0.375% n=102) — no divergence
- 🧬 freqtrade-avo-maria-lighter arms differ on FILE SET, not necessarily code: live 1d4c97edab9f (n=18) vs shadow ed84858fc09f (n=17) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-mum-lighter arms differ on FILE SET, not necessarily code: live 1d4c97edab9f (n=18) vs shadow ed84858fc09f (n=17) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-avo-maria-lshadow matches the repo tree: ed84858fc09f (n=17)
- 🧬 freqtrade-mum-lshadow matches the repo tree: ed84858fc09f (n=17)
- 🧬 freqtrade-avo-maria-lighter matches the repo tree: 1d4c97edab9f (n=18)
- 🧬 freqtrade-mum-lighter matches the repo tree: 1d4c97edab9f (n=18)

## Summary

23 alert keys reviewed: 2 active, 2 resolved, 19 stale. No divergence and no drawdown-governor trigger. 18 new-evidence items scanned.

---

# Human layer — added after the run (Lucy, 10-Sep 2026, ~12:05 Sydney)

## ⚠️ ACTION — decisions that are yours

**A1 · 🎫 The Ticket Taker is READY for a SIXTH day, and its `t` is drifting DOWN.**
n=204 · 40.4d · mean **+0.970%** · **t=2.25** · halves +48.15/+83.73 · maxDD 4.6% (mtm).
All six bars. But the trend is the point: **t 2.60 (6-Sep) → 2.29 (9-Sep) → 2.25 (10-Sep)**.
It is not decaying toward failure quickly, but a READY verdict that sits unactioned is a
decision going stale, and the bar it is nearest is the one that is moving.
**The one test it still owes is unchanged and unbuilt:** the (hm) random-entry null. On this
venue a random long earns money for free, so "positive mean, t≥2" on a DIRECTIONAL book in a
single-regime tape is not yet an edge claim. Tape coverage is there (42/42 era days, 11.6k
snapshots); `lighter_ticket_replay` reads it and has no random-entry mode.
**Decision: go-live, or keep grading?** My recommendation is unchanged from 9-Sep — **do not
promote until the null is run**, and the null is a ~1-session build, not a research project.
*Owner: the null is Lucy (needs an interactive pass); the promotion is Eamon's explicit act.*

**A2 · 👩 mum's LIVE pre-registration is NOT_CONFIRMED — second day, and now with a full fresh sample.**
Registered 2026-09-02 at n=52, t=2.71, +0.703%/trade. **Fresh: n=62, t=−0.78, −0.222%/trade,
−$45.33**; cluster-robust **t=−0.61** over 50 batches. Her `tag:long-oversold-rebound`
registration reads identically. Pooled would say t=1.03 — the docket correctly refuses to crown
it on the re-mined window.
**This is I21 and I25 working exactly as designed, not a malfunction**: she was registered off a
hot window and the forward sample reverted to her own mean. Nothing in the fleet acted on the
registration, so nothing needs unwinding.
**What it means for the geometry question:** `(zr)` (this morning) established that the 9-Sep
−$52 day was **the geometry Eamon chose, not a defect** — six of twelve $221 slots stopping at
−4% on a $531 book is −10% by construction at `gross_x 5.0`, half of the published
`all_slots_stop_pct 0.20`, which sits **5pp above the gate's 15% bar**. That entry deliberately
moved nothing. I agree with that call and am not re-opening it. **But A2 is new information
against it**: the registered edge that justified running her hot is now formally unconfirmed on
fresh closes. *Owner: Eamon — the `mum-live-rho-read-preregistered` read is set for 7-Oct and I
would leave it there; what is worth knowing today is that the 5× is now riding an unconfirmed
claim rather than a confirmed one.*

**A3 · No real-money problem anywhere.** Both live rows online and fresh (0–1 min), unhalted,
no divergence (avo −0.775pp, mum −0.171pp, both inside noise), every container CURRENT,
`two_writers false`, fleet-risk **yellow** (longs 15/20; live cohort 9/20, shadow 14/26 — both
green), 7d DD −3.45%, `clip_scale 1.0`. The one `DEFERRED` row is retired georgia behind her
marker gate, working as designed.

## What I shipped this run

**`(zt)` — the cohort reader knew the live key's dict and not the history's compacted list.**
Clearing yesterday's carried option #5 turned up a worse defect than the option described. It
said the accessor "falls through to the pooled read"; the fall-through is real but history
carries **no pooled pair either**, so it lands on the no-veto default and returns
**`(0, 10**9)`** — *zero longs held, against a budget of one billion*, on a payload that plainly
records longs held.

| | `cohorts` shape | `cohort_long_state(live)` |
|---|---|---|
| LIVE key | `{"live": {"long_positions": 9, "long_budget": 20}}` | **(9, 20)** ✓ |
| HISTORY | `{"live": [9, 20, "green"]}` | **(0, 1000000000)** ✗ |

**Measured: 2,174 of 17,261 `fleet-risk` history rows.** **No live veto was affected** — checked,
not assumed: every enforcing consumer reads the live key, which is and was the dict. So it is a
**measurement** defect, and it corrupts precisely the number the REACH option is argued from: a
study asking "how often did the live cohort sit at budget?" over that window could only answer
"never". Reader fixed positionally + length-checked; degradation contract unchanged (junk still
skips to pooled, never scored as a zero-position cohort); a FULL cohort read through the list
shape must still read `lp >= lb`, pinned. Fixture is the **publisher's own** (calls the real
`fleet_risk.cohort_view`), plus an **AST pin at the writer** so re-ordering the compaction
reddens instead of silently swapping budget and positions. **4/4 mutations RED.**

## Assessment of the work since yesterday's report

| Entry | Verdict |
|---|---|
| `(zq)` lock-convoy fix (#300) | **Confirmed good, and it closed my carried row.** `_add_columns_if_missing` now checks `information_schema` first and bounds the real DDL at `DDL_LOCK_TIMEOUT_S=3`. Both halves my `(zn)` row asked for. **This review ran in 19.6s** against yesterday's 10-minute-plus stall. |
| `(zr)` "fix mum" (#302, live) | **Sound, and its refusal is the right one.** Ruled out code/rails/deploy/writers individually rather than collectively; correctly declined to cut a real-money clip on the day of a hot window (I25). Its breadth finding is honestly sized as hypothesis-grade (7 events, permutation P=0.097) and shipped INERT. No objection. |
| `(zs)` Counterweight 1-Oct → 10-Oct | Correct: moves the read onto the sample its own registration asked for. |
| `(zo)` georgia v1 retirement | Holds. Grader shows her off the feed; v3 trading, n=146. |

## Carried suggestions from 9-Sep — status

| # | Suggestion | Status |
|---|---|---|
| 1 | Random-entry null on 🎫 the taker | **STILL OPEN** — 2nd day. Now the *only* thing between READY and a defensible go-live. Escalated to A1. |
| 2 | 🧮 Hull clip-for-slots | **MOVED — measured today, see G1.** Was "needs a replay"; the replay was not needed. |
| 3 | 🙏 avo LIVE at capacity, levers exhausted | **STILL OPEN, Eamon.** Unchanged: 6/6 slots, gross 2.0×, all-slots-stop 20% already above the 15% bar. |
| 4 | Store-side DDL / `lock_timeout` | ✅ **SHIPPED** by `(zq)` — verified in code, not assumed. |
| 5 | `cohort_long_state` history shape | ✅ **SHIPPED this run as `(zt)`** — and it was worse than described. |
| 6 | 🚀 book-bezos keep-or-retire | **STILL OPEN, Eamon.** n=40, 8.3d window. My advice stands: let it reach 30 days; the sample that decides a retirement should be the sample the gate grades. |

## 📈 OPTIONS TO OPTIMISE — ranked, each with an owner

**G1 · 🧮 Hull's cap is binding 83% of the time and turns away eligible coins in 38% of all
snapshots. This is the capacity case, and it is now measured rather than argued.**
Yesterday this was "needs a replay against Hull's founding cell". It did not — **the book's own
census has a 13-day time series** (`book-hull-lshadow:census`, n=3,906 since 27-Aug), which
answers the question directly:

| over 3,906 snapshots / 13 days | |
|---|---|
| at cap (`held >= 10`) | **3,249 — 83.2%** |
| at cap **and** `eligible > 0` (a coin it cannot take) | **1,470 — 37.6%** |
| coin-snapshots denied a slot | **2,809** |
| `held` distribution | `{8: 9, 9: 648, 10: 3249}` — never below 8 |
| queue depth when denied | `{1: 550, 2: 587, 3: 295, 4: 9, 5: 34}` — 1–3 deep in 97% of cases |

**The change:** `HULL_CLIP_USD` 80 → 60 and `HULL_MAX_POSITIONS` 10 → 13. Gross is held
near-constant ($800 → $780), and going to 13 captures essentially the whole observed queue.
**Why it is worth it:** Hull's binding bar is **closes, not `t`** ((ny)) — n=5 in its life,
`oldest_held_h 502.71` against a 504h max hold, so it is a slow max-hold grinder whose close
rate is ~`cap / hold`. +30% slots ≈ +30% closes: **~5 months to 30 closes becomes ~3.8**.
**THE EXPECTANCY PRICE IS NOT ZERO, and I will not claim it is.** Per-trade % is clip-invariant
for a funding book (P&L is `acc − RT·CLIP`, both scale with clip), so the *clip* half is free —
but candidates are **ranked best-|apr| first** (`out.sort(key=lambda x: -abs(x[2]))`), so slots
11–13 take marginally weaker coins. They are all still above Hull's **derived 7.82% payback-
velocity floor**, so the price is bounded and it is a decidability buy in the I26 shape — the
same trade `(ty)` priced correctly on the sniper. **The one number I could not get from the
census is the marginal coin's apr distribution**, which is what would turn "bounded" into a
figure. *Owner: Lucy can ship it next interactive pass; I did not hand-set it on an unattended
cron run, because it changes what a book trades and the marginal-coin price is stated but
unquantified.*

**G2 · Give 🏦 Rich Dad the census series Hull has — it is the only funding book without one.**
Enumerating **all** `:census` series (no `LIMIT` — see the note below) returns 10, and
`book-kiyosaki-lshadow` is absent while it sits **5 of 6 slots with `eligible: 0`**. Without a
time series, "is Rich Dad starved, or at cap, or both?" is answerable only at the current
instant — which is exactly the ambiguity I18 exists to remove, one level up: the census makes
`{open: 0}` unambiguous *now*, but a missing series makes the **rate** unmeasurable forever.
`bot_pnl_store.snapshot_census` already exists and Hull calls it in its publish path; this is one
call. Cost: none in expectancy, one shadow-service deploy. *Owner: Lucy, next interactive pass.*

**G3 · The changelog letter guard cannot see open branches, and it cost SEVEN collisions today.**
`(zn)→(zq)→(zr)→(zs)→(zu)` on one entry, plus `(zt)`. Each renumber is a real cost: citations
from tracked code go ambiguous, and `git log` subjects keep the old letter. The guard is working
— it caught four of them — but it compares against `origin/main` only, and `origin/main` gained
`(zr)` and `(zs)` *during this session*. **The cheap fix is to compare against `git ls-remote
--heads origin` / all `origin/*` refs, not just `origin/main`**, so a letter taken on an open
branch is refused at write time rather than at push time. `(zq)` already recorded this diagnosis
("it sees `origin/main`, never the other OPEN branches") without shipping the fix. *Owner: Lucy,
next run — this is a guard improvement and it is squarely in scope.*

**G4 · A refusal with the number: I am not proposing anything on 👩 mum today.**
Her live geometry is the largest single lever in the fleet (`gross_x 5.0`, all-slots-stop 20%
vs a 15% bar), and it is tempting to argue it down on the back of A2. **I am not, because
that is precisely the I25 error**: the fresh sample that refuted her registration is also the
window that would motivate the cut, and `(zr)` already declined the same move this morning for
the same reason. The pre-registered `mum-live-rho-read` on 7-Oct is the right instrument and it
already exists. **Refusing this is the correct output, not a gap in the report.**

**Also checked and rejected today, with the number:** raising 🎫 the taker's capacity (it is
4 open, unbounded `max_positions` in the payload — not slot-limited, so there is nothing to
lift) · widening 🚀 book-bezos (`quiet 16 / signal 0 / eligible 0` — supply-starved, not
slot-starved; widening a book with no signal buys nothing) · 🪁 band-kelly (2 of 4 slots, maxDD
**26.0%** against a 15% bar — capacity is the wrong direction entirely; its pre-registered
1-Oct read governs).

## Housekeeping / things that are not mine to fix

- **The suite has ONE failure and it is another session's uncommitted file.**
  `tests/test_selftests.py::test_no_unregistered_selftest` fails on
  `scripts/study_regime_split_2026-09-07.py`, which is **untracked** (`??`) in the shared tree.
  Proven rather than asserted: moving it aside makes `test_selftests.py` **fully green (138
  passed)**, and I restored it byte-for-byte. Whoever owns it should register it in
  `SELFTEST_MODULES` or `SELFTEST_EXCLUDE`, or commit it. **I did not delete it** (doctrine: read
  and escalate, never tidy away another session's work).
- **Eight commits are unpushed** (`(zi)`×3, `(zn)`, `(zq)`, `(zt)`×2, `(zu)`). This job may not
  push. They are rebased clean on `origin/main` and the letters guard is green.
- **A method note against myself:** my first pass at G2 concluded "no book but the two PMs has a
  census series", from a probe ending in `LIMIT 5`. That is the `(qz)` truncation trap — a cap
  that reaches your reasoning is an undeclared sampling step. Re-run without the cap, Hull's
  series was there all along, and it is what made G1 measurable instead of a guess. Recorded
  because the near-miss was a wrong finding, not a wrong query.

## Summary

Real money is clean: both live rows fresh, unhalted, undiverged, every container current, no
governor trigger. **The fleet's first READY book is READY on its sixth day with `t` drifting
down, and still owes the one test that would make its promotion defensible.** 👩 mum's live
pre-registration is formally NOT_CONFIRMED on fresh closes — the docket refusing to crown it on
the re-mined window is I21 working, and it means her 5× now rides an unconfirmed claim.
One correctness fix shipped and mutation-verified; two carried options closed (one by origin's
own work, one by mine); **the perennial capacity question on 🧮 Hull is finally a number —
83.2% at cap, 2,809 coin-snapshots denied — and it needed no replay at all.**

---

# Addendum — the four flagged fixes, worked (Lucy, 10-Sep ~17:30 Sydney)

Eamon: *"continue and fix."* Four tracks investigated in parallel, each verified
by three adversarial lenses (16 agents, 0 errors). Verdicts and what landed:

| Track | Lenses refuted | Outcome |
|---|---|---|
| G2 🏦 Rich Dad census | **0/3** | ✅ **SHIPPED** `(zz)` |
| G3 letters guard | 2/3 (real defects) | ✅ **SHIPPED** `(zy)` after fixing all three blockers |
| G1 🧮 Hull capacity | 0/3 | ⏸️ **DEFERRED with a date** `(aaa)` — four measured reasons |
| A1 🎫 taker null | **3/3 — CI-red** | ⛔ **NOT APPLIED**; blockers named and carried |

## The thing worth reading: mutation testing caught the same defect twice

Both shipped fixes passed their tests and were **not actually guarded**. In each
case the test was a **substring check on source** and the *wiring* was deletable:

- 🏦 Rich Dad: replacing `store.snapshot_census(bot_id, census)` with `pass`
  stayed GREEN — the substring `limit=CENSUS_LIMIT` still matched the surviving
  `census_window` call two lines below. So did rewriting `census_24h=_cen24` to
  `census_24h=None`. The book would read a window it never wrote, or accumulate
  one and discard it at the publish — publishing a permanently-null field while
  every gate still reported per-loop counts.
- G3 letters guard: **24 mutations, 16 killed, 8 survived — every survivor in
  `main()`'s wiring rather than the comparison**, including
  `if open_clashes:` → `if False and open_clashes:`, which disables the entire
  detection with nothing going red. The pure helper was perfectly pinned; the arm
  that calls it had no test at all.

Both now pinned **by AST at the call node**, plus an end-to-end drive of `main()`
against stubbed git seams with a positive control. **23/24 and 6/8 red.** The
generalisable finding, and it is this session's real output: *a substring test can
pin a helper and leave both halves of its wiring deletable.*

## G3 — and the guard was proven live, on me

Nine renumbers in one day: `(zn)→(zq)→(zr)→(zs)→(zu)→(zx)` on one entry, plus
`(zt)→(zw)`. While fixing it, `origin/main` moved **three more times** and took
`(zr)`, `(zs)`, then `(zt)` — my own letter. Measured proof of the blind spot:
**`(ze)` is held by two open branches and `(zf)` by one, and the shipped guard
reports OK on all three.** The key design decision is the **subtraction** —
comparing raw against all 96 refs yields **592 findings** (10,997 worst case),
reducing both sides to what they *add* vs main yields **1**, and that 1 is real.
`--next` now returns `zy`, correctly skipping `ze`/`zf`/`zw`/`zx`.

## G1 — Hull: deferred, and it corrects three of my own numbers this morning

The direction was right, the marginal-coin worry was **wrong in the good
direction**, and my case was overstated. Corrections, per I12:

- *"+30% closes"* → measured **+12.5% to +16.7%**.
- *"n=5, ~5 months to 30 closes"* → a **ramp artifact**: the cap itself moved
  4→6→10 inside the measured window. True rate is **17.6 closes/30d**; 30 closes
  lands ~late-Oct regardless. The change buys **~10 days, not a rescue**.
- The marginal coin costs **exactly zero** — the |apr| ranking is **degenerate**
  at the venue's 10.512% resting pin (ranks 1-10 and 11+ both 10.5000%, delta
  0.0000pp; all 16 positions ever opened carry `entry_apr 0.10512`).

**Deferred on four measured reasons**, not inertia: the two tapes **disagree in
sign** (−15% vs +1.6%, and `(ne)` says opposite verdicts REFUTE); the 26-Aug cap
change **has not completed one 504h turnover** (~16-Sep), so changing again makes
neither gradeable; the migration transient carries **98% gross** for up to 21
days against the module's own 80% assert; and the urgency was the artifact.
**Pre-registered to re-read on/after 16-Sep**, carried executably.

## A1 — the taker null is designed but NOT shipped, and that is the right call

3/3 lenses refuted on reproduced CI-red defects. The engineering core was
verified sound; three concrete blockers stop it, all now carried so the next
session starts from them rather than redesigning: an `os.environ.setdefault`
race that **mutates process-global env and reddens three green selftests**; a
registered selftest at **84–107s against a hard 120s timeout** (before CI's
coverage multiplier); and an `edits` array that registers a module **no edit
creates**. 🎫 the taker is READY on a sixth day with `t` drifting 2.60 → 2.25 —
this remains the single highest-value open item in the fleet.

## State

**17 commits, unpushed.** Full suite green except one pre-existing failure that
is **not mine** — `scripts/study_regime_split_2026-09-07.py` is another session's
**untracked** file; `test_selftests.py` is fully green (138 passed) with it moved
aside, and I restored it byte-for-byte. Guards: letters ✅ · session_state ✅
(18 carried) · doctrine ✅ · image imports ✅ · deploy coverage ✅.

**I did not push.** This job's safety rules forbid deploys and pushes, and a push
auto-deploys the shadow services (`fleet_bus.py`, `lighter_book_kiyosaki_bot.py`
are on the `paths:` list). Nothing here touches a live entry file and no commit
carries a `[deploy-live*]` marker, so it is shadow-only when you choose to send it.
