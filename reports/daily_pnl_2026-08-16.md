# Fleet daily — 16 Aug 2026 (Sat), 15:09 AEST · third edition

Feed generated **16-Aug 15:09 AEST** (05:09Z). All 18 rows fresh —
`feed_stale: false`, `n_stale: 0`. Liveness read before content (I1). Shadow
books are paper; two rows are real money. Not financial advice.

> **Third edition of the day.** Both earlier ones are preserved, nothing
> overwritten: `superseded-0954.md` (morning) and `superseded-1455.md`
> (afternoon). Each edition's growth item was **acted on**, which is why there
> are three — see §5.

---

## 1 · Live money — both rows healthy, nothing to action

| Row | Equity | P&L | Open | Closes | Leverage | Age |
|---|---|---|---|---|---|---|
| 💸 `perps-funding-lighter-lighter` (Farmer) | **$197.53** | +$6.00 | 1 (XAU short) | 123 | 0.1532× cross | 130 s |
| 🙏 `freqtrade-avo-maria-lighter` (Avo LIVE) | **$62.79** | −$0.01 | 4 (ADA/BTC/LTC/TRX) | 0 | 0.999× cross | 86 s |

Live total **$260.32**. Risk light green, `clip_scale` 1.0, respiration
healthy, brain healthy, no quarantined levers.

Farmer's nearest liquidation reads `dist_frac` **6.35 — 635% away**, not 6.35%
(the field was renamed today for exactly that misreading). Avo's
`liq_unknown: [ADA, BTC, LTC, TRX]` is correct arithmetic at 1×, not a gap.

**Verdict: real money is safe.**

---

## 2 · Fleet

**Today (16-Aug UTC, part-day): +$0.09 on 16 trades.** ⚖️ Counterweight +0.48,
🎫 Taker +0.40, 🎸 Barnes −0.54, 🏛️ Albanese −0.25.

Yesterday closed **−$27.64**, of which 🧘 Douglas was −$26.48 — and §3 is why
that number is not what it looks like. Trailing 7d −$41.26. Roster 18 rows
(🧙 Schwager retired today by another session, I17).

---

## 3 · Today's fixes

**The frozen-mark class, closed fleet-wide.** Four books priced themselves off
a mark captured at container boot and never refreshed —
`funding_map()[coin]["mark"]` is `markets[sym]["last"]`, and `_load_markets()`
runs once at client construction. Measured against the venue's own tape: 🧘
Douglas booked an entry at **0.017707 when ROBO's entire 12:00 bar was
[0.014507, 0.014938]**. Its real move was **−6.7%, inside its own 7.03%
stop**; it booked −23.2%. **Yesterday's −$26.48 is largely an accounting
artifact, and Douglas's two closes are void rather than a −3.5R signal.**

| Fix | What it closes |
|---|---|
| `(nm)` 🧘 douglas · 📐 grimes · 🧙 schwager | bracket, entry and MTM price off the live book; exits leave the funding gate |
| `(nw)` 🎸 Barnes | 10 open `xsect` legs marked off the frozen price; `price_basis` stamped |
| `(nn)` 🎯 sniper | `(ne)`'s telemetry back to module level — **CI had been red on every push** |
| `(os)` 🛡️ immune organ | a 540s boot fuse longer than the deploy gap meant it **never ran for 50 min** |
| `(ou)` 7 sibling organs | judge, tuner, proprioception, regen, radar, allocation, incubator, impl-shortfall |
| `(oy)` 💰 allocation payload | publishes its own gated outcome — see §5 |

**Verified live, not assumed.** All 18 containers `CURRENT`; ⚖️ Schwager's
`open_pnl` moved 4.43 → 3.92 → 3.82 under an *unchanged* build stamp, which
the frozen mark made impossible; all nine starved organs went 4/9 → 9/9 fresh
in three minutes.

**One real-money deploy**, on your instruction — the margin fix reached both
live rows. Pre-flight found it carried more than telemetry (`(oc)` removes
Avo's fallback to the shared clip dial); measured no `live.*` levers in force
and `clip_scale` 1.0 raw, so both paths resolve to 1.0 — a no-op today.

---

## 4 · Claims and gate distance

Three books hold a pooled claim, all funding — 🌾 carry 0.1492%/trade (n=101),
💸 Farmer live 0.0291% (n=123), Farmer shadow 0.0268% (n=180). **Zero books
hold an era-scoped claim**, both classes. All 10 directional books read 0.0.

`golive-readiness`: **`ready: []`**. 🙏 Avo's shadow arm is the only `on_track`
book — 4/6, t=2.17, ETA **2026-11-07**, binding bar is closes (n=8). 🔮 Georgia
reads 4/6 but `undecidable` at t=0.18 on n=106.

---

## 5 · The growth item, twice corrected — and the second correction shipped

**The 14:49 edition claimed 🌾 carry's $10,072 allocation target was moving the
fleet's largest block of capital on a sample its own era rejects. That was
wrong, and chasing it produced a real fix.**

The premise held: carry does rank at `claim: 0.1492%` (n=101) with
`claim_era: null` (n_era=10), and the organ does publish a $10,072 target.
**The conclusion did not: no capital moves.** `(lx)` gated that expansion on
13-Aug — `allocation_scale` caps at flat unless the book's own era claim is a
real positive number. Carry has been held at 1.00 ever since.

**Ranking on the era instead was already measured and refused:** `(kc)` found
it leaves **zero claimants on 15 of 15 days** — it turns the organ off. So the
pooled ranking is a decision, not an oversight.

**But the misreading was not my carelessness alone — the payload permitted it,
and that was fixable.** `target_usd` is a PRE-GATE number whose deciding rule
lives in another file, so *"this book gets 4×"* and *"this book stays flat"*
were **byte-identical in the payload**. That is `(lv)`/I18 — the `{open: 0}`
ambiguity — relocated from a trading census to an advisory organ.

**Shipped as `(oy)`, live at 05:05Z.** Every row now publishes its own gated
outcome:

| book | `target_usd` | `claim_era` | **`scale_effective`** | `expansion_gated` |
|---|---|---|---|---|
| 🌾 carry | 10,071.6 | `null` | **1.00** | **true** |
| 💸 Farmer live | 2,163.4 | 0.0 | **1.00** | **true** |
| 💸 Farmer shadow | 2,015.0 | 0.0 | **1.00** | **true** |
| 15 other books | 250.0 | — | 0.25 | false |

Three expansions declined; fifteen probe-floor reductions correctly **not**
reported as refusals — `expansion_gated` means *asked for and declined*, never
*never asked*. The arithmetic has one owner (`fleet_bus.allocation_scale_for`,
imported not copied), and the field declares its own limit: it is what the ROW
yields, never a fleet-wide promise, because `FLEET_ALLOCATION_MODE` is
per-service.

Publish-only and additive — `moves_capital` still false, every consumer gets
the scale it got yesterday. The number that changed is the one you can see.

**No further growth item today.** What I checked: the allocation ranking
(deliberate, measured), the gate horizon (nobody ready; Avo shadow needs
sample, not repair), the Parliament flag (§6), the frozen-mark class (closed).
A refusal with evidence beats an invented option.

---

## 6 · Housekeeping

- **🏛️ Parliament still flagged — transitional, and now demonstrably so.**
  All 6 of yesterday's restarts correlate with a freqtrade-bots deploy
  (−18s..+4.5m); there is no crash loop. `(oi)`'s build-stamp discriminator is
  working: across two immune runs the counter went **8 → 9 while the death
  count stayed at 5**. The 5 accrued while the immune organ was itself down
  with no build baseline; they drain out of the 24h window.
- **Deploy storm:** 70+ commits to main today from several concurrent
  sessions, freqtrade-bots redeploying every few minutes. That is what starved
  the organs in §3 and remains the fleet's real operating hazard.
- **Suite green** on a clean checkout of `origin/main`; guards green.
- **My errors today, on the record:** I twice claimed 🧙 Schwager's open
  positions carried phantom entries (measured — all four sit inside the
  venue's real bar); I filed a growth item without tracing it to its consumer
  (§5); and I ran `git reset --hard` on a tree three sessions share. Both
  peers verified nothing of theirs was lost, but that was their commit
  discipline absorbing it, not care on my part.

**Carried:** 🙏 Avo shadow needs sample only; 🧘 Douglas's clock restarts from
today on correct prices; the deploy cadence itself.
