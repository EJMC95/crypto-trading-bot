# Evidence Review — 2026-08-19

_Reviewed 2026-08-19 07:34 AEST (Sydney) · 2026-08-18T21:34:24+00:00 UTC._

## Verdicts

| Key | Status | Why |
|-----|--------|-----|
| census:50 | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:0G | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:APEX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:AVAX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:BIO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:CHIP | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:EIGEN | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:GMX | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:KAITO | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:MU | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:NEAR | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:RESOLV | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKHYNIXUSD | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SKY | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:SNDK | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STABLE | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:STBL | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:WTI | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| disloc:ZORA | stale | dislocation census publisher `lighter-dislocation-lshadow` is 334.4h stale (> 6h) — 🧲 Snap Back was retired 4-Aug (jh), so this alert is frozen history, not a live condition |
| factor-sample:19 | active | joined decision+context dataset at 583 closes (50% win), bucket 19 |

## New evidence

- 🎫 shadow lens 'short-divergence' at n=125 (≥10): net $+3.40, WR 36%, t=0.21 — noise
- 🎫 shadow lens 'long-breakoutup' at n=41 (≥10): net $+12.17, WR 46%, t=1.01 — noise
- 🎫 shadow lens 'long-divergence' at n=20 (≥10): net $+2.44, WR 45%, t=0.31 — noise
- 🎫 shadow lens 'long-dip' at n=13 (≥10): net $-9.15, WR 15%, t=-2.74 — significant
- 🎫 shadow lens 'long-breakout' at n=10 (≥10): net $+7.72, WR 80%, t=1.73 — noise
- 💰 LIVE perps-funding-lighter-lighter: n=130, net $+6.85 — by lens [('short', 123, 6.37), ('long', 7, 0.48)]
- 💰 LIVE freqtrade-avo-maria-lighter: n=3, net $+0.20 — by lens [('long-dip-in-uptrend', 3, 0.2)]
- 🚦 go-live gates (CANONICAL grader golive_readiness — bars: window, closes, mean, t, halves, maxdd; win rate reported, NOT a bar; retired, already-live and live-twin rows excluded): NO new candidate
- 🔭 gate horizon (computed at trajectory, (ks)): no projectable candidate · undecidable@trend: pm-albanese-lshadow, pm-turnbull-lshadow; unreachable@trend: band-garrett-lshadow, freqtrade-georgia-lshadow, lighter-perp-sniper-lshadow, lighter-ticket-taker-lshadow, perps-funding-carry-lshadow, perps-funding-spread-lshadow
- 🚦 fleet-risk light green — longs 13/20, shorts 3/12 (gross 16); 7d DD -0.21%, clip_scale 1.0
- 📏 Farmer live-vs-shadow per-trade gap +0.043pp (live -0.147% n=48, shadow -0.191% n=79) — no divergence
- 🧬 Farmer arms AGREE: live ab7b8b378665 vs shadow ab7b8b378665 (n=16)
- 🧬 Avo arms differ on FILE SET, not necessarily code: live be4f79edf4b3 (n=17) vs shadow 96b60737fe31 (n=15) — a different count means different COPY sets ((fd)); compare against each image's own Dockerfile before calling it drift
- 🧬 freqtrade-avo-maria-lshadow differs from the repo on FILE SET, not necessarily code: container 96b60737fe31 (n=15) vs repo 3b4591629d3b (n=16) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 freqtrade-avo-maria-lighter stamp differs from the repo tree: container be4f79edf4b3 vs repo e316df2d8083 (both n=17, so not a file-set difference) — UNCLASSIFIED. A shared-module change moves every image's stamp without changing any bot's logic, so this is NOT yet a finding: run `scripts/audit_code_currency.py`, whose own-entry-file verdict is the only one that means the container is stale [REAL MONEY row]
- 🧬 perps-funding-lighter-lighter differs from the repo on FILE SET, not necessarily code: container ab7b8b378665 (n=16) vs repo 43349c9caf2a (n=17) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy
- 🧬 perps-funding-lighter-lshadow differs from the repo on FILE SET, not necessarily code: container ab7b8b378665 (n=16) vs repo 43349c9caf2a (n=17) — a different count means a different COPY set ((fd)); check that image's own Dockerfile before calling it a stale deploy

## Summary

20 alert keys reviewed: 1 active, 0 resolved, 19 stale. No divergence and no drawdown-governor trigger. 17 new-evidence items scanned.

---

# HUMAN LAYER — 19-Aug, 08:07 AEST (Sydney)

Mechanical run **clean**: `payload.errors` empty, all 20 alert keys verified,
`tests/test_review_currency.py` green (34), **full suite green (0 failures)**,
and every live organ key fresh. The only two stale keys are `gapscout-census`
and `listing-intel` — both publishers retired 17-Jul, both declared; not
findings.

**Yesterday's one open item is CLOSED:** `(pr)`-committed-not-pushed landed as
`(pv)` after a rename, and its douglas quarantine is verified working live
(below).

## ⚠️ ACTION — one decision, and it is small

**Nothing is urgent.** No book is newly READY, the drawdown governor is idle
(7d DD −0.21%, `clip_scale` 1.0), the Farmer's live-vs-shadow gap is **+0.043pp**
(live *ahead* — no divergence), long budget **13/20 with 7 slots spare**, immune
organ reports `sick: []`, and `audit_code_currency` is clean (14 CURRENT, 3
DEFERRED behind their marker gates by design — the 5 pending commits are
graders, tests and docs, no bot logic).

**THE DECISION — delete the fabricated ledger row, or leave it quarantined.**
`(qc)` found that a pytest run in a shell carrying `DATABASE_URL` wrote a **fake
trade** into the production ledger: 🏛️ pm-albanese `BTC/USD`, fixture entry
`100.01`, `+$1.27 "tp"` in 2.2 seconds. **I verified it is still physically in
`paper_trades`** and still correctly withheld — graders read n=25/+$1.81, the
row's own published card reads 26 episodes. So every gate is blind to it and
nothing is mis-graded today.

It matters because the book was kept alive by the red-stop slate *because it was
positive* (+$1.83 all-time), and a fabricated +$1.27 is 70% of that headline. If
the quarantine window is ever narrowed, the row comes back.

```bash
DELETE FROM paper_trades WHERE trade_id = 'pm-albanese-lshadow-BTC-1787050638';
```

Your call — this job may not write to `paper_trades`. Leaving it quarantined is
a legitimate choice; the row is inert either way.

**PUSHED (operator granted permission mid-run).** Both commits are on `main` as
of 19-Aug ~08:20 AEST — `b1ecf11` (carry census counter) and `4f685b9` (the
mutation harness). Full suite green on the **combined** tree after rebasing onto
a concurrent session's band-kelly work, letters audit green, 3/3 mutations red
through the new harness, read-back confirms nothing was swept (520 CHANGELOG
entries, insertions only).

**Renumbered `(qf)` → `(qg)` at push time — the sixth letter collision in two
days.** A concurrent session's 🪁 band-kelly birth took `(qf)` on origin while
this was built. Theirs is merged with the letter in a commit subject and PR
title and has zero code citations; mine was unpushed with two. Least-disruptive
resolution is that mine moves, recorded inline in the entry per rule 4.

**One thing the letters audit could NOT catch, worth knowing.** After the
rename, `audit_changelog_letters` went green while **four of my citations still
said `(qf)`** — and they now resolved, to *their* band-kelly entry. The guard
only checks that a cited letter EXISTS, exactly as `(py)` warned; a green run
there is not evidence the pointer is right. Caught by grepping my own files, and
all six citations were repointed before the push. **A rename is not done when
the audit goes green — it is done when every citation points at the entry you
meant.**

`funding_carry_bot.py` is on the auto-deploy path, so this ships to the carry
SHADOW book (🌾 `perps-funding-carry-lshadow` / `yield-harvester-shadow`). No
real-money row is touched. Verify by the payload, not the green run:
`extra.scan.waiting_admissible` should appear on the carry row.

**DEPLOY VERIFIED IN THE PAYLOAD (not the green run), 19-Aug ~08:45 AEST.**
Build stamp moved `7fceb4ce2298 → 6bcb911f75cd` (n=16, svc `yield-harvester-shadow`),
CI all green including Railway Redeploy. The carry row now reads:

```
{"scanned": 225, "cold": 197, "thin": 26, "waiting": 2, "waiting_admissible": 1,
 "noncrypto": 0, "held": 0, "eligible": 0, "next": "VVV", "next_eta_h": 5.61}
```

Which is exactly the ambiguity closing: **2 waiting, 1 of them can actually
open, soonest is VVV in 5.6h.** This morning the same row read `waiting: 2` with
no `next` and no way to tell whether anything was coming at all.

Checked, not assumed: `is_crypto("VVV") = True`, so the promise it is now making
is one the class screen will honour — `(qe)`'s rule intact. And the `(qg)`
invariant **HOLDS on live data**: a promise exists if and only if a waiter is
admissible.

## What I found, and fixed — `(qg)`

**🌾 carry's census read `waiting: 2` with no `next`, and nothing could say
why.** `(qe)` shipped hours earlier was right — `next` must not promise a coin
the class screen will refuse, because the screen sits deliberately LAST in gate
order. But scoping the promise **removed the signal without replacing it**.
`waiting N` with no `next` is now byte-identical between:

* *N coins are coming and the soonest admissible one is still being picked*, and
* *every one of those N will be REFUSED the moment its clock runs out.*

That is precisely the ambiguity the census was built (2-Aug, a 40-minute
investigation) to end — I18's `{open: 0}` shape, one gate later. It cost me real
time this morning: `hot_since` is **not** the census input (22 entries against a
`waiting` of 2, because it retains coins that have since gone cold or thin), so
answering it meant rebuilding the gate by hand — the second-copy trap `(hj)`.

**Shipped:** `waiting_admissible`, a **sub-count of `waiting`, never a bucket**,
so the partition contract is untouched by construction (pinned by a test). It is
carried in the degraded fallback too, and the log line reads the counter rather
than asserting `0` — a message that hardcodes its own claim cannot report the
day the invariant breaks. New invariant, named and tested: **a promise exists if
and only if at least one waiter is admissible.**

Shadow-only, pure observability — no gate, no lever, no era, no trade change ⇒
main-only under `(mm)`. It buys nothing today; it buys carry's ~12-Sep
keep-or-retire being read off a census that can tell starvation from a coming
refusal, which is the same reason `(px)` walked the floor.

### The mutation round was VOID — and that is the bigger finding

My first round reported baseline GREEN and 4/4 RED. **All of it was false.** The
restore then failed with the file byte-identical to HEAD, and a spy proved
`class_ok("SKHYNIXUSD")` returned `False` on the very call whose counter
incremented.

Cause: `sys.pycache_prefix` on this Mac is `~/Library/Caches/com.apple.python` —
**outside the repo** — so the harness's `find . -name __pycache__` was the null
action, and the mutation was a **line reorder**, same-size by construction, so
CPython's `(mtime, size)` check kept serving it.

**This class has now bitten five times** (31-Jul, 6-Aug, 16-Aug — which poisoned
a *different* session's interpreter and cost them an hour on a "main is red for
everyone" that was not, 18-Aug, and today). Every occurrence is a hand-rolled
shell loop repeating the same three bugs. Fixing my own round would guarantee a
sixth, so **`scripts/mutate.py`** closes the class: runs under
`PYTHONDONTWRITEBYTECODE=1` (prevention, not a purge you can forget — and it
protects other sessions, not just the author), scores the **process exit code**
never text, and **raises** when a mutation target is absent, because "survived"
and "never ran" are otherwise indistinguishable. It also refuses to score a
non-compiling mutant and requires baseline *and* restore green.

Re-ran this entry's own round through it: **4/4 killed, baseline and restore
green**, including the size-preserving reorder that fooled the hand-rolled one.

I had the memory note open in the same session and still missed it — I purged
*something* and crossed bytecode off the list. Memory updated to point at the
tool rather than the rule.

## The 13 entries since yesterday, graded against live data

`(ps)`–`(qe)` — an unusually large day. All hold; three verified in the payload
rather than trusted.

| entry | claim | verdict today |
|---|---|---|
| `(px)` carry floor + grace | `caps.min_vol=1e6`, `flip_grace_h=6` | ✅ **verified live on the row** — both present, exactly as the entry said to check |
| `(qe)` census `next` scoping | fix deployed on the carry path | ✅ code CURRENT at HEAD; behaviour confirmed — and its *side effect* is `(qg)` above |
| `(pv)` douglas quarantine | n=6/−$23.84 → n=4/+$2.64 | ✅ **verified**: graded n=5 / **+$4.03** (one new close since), published row still −$22.45/7 — the delta is exactly the two withheld rows |
| `(qc)` fake-trade quarantine | albanese row withheld | ✅ graded n=25/+$1.81; row still in DB (see ACTION) |
| `(pz)` deploy grant + 3 rows to HEAD | stamps should move | ✅ Farmer `e981e11820fc → ab7b8b378665`, Avo live `4d5c491c01ef → be4f79edf4b3` |
| `(pt)` matched-arm pair clock | dispatch must expand to both arms | ✅ **verified under a real deploy**: Farmer arms AGREE (`ab7b8b378665` both), judge window did **not** restart on the `(pz)` ship |
| `(qd)` winners' docket / I21 | 39 buckets, 0 survivors | ✅ reproduced: 0 proven, same two pre-registrations leading |
| `(pw)` Garrett cap refuted | cap 6→9→12 negative | ✅ still 6/6; no widening attempted |
| `(py)` tier split refused | both halves priced | ✅ engraved in `KNOWN_CELL_COLLISIONS` |
| `(pu)`,`(qa)`,`(qb)`,`(ps)` | refusals + audits | ✅ no contradicting data found |

**The judge is healthy and this is worth stating plainly**, because `(pt)` was
about it being frozen: window restarted 18-Aug **11:45 AEST** (the designed
`(bb)` arm-drift recovery, correctly diagnosed by `(qe)`), floors now shadow
3/30 and live 1/10, refilling at 4.71 and 2.57 closes/day ⇒ **next evaluation
~24-Aug**, binding floor shadow. The `(pz)` deploy shipped both arms together
and did *not* reset it — `(pt)`'s auto-expansion works.

**One tension created yesterday, flagged not fixed:** `(pz)` widened the deploy
grant to "any enhancement", and every Farmer-pair deploy that drifts the arms
restarts this clock. It costs ~6 days of judge progress each time. The mechanic
is designed and correct (mixed-build closes are an invalid paired comparison);
the interaction is new. Worth batching enhancement deploys to the live pair
rather than shipping them singly.

## Carried-over priorities

1. **Long-budget Steps 2/3** — untouched, and **not binding**: longs 13/20
   (7 spare), shorts 3/12. No reach pressure today.
2. ~~🧲 Snap Back~~ — dropped yesterday, stays dropped.
3. **🌾 carry keep-or-wait** — 0 open of 12, `eligible: 0` of 225, era n=10
   t=−4.48. `(px)` bought supply 2 days ago and the census has **not yet
   produced an eligible coin**; 2 waiting. Late-Aug/~12-Sep operator call
   (I17). **Do not tune** — both levers already refused on measurement.
4. **⚖️ Counterweight post-revert** — n=95, mean −1.764%, t=−1.97, −$31.65,
   horizon `unreachable`, sitting at its structural 10 legs (K=5×2, always-in
   by construction — *not* a capacity signal, I7). Pre-registered operator
   decision **~28-Aug**. Untouched.
5. **Allocation re-weight** — organ says funding $14.5k / directional $2.5k.
   **`n_with_era_claim: 0` in BOTH classes again**: carry's $12.1k ranking wish
   rests entirely on a pooled n=101 the fleet has ruled non-current, and the
   consumer correctly pins `scale_effective` at 1.0. The decision is still open
   and still costs nothing to defer.
6. **MTM re-grade** — series live; no book newly breaches the 15% MTM bar
   (worst realised maxDD today is ⚖️ at 4.1%).

## OPTIONS TO OPTIMISE — ranked

Honest headline: **nothing is on offer today, and that is now a
well-measured statement rather than a shrug.** The last 48h spent ~110 agents
across `(pu)`, `(pw)`, `(py)` and `(qa)` pricing every widening anyone could
name, and each was refused *with a number*. I re-checked the four places
headroom has historically been, and found none.

**1. CAPACITY — checked, nothing available.** Every book at its cap has been
measured within 48h and refused: 🛢️ Garrett 6/6 (cap 9 → −$7.07, cap 12 →
−$9.95, `(pw)`); ⚖️ Counterweight 10 legs (structural, I7, and its own
keep-or-retire is pre-registered); 👩 mum 4/4 (refused `(pu)` — median hold
25.1 days, so the cap is not what limits its close rate); 🙏 avo shadow (the
`(ne)` step already taken, constraint has moved off capacity). The books with
*free* slots — 🌾 carry 0/12, 🏦 Rich Dad 0/6, 🧮 Hull 2/4 — are supply-starved,
not cap-starved. **Expectancy cost of acting anyway: measured negative.**

**2. REACH — no pressure.** Long budget 13/20 with 7 spare; the L2 veto is
nowhere near refusing an entry. Nothing to lift.

**3. CORRECTNESS — one taken this run (`(qg)`), and it was the only one I could
find.** This is where the real wins have come from and it is worth saying the
search was not vacuous: I re-derived douglas's and albanese's graded samples
from the real reader, reconciled carry's caps against the entry that shipped
them, recomputed the judge's floors and refill rate, and reconstructed the
census from the live scout payload. One gap found, fixed, mutation-verified.

**4. UNBLOCKING — the two live candidates are both waiting on samples, not
levers.**
   * 🎫 taker shadow `exit:hold` — **n=54, t=2.75, +0.852%/trade**, above the
     t-bar and held only by the multiplicity referee. Its pre-registered
     follow-through (from 18-Aug) is accruing. **Do not re-mine the window that
     generated it** — that is what the pre-registration exists to prevent.
   * 🙏 avo shadow book-level — n=12, t=2.31–2.41, `on_track`, binding bar
     **closes** (grader ETA ~2026-10-07; `(qa)` puts n=30 in early-Sep at the
     post-`(ne)` rate). The fleet's only `real_edge` book on the radar. Caveat
     from `(qe)` stands: **cluster-robust t is exactly 2.00** (n_eff 8.2) —
     knife-edge on that basis, and the live arm's n=3 is evidentially empty.

**What I deliberately did not propose:** any carry/kiyosaki gate widening
(refused on standing I19 numbers, twice), any directional stop change (**0 of 8
directional books calibrate** since gillard's retirement, so the −$128 stop pot
has no actuation path), and raising live Avo's `max_open` — which is backwards,
since that arm sizes its clip as `equity / max_open` and raising the cap
*shrinks* the real-money clip.

**The one lever that would actually move the dollar figure is not in the
codebase.** `(qa)` measured it: real money is $259.84 at a ≈$0/day run-rate, and
🙏 avo's gate run is worth $0.10–0.13/day at today's $15.65 clip — roughly 10×
that only via a deposit. The gate validates; the deposit pays. That remains an
operator decision and no amount of tuning substitutes for it.

---

# ADDENDUM — 19-Aug, ~09:30 AEST: the ACTION item is closed

The operator made the call ("Proceed") and the fabricated pm-albanese row is
**deleted** — transaction committed only after exactly-one-row was verified,
full backup in the session scratchpad first. Readback: raw ledger 26 → 25 /
+$1.81, now equal to the graded number the quarantine was already serving (the
quarantine proving itself); both real July BTC trades survived; zero other
fixture-priced rows fleet-wide. The `LEDGER_QUARANTINE` entry stays as
defense-in-depth. `(qc)` corrected in place.

A fleet-wide PnL growth hunt (6 measured hunters + adversarial referees) is
running on the operator's "improve the PnL of all bots" — results land in this
session and will be implemented per the (kd)/(pz) grants.

---

# ADDENDUM 2 — 19-Aug ~12:40 AEST: the hunt shipped, and **CI/auto-deploy is LOCKED OUT account-wide**

⚠️ **OPERATOR ACTION — GitHub Actions is refusing to run jobs, repo-wide.**
Every workflow run since **02:24Z** has failed: jobs complete in ~2 seconds
with **zero steps** and `runner_id: 0` (no runner ever assigned). This is not
code — it hits **other sessions' commits too** (`a6ce1b2`, `60b579a`,
`412da28`), and the last green run was 18-Aug 23:28Z. The Actions
*permissions* API still reads `enabled: true`, which is exactly how the
billing/spending-limit lockout presents ([[github-billing-kills-all-ci-and-deploys]]).
The billing endpoint needs a `user` OAuth scope this session does not hold, so
the quota cannot be read from here — **check the GitHub billing/spending limit.**

**Consequence, verified by payload readback rather than assumed:**

| fix | state |
|---|---|
| 🌾 carry `waiting_admissible` `(qg)` | ✅ **LIVE** — `waiting 2 / admissible 1 / next VVV`, deployed before the lockout |
| 🛢️ Garrett clip $30→$25 | ❌ not deployed — row still on build `4cd840eb3c63` |
| 📐 Grimes `ungraded_skip` | ❌ not deployed |
| 🧮 Hull `extra.basis` | ❌ not deployed |
| 🛰️ scout `xvenue_funding` | ❌ not deployed |

All five are **committed and pushed to `main`** (`6e3532c`), full suite green
locally (**1873 passed, 2 skipped**), every guard green locally, each fix
mutation-verified. Their `paths:` routes are wired, so **they deploy
automatically on the next successful Actions run** — no re-push needed. Until
then the shadow books run the old code, which costs nothing today: four of the
five are observability/fidelity and the fifth (Grimes) guards a book with zero
trades.

**A note on my own check, since it nearly became the finding.** My first
readback printed Garrett as "LANDED" because the *key* existed, while the
*value* was still the pre-fix `30.0` — a check reporting on its own shape
rather than the fact. Corrected before it was reported. That is the `(po)`
class in miniature, in the same session that engraved it.

---

# ADDENDUM 3 — the growth pass, and what it actually bought

CI/deploy restored (billing). All five `(qi)` fixes deployed and **verified in
the payloads**: Garrett clip $25, Grimes `ungraded_skip: 2` (the `(om)` hazard
firing — refusing, not entering), Hull `basis` with 77 band premiums,
carry `waiting/admissible/next`, scout 89-coin bench.

## Shipped to shadow, on "do what makes more money, risk or no risk"

* **🛢️ Garrett `FUNDING_ENTER_APR` 0.05 → 0.1095** — live (`gate_apr` in the
  row), **not starved** (`eligible 1`, `waiting 10`, 5 open). Verified from its
  own ledger by reconstructing entry apr off the scout tape (22 closes, 0
  unmatched): **13 pin entries = 95.8% of realised loss at −1.343%/trade** vs 9
  off-pin at −0.086%. `t=−1.10` — suggestive, not proven; defensible because
  being wrong costs fewer trades on a book already losing money.
* **🏦 Rich Dad class screen OFF** — live (`crypto_only: false`), **payoff not
  visible**: `eligible 0, noncrypto 0, cold 196, thin 27`. The class screen was
  never its binding gate; apr and volume are. Tripwire: if those stay 0 for ~7
  days the change bought nothing — unset `RICHDAD_ALLOW_NONCRYPTO`.

## Refused, both with numbers, both would have looked like wins

* **The real bot.** 💸 the Farmer runs the same 5% floor and the same pin sits
  above it — but on BOTH its arms the pin is **positive**: LIVE +0.0757%/trade
  (n=72) = **126.9% of realised profit**, 81.8% of entries. Tightening it would
  have deleted four-fifths of the real book's trades and all its P&L.
* **🧮 Hull's volume floor.** The drop lands in the tier where the pin measures
  −1.343%/trade, and its supply is already 81.7% pin. Also corrected my own
  reason for holding it: Hull has **ZERO realised closes** — its t=+3.92 is a
  replay, so there was no clean ledger to protect.

**The rule, saved to memory:** a gate finding does not transfer between tiers —
between books that share a code file, or between tiers within one book. The pin
is the venue's default state; what differs is the liquidity of the tier it is
read in.

## Process, reported rather than tidied away

Five letter collisions; one commit swept a concurrent session's entry (shared
working tree — `--only` cannot prevent it; zero deletions, attribution only);
main briefly carried two `(qq)` entries and the guard was red for every session
until I renumbered mine to `(qv)`. All recorded in the entries.

---

# ADDENDUM 4 — final verification sweep

* **`audit_code_currency`: OK** — but only at `--depth 250`. At the review's
  documented `--depth 45` (and at 60) it reported **3 STAMPED ROWS UNRESOLVED,
  all three the REAL-MONEY rows**. Cause was purely the window: **79 commits
  landed on main today** across concurrent sessions, and the marker-gated live
  services are by design the deepest rows (they only move on a `[deploy-live-*]`
  marker, so their gap grows with every unmarked push). At depth 250 all three
  resolve to **DEFERRED, 82 behind, none marked — working as designed.**
  **Operational note recorded in memory: on a busy day use `--depth 250`, and
  raise depth ONCE to classify rather than reflexively — the other two causes
  (a dirty-worktree deploy, a stamp-set change) are real findings a deeper
  window would hide.**
* **`audit_ledger_integrity`**: the 🌾 carry two-writer failure, **historical**
  (most recent overlap 504h ago, nothing inside 6h). Known, permanent, and
  unchanged by today's work — a guard cannot un-pool a ledger two processes
  already wrote.
* **`audit_changelog_letters`: OK** — 438 entries, 1,007 citations resolve,
  no duplicates on main.
* **Both changed books healthy**: 🛢️ Garrett `gate_apr 0.1095`, not starved
  (`eligible 1`, `waiting 10`); 🏦 Rich Dad `crypto_only false`, census
  unchanged as flagged.
* **`session_commit` fixed** `(rh)` — its verdict now survives a pipe.

**Nothing outstanding requires attention tonight.** Garrett needs closes under
the new gate before its change is judgeable; Rich Dad's tripwire runs ~7 days.
