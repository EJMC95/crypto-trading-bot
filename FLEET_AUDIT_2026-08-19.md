# FLEET AUDIT — 19-Aug-2026

**Operator:** *"Where are we losing and why? More importantly how can we use
this information to let our bots win? … No more circles maverick lets hit
targets on the point."*

Method: 8 parallel diagnosis agents over the live payloads and every book's
own ledger, era-scoped (I1/I14), plus three measured growth studies. Six
agents returned; two (gate-path, organ sweep) and the adversarial verify pass
hit a session limit — **the findings acted on below were re-derived by hand
instead**, and the two unrun scopes are named as not-covered rather than
quietly dropped.

---

## 1 · WHERE WE ARE LOSING — and the answer is mostly "we already stopped"

Realised losses across the whole fleet, decomposed. **Four of the five loss
centres are closed populations the fleet no longer trades**; the fifth is a
book inside its own founding distribution.

| Book | Row P&L | What the loss actually is | Still bleeding? |
|---|---|---|---|
| ⚖️ Counterweight | −$31.03 | Non-crypto legs made unenterable at (ki)/(jg): **−$36.48 over 21 closes = 114% of era net**. Last legacy leg crystallised 5-Aug 00:37Z. Since: 14 days, 30 closes, −$0.72 | **NO** — screen holds, 0 inadmissible pairs |
| 🧘 Douglas | −$24.68 | **97% is two VOID rows** — phantom boot-frozen entry marks the venue never offered ((nm)/(pv)). ROBO's entry sat 21% above its whole hour's bar; its real move was −6.7%, INSIDE its own 7.03% stop. Clean post-fix record: **n=6, +$1.81, +0.63R** | **NO** — mechanically fixed 16-Aug |
| 🌾 carry | −$15.45 era | **9 of 10 era losers are the class screened 13-Aug** (SKHYNIXUSD/WTI/SPCX, −$14.97). One crypto close ever: KAITO −$0.49 | **NO** — unrepeatable by construction |
| 🛢️ Garrett | −$5.82 | 77% is two real hard-stops (LINK −10.29%, VVV −10.02%), all crypto, all short-side. Live mean sits **1.5 SE** from its founding study — 5.1 days into a 30-day clock | Within its own distribution |
| 🎯 Sniper | −$3.81 | −$5.48 over 16 closes in the non-crypto surge/young classes screened at (lk); everything else **+$1.67**. Post-screen sample: n=3, **+$0.35** | **NO** — screen verified working |

**The one-line answer: the fleet is not bleeding from an open wound.** Every
material loss centre traces to a population that was identified, measured and
gated off between 13-Aug and 18-Aug. What remains is not a leak to plug — it
is a **supply drought and a decidability problem**.

### The structural cause, measured at (qa) and corroborated here
Venue alt-volume has **halved every 9.4 weeks for 44 weeks** (ex-majors
$2.2B/day → $54M/day, −97.5%, zero recovery episodes), while funding
dispersion is intact. Carry's cell went from 2,038 persistent coin-hours/week
to **zero**. The books are **starved, not edgeless** — which is why the honest
move on several of them is a decision, not a tuning pass.

---

## 2 · THE REAL PROBLEM — three instruments answering on a dead sample

This is where the audit changes what happens next. The losses above are
closed; the **measurement of them is not**, and stale measurement is what
turns a starved book into a wrongly-retired one.

1. **The go-live grader still grades screened-out populations.** 🌾 carry
   publishes `t=−4.48, horizon "unreachable", why: "more of the same closes
   cannot flip mean"` — but "more of the same closes" is *structurally
   impossible*: 90% of that sample is instruments the entry gate refuses.
   Same shape on 🎯 sniper (`era=null`, unreachable, entire loss in screened
   classes). **A ~12-Sep keep-or-retire read off these numbers retires a
   policy the book no longer runs.**
   *Deliberately NOT "fixed" by moving the era* — (pf) already ruled that a
   narrowed universe is ordinary tuning that does not reset the clock, and
   re-cutting on every screen makes the 30-day bar unreachable by design. The
   blessed precedent is REPORT the class split beside the pooled grade. Named
   here so the 12-Sep decision is made with it in hand.
2. **The fleet's only capital claim rests on a disowned sample.** 🌾 carry's
   allocation claim (0.001492 → target $12,806, **71% of the fleet's total**)
   is computed on pooled n=101, of which **91 rows predate the era** (the
   two-writer window) and which still contains the (nc) ~$13 phantom accrual.
   Era-scoped the claim is `None`; current-policy it rests on **n=0**.
   *No money acts on it* — the (lx)/(oy) era gate holds every consumer at
   `scale_effective 1.0` — but it is the number the fleet's own narrative
   calls "best-claimed book", and it is not that.
3. **Two instruments were re-seeding numbers the fleet had already ruled
   inadmissible.** Both fixed this pass — see §4.

---

## 3 · CAN ANY BOOK STILL WIN? — the honest path-to-live table

| Book | Status | Verdict |
|---|---|---|
| 🙏 **avo shadow** | era n=12, **t=2.41**, +1.313%/trade, both halves +, on_track ETA 7-Oct | **The one book quietly earning its gate.** Post-(ne) cap raise is buying closes (~1.0/day vs 0.36 window rate). *Leave it alone.* |
| 💸 Farmer LIVE | era n=85, mean +0.039%, **t=0.29**, +$0.22 over 25.5d | Formally **undecidable** — needs ~4,337 closes ≈ **1,332 days**. Exit tuning refuted on this cell (48-cell grid, zero winners). **I17 call pending.** |
| ⚖️ Counterweight | admissible +0.163%/trade, t=+0.25 | t≥2 is **~5.1 years** away. Even 20 consecutive +1% closes before 28-Aug moves t to only +0.70 — **no achievable data flips it to KEEP.** |
| 🧮 Hull | 0 closes, 6.0/30d measured | On pace; binding bar is closes not t. Healthy. |
| 📐 Grimes | gate flipped back **all-three-CLOSED** since (om) | Born-trading-nothing again, as its own fixed-universe study predicted (0/31 retests open). I17 clock running. |
| 🏦 Kiyosaki | 0 entries, 6 days | **Supply-true, not a defect** — control group: parent carry opened 0 at a *looser* gate. |
| 💼 turnbull | **zero entries for 16.4 days** | Binding gate `ml_gate=0.45`; every observed skip 0.19–0.43. ML ensemble is near chance (oos_acc 0.51–0.57). Registered-but-authorless — the I18 shape. |
| 👩 mum | 3 closes in 5.3 weeks, era n=0 | The **exact profile swing-daily was retired on** at (nf) (n=3 in six weeks). Funding drag −$2.15 exceeds +$0.69 realised. |
| 🔮 georgia | 124 closes, **mean −0.004%** | Not a bleeder — two exit paths cancelling: trailing-stop **−$17.14/71** vs roi **+$16.62/27**. Brain has flagged `exit_too_tight` (reclaim **1.0**, fwd +1.63%) in **129 of 129 publishes for 8+ days** with no reachable actuator. |

---

## 4 · WHAT SHIPPED THIS PASS (correctness — implemented under (hn) routing)

**A · The quarantine now reaches the seed path.** `fetch_paper_aggregate` —
the totals ~13 books re-read at boot — was raw SQL with **no
LEDGER_QUARANTINE filter**, while every grading consumer withholds those rows.
🧘 Douglas therefore **re-seeded the two void rows on every reboot**,
publishing −$24.68 / 8 closes against a graded record of +$1.81 / 6 — a
**$26.48 permanent disagreement** between a book's own card and the record
that decides it. `is_quarantined` stays the one owner; fail-OPEN inherited.

**B · Parliament no longer publishes a book whose state it could not read.**
`save()` has refused since the 21-Jul audit fix ("never overwrite durable
state we could not read"); `publish()` did not — so a transient Postgres blip
published the **fresh-book constructor state** (equity $1000.00, closed 0) as
fact, and `snapshot_equity` wrote it into the MTM series the (ia) worse-of-both
drawdown bar reads. **Measured: pm-albanese's published MTM drawdown is 0.18%,
and (1001.84−1000.00)/1001.84 = 0.1838% — the flicker, exactly.** Immaterial
at this book's P&L and *not* immaterial in general: the artifact is
`pnl_abs/equity`, so it **scales one-for-one with the book's own success**.

**C · The funding machine can now answer its own entry-quality question.**
The ledger recorded the `enter_apr` **bar** and never the **observation** the
gate admitted on — so "were the entries taken at weak rates?" was structurally
unanswerable from the book's own record, and the brain's 19-Aug
`entry_quality` diagnosis on 🛢️ Garrett **could not be tested against the one
variable its entry gate is built on**. Close rows now carry `entry_apr` (the
signed TRUE apr at admission, ABSENT-when-unknown per (ht)) and `accrued` (the
funding component, so price and carry can finally be graded apart). Same class
and same fix as (ir)'s `hot_h`. Telemetry only — no bar, threshold or decision
reads it.

All three are mutation-verified (4 + 7 + 4 mutations red, controls green).

---

## 5 · WHAT WAS REFUSED, WITH EVIDENCE (I19 — the refusals are deliverables)

All three growth leads admitted at (qa) were measured this pass. **All three
died**, which is the outcome (qa) priced in.

* **Divergence-conditioned harvesting — REFUSED, and the prior WITHDRAWN.**
  (qa) named the LIT stop cluster as the class a divergence veto would screen.
  Graded at entry against both venues' settled series, the cluster's core is
  **agreement, not divergence** (Lighter +0.11 vs HL +0.11, div = 0.00).
  Worse, **0 of 300 pooled entries ever occurred at |div| ≥ 50pp** — the
  books' own gates never admit an entry during a real divergence. UNDECIDED
  per the pre-registered n-floor; nothing to wire.
  `STUDY_DIVERGENCE_VETO_2026-08-19.md`
* **Funding-extreme squeeze events — REFUTED across 1,139 events.** Not one of
  18 cells clears any bar; the grid centre is **t=−2.71, both halves negative**.
  The null is sharper: random non-event entries **BEAT** squeeze entries
  (P_null 0.36–0.99). A fresh funding extreme is *actively bad* entry
  information — it marks the moment the adverse price leg is largest, which is
  the Farmer's LIT stops seen from the inside.
  `STUDY_SQUEEZE_EVENTS_2026-08-19.md`
* **Listing lifecycle — NOTHING ESTABLISHED.** n=12 births; the tempting 24h
  long fails on n, on h2 negative, and on **top-3 concentration 223%**. By a
  week the drift is decisively negative *with survivorship bias running the
  optimistic way*. 🎯's listing source keeps its founding-thesis status
  honestly. `STUDY_LISTING_LIFECYCLE_2026-08-19.md`

Also refused: **moving 🌾 carry's / 🎯 sniper's era** to escape the dead-class
grade (contradicts (pf)'s standing ruling — report the split, don't re-cut);
**acting on the brain's Garrett `entry_quality` proposal** (every tightening it
implies is measured-REFUTED or UNMEASURED, and the variant is env-only with no
reachable actuator); **porting carry's 6h flip-grace to the Farmer** (refuted
in all 16 cells on that cell — grace is a cell property, not a doctrine).

---

## 6 · WHAT ONLY THE OPERATOR CAN DECIDE (queued with evidence)

The fleet's forward metric is BOOKS THAT CAN BE GRADED, THEN GO LIVE. Five
books are now formally undecidable and **no tuning pass changes that** — these
are I17 calls, and they are the highest-value acts available:

1. **⚖️ Counterweight** — pre-registered ~28-Aug. On today's numbers the read
   is **RETIRE on undecidability** (not bleed): t≥2 is 5.1 years away and no
   achievable data flips it. *Do not pre-empt the pre-registration.*
2. **💸 Farmer LIVE** — I17 keep-or-retire on the **only live funding book**;
   1,332 days to decidability, real capital ($197.31) on it.
3. **👩 mum** — swing-daily's retirement profile, one row over.
4. **💼 turnbull / 📐 Grimes** — starved by their own gates; both were HELD at
   (nf) on numbers that have since been measured.
5. **🙏 Avo LIVE notional cap** — published `cap_usd $200` against **$62.53
   equity** (3.2×), so SafetyRails provides no defence-in-depth at the book's
   real scale. Restrict-only, **zero expectancy price** (no trade the sizing
   can express reaches it).

And the single biggest lever, unchanged from (qa) and worth restating: real
money is **$259.84 at a ≈$0/day run-rate**. The nearest genuine event is 🙏
avo's gate run (n=30 lands early-Sep, t=+2.31 already passing). **No lever in
this codebase is worth two orders of magnitude — the capital decision is.**

---

## 7 · NOT COVERED (stated, not dropped)

The gate-path and organ-liveness agents and the adversarial verify pass died
on a session limit. Everything acted on above was re-derived by hand
(arithmetic reproduced, code read, mutations run). **Not swept this pass:** a
full organ TTL/staleness sweep and the open-lever inventory across
`fleet-tuning` lanes. The immune organ reads `sick=[]` and the watchdog
`problems=[]` at 00:26Z, so nothing is currently flagged — but that is a
spot-check, not the sweep, and it is the first thing the next pass should run.

### Carried into the next pass
* 🔮 georgia's trailing-stop family is the one **measured fix-direction with no
  actuator** (brain reclaim 1.0, fwd +1.63%, −$17.14/71). ~~Its closes carry
  prices since (gr), so `study_exit_sweep`'s calibration gate can be
  attempted~~ — **CORRECTED IN PLACE per I12 by (qj), which ran it: the gate
  cannot be attempted at all.** georgia exits on a time-decaying ROI ladder
  under an ATR ratchet, which is not a member of the harness's
  {tp, sl, trail, max_hold} rule space — so a `BOOK_EXITS` entry could only
  calibrate against a rule the book does not run (the (ps) false-pass). Now
  DECLARED in `study_exit_sweep.UNSWEEPABLE_EXITS` (with 🙏 avo maria, same
  SwingDip ladder). **Answering georgia's exit question needs a ladder-aware
  replay — a build, not a re-run.**
* 🧘 Douglas's persisted `recent` sample still carries the two void rows. It is
  **reporting-only** (`_open_position` takes no sample input — the book's
  structural-consistency pin) and its entries carry no pair/date, so filtering
  it would require a second copy of the quarantine rule keyed on something else
  — the (hj) trap. It self-heals in 20 closes (~1 week at 92 closes/30d).
  **Declared, not fixed.**
